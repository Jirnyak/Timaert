
# time.md — ВРЁТ частично (последняя дата внутри файла 2026-09-09). Ядро верно коду: лестница, цикл «никогда быстрее номинала», MAILBOX, дверь промоушна, знаменатель сглаживания; частично ДУБЛИРУЕТ CANON S3 и problems.md §36. Ложны: таблица ставок и абзац про ходьбу (96 t/s «в app/main.cpp» вместо выведенных 24 в movement_cost.h; 1 SP/клетку вместо 2; реген «в обоих мирах»; файл player_recovery.cpp), «kAiTicks = полчаса» (на деле 5.625 мин), «ничто вне двери не сбрасывает simSpeed», «ровно три чтения реального времени», сейв v42 (сейчас 110).
## Вердикты НЕ в ядре (2/5)
- [en-пересказ; 2026-09-03; вердикт владельца 2026-09-03; русский оригинал живёт только в коде: «пройти клет; :165; → S2] The divisor is also **the visual-pace rung** (owner verdict, 2026-09-03): the subworld walking speed DERIVES from it (`movement_cost.h kSubworldWalkTilesPerSecond` — a formula, not a number) under the parity anchor «crossing a cell's scene below costs the same game minutes as crossing that cell above». ÷16 made every body race at 96 tiles/s; ÷64 reads as 24 — tested in play and accepted. Retuning the feel = moving this one rung (128 → 12 t/s, 256 → 6 t/s); the trade of bigger rungs is that a dungeon crawl spends almost no world time.
- [ru-дословно; 2026-08-24; владелец (owner: «степени двойки») — про kMacroWalkCellsPerHour = 8; в коде «сте; :207; → S7] «степени двойки»
## Ложь (10)
- macro NPC AI is quoted in world ticks (`kAiTicks = 32`), so it wakes once per half-hour of game time — macro/npc_ai.h:40-44: kAiTickGameHours = 24·32/8192 = 0.09375 игрового часа = 5.625 игровой минуты на одну думу (256 дум в сутки). «Полчаса» ложно (ошибку уже ловил problems.md:796).
- bar recovery | ⅛ of the bar per game hour, in BOTH worlds | `macro/player_recovery.cpp` — app/main.cpp:3502-3508; macro/recovery.h:6,26-33; macro/recovery.cpp:41-63; macro/attributes.h:567: Внизу бары не восстанавливаются ни у кого (вердикт владельца 2026-09-10, комментарий в main.cpp:3502). rest_pools вызывается только из макро-лагеря (main.cpp:3595-3611) и из думы сквада (npc_ai.cpp:383). Файл переименован: player_recovery.* стал macro/recovery.*. Ставка ⅛ в час верна (kRestRegenPctPerHour = 0.125). Та же устаревшая фраза есть в problems.md:1593.
- travel stamina | 1 SP × terrain weight per **cell** — macro/movement_cost.h:66: kStaminaPerCell = 2.0f. Абзац ниже (строка 209) сам пишет 2 — таблица противоречит своему же тексту.
- subworld walk | 96 tiles per **real** second | `app/main.cpp` — macro/movement_cost.h:146-151: kSubworldWalkTilesPerSecond выводится формулой в macro/movement_cost.h, а не задан в app/main.cpp; при делителе ÷64 даёт 24 t/s, это прибито static_assert(== 24.0f).
- `kSubworldWalkTilesPerSecond = 96` carries its derivation beside it (`app/main.cpp`): 8 cells/game hour = 8000 tiles per subworld game hour, and an hour down there lasts 85⅓ real seconds, so the honest rate is 8000 / 85.33 = 93.75 tiles/s — rounded up 2.4 % to 96, a **named** fantasy allowance — macro/movement_cost.h:126,146-151; core/time.h:48: В клетке 1024 тайла (kSubworldTilesPerMacroCell = 1024), не 1000. Делитель 64, поэтому час внизу длится 341.33 реальной секунды, а не 85⅓. Значение ровно 24, без округления и без «фантазийной надбавки»; живёт в movement_cost.h.
- spell cooldown | authored seconds → steps | `content/spells/spell_book.cpp`; melee cooldown | authored seconds → steps | `ecs/systems.cpp` (two separate cooldown rates) — content/spells/spell_book.cpp:243-244; ecs/systems.cpp:35-40; sub/engine.cpp:2027,3208: Отдельных кулдаунов больше нет: у тела ОДИН гейт recoverySteps (вердикт 2026-09-09). Его заряжают и удар (steps_from_seconds в sub/engine.cpp), и каст (recovery_steps в spell_book.cpp:178), а гасит один tick_combat_recovery. Единица «шаг» указана верно.
- `App::simSpeed` is therefore an INTENT, not a permission. Nothing outside that door may reset it — the load path, the menu and the subworld entrance deliberately do not know the field exists. — app/main.cpp:1948-1953,1514,3397: reset_scene_session_state (вердикт владельца 2026-09-20) явно ставит simSpeed = 1, simStepCarry = 0, restUntilTick = 0. Её зовут на входе в субмир (enter_subworld, main.cpp:1514) и на выходе из него (main.cpp:3397). То же ложное утверждение есть в problems.md:2036-2038.
- Real time is read in exactly three places in the whole game … There is no fourth row — macro/save.cpp:90-94,114; app/main.cpp:6333-6341,3838-3841; app/main.cpp:5571-5576; sub/engine.cpp:4666-4731; sub/seamless_manager.cpp:224-260,931-1008: Кроме трёх названных мест (ожидание цикла main.cpp:6361, шейдерные часы main.cpp:5736, сид main.cpp:378) реальное время читают ещё: метка времени сейва (system_clock), счётчик тиков в секунду для HUD F3, профилировочные таймеры (TIMAERT_GPU_STATS, тайминги шва). Мир не пишет ни одно из них: свойство «реальное время не трогает мир» держится, счёт «три» — нет.
- the save states the instant exactly, in one `uint64` (since `kSaveVersion` 18; today the save is v42) — macro/state.h:356; macro/save.cpp:1042,1126: kSaveVersion = 110. Часы действительно пишутся одним POD WorldTime{uint64 tick}; неверен только номер версии.
- Four rhythms (… a daily queue budgeted per frame) … **All four are gone** — each is now a whole number of ticks on the one ladder. — macro/world_tick.cpp:515-536; app/main.cpp:3556-3557,3491-3494: Очередь дней с бюджетом жива: advance_world_clock копит pendingDailyTicks, а process_world_daily_ticks разбирает не больше max_daily_ticks за вызов (32 за тик на карте, kSubworldDailyTicksPerStep = 1 за шаг внизу). Бюджет стоит на тик или шаг, а не на кадр, но «очереди больше нет» — неправда.
## Правда для SKELETON (15)
- Лестница — две ручки и делитель: kTicksPerRealSecond = 64, kTicksPerDay = 8192, kSubworldTickDivisor = 64. Степени двойки, делимость и точность вывода минут и часов прибиты static_assert (`core/time.h:29`)
- Минута и час не хранятся, а выводятся: absolute_minute = (t*45)>>8, absolute_hour = (t*3)>>10; настенный календарь (minute_of_day, hour_of_day, day_of) выводится из них (`core/time.h:112`)
- ticks_to_advance_minutes целится в ПЕРВЫЙ тик, который ПОКАЗЫВАЕТ целевую минуту (tick_at_absolute_minute = ceil(m*256/45)), а не в округлённую длину (`core/time.h:158`)
- WorldTime — одно поле uint64 tick; day(), hour(), minute() — методы; в сейв пишется одним POD (save.cpp:1042/1126) (`core/time.h:204`)
- advance_world_clock считает продвижение вычитанием абсолютных счётчиков (минуты, часы, дни); перекатившиеся дни копятся в pendingDailyTicks, а process_world_daily_ticks разбирает их с бюджетом (`macro/world_tick.cpp:495`)
- Внизу tick_world_subworld_steps покупает 1 тик за kSubworldTickDivisor шагов; остаток хранится целым в subworldStepRemainder и едет в сейв (save.cpp:1087/1186) (`macro/world_tick.cpp:633`)
- Главный цикл: ticks = promote_turn_ticks(app) → frame(app, ticks) → ожидание до дедлайна turnStart + 1/64 с (sleep_for до остатка kSpinGuardMs = 0.2, затем спин). Раньше дедлайна тик не начинается; перебор даёт просто опоздание; накопителя долга нет (`app/main.cpp:6320`)
- Present mode: MAILBOX, если поверхность его даёт, иначе FIFO. HUD F3 показывает «World: x / 64 ticks/s (SLOW MOTION)» и «Present: mailbox/fifo» (main.cpp:3833-3846) (`gpu/vk_swapchain.cpp:42`)
- Одна дверь промоушна: fast_forward_allowed = Playing && worldLoaded && !subworld.active(), выводится каждый ход. При отказе сбрасываются simSpeed, simStepCarry и restUntilTick. simSpeed переносит дробную часть; отдых ЗАМЕНЯЕТ множитель, давая до kRestTicksPerTurn = 128 тиков за ход (apply_rest_promotion, main.cpp:2462-2482) (`app/main.cpp:2505`)
- Сглаживание визуалов макро-NPC получает ticksAdvanced × kStepSeconds, а не длину кадра (`app/main.cpp:5598`)
- Каданс думы kAiTicks = 32 тика мира; kAiPeriodSeconds — только представление; kAiTickGameHours = 0.09375 ч. Время игрока в клетке копится тем же kAiTicks на полях флага (main.cpp:3622-3633) (`macro/npc_ai.h:29`)
- kSubworldWalkTilesPerSecond ВЫВЕДЕН в коде из kMacroWalkCellsPerHour = 8, kSubworldTilesPerMacroCell = 1024, лестницы и делителя; равен 24 t/s под static_assert (`macro/movement_cost.h:146`)
- Якорь марша стоит компайл-тайм гейтом: kFreshBarSp = 110 и kStaminaPerCell = 2 ⇒ kRoadHoursPerFreshBar в пределах 6..9 ч; kRoadStaminaPerHour больше часа отдыха — два static_assert (`macro/movement_cost.h:249`)
- Отдых ⅛ бара в игровой час (kRestRegenPctPerHour = 0.125) идёт только в макро-лагере: вызов rest_pools при resting && minutesAdvanced > 0 (main.cpp:3595-3611); внизу восстановления нет (main.cpp:3502-3508) (`macro/attributes.h:567`)
- Бой считается в ШАГАХ: steps_from_seconds округляет к ближайшему (time.h:68); один гейт тела recoverySteps гасит tick_combat_recovery; дрейн поддерживаемых спеллов снимается за шаг через spellbook_tick (spell_book.cpp:240-274) (`ecs/systems.cpp:35`)
## Выжить (9)
- Закон цикла «мир никогда не бежит быстрее номинала»: сон плюс спин ~0.2 мс, раньше дедлайна нельзя. Выбор present mode — решение о ВРЕМЕНИ, а не о графике (MAILBOX предпочтителен, при FIFO темп мира задаёт монитор). Диагностика: строки HUD F3 «World … (SLOW MOTION)» и «Present: …» → SKELETON слой 1½: строка ПРАВДА на main.cpp:6320-6372 и vk_swapchain.cpp:42-55 вместо ЛОЖНОЙ SKELETON.md:220 («на 144 Гц мир проживается в 2.25 раза быстрее»). CANON S3 — принцип present mode. README — смысл строк HUD F3: SKELETON.md:220 и пример в AGENTS.md §3 («на 144 Гц — 57 секунд») противоречат коду: цикл ждёт до дедлайна 1/64 с, поэтому на 144 Гц сутки те же 128 с. Медленнее бывает только при FIFO на 60 Гц (136 с). Нужно сказать владельцу.
- Дверь промоушна времени: ровно два покупателя (>> / simspeed и Z / rest); разрешение выводится каждый ход; отдых заменяет множитель (128 тиков за ход); отказ сцены сбрасывает всё в 1×. Плюс нынешний сброс состояния сцены reset_scene_session_state с обоих концов перехода → SKELETON слой 1½ (main.cpp:2462-2536, 1948-1953, 1514, 3397): В ядре есть только история бага (problems.md:2029-2038), и там же устаревшая фраза «вход в субмир про поле не знает» — с вердикта 2026-09-20 знает.
- Перепись чтений реального времени с file:line: ожидание цикла, шейдерные часы macro.record, сид новой игры, метка времени сейва, счётчик тиков HUD, профилировочные таймеры. Ни одно не пишет мир → SKELETON слой 1½ (расширить строку SKELETON.md:218, которая покрывает только src/macro/): Инвариант «реальное время только заставляет ждать» держится только переписью; таблица из time.md неполна.
- Правило «продвижение по длительности целится в минуту, а не в длину» (ticks_to_advance_minutes). Цена точного вывода: минута длится 5 или 6 тиков, граница часа лежит между тиками → SKELETON слой 1½ (core/time.h:112-164); одна фраза в CANON S3: CANON S3 говорит только «минута выводится»; без этого правила возвращается баг «заплатил 6 минут, прошло 5».
- Делитель ÷64 — ступень визуального темпа. Ходьба внизу выводится из него (24 t/s) через якорь «клетка ≈ клетка»; следующие ступени 128 → 12 t/s и 256 → 6 t/s; цена крупной ступени — данж почти бесплатен во времени мира → CANON S2, абзац «Время внизу медленнее» (там CANON.md:316 говорит «делитель 16», что ложно против core/time.h:48); SKELETON — строка на movement_cost.h:146-151: Вердикта владельца 2026-09-03 нет в ядре, а канон держит неверное число.
- «Всё в тиках» буквально отвергнуто: бой считается в шагах, а не в тиках мира → problems.md:1587 (уже есть) — обновить числа под ÷64: кулдаун 1 с стал бы 64 реальными секундами, а не 16; снять ложную problems.md:1593 «Реген включён в обоих мирах»: Кейс живёт в problems.md, но с устаревшим делителем и опровергнутым утверждением про реген.
- Свидетели лестницы: time_ladder_test (все 8192 тика, биекция hh:mm, год 2^20); world_tick_parity_test (10000×1 == 1×10000, остаток делителя при разбиении); save_roundtrip_test (часы середины минуты); macro_travel_parity_test → SKELETON слой 1½ — как свидетели строк ПРАВДА: Строка ПРАВДА требует точки исполнения; тесты — готовые свидетели.
- Команды консоли `simspeed [mult]` и `rest`, кнопка тулбара `>>` (4×); промоушн действует только на живой карте → README (рядом с README.md:137 Z и :170 F3): README знает Z, F3 и консоль, но не `simspeed`/`rest`/`>>` и не их ограничение живой картой.
- Устаревшие шапки в коде по теме времени: npc_ai.h:22-25 («underground … eight real seconds» — при ÷64 выходит 32 с); seasons.h:3 и celestial.h:4 («macro/state.h WorldTime» — на деле core/time.h:204) → SKELETON — как РАСХОЖДЕНИЕ, до правки шапок: §0 и §5 п.1: шапки врут; при сносе time.md останется только ложный код-комментарий.

# seasons.md — ДУБЛЬ CANON S19.1/S19.2 в части сезонной границы и климата, и ВРЁТ о потребителях (последняя дата в файле 2026-09-17). Не существуют: сниппет resolve_context с gs_->worldTime.day, «CPU tree dispatch в main.cpp», tick_economy_, gather в economy.cpp. Живое уникальное — только цитата владельца 2026-08-13 и перечень погодных потребителей, причём он частично отменён S19.1.
## Вердикты НЕ в ядре (1/9)
- [ru-дословно; 2026-08-13; владелец (owner's vision); :111; → S19] «сезонность — это не ресурсы, а отдельная большая система: и снег, и времена года, и снег в макромире, и дожди, и засухи — расширяемо»
## Ложь (5)
- applied at a single source — `SubworldEngine::resolve_context`: `const float seasonOffset = gs_ ? season_temp_offset(gs_->worldTime.day) : 0.0f; c.macroTemperature = std::clamp(t + seasonOffset, 0.0f, 1.0f);` — sub/engine.cpp:1321,1331-1332; macro/cell_facts.cpp:54; macro/cell_facts.h:56: Такого кода нет. Смещение считается в двери cell_facts: f.seasonTempOffset = season_temp_offset(w.gs->worldTime.day()), где day — метод, а не поле. Оно лежит отдельной колонкой CellFacts, а resolve_context только складывает: std::clamp(f.temperature01 + f.seasonTempOffset, 0, 1).
- both the CPU tree dispatch (`main.cpp` → `tree_type_for_temperature`) and the renderer's atlas bake (`vk_renderer_3d.cpp`) read `macroTemperature` — sub/vk_renderer_3d.cpp:2312-2322; sub/seamless_manager.h:221-222; rg tree_type_for_temperature → только tree_atlas.cpp:13, vk_renderer_3d.cpp:2322, app/smoke.cpp:4873: В main.cpp вызова нет. Единственный живой читатель сезонной температуры — выбор породы для каждого дерева в рендерере (билинейно по окну 3×3 через mgr.cell_temperature), это не запекание атласа. Второй вызов есть только в смоуке.
- Biome classification is deliberately left on the raw climate `t` (the `biome_from_climate(t, m)` call in the same function keeps the unmodified temperature) — sub/engine.cpp:1333-1338; macro/cell_facts.h:48,54-55: Поведение верно (классификация по сырому климату), механизм нет: c.biome = f.biome приходит из двери cell_facts (biome_at_cell), а biome_from_climate(f.temperature01, f.fertility01) в этой функции считает только groundBiome для водной клетки.
- The daily economy tick (`world_tick.cpp` → `tick_economy_(gs, day)`) already carries the `day` … the current `economy.cpp` gather/produce functions take no `day` and gather all resources uniformly — macro/world_tick.cpp:402-403,526-549; rg tick_economy_ → 0 совпадений: tick_economy_ не существует. Дневной проход — process_world_daily_ticks → tick_settlements_/tick_villages_(gs, day, …) с econ_produce_day. Сбора в дневном тике нет вовсе: «gathering is AGENTS now» (world_tick.cpp:402).
- Nothing new is serialized, so old saves keep loading and `kSaveVersion` does not move — macro/save.cpp:1228: Загрузка отвергает любой файл с version != kSaveVersion, так что старые сейвы не грузятся в принципе. Довод о совместимости запрещён законом о формате сейва и в канон переноситься не должен.
## Правда для SKELETON (11)
- Season — enum uint8 из 4 родов плюс Count; kSeasons — 4 строки (tempOffset +0/+0.15/−0.05/−0.20, yieldMul 1.10/1.25/0.90/0.60, tintRGB), порядок строк страхует rows_in_enum_order (`macro/seasons.h:25`)
- kDaysPerSeason = 32 — единственная ручка длины года; kDaysPerYear = 32·Count = 128 (`macro/seasons.h:48`)
- season_at(day): счёт с 1 (день 1 — весна), тотальна для неположительных дней, без состояния (`macro/seasons.h:64`)
- season_boundary(day): (day−1) % 32 == 0, день 1 — первая граница. Ей подчинены: граница долга мест (world_tick.cpp:65), начисление дани (:162), содержание гарнизона (:268), опись округи и ведомость (:585-592), сезонное окно сквадов (npc_ai.cpp:3470), погрузка ротации (npc_ai.cpp:3852) (`macro/seasons.h:81`)
- Сезонное смещение температуры — своя колонка CellFacts.seasonTempOffset, её заполняет дверь cell_facts (`macro/cell_facts.cpp:54`)
- Единственный сток: resolve_context клампит temperature01 + seasonTempOffset в macroTemperature; biome и groundBiome считаются по сырому климату (`sub/engine.cpp:1331`)
- Единственный живой читатель сезонной температуры — выбор породы дерева в рендерере, билинейно по 3×3 (через seamless_manager.h:221) (`sub/vk_renderer_3d.cpp:2312`)
- tintRGB → build_sky_context смешивает с белым на kSkySeasonTintStrength = 0.30 → sky.frag делает col *= mix(1, tint, dayF), ночь не тонируется (../shaders/sky.frag:235) (`sub/sky.h:256`)
- yieldMul объявлен и не читается нигде в src/ и tests/ (rg: единственное вхождение — объявление) (`macro/seasons.h:36`)
- Погода сегодня — ГЛОБАЛЬНАЯ чистая функция weather_at(day, tod) по kSeasonPrecip (шанс, гроза, веса дождь/снег/град), без позиции; макро-поля погоды или температуры нет (rg) (`sub/sky.h:131`)
- seasons_test зарегистрирован (CMakeLists.txt:1173); вердикт выносится через счётчик check.h, main заканчивается report (`../tests/seasons_test.cpp:24`)
## Выжить (6)
- Видение владельца 2026-08-13 дословно: «сезонность — это не ресурсы, а отдельная большая система: и снег, и времена года, и снег в макромире, и дожди, и засухи — расширяемо» → CANON S19 (в начало раздела, дословно, с датой): Цитаты нет ни в одном ядерном документе; seasons.md сам называет себя единственной записью этого видения.
- Перечень будущих потребителей сезона и погоды: сезонные посевы (золото осенью, голое поле зимой — «Session 24 остаток»); снег и грязь как вклад в цену шага (тот же закон ложа и вкладов); сезоны размножения фауны; палитра макрокарты по tintRGB и снегу → CANON S19 — список потребителей. Строки «сезонный множитель в growthAt» и «yieldMul finally wired» НЕ переносить: S19.1 постановила «без сезонного множителя и без гейта» (CANON.md:4946-4948): Потребителей в ядре нет, кроме снега и палитры (CANON.md:5122); часть таблицы противоречит более позднему вердикту.
- yieldMul — колонка-сирота: ноль читателей, а канон S19.1 сезонный множитель отменил → SKELETON — РАСХОЖДЕНИЕ (macro/seasons.h:36); наряд в macro-registry — снести колонку или назвать читателя: Нарушение DOD п.9 (у колонки нет системы-читателя) при прямом противоречии канону.
- Закон «классификация биома — по сырому климату; сезон — своя колонка фактов; лес зимой не становится тундрой, меняются только деревья» → SKELETON (cell_facts.h:54-58, sub/engine.cpp:1325-1338); CANON S19.1 — одна фраза при переходе на поле температуры: NEXT_SESSION.md:3409 переводит season_temp_offset на волну; без явного закона поле температуры может начать переклассифицировать биомы.
- Погода сегодня — ГЛОБАЛЬНАЯ функция календаря (weather_at по kSeasonPrecip: одинаковый дождь на всей планете). Приёмные полосы SkyContext уже есть: precip01, cloudiness01, windX/Z, precipKind, storm01 → SKELETON — РАСХОЖДЕНИЕ против CANON S19 (sub/sky.h:60-66,131-164,268-281). Поправить CANON.md:4916 («сегодня погода — функция позиции и времени»): позиции в weather_at нет: Канон неточен о состоянии кода, а у скелета нет строки о погоде.
- Семантика season_boundary: день 1 — первая граница, новый мир платит за первый сезон утром своего рождения из стартовых запасов; перечень потребителей границы → SKELETON (seasons.h:74-85; world_tick.cpp:65,162,268,585-592,603; npc_ai.cpp:3470,3852): CANON S19.2 даёт замысел, но не точки исполнения; строки в SKELETON нет.

# celestial.md — ЖИВОЙ, почти целиком верен коду (дат внутри файла нет; в git последний коммит 2026-09-22, 103d38e). Мелкая ложь: «зависит только от stdlib» (включает core/table_guard.h); пол размера звёзд не защищает тусклую звезду; небо не читает night_light. В ядре от документа только строка CANON S19 и фраза S18 о доминирующей луне.
## Вердикты НЕ в ядре (3/3)
- [en-пересказ; нет; одобрение владельца в пересказе агента; :75; → S19 / S18] On a night when every moon is new or down, `strength01 == 0` and the world honestly goes dark (the ambient floor in `lighting.h` prevents pure black) — an owner-approved feature: dark new-moon nights are context.
- [en-пересказ; нет; вердикт владельца в пересказе агента (то же в коде: sub/sky.h:194-196); :133; → S19] `sky.frag` draws them as bright anchor stars over the procedural field — stars only, per the owner ruling; the edge tables stay authored for a future star-map UI.
- [en-пересказ; нет; без явной атрибуции; по коду (macro/celestial.h:35 «address "stars too big"») — ; :110; → S19] The star-size seam ("stars too big" — WIRED)
## Ложь (4)
- `src/macro/celestial.h` (header-only, zero deps beyond `<cmath>`/`<cstdint>`) / The header depends only on the standard library — macro/celestial.h:23-26: Кроме <cmath> и <cstdint> включает <cstddef> и проектный "core/table_guard.h" (ради rows_in_enum_order; сам он зависит только от <cstddef>). То же ложное утверждение на строке 123 дока.
- `kSkyStarSizeMin` — a floor so the dimmest star stays ≥ a pixel (applied CPU-side in `build_sky_context`, so the shader trusts the one scale it gets) — sub/sky.h:253-254; ../shaders/sky.frag:125-130: Пол кладётся на ОДИН общий множитель: starSizeScale = max(0.60, 0.25), и при нынешних числах не срабатывает никогда. Порога на каждую звезду или в пикселях нет: шейдер умножает радиусы трёх слоёв на один масштаб.
- Directional night light, the water's moon-path and the sky's bloom all read this one answer (`night_light`), so they can never disagree. — sub/sky.h:240-249; sub/lighting.h:257-264: night_light читает только compute_light_parameters (направленный свет и лунная дорожка через тот же слот sunDir). build_sky_context не зовёт night_light: для каждой луны он сам считает moon_dir и moon_illumination01f. Согласие держится общими формулами, а не одним ответом.
- Nothing new is serialized, so old saves keep loading and `kSaveVersion` does not move — macro/save.cpp:1228: Загрузка отвергает любой version != kSaveVersion; старые сейвы не грузятся. Довод о совместимости запрещён законом о формате сейва.
## Правда для SKELETON (12)
- kMoons — 2 строки: Selûne (28 д, сдвиг 0, размер 1.00, E8ECF5, наклон 9°) и Vharûn (11 д, сдвиг 3, 0.55, D98A6A, −16°); порядок страхует rows_in_enum_order; совпадение полнолуний раз в ~308 дней (`macro/celestial.h:62`)
- moon_phase01f — главная формула: fmod(dayf − 1 + offset, period)/period, тотальна; moon_phase01 делегирует ей через float(day) (`macro/celestial.h:82`)
- Освещённость (1 − cos 2πφ)/2: 0 в новолуние, 1 в полнолуние (`macro/celestial.h:100`)
- moon_dir: угол = (tod − 0.25)·2π − phase·2π; наклон орбиты уводит дугу в Z; фаза берётся в дробный день day + tod (`macro/celestial.h:145`)
- sun_dir(tod): 0.25 — восход (+X), 0.5 — зенит; одна формула для lighting.h:224 и build_sky_context (sky.h:237), в шейдер уходит через push (`macro/celestial.h:137`)
- night_light: доминирующая луна = max(освещённость × smoothstep горизонта); при нуле направление {0,1,0} и moonIndex −1; lighting.h:257-264 перенаправляет слот солнца на луну; ночной пол ambient 0.07/0.08/0.15 (lighting.h:268-272) (`macro/celestial.h:177`)
- kSkyMaxMoons = 3 со static_assert(MoonId::Count ≤ 3) — вердикт владельца «1–3 луны» в коде (`sub/sky.h:31`)
- Созвездия: счёты выводятся через arr_count(sizeof), constexpr celestial_edges_all_valid с static_assert отвергает висячие рёбра и петли на сборке (`macro/celestial.h:274`)
- SkyStarsUbo (std140, кап 32, static_assert на celestial_total_stars и sizeof) заполняется fill_sky_stars один раз при инициализации (vk_renderer_3d.cpp:709-710); sky.frag рисует якорные звёзды (../shaders/sky.frag:143) (`sub/sky.h:197`)
- Масштаб размера звёзд идёт через SkyPush.right.w во все три процедурных слоя звёзд (../shaders/sky.frag:125-130) (`sub/vk_renderer_3d.cpp:3151`)
- build_sky_context зовётся один раз за кадр в record_main и кормит и небо, и тени облаков (`sub/vk_renderer_3d.cpp:3068`)
- celestial_test зарегистрирован (CMakeLists.txt:1435), проверяет через CHECK/report: фазы, освещённость, рассинхрон лун, орбиты (в т.ч. знак лага по четвертной фазе), night_light, рёбра и направления звёзд (`../tests/celestial_test.cpp:282`)
## Выжить (6)
- Модель лун: две луны, периоды, лаг-формула, эмерджентное антисолярное полнолуние. Правила неба владельца, живущие только в коде и доке: 1–3 луны (sky.h:28), «только звёзды, рёбра — для будущей звёздной карты» (sky.h:194), тёмные безлунные ночи как фича → CANON S19 — замысел: число лун, тьма новолуния, звёзды без рёбер. SKELETON — строки на celestial.h:62-199, sky.h:31-34: В ядре только «процедурные орбиты солнца и лун, фазы, серп, созвездия» (CANON.md:4911); вердиктов владельца там нет.
- Кейс UB: рукописный пересчёт рёбер (8 вместо 7) дал чтение за границей, которое прошло на -O0 и ЗАВИСЛО на шиповой -O3 -flto. Лечение — выведенные счёты arr_count и constexpr-проверка рёбер со static_assert → problems.md (кейс-стади): В ядре нет (rg по -flto, arr_count, UB-зависанию пуст); это оплаченный урок про рукописные счёты.
- Урок негативного контроля: знак лага луны ловит только четвертная фаза — при полнолунии и новолунии ±π дают тот же азимут, и проверка антисолярности проходила с перевёрнутым знаком → problems.md (или пример к тестовому закону §8 п.6): В ядре нет; это конкретный пример детектора, который не видел дефект.
- Ночной свет — ЗАПРОС (освещённость × плавное затухание у горизонта), а не правило; пол ambient держит от чёрного → SKELETON (lighting.h:246-272, celestial.h:163-199); CANON S18 уже несёт принцип (CANON.md:4848): Принцип в каноне есть, а строки с file:line в SKELETON нет.
- Шапка celestial_test.cpp:13-17 врёт: «celestial.h is currently included by NO file in src/ … the sky renderer draws one always-full moon». На деле его включают sub/sky.h:23 и sub/lighting.h:20, фазы доходят до экрана → SKELETON — РАСХОЖДЕНИЕ (шапка теста) до её правки: §5 п.1: шапки врут. После сноса celestial.md ложная шапка останется единственным описанием области действия теста.
- Шов размера звёзд: kSkyStarSizeScale = 0.60 и kSkyStarSizeMin = 0.25 (пол на общий масштаб, сейчас не срабатывает), путь celestial.h → SkyPush.right.w → sky.frag → SKELETON (celestial.h:35-39, sky.h:253-254, ../shaders/sky.frag:125-130): Нужна честная строка; в доке смысл пола описан неверно.

# resources.md — ВРЁТ. Последняя дата внутри файла — 2026-09-16. Описывает слой ресурсов, каким он был до вердиктов владельца 2026-09-22: два диалекта хранения, закон аннигиляции жил, рождение железа (Geology), домен None, постройку шахт и мостов артелями, рудокопов в толпе. Всё это из кода снято. Правду о слое уже честнее держит SKELETON.md (слой полей, строки 108-210), вердикты лежат в CANON S5/S8/S10. Живого и уникального в файле осталось два: замысел разведки (prospecting) и две дословные цитаты.
## Вердикты НЕ в ядре (3/14)
- [en-пересказ; 2026-08-13; владелец, замысел 2026-08-13; :136; → S11] Prospecting-class skills: the per-cell truth already exists — resource_field_read(w, row, x, y) for any cell; prospecting is a KNOWLEDGE/UI layer that reveals it to the player (which cells hold stone, iron, thick forest, rich soil), never a second store of the world. Rank can widen the radius, deepen the detail (presence → kind → remaining), or reveal what discovery has not yet struck.
- [ru-дословно; 2026-08-30; владелец (около 2026-08-30); :164; → S14] «SP тратится столько же, добывают кратно больше»
- [ru-дословно; 2026-08-31; владелец, 2026-08-31 (econ_gather_day удалён); :172; → S10] «уже собирают крестьяне»
## Ложь (13)
- One row per resource (ResourceFieldId): Wheat, Fauna, Trees, Clay, Iron, Stone (+ Silver в разделе 2026-08-30) — macro/resource_field.h:64-83; macro/macro_stock.cpp:358-409: В реестре ДЕСЯТЬ рядов: Wheat, Fauna, Trees, Horses, Clay, Iron, Stone, Silver, Copper, Gold. Совпадение со счётом проверяет static_assert на ResourceFieldId::Count
- Two storage dialects live behind the one door: Sparse scar rows (Wheat, Fauna) — scars self-erase when healed, persisted set stays "cells play has scarred" — macro/resource_field.h:12-24 («STORAGE IS ONE SHAPE FOR EVERY ROW… There is no second dialect»); ResourceGrid с FieldCell = uint16 (resource_field.h, блок ResourceGrid); macro_stock.cpp:596-600: У каждого ряда поле — плоский массив uint16 над тором (ResourceGrid). Шрам хранится в клетке плотного массива. Залеченная клетка — это просто 0, ключей и стирания нет
- Carrier rows: the derivation is the field's INITIAL CONDITION, not an attractor (сказано про Trees и Clay/Iron/Stone) — macro/macro_stock.cpp:204-235 (deposit_apply: потолок = deposit_virgin_at), :377-406 (все жилы HealToBaseline): Для жил вывод теперь АТТРАКТОР: ёмкость = чистая функция терраина и сида, выработка заживает к ней на сезонном визите. «Начальное условие» верно только для леса (CarrierSpread)
- a worked-out vein is ANNIHILATED — the cell leaves the map, the chronicle's Drained fact is its only memorial; scarcity is measured against DepositLayer::virginUnits — macro/deposit_layer.cpp:228-231 (закон аннигиляции ОТМЕНЁН 2026-09-22); macro/macro_stock.cpp:224-231 (клетка в нуле оживает через create_deposit); virginUnits читает только отпечаток [deposits] deposit_layer.cpp:188-197: Выработанная клетка держит 0 и заживает обратно к ёмкости. Скудность против virginUnits никто не меряет: у поля нет читателя, кроме печати в stderr
- Wheat: a reaped cell replants one stand per visit (the old 32-day law, preserved by construction) — macro/macro_stock.cpp:240-242; macro/macro_stock.h:55 (kWheatSeasonsToRegrow = 1): За визит прирастает max(1, baseline / kWheatSeasonsToRegrow), то есть вся базовая за один сезонный визит. Сам файл в строках 153-157 себе противоречит
- Iron | Geology | Born where SCARCE: chance/day = depletion × 1/8, the lump lands on a stone host that lacks iron; GrowthDomain::Geology branch is the one implementation — macro/resource_field.h:130-135 (Geology вырезан 2026-09-22, «НЕ БУДЕТ РОЖДАТЬСЯ ЖИЛ В ТЕЧЕНИЕ ПАРТИИ»); macro/macro_stock.cpp:385-389: enum GrowthDomain { HealToBaseline, CarrierSpread }. Железо заживает к базовой, как пашня. Рождения жил нет
- Clay, Stone | None | Quasi-static — macro/resource_field.h:132-134 (None умер вместе с Geology); macro/macro_stock.cpp:380-384, :390-394: Глина и камень идут доменом HealToBaseline с темпом baseline/kWheatSeasonsToRegrow
- Standing today: … iron discovery — macro/resource_field.h:130-134; macro/deposit_layer.h:189-192: Открытия и рождения железа в коде нет вовсе
- a live vein in reach puts the miner / quarryman / clay-digger into the street crowd and into the houses (и строка 131: the street crowd still rolls miner/quarryman looks off the deposit gate) — macro/fauna.cpp:216-219 (гейт); macro/npc.h:348 (depositGate = -1 по умолчанию); macro/npc.h:27-81 (в NPCType нет Miner/Quarryman/ClayDigger); grep 'depositGate =' — ни одного присваивания в kNpcTypeDefs: Гейт в pick_crowd_row существует, но инертен: ни одна строка каталога его не ставит, и рудничных родов в NPCType нет
- Silver: DepositKind::Silver (veins 1/1024 of mountain cells × 512 units — 4× rarer than iron… × the catalog value 32), ResourceFieldId::Silver with the same born-where-scarce Geology growth iron uses — macro/deposit_layer.cpp:64-77 (kDepositGen: серебро Ridge, period 6, threshold 0.96), :130-165 (deposit_virgin_at); macro/macro_stock.cpp:395-399 (HealToBaseline); macro/items.cpp:101 (silver 1280): Серебро рождается полем концентрации, одним законом количества на все роды (колонка unitScale снята). Растёт HealToBaseline. Цена каталога 1280, а не 32. Рядом появились медь и золото
- A mining crew arriving at a bare vein spends its first work day BUILDING the kind's mine: consolidate_deposit_cluster folds the connected veins… the stamp rides the save as a Built work — macro/npc_ai.cpp:1149-1153 («ЗДЕСЬ АРТЕЛЬ СТРОИЛА ШАХТУ… ВЫРЕЗАНО 2026-09-22»); consolidate_deposit_cluster (deposit_layer.cpp:274) зовётся только из tests/deposit_reach_test.cpp:178; DepositDef::mineFeature (deposit_layer.h:60) не читает никто: Артель только добывает. Шахт не строит никто, консолидации в продакшене нет, колонка mineFeature — сирота
- WHO a place raises is its own registry row (LandmarkDef.crews + labourShift — village ½ of souls, city couriers only) — macro/labour.h:15-22, :60-73 (field_pool, kFieldShareShift = 1); macro/npc_ai.cpp:4078: Строки crews живы (landmark_registry.h:212-214), но пул рук больше не pop >> labourShift. Это половина ПАСТВЫ через field_pool. labourShift остался только для станков города
- a crew whose route needs one bridged water cell BUILDS the bridge from the home store's more abundant material — stone or plank, one worker-day of it per span — macro/npc_ai.cpp:853-857 («мостовой случай… вырезан 2026-09-22 вместе со стройкой артелей»), :1049-1051 (чужая округа отбрасывается); macro/spawners.cpp:367-370 и features.h (FT_Bridge кладёт планировщик дорог при генерации): Артели мостов не строят. Мост через одноклеточную воду кладёт генерация дорог
## Правда для SKELETON (15)
- Реестр рядов — одна constexpr-таблица kResourceFields, её счёт прибит static_assert к ResourceFieldId::Count (`macro/macro_stock.cpp:358`)
- Одна дверь чтения и записи: resource_field_read отдаёт max(0, baseline − scar) или хук носителя; resource_field_apply зажимает шрам в [0, baseline] (`macro/macro_stock.cpp:568`)
- Один ходок роста resource_fields_daily_growth, ровно два домена (CarrierSpread — лес, HealToBaseline — все остальные); в день навещаются клетки idx ≡ day mod 32 (`macro/macro_stock.cpp:610`)
- kGrowthEpochDays = kDaysPerSeason = 32 (`macro/resource_field.h:107`)
- Рост леса: сумма 3×3 × 32 / (9 × 1024), гейт биома min(base, 1024)/1024; вырубка внутри массива зарастает примерно за 3.5 года (`macro/macro_stock.cpp:270`)
- Рост фауны: +1 голова за визит, если живо не меньше половины ёмкости 3×3; обход идёт через cell_step (`macro/macro_stock.cpp:253`)
- Пашня и жилы заживают по baseline / kWheatSeasonsToRegrow (= 1) за визит (`macro/macro_stock.cpp:240`)
- DepositLayer — шесть родов, каждый своим ResourceGrid; одна клетка может нести несколько родов (`macro/deposit_layer.h:101`)
- kDepositDefs {kind, commodityId, siteWorth, mineFeature} кормит терм жил в скоре поселения и словарь товаров (`macro/deposit_layer.h:62`)
- Добыча не изобретает геологию: deposit_apply отказывает клетке с ёмкостью ≤ 0; create_deposit отказывает amount ≤ 0 (`macro/macro_stock.cpp:223`)
- Геология — поле: deposit_virgin_at = fbm × вес сродства (высота⁴ или речная влажность) выше порога; строки kDepositGen; печать отпечатка [deposits] units/cells (`macro/deposit_layer.cpp:130`)
- save_game и load_game принимают treeCounts и DepositLayer ОБЯЗАТЕЛЬНЫМИ параметрами (`macro/save.h:47`)
- CellFacts.depositsNear считается за O(1) из поля досягаемости (kind_near) по всем шести родам (`macro/cell_facts.cpp:45`)
- ai_gatherer: take = min(руки, have, carryMax), сначала кредит, потом дебет; цена цикла sp_price(maxSp, perWorkerDay) (`macro/npc_ai.cpp:1128`)
- Ряды ведомости tree_count, fauna_count, crop_count ходят через реестр; квитанции MacroDebt гасит settle_macro_debt (`macro/macro_stock.cpp:715`)
## Выжить (5)
- Замысел разведки ресурсов: правда по клетке уже есть (resource_field_read); разведка — слой ЗНАНИЯ/UI, а не второй склад мира; ранг расширяет радиус, углубляет деталь (наличие → род → остаток) или открывает ещё не найденное → CANON S11 (с пометкой «замысел, НЕ ПОСТРОЕНО», это пересказ агента — нужно подтверждение владельца): Больше нигде в ядре не записано
- Цитаты «SP тратится столько же, добывают кратно больше» и «уже собирают крестьяне» (дословно) → CANON S10/S14: В ядре только пересказ (CANON:3512) или ничего. Авторский текст обязан переноситься дословно
- Список свидетелей слоя ресурсов: resource_growth_test, macro_stock_test, fauna_stock_test, crop_stock_test, tree_layer_test, deposit_layer_test (+ deposit_reach_test) → SKELETON (колонка «свидетель» у строк слоя полей): Какие тесты охраняют слой, записано только здесь
- Входящие ссылки кода на resources.md: macro/npc.h:29, macro/npc_ai.cpp:685, tests/woodcutter_gather_test.cpp:573 → перенаправить на CANON S10 / SKELETON слой полей: После сноса ссылки повиснут. В npc.h:29 ссылка ещё и сопровождает мёртвый комментарий о профессиях
- Расхождения канона, всплывшие при сверке: CANON:5271 держит с ✓ закон аннигиляции, CANON:3797-3798 держит «шахта строится сквадом» — оба отменены 2026-09-22 → CANON S10 (исправить), macro-registry M-79/M-80: Если файл снести, а канон не поправить, отменённые законы переживут отмену

# macrosim.md — ВРЁТ. Последняя дата внутри файла — 2026-09-21. Мертвы: SoldierSquad, macro/aura.h, PlayerState::army, SquadRoster в роли ростера, kNpcTypeDefs[52], «think = полчаса», профессии в kGathererDefs, EconomyState. Концепт лидера снят владельцем 2026-09-21 (CANON:1107, 1166). Вердикты 2026-08-06 уже в CANON S4/S13. Уникально живые: дословная цитата о причинности 2026-08-09, «степени двойки», «игрок ничем не особенен», вердикт SquadOrders, замысел леса-экосистемы, остатки поля следов v1.
## Вердикты НЕ в ядре (5/20)
- [ru-дословно; 2026-08-24; владелец, 2026-08-24: темп марша kMacroWalkCellsPerHour = 8, прежние 32 — «курье; :31; → S7] «степени двойки»
- [ru-дословно; 2026-08-09; владелец, 2026-08-09; :63; → S8] «Клеточный мир → рельеф → климат → РЕСУРСЫ → и уже потом мир заселяется. Города и деревни рядом с месторождением — это должно быть основой всего (и генерации и организации).»
- [en-пересказ; 2026-08-13; владелец (одобренный дизайн, 2026-08-13; в коде «вырубка среди леса зарастает ~4; :141; → S5] Regrowth = an ECOSYSTEM: the 3×3 density gradient seeds the forest, the biome ambient table gates it (deserts almost never, water never), timescale years (a clear-cut inside a living massif ≈ 3.5 game years). Seasonal modulation remains a future data-entry into the same growthAt door, not a mechanism. Ecosystem growth may push cells past their derived value and seed virgin neighbours — how far expansion reaches is a balance knob (owner is fine iterating with tests).
- [ru-дословно; 2026-08-29; владелец, 2026-08-29 (один закон зрения: сквад игрока стоит в общем SquadIndex); :255; → S4] «игрок ничем не особенен»
- [en-пересказ; 2026-08-06; владелец (owner's ruling), Session 15; :301; → S4] ecs::SquadOrders is a waypoint route and only that — the route's presence IS the order; new kinds of squad AI are type rows with their own ai column.
## Ложь (12)
- Code: macro/army.h (`SoldierSquad`) — macro/army.h:179-183: SoldierSquad / SoldierSlot / kMaxSquadSlots вырезаны слиянием M-71 (2026-09-24). Существа лежат строками единого контейнера Inventory, двери — macro/world_row.h
- a macro NPC thinks every kAiTicks = 32 WORLD ticks — half an hour of game time — macro/npc_ai.h:40-44 (kAiTickGameHours = 24 × 32 / 8192 = 0.09375 ч); ошибка уже названа в problems.md:796: Один think — 0.09375 игрового часа, около 5.6 минуты, а не полчаса. Та же ложь стоит в шапке macro/entry_context.h:31
- NpcTypeDef::hireGold (derived as 30 × the row's daily upkeep) … the coin lands … through transfer_value — macro/npc.h:478 (Peasant upkeepGoldPerDay = 0), :494 (hireGold = 30), :465-466 («Цена найма — своя колонка hireGold, от нуля жалованья не зависит»); ui/overlays.cpp:2201 (transfer_value_dense): hireGold — независимая колонка: у крестьянина жалованье 0, а цена найма 30. Монету переносит transfer_value_dense; функции transfer_value нет
- Balance baseline: 1 gold/day for the weakest hireable — macro/npc.h:478-479 (Peasant: upkeepGoldPerDay = 0, hireable = true); npc_hireable macro/npc.h:1902-1905: Слабейший нанимаемый — крестьянин с жалованьем 0 в день (он «работает за еду»)
- every resource of the world is a row of ONE registry — Wheat, Fauna, Trees, Clay, Iron, Stone — with two storage dialects … iron is born where the world ran scarce — macro/resource_field.h:12-24, :64-83, :130-135: Десять рядов, одна форма хранения (плоский ResourceGrid) и два домена роста. Рождения железа нет с 2026-09-22
- ONE ai_gatherer behaviour + kGathererDefs rows — five professions as five data rows (peasant/grain, woodcutter/wood, miner/iron, quarryman/stone, claydigger/clay), each {NPCType, ResourceFieldId, good, worksite}. A village raises the professions of its own ground — macro/npc_ai.cpp:715-762 (GathererDef {row, commodity, worksite, perWorkerDay, rosterYield}, 9 строк), :692-696 (колонка type умерла 2026-09-02); macro/npc_ai.h:284-291 (аукцион): В kGathererDefs девять строк целей: пшеница, фауна, лён, серебро, лес, железо, камень, глина, кони. Колонки NPCType нет. Цель артели выбирает аукцион-рулетка, профессий не существует
- their number is a function of population + the village's needs (EconomyState) … deposits Wood into the village Inventory/eco.resources — macro/economy.h:4-7; macro/state.h:92 (EconomyState снят, v29): EconomyState и eco.resources не существуют. Пул рук — field_pool (labour.h:60-73), груз ложится в Landmark::inventory через deliver_bag_home
- Carry capacity is the sum of the members' own get_carry_capacity(Attributes, Skills) — macro/squad.h:126-134, :150-167 (refresh_squad_carry): Обоз = спина ЛИДЕРА на душу (carryPerSoul, из листа лидера × haulMult) × Σ отношений haulMult членов. Собственных листов у членов в сумме нет
- Each roster entry embodies as its row of kNpcTypeDefs[52] (macro/npc.h — 16 roles + 36 creatures) — macro/npc.h:27-81; компиляция пробы: std::size_t(NPCType::Count) == 46: В таблице 46 строк: 9 гуманоидных ролей и 37 существ, включая Dragon и Horse
- the leader's aura lands IN the sheet before anything is projected from it — macro/character_sheet.h:400-410 (effective_sheet — копия, apply_aura умер), :436-455 (squad_bonuses возвращает пустой BonusTotals): Ауры нет. Лист не мутируется: действующий лист — копия base + BonusTotals, а бонусы сквада пусты с чистки перков 2026-09-03
- Every macro entity IS a squad — ecs::SquadRoster on every macro NPC (the entity is the leader, the roster holds everyone else) … PlayerState::army is his roster — macro/roster.h:24-37 (Roster = только счёт харча + плата, 72 Б); ecs/components.h:588; macro/state.h:715-719 (у PlayerState нет поля army): Члены лежат в области существ NpcInventory (M-71), SquadRoster — лишь обвязка долгов. Армия игрока — обычный инвентарь его сквада (player_inventory)
- The leader's buff travels through the sheet — macro/aura.h: a SET of modifiers … perk rows live — the Leader perk is +1 vit to every soldier — файла src/macro/aura.h нет; macro/character_sheet.h:436-455: Модуля ауры нет, строка Leader → +1 vit умерла вместе с перками 2026-09-03. Осталась одна дверь squad_bonuses, и она пуста
## Правда для SKELETON (15)
- kAiTicks = 32 мировых тика, делит сутки (static_assert); kAiTickGameHours = 0.09375 ч (`macro/npc_ai.h:29`)
- kMacroWalkCellsPerHour = 8.0 — один темп для игрока и сквада; один think даёт 0.75 клетки (`macro/movement_cost.h:121`)
- Оба драйвера ИИ принимают MacroWorld& целиком; TickContext встраивает `MacroWorld mw` (`macro/npc_ai.h:156`)
- Подземный драйвер tick_macro_npc_ai_budgeted зовётся с allowAutoBattle=false, макро-путь — с true (`app/main.cpp:3541`)
- Цена шага марша — edge_weight (ложе + полог + подъём ребра) по запечённой сетке (`macro/npc_ai.cpp:413`)
- Цена найма = hireGold × soldier_level_factor, где фактор = 1 + (L−1)/3 (`macro/npc.h:1952`)
- Найм всё ещё отдельная кнопка Hire (долг S25), монета идёт в склад места через transfer_value_dense (`ui/overlays.cpp:2188`)
- Ряд MacroStock::Roster: subject = ординал MacroSpawnId, detail = entityId члена; члены вычёркиваются из области существ NpcInventory (`macro/macro_stock.cpp:68`)
- drain_dead_leader_squads всё ещё льёт выживших мёртвого лидера в gs.deserterPool (названный долг Б-3) (`macro/squad.h:178`)
- settle_dead_squads в конце обоих драйверов: слив в пул, затем снос мёртвых (destroy_dead_macro_squads) (`macro/npc_ai.cpp:4843`)
- Порядок dispatch: вклад следа → squad_threat_step → scent_hunt_step → switch по SquadType → роль каталога тел (`macro/npc_ai.cpp:3097`)
- Поле следов: два канала uint16 на фракцию плотными планами; в день уходит t/8 восьми соседям; распад >>1 раз в kScentDecayDays = 4; едет в сейве v75 (save.cpp:569-605). РАСХОЖДЕНИЕ: заворот тора в диффузии рукописный (scent_field.cpp:21-33), а не cell_step (`macro/scent_field.cpp:16`)
- TickContext::factionHostileMask[kMaxFactions] — маска uint64 на фракцию; kMaxFactions = 64 (faction.h:108); kHuntBoldShift = 1 (`macro/npc_ai.h:179`)
- SquadSpec (leaderType — любой NPCType, заявка SquadSpecMembers с капом 256) + spawn_squad через make_npc; ecs::SquadOrders — маршрут из путевых точек (`macro/npc_spawn.h:103`)
- Принудительная встреча: detect_forced_encounter + GameSubStateKind::PreBattle; route_macro_npc_attack — строка «драться»; смоук force_encounter (`app/app_state.h:422`)
## Выжить (7)
- Дословная цитата владельца 2026-08-09: «Клеточный мир → рельеф → климат → РЕСУРСЫ → и уже потом мир заселяется. Города и деревни рядом с месторождением — это должно быть основой всего (и генерации и организации).» → CANON S8: В S8 только пересказ; авторский текст обязан переноситься дословно
- Замысел леса-экосистемы: градиент плотности 3×3 сеет лес, таблица биомов гейтит, горизонт — годы (~3.5 года для вырубки в массиве); сезонная модуляция — будущая строка данных в ту же дверь growthAt; дальность экспансии — ручка баланса, «владелец согласен итерировать тестами» → CANON S5/S10 (лес — единственное исключение HealToBaseline): В CANON закона леса нет вовсе, в SKELETON только код-состояние
- Вердикты «игрок ничем не особенен» (2026-08-29), «степени двойки» (темп 8 клеток в час, 2026-08-24) и «наличие маршрута и есть приказ» (SquadOrders) → CANON S4 и S7: В ядре либо пересказ, либо ничего
- Остатки v1 поля следов и невыметенные ручки: диффузия слепа к воде — запах переплывает проливы (scent_field.cpp:16-47 не читает терраин); вклад пересчитывает squad_power на каждом think — подозреваемый перфа §34.8 (npc_ai.cpp:2857); жертва не уклоняется от следа хищника; ручки kScentDecayDays / kScentDiffusionShift / kScentQuantShift, kHuntBoldShift / kHuntScentFloor не выметены → macro-registry.md (наряд): Ни в реестре, ни в problems этих хвостов нет
- Рукописный заворот тора в диффузии следов (scent_field.cpp:21-33: y==0 ? H-1 …) в обход cell_step; там же ежедневная аллокация delta(plane) в тике → SKELETON (РАСХОЖДЕНИЕ; SKELETON:36 объявляет все обходы соседей переведёнными на cell_step): Найдено при сверке этого дока; строка скелета врёт
- Входящие ссылки кода и доков на macrosim.md: macro/squad.h:2, :169; ecs/components.h:557; macro/auto_battle.h:1; macro/npc_ai.cpp:2631; app/main.cpp:561; sub/engine.cpp:3491; problems.md:1159 → перенаправить на CANON S4 / S13: После сноса повиснут. squad.h:169 ссылается на отменённый «ruling 3»
- Стенд сквадов через дев-консоль: `spawn_squad`, `squad_orders` (app/main.cpp:4165, :4232), `chop`, и смоук `force_encounter` → README (раздел отладки), если debug.md его не переносит: В README есть только клавиша консоли, самих команд нет

# macroworld.md — ВРЁТ ЧАСТИЧНО, остальное — ДУБЛЬ кода без записи в SKELETON. Последняя дата внутри файла — 2026-08-29. Ложно: kSaveVersion 62 (в коде 110); генерация в boot_world/main.cpp (на деле world_gen.cpp); порядок генезиса (грунтовки идут после зон, шпилей и руин); «ОДНА свёртка» (снято ЗАКОНОМ АДРЕСА); «дороги отвергают воду» (одна клетка воды даёт мост); find_road_path (такой функции нет); «CellContext — единственный источник» (engine.cpp читает gs_ 28 раз). Реки, дороги, свет и знание описаны верно, но в SKELETON их нет.
## Вердикты НЕ в ядре (0/1)
## Ложь (8)
- Serialised at kSaveVersion (62 today, state.h) — macro/state.h:356: kSaveVersion = 110 (v110 — слияние M-71)
- Generation order (boot_world, src/app/main.cpp) — macro/world_gen.cpp:37 (generate_macro_world); macro/world_gen.h:4 («Extracted from the app's boot_world (2026-08-30)»); в app/main.cpp функции boot_world нет: Генезис — одна функция L1 generate_macro_world в src/macro/world_gen.cpp. Её делят игра и balance_run
- … landmarks → roads → dirt roads → feature layer → zones → spires — macro/world_gen.cpp:172 (trace_roads), :205 (build_feature_layer), :213 (generate_zones), :228 (generate_spires), :233 (generate_ruins), :236 (build_landmark_grid), :299 (trace_dirt_roads): Грунтовки кладутся ПОСЛЕ зон, шпилей, руин и сетки ландмарков, чтобы A* мог целить в шпили и оценивать камень по его ложу
- core/torus.h, which owns the ONE wrap — core/torus.h:9-35 (перепись 2026-09-23: семь механизмов заворота), :59-95 (wrap_axis + четыре двери адреса cell_of/cell_x/cell_y/cell_step), :106 (wrapi — только периоды не-мира); macro/scent_field.cpp:21-33 (рукописный заворот): Заворот мира — маска через четыре двери адреса и wrap_axis. wrapi оставлен только для периодов, которые миром не являются. Рукописный заворот ещё живёт в диффузии следов
- trace_roads … binary-heap A* that reuses existing road cells and rejects water (и стр. 148: Water is not priced, it is REJECTED) — macro/spawners.cpp:367-378; macro/pathfinding.h:300-316 (waterCrossAxes): Отвергается только вода шире одной клетки. Одноклеточная вода оплачивается ложем воды, по кардинальной оси, и такой шаг КЛАДЁТ МОСТ (FT_Bridge)
- the uphill climb is priced on the edge inside find_road_path — rg 'find_road_path' по src/ и tests/ — ноль вхождений; macro/spawners.cpp:360-361, :425 (вызов find_path): Функции find_road_path нет. Подъём оценивает единый find_path (pathfinding.h:310)
- AppState::pathScratch serves map-click travel and the smoke probe — app/app_state.h:108 (struct App), :310: Поле называется App::pathScratch, структуры AppState нет
- Feeds every subworld cell a CellContext (single source of truth — the subworld never re-derives macro data) — sub/engine.cpp:621 (gs_ = mw_.gs), :1438, :1602, :1714 (28 прямых чтений gs_->); src/sub включает macro/ 82 раза (grep): Движок субмира напрямую читает GameState (worldTime, worldSeed, mapW и т.д.) в обход CellContext. Канал широкий (ЗАКОН ДВУХ МИРОВ — узость НАРУШЕНА)
## Правда для SKELETON (15)
- Генезис: terrain → spawn_trees + tree layer → deposit layer → politik → snap → finalize → landmarks → roads → features → zones → spires → ruins → landmark grid → dirt roads (`macro/world_gen.cpp:57`)
- Широта — косинус 0.5 − 0.5·cos(2π·uy), смешанный с fbm весом temperatureVariation (`macro/map_generator.cpp:104`)
- biome_at_cell — один классификатор клетки: маска суши (A) → Water, высота ≥ kMountainBiomeLevel → Mountain, иначе матрица климата; fail-closed в Water (`macro/map_generator.h:112`)
- worldSeed — uint32; часы — один uint64 WorldTime::tick (core/time.h:204-205) (`macro/state.h:855`)
- Landmark — одна запись с колонкой type (строка реестра) (`macro/state.h:466`)
- landmark_grid: слот uint16 на клетку, kNoLandmark = 0xFFFF, ответ за один поиск (`macro/landmark_grid.h:37`)
- rebake_world пересобирает зоны, сетку ландмарков, сетку цены, свечение и текстуру зон; зовётся при загрузке (:1903), входе в субмир (:1509), выходе (:3393) и раз в сезон (:3575) (`app/main.cpp:1476`)
- Реки: A* с эвристикой waterDist, тай-брейк по индексу, подъём (nH−curH) >> kRiverClimbShift (= 1), вырез carveH = max(1, sea8 − 8) (`macro/map_generator.cpp:229`)
- river_generation_test: kMaxRunBound = 120 (heap A* даёт 47-62, DFS давал 239-494) (`tests/river_generation_test.cpp (от корня репо):225`)
- Один поисковик пути find_path: PathScratch, maxSteps, blockAtOrAbove = kPathNoBlock, stepsOut, waterCrossAxes (`macro/pathfinding.h:310`)
- kRoadClasses {surface, link}: FT_Road между городами, FT_DirtRoad деревня→город и деревня→ближайший ландмарк (`macro/spawners.h:56`)
- Планировщик дорог оценивает путь по build_cost_grid; kRoadShare = feature_bed_weight(FT_Road) = 1.0 (features.h:130); вода шире клетки → kRoadWaterReject (`macro/spawners.cpp:371`)
- Топология: Prim MST на королевство от столицы + одно ребро избыточности на город с веерным стражем kRoadFanCosThreshold = 0.90 через torus_bearings_parallel (torus.h:195) (`macro/politik.cpp:305`)
- Ночное свечение: collect_macro_lights → bake_light_field, kMacroGlowGain = 0.45; перезапекание при dailyTicksProcessed > 0 (main.cpp:3501, :3559) (`macro/macro_lighting.h:51`)
- CellContext.cx/cy — завёрнутый макро-индекс [0, worldCells); знание клетки Unknown/Explored/Visible (macro/knowledge.h:38-40) (`sub/map_data.h:284`)
## Выжить (4)
- ЗАКОН ШУМА (2026-08-20): решётка процедурного поля обязана замыкаться на мире — период есть целое число ячеек, делящее сторону. Итог переписи пяти аудиторов: всё, что ХОДИТ по клеткам, было тороидальным, всё, что СЭМПЛИРОВАЛО шум, — резалось. Ридж-октава с периодом 5.6 проводила три вертикальных и три горизонтальных обрыва через каждый мир (8.3× медианного градиента, до 140 байт) → CANON S1 (правило) + problems.md (кейс-стади): В ядре закона нет: AGENTS §3 п.4 говорит лишь, что wrapi оставлен для периодов шума. Детали живут только в комментариях кода (zones.cpp:38-48, map_generator.cpp:66-80 и :197, spawners.cpp:39)
- Архитектура генерации рек, дорог и света строками ПРАВДА: A* рек с согласованной эвристикой waterDist (первый поп оптимален, ленивое удаление законно) и тай-брейком по индексу ради детерминизма MSVC/libc++; вырез ниже моря, выведенный от уровня моря; река рисуется путём моря, riverOverlay удалён (shaders/macro.frag:388); спуск суша→вода внутри клетки субмира (kLandMargin, sub/base_generator.h:26); Prim MST + веерный страж; ночное свечение → SKELETON (строки с file:line из true_claims): В SKELETON нет ни одной строки про реки, дороги, свечение и знание
- Замер веерного стража: 0 несвязных королевств на 12 сидах; почти-параллельные пары рёбер 494→281 (−43 %) → problems.md (или колонка свидетеля в SKELETON): Числа нет ни в коде (politik.cpp:356-363 — только рассуждение), ни в тесте
- Входящие ссылки на macroworld.md: ARCHITECTURE.md:762, :825 → перенаправить на SKELETON (поля) / CANON S1: После сноса повиснут

# landmarks.md — ВРЁТ + ДУБЛЬ (последняя дата внутри файла 2026-09-18). Модель «одна запись, один ростер» подаётся как окончательная, но её отменил закон владельца 2026-09-22 (ландмарков нет, место — неподвижный сквад; CANON S9 от 2026-09-21, AGENTS ЗАКОН ПОЛЯ п.4). Ложны: колонки mood/famineActive, горное вето, закон деревень «квота/сепарация», «руины не порождаются», «шпиль — единственный ландмарк после зон», «tier 5 = 255». Остальное повторяет CANON S9/S10/S24.
## Вердикты НЕ в ядре (0/2)
## Ложь (8)
- `population` / `mood` / `inventory` / `garrison` / `famineActive` are the settlement's [columns] — macro/state.h:466-576 (struct Landmark); rg 'famineActive' src — 0 совпадений; macro/save.cpp:753-776 (write_landmark): Колонок mood и famineActive у Landmark нет. Суточные показания места хранятся в starvedYesterday (state.h:510), seasonWellbeing (:515), popGrowthCarry (:516), долг потребления needDebt (:575).
- `settlement_site_score` ... vetoes on water / forest-massif / mountain rock — macro/settlement_score.cpp:118-128 и :145-149: Вето осталось два: вода и внутренность лесного массива. Горное вето снесено 2026-09-18 (вердикт владельца цитируется в коде). Шапка settlement_score.h:21,91 всё ещё говорит про горное вето, то есть шапка врёт.
- Capacity answers three questions with one number: ... how many souls live there (population = rate × score, floored), and how many villages a city's land feeds (summed hinterland capacity / quota ...). Villages divide that land — separation is half the hinterland — macro/state.cpp:217-228 (комментарий: «The old separation rule, the capacity quota and the per-city cap died into the field»), :295-333 (цикл по вдавленному полю, village_pressure, гейт самопрокорма), :345-347: Деревни ставятся так: лучший кандидат по вдавленному полю берётся первым, после каждой постановки поле вокруг неё вдавливается (village_pressure). Остановка — когда вдавленный скор ≤ 0 или не проходит гейт самопрокорма. Население деревни = kVillageBornBase + бросок сида, скор на него не влияет. Формула «rate × score» верна только для городов (settlement_score.h:158-167).
- tier 1 opens at the Spire row's minZone (128 ...) and tier 5 demands its maxZone (255, "Hellgate") — macro/spires.cpp:66-71: tierStep = (255−128)/4 = 31 (целочисленное деление), gate = min(maxZone, 128 + (tier−1)·31). Для tier 5 порог равен 252, а не 255.
- Placed AFTER `generate_zones` in boot (the one landmark outside `populate_landmarks_from_politik`, which only clears the list) — macro/world_gen.cpp:228-233; macro/ruins.cpp:47-111: После зон ставятся ДВА рода мест: generate_spires, а за ним generate_ruins (руины: цель 4 + число городов, полоса опасности 51..229).
- Today `type` is still assigned only at creation (`state.cpp` ×2, `spires.cpp`) and load (`save.cpp`) ... The registry kinds Ruin / Lair / Shrine / Mine / Tower have rows but are never instantiated — macro/ruins.cpp:96 (ruin.type = LandmarkType::Ruin); macro/landmark_registry.h:300 (строка Ruin, worldPlaces=true); sub/gens/dispatch.cpp:60: Руины порождаются каждым новым миром, и в субмире у них свой режим генерации SubworldMode::Ruin. Не порождаются только Lair/Shrine/Mine/Tower; для них resolve_mode откатывается к генерации земли (dispatch.cpp:62-69).
- A second line prints `spires=placed/spells zones=[lo..hi] minPairDist` — every spell offered, every spire in the wild band — macro/world_gen.cpp:240 (int zoneMin = 9, zoneMax = 0 ...), :254-265: Минимум инициализирован старой шкалой 0..9. Шпили стоят на байтах ≥128, поэтому lo всегда печатается как 9, и строка не может показать нижний край полосы шпилей. Это дефект отчёта.
- LIVE fields — population, tier, kingdom, depleted — are resolved from `GameState` by the `{type, id}` — macro/state.h:489-494: Колонки kingdom больше нет: её заменил factionIdx, индекс в реестре фракций (королевства вырезаны 2026-09-11). Шапка landmark_grid.h:12 тоже устарела.
## Правда для SKELETON (15)
- Место — одна struct Landmark с колонкой type; все места в одном std::vector<Landmark> GameState::landmarks (РАСХОЖДЕНИЕ с ЗАКОНОМ ПОЛЯ п.4 / ГЛАДКОЙ ПАМЯТИ) (`macro/state.h:466`)
- Ростер мест объявлен как gs.landmarks (`macro/state.h:864`)
- Один сериализатор места: write_landmark/read_landmark пишут все колонки всех родов (`macro/save.cpp:753`)
- Приоритет владения клеткой: kLandmarkYieldOrder City→Village→Spire→Ruin→Lair→Shrine→Mine→Tower; for_each_landmark проходит весь ростер один раз на каждый род (O(родов·N)) (`macro/landmark_iter.h:33`)
- LandmarkGrid: слот u16 на клетку, kNoLandmark=0xFFFF, клетку берёт первый выданный; на переполнении пишется stderr, клетка остаётся пустой (`macro/landmark_grid.h:57`)
- rebake_world пересобирает зоны → сетку мест → сетку стоимости (GPU: свет и зоны сразу или через dirty-флаш); вызовы: загрузка :1903, вход в субмир :1509, сцена :1526, край выхода :3393, сезон :3575 (`app/main.cpp:1476`)
- Шпили: по одному на ординал kSpellDefs; gate = min(maxZone, minZone+(tier−1)·(max−min)/4); ослабление шагом 256/kZoneCount; выбор кандидата по Митчеллу; вето на занятую клетку (`macro/spires.cpp:57`)
- Руины: generate_ruins, цель 4+cities, полоса Ruin 51..229, население от байта опасности (landmark_born_population) (`macro/ruins.cpp:47`)
- Порядок генезиса: деревья → поле деревьев → жилы → политика → места → дороги → слой фич → зоны → шпили → руины → сетка мест → грунтовки → поля → сетка стоимости (`macro/world_gen.cpp:64`)
- settlement_site_score: вето — только вода и лесной массив; скор = Σ weight×term по строке kSettlementScoreRows (`macro/settlement_score.cpp:138`)
- Деревни: вдавленное поле (village_pressure), гейт самопрокорма, население = kVillageBornBase + бросок (`macro/state.cpp:217`)
- spawnFaction — колонка const char* ("demons" у Spire/Ruin); при спавне её разрешает faction_index(строка) — строка на пути тика (РАСХОЖДЕНИЕ с ЗАКОНОМ СЛОВАРЯ) (`sub/engine.cpp:1381`)
- Суточный тик разделён по роду: tick_settlements_ отбирает City, tick_villages_ отбирает Village; оба зовут econ_produce_day с landmark_sheet(type).skills (`macro/world_tick.cpp:404`)
- Флаг depleted у шпиля ставится прямо из субмира (мутация макромира снизу — РАСХОЖДЕНИЕ с ЗАКОНОМ ШВА); у истощённого шпиля нет свечения (macro_lighting.cpp:124) (`sub/engine.cpp:3980`)
- Во время игры ростер не меняется: add_landmark зовётся только на генезисе (state.cpp:191,369; spires.cpp:124; ruins.cpp:111) и при загрузке (save.cpp:1150) (`macro/save.cpp:1150`)
## Выжить (3)
- Отчёт генерации мира и его включатель: TIMAERT_BOOT_TRACE=1 (в Release; в Debug включён всегда, app/main.cpp:155-162) печатает `[worldgen] cities= villages= villageless= vilSpacing= nearWater%/nearPlough%/nearDeposit%` (world_gen.cpp:158-166), `[worldgen] spires=placed/spells zones=[lo..hi] minPairDist=` (:260-265), `[worldgen] ruins: N of M placed` (ruins.cpp:113), `[roads] ... first_bridge=x,y fork_bridge=x,y` (world_gen.cpp:336-343); координаты моста подаются в TIMAERT_SMOKE_MACROPOS="x,y" (app/smoke.cpp:6068-6083) для репро кадра → README (процедуры/приборы): Это единственный прибор причинности расселения и мостов. Переменная окружения не названа ни в одном ядерном документе. Попутно найден дефект zoneMin=9 (world_gen.cpp:240).
- Роды реестра без генезиса: Lair/Shrine/Mine/Tower имеют строки, но worldPlaces=false, и модуля генератора субмира у них нет (dispatch.cpp:62-69 откатывает их к земле). Смена рода и рождение мест во время игры не построены → SKELETON (слой места, строка РАСХОЖДЕНИЕ с CANON S9): Для эпика слияния мест со сквадами нужна честная точка отсчёта. Доку снесут, а сам счёт «что не порождается» в ядре не записан.
- Закон размещения шпилей: порог выводится из полосы строки реестра, шаг ослабления 256/kZoneCount, выбор по Митчеллу, вето на занятую клетку, собственный поток RNG → SKELETON (слой места): Строки ПРАВДА с file:line по шпилям в SKELETON нет. Шаг ослабления читает число ПОЛОС отображения — это надо пометить.

# features.md — ЖИВОЙ, но ВРЁТ в деталях (последняя дата внутри файла 2026-08-30). Он единственный носитель законов пролёта моста, закона опоры и закона «один движитель, одна земля» с цитатами владельца. Ложны: список фич (4 рода против 12), порядок проходов, binding 5, fell_tree_near, kFeatureOpticalCost, backend-заметка, «27 строк», «граница на 5», «нет if-цепей».
## Вердикты НЕ в ядре (3/8)
- [ru-дословно; 2026-08-30; владелец, в игре, 2026-08-30; :102; → S17] у моста дороги становятся идеально прямыми, и это бросается в глаза
- [en-пересказ; 2026-08-30; следствия вердикта владельца 2026-08-30, сформулированы агентом; :183; → S13] The player joined it. He used to be `BU_Pinned` — moved by his own input path — which is exactly why he alone paid nothing for ground or slope while every peasant did (CANON S4: player-specific code does not exist). ... One ground law for both scales ... The scale is the march's, everywhere.
- [ru-дословно; 2026-08-30; владелец, 2026-08-30; :215; → S13] как по льду скользит, невозможно играть
## Ложь (9)
- man-made structures: **road, dirt road, field, bridge** ... future: railways, canals — macro/features.h:19-37: Родов фич 12: к названным добавились FT_ClayPit/IronMine/Quarry/SilverMine/CopperMine/GoldMine (5-10), FT_Pasture (11) и FT_FlaxField (12).
- `FeatureLayer` — a byte grid stamped once at generation in pass order **DirtRoad → Road** (last-writer-wins) — macro/world_gen.cpp:204 (build_feature_layer(roads, nullptr, …)), :298 (trace_dirt_roads), :356 (stamp_field_features); macro/spawners.cpp:528-534: Сначала кладётся камень (dirtMask = nullptr), потом грунтовки: они пишут только в клетки FT_None (на воде кладут FT_Bridge), потом поля. Фактический порядок Road → DirtRoad (заполнение пустых) → Field, правила «последний пишущий побеждает» нет.
- **Macro sprite** (`u_treeMap`, binding 5, R8 = count/16384) — ../shaders/macro.frag:13-14; macro/vk_macro_renderer.cpp:325: u_treeMap стоит на binding 4. Binding 5 — это u_knowledgeMap.
- removes its `Structure::Tree` from the owning cell + composite (`SeamlessSubworldManager::fell_tree_near`) and decrements the owning macro cell's count — sub/seamless_manager.h:186; sub/seamless_manager.cpp:465; sub/engine.cpp:2230-2245; macro/macro_stock.cpp:40: Функции fell_tree_near нет. Дверь называется fell_prop_near и снимает любой проп с лут-строкой. Запись назад делает engine: macro_stock_apply(MacroStock::TreeCount, −1) → resource_field_apply(Trees). Макромир при этом мутируется прямо из тика субмира.
- `feature_layer_parity_test` (byte 4 first-class, frontier at 5, water-mask stamp law) — ../tests/feature_layer_parity_test.cpp:273-281; macro/features.h:37: Граница fail-closed стоит на FT_Count = 13, а не на 5.
- All 27 rows are the old numbers divided by the peasant's — macro/npc.h:27-73: В каталоге NPCType сейчас 46 строк. Фраза «все 27» описывает миграцию в прошлом и о сегодняшней таблице неверна.
- Add a feature → one `FeatureType` value + one placement handler + one GLSL overlay branch. No if-chains in the engine. — macro/features.h:127-156 (static_assert rows_in_enum_order); sub/gens/dispatch.cpp:78-84: Обязательна ещё строка kFeatureDefs, без неё не соберётся static_assert. Режим субмира выбирается if-цепью по байту фичи. FT_FlaxField и шесть шахт в ней не названы и проваливаются в генерацию по биому.
- `u_featureMap` is an R8 texture sampled by GLSL today; under Vulkan it becomes a sampled image read by the terrain pipeline — ../shaders/macro.frag:10; macro/vk_macro_renderer.cpp:101-111: Vulkan уже единственный бэкенд. u_featureMap — combined image sampler на binding 1 макро-пайплайна.
- `bake_light_field` spreads each emitter's light by bounded Dijkstra whose per-cell step cost is `kFeatureOpticalCost[feature]` — macro/optics.h:28-31, :50-53, :145-146; macro/features.h:90: Массива kFeatureOpticalCost нет. Цена шага: feature_optical_cost(t) = kFeatureDefs[t].opticalCost (снизу ограничена 0.05) плюс kCanopyOpticalCost·плотность.
## Правда для SKELETON (15)
- FeatureLayer: плоский uint8 на клетку; decode → FT_None для байтов ≥ FT_Count; at/set через cell_of под world_shape_ok (`macro/features.h:171`)
- Реестр фич: строка {bedWeight, opticalCost, civStrength, buildsPerDay}, порядок строк охраняется rows_in_enum_order (`macro/features.h:127`)
- У FT_Bridge колонки каменной дороги: bed 1.0 / optics 0.65 / civ 0.35 (`macro/features.h:139`)
- build_water_cross_axes: водная клетка получает бит оси, если оба ортогональных соседа по этой оси — суша (`macro/spawners.cpp:88`)
- find_path, закон моста: вода→вода запрещена, диагональ запрещена, нужен бит оси (`macro/pathfinding.cpp:164`)
- Непроходимая для моста вода отвергается ценой 167 (kRoadWaterReject), порог 166.99 (`macro/spawners.cpp:183`)
- build_feature_layer: дорога на воде биома → FT_Bridge, на суше → FT_Road (`macro/spawners.cpp:653`)
- Субмир: FT_Bridge → SubworldMode::Road; is_road_feature засчитывает мост (`sub/gens/dispatch.cpp:82`)
- Пролёт: gen_bridge_crossing; дека = kSeaLevelM + kBridgeFreeboardM (3 м); ветка для Biome::Water (`sub/gens/road.cpp:245`)
- Закон опоры: StructureKindRow::walkTile, дверь StructureIndex::walk_tile_at; движитель спрашивает её колбэком (`sub/collide.h:96`)
- Одна земля: kTileGroundWeight выведен из макро-постелей (biome_sp_weight/feature_bed_weight); скорость = terrain_speed_mult(вес); сцепление kTileGroundGrip (`sub/map_data.h:114`)
- kSubworldWalkTilesPerSecond выведен в коде (=24, static_assert); march_speed(mult); kHumanMarchMult=1.0; колонка CombatTemplate::speedMarchMult (army.h:55) (`macro/movement_cost.h:146`)
- sub/battle переименован в sub/movement: kMaxBodyCrowd=16384, UnitGrid; тест movement_steering_test (test_stop_is_a_stop_not_a_skid :1334). AGENTS.md:513,667,700,723,769 всё ещё зовут sub/battle.h/kMaxBattleUnits/battle_ai_test (`sub/movement.h:77`)
- TreeLayer: uint16 на клетку, kMaxTreesPerCell=16384, kForestClassTreeCount=8192, derived_tree_count (вода = 0, база биома + 16384·доля массива 3×3) (`macro/tree_layer.h:94`)
- Поле деревьев перезаливается на GPU только при смене revision (binding 4) (`app/main.cpp:3660`)
## Выжить (6)
- Законы пролёта моста: дека на мировом уровне kSeaLevelM+freeboard; подъём арки выводится (полметра на хорду полупролёта, с потолком); на суше рампа от высоты берега до грунта; каждый стык ниже kStepUpM; перила — своя строка Structure::Kerb. Прямая линия только между предмостьями, подходы — обычная дорога (A* + органическое петляние). Предмостье ищется от конца рукава внутрь до последней точки берега, а «берег» меряется высотой ДЕКИ. Под арками дорожные тайлы ниже воды возвращаются в воду. → CANON S17 (замысел и цитаты владельца) + SKELETON (строки sub/gens/road.cpp:40,245,314,449): В ядре лишь пятистрочная сводка (problems.md:1907-1914). Алгоритм поиска предмостья и правило «берег по высоте деки» нигде больше не записаны, как и вторая цитата владельца.
- «Один движитель, одна земля»: игрок — B_Passive тело общего движителя (set_move_intent, engine.h:482); вес тайла в единицах макро-закона, скорость 1/√w, дорога быстрее травы на 41 %; CombatTemplate::speedMarchMult — доля марша, ×0.4 игрока снят; тормоз a=v²/2d, поворот платит полный лимит ускорения, обе половины масштабируются сцеплением; лёд — одна строка сцепления → CANON S13 (закон) + SKELETON (слой микромира, file:line): Ни одно следствие вердикта владельца в ядре не записано; CANON S13 знает только «боевого режима нет».
- Карта переименований: sub/battle→sub/movement, steer_battle→steer_bodies, BattleTerrain→MoveGround, BattleUnits→BodyCrowd, BU_*→B_*, battle_ai_test→movement_steering_test, kMaxBattleUnits→kMaxBodyCrowd (movement.h:77) → SKELETON (+ правка AGENTS.md §4 п.4, §6, §7, §8 п.3-4): AGENTS.md цитирует мёртвые имена как эталон и как инвариант капа. Без карты следующий агент не найдёт носителя.
- Цитата владельца, которая живёт только в коде: «если потом в игре добавим лёд, то скольжение по льду бесплатное» (sub/map_data.h:141) — лёд есть одна строка сцепления, коду о льде знать нечего → CANON S13/S19 (рядом с CANON.md:4954 «ЛЁД — ЭМЕРДЖЕНТЕН»): Авторский текст переносится дословно. В ядре его нет.
- Потребители поля деревьев: kForestClassTreeCount — ОДИН бинарный порог (режим Forest субмира, лесная фауна, тултип, спрайт карты); калибровка разброса kTreeScatterYield (placed ≈ count, base_generator.cpp:566-576); запись назад только дельтой (нетронутый визит не меняет ничего); фичи и места не дают крон → SKELETON (слой поля, строки TreeLayer): Строка TreeLayer в SKELETON называет только форму. Порог и закон калибровки — несущие инварианты, и их нигде больше нет.
- Замки тестов и смоуков: смоук subworld_walk (app/smoke.cpp:175,469), блок консольного смоука chop, road_river_generation_test (test_road_bridges_one_cell_river / test_two_separating_straits_stay_unbridged / test_dirt_lane_lays_a_stone_bridge), subworld_generator_parity_test check_bridge_chain + негативный контроль развилки, structure_collide_test часть D → README (реестр смоуков и тестов): Правило 6 тестового закона велит при сносе механики искать её носителя. Здесь перечень носителей этих законов, и больше его нигде нет.

# biomes.md — ВРЁТ (последняя дата внутри файла 2026-08-13). Главное утверждение «каскад в четырёх CPU-копиях» ложно: с canon-audit C5 CPU-дверь одна (biome_at_cell), а biome_at мёртв. Ссылка на sub/textures.cpp ведёт на несуществующий файл. Рецепт расширения неполон. Остальное — тривиальный дубль кода.
## Вердикты НЕ в ядре (0/0)
## Ложь (5)
- **Honest state: the cascade is NOT single.** It exists in four CPU copies — `biome_at()` (`biomes.h`), `resolve_context` (`sub/engine.cpp`), `build_tree_layer` (`tree_layer.cpp`) and the zone bake (`zones.cpp`) ... `biome_at` keys Water off `height < seaLevel` while the others key off the land/water MASK — macro/map_generator.h:112-125 (biome_at_cell); macro/cell_facts.cpp:29; sub/engine.cpp:1321; macro/tree_layer.cpp:25-27; macro/fauna.cpp:326; macro/travel.cpp:39; macro/pathfinding.cpp:71; rg '\bbiome_at\b' src — 0 вызовов в продакшене (только tests/biome_classifier_test.cpp): На CPU одна дверь, biome_at_cell: сначала маска A (0 → Water), потом h ≥ 0.75 → Mountain, иначе климат. Через неё идут resolve_context (через cell_facts), дерево, фауна, путь, ход, сток и UI. biome_at вызовов не имеет. is_mountain в zones.cpp:121-125 следует тому же закону «маска, потом высота». Реальное расхождение осталось одно: шейдер bt_biome определяет воду по m.r < pc.seaLevel (../shaders/macro.frag:235).
- **Rivers reuse the `Water` override** ... so `biome_at()` returns `Water` — macro/map_generator.cpp:713-723: Резка реки опускает высоту до seaLevel8−8 и переписывает маску A, после чего Water возвращает biome_at_cell по маске. biome_at на этом пути не участвует.
- **Code:** ... [sub/textures.cpp](src/sub/textures.cpp) — rg --files src — в src/sub нет textures.cpp (есть только gpu/vk_texture.cpp): Файла не существует.
- each biome has a `BiomeConfig` (tree density/step/size, heightScale, waterLevel, swampPools, duneNoise) — sub/base_generator.h:184-197; sub/base_generator.cpp:15-27: Вместо «size» теперь метрическая полоса высоты дерева treeMinHeightM/treeMaxHeightM. Таблица kConfigs[11] позиционная, размер задан руками, охраны по enum у неё нет.
- Add a biome → one `BiomeConfig` entry + one `bt_<biome>()` GLSL function + one `bt_tex()`/matrix branch. No engine code. — macro/biomes.h:9-18, :43-46, :53-57; macro/tree_layer.h:69-83; sub/base_generator.cpp:15; sub/gens/dispatch.cpp:85-87: Нужны ещё: значение enum Biome, строка kBiomes (static_assert), строка kBiomeBaseTreeCount (static_assert) и правка ручного размера kConfigs[11]. Матрица 3×3 — код фиксированной формы. Режим субмира выбирается if-цепью по биому.
## Правда для SKELETON (9)
- Biome: 9 климатических + Water(9) + Mountain(10); kBiomes охраняется rows_in_enum_order (`macro/biomes.h:9`)
- biome_from_climate — округление к ближайшему по матрице 3×3; kMountainBiomeLevel = 0.75 (`macro/biomes.h:50`)
- biome_at_cell — ЕДИНСТВЕННЫЙ CPU-каскад клетки: маска A → Water, h≥0.75 → Mountain, иначе климат; заворот через wrap_axis (`macro/map_generator.h:112`)
- biome_at не имеет продакшен-вызовов (мёртвая дверь, жива только в biome_classifier_test) (`macro/biomes.h:69`)
- Резка реки: клетки реки опускаются ниже моря, маска A переписывается от высоты (`macro/map_generator.cpp:713`)
- Шейдер bt_biome определяет воду по высоте (m.r < seaLevel), а не по маске — РАСХОЖДЕНИЕ с CPU-дверью на граничном байте (`../shaders/macro.frag:232`)
- Синтез bt_<biome> на решётке mapSize·16; оверлей снега и льда по температуре (`../shaders/macro.frag:297`)
- BiomeConfig {treeDensity, treeStep, treeMin/MaxHeightM, heightScale, waterLevel, swampPools, duneNoise}; таблица kConfigs[11] в base_generator.cpp:15 (`sub/base_generator.h:184`)
- У макро-пайплайна 6 сэмплеров: master, featureMap, zoneMap, lightField, treeMap, knowledgeMap; u_riverMap нет (`macro/vk_macro_renderer.cpp:101`)
## Выжить (2)
- Дверь классификации клетки: biome_at_cell (маска первой) — ПРАВДА; biome_at мёртв и подлежит сносу; шейдер bt_biome — РАСХОЖДЕНИЕ (вода по высоте) → SKELETON (слой поля: строки ПРАВДА/РАСХОЖДЕНИЕ с file:line): В SKELETON дверь биома не описана вовсе. Док описывает её ложно, и после сноса никто не будет знать, что дверь одна.
- Честный рецепт нового биома: enum Biome + kBiomes + kBiomeBaseTreeCount + kConfigs (ручной размер 11) + bt_<biome> + ветка bt_tex + матрица; спутниковые таблицы по ключу Biome (kConfigs, kBiomeBaseTreeCount) — признак нарушения правила 9 DOD («у двух таблиц один ключ») → SKELETON (РАСХОЖДЕНИЕ, слой поля): Докова формула «No engine code» ложна, а список спутников по ключу Biome в ядре не перечислен.

# zones.md — ВРЁТ в несущих местах (последняя дата внутри файла 2026-08-29). Ложны: «civ-BFS не смёржен» (смёржен 2026-08-29 через optical_sweep), «гейт выхода читает байт зоны» (с 2026-09-11 не читает, у kSafeExitDanger ноль читателей), «честная заметка про 76» (коммент исправлен), «tier 5 = 255». Живо: вердикт владельца о континууме 0..255 и закон подбора спавна (CANON S12 до сих пор пишет «0–9»), урок шва периода шума. THE FIELD — дубль lore.md §1.1.
## Вердикты НЕ в ядре (3/7)
- [en-пересказ; 2026-08-24; owner, 2026-08-24 (пересказ агента); :5; → S12] A universal per-cell **danger field** — like a terrain heightmap, but for danger, and a **CONTINUUM**: one byte per cell, `0` = absolutely safe, `255` = where the strongest demons stand (owner, 2026-08-24). The ten old steps were a false discreteness ... **no mechanic may branch on a band; mechanics read the byte.**
- [en-пересказ; 2026-08-24; owner's word (пересказ агента); :88; → S12] **The match is symmetric** around strength == danger (owner's word: hell is peopled by demons, not by demons plus rabbits)
- [ru-дословно; 2026-08-24; подано как дословное, предположительно владелец (в доке атрибуции нет); :91; → S12] исчезающе малая вероятность
## Ложь (5)
- **Civ-baking: MEASURED, NOT MERGED (2026-08-29).** ... exact parity was the bar, so the BFS stays (и в «Model»: «(1) BFS civilization potential») — macro/zones.cpp:127-161: Civ-пулл идёт через optical_sweep по однородному OpticalWorld: бюджет = strength/decay, побеждает максимум. Комментарий: «Re-tied to optical_sweep by the owner's ruling (2026-08-29)». BFS удалён. Шапка zones.cpp:1-13 всё ещё говорит «BFS» и «wavelength 96», то есть шапка врёт.
- **Subworld exit gate** — `kSafeExitDanger = (3·256)/10 − 1 = 75` ... Above it, hostiles near the player block the subworld exit (`exit_blocked_by_danger`, `sub/engine.cpp`) — sub/engine.cpp:2447-2459; rg kSafeExitDanger — только определение в macro/zones.h:24: Гейт выхода = has_hostile_near_player(kDangerProximityM), и больше ничего; байт зоны он не читает (вердикт владельца 2026-09-11 в коде). У kSafeExitDanger ноль читателей, это константа-сирота.
- (Honest note: the code's own comment says `ceil(…) − 1` = 76, but the expression is integer division — the constant is 75 ...) — macro/zones.h:22-24: Комментарий в коде уже говорит «3 * 256 / kZoneCount - 1 = 75». Расхождения между комментарием и выражением больше нет.
- tier 1 opens at the row's minZone, tier 5 demands its maxZone — macro/spires.cpp:67-71: tierStep = 127/4 = 31, поэтому порог tier 5 = 128+124 = 252, а не maxZone 255.
- Reshape the whole world by editing the top-of-file `constexpr` tunables (`kCiv*`, `kMountain*`, `kWaterBoost`, `kNoise*`) — macro/zones.cpp:27-51; macro/features.h:94: Имена в коде другие: CIV_DECAY_PER_STEP, CIV_CITY_STRENGTH, CIV_VILLAGE_STRENGTH, MOUNTAIN_*, WATER_BOOST, FOREST_BOOST, NOISE_BASE_CELLS, NOISE_OCTAVES. Сила цивилизации дорог — колонка kFeatureDefs.civStrength, в zones.cpp её нет.
## Правда для SKELETON (13)
- ZoneLayer: vector<uint8> — байт опасности на клетку; at() через cell_of под world_shape_ok (`macro/zones.h:28`)
- Civ-пулл: optical_sweep от городов (1.10), деревень (0.55) и фич (kFeatureDefs.civStrength), затухание 0.06 за шаг, побеждает максимум (`macro/zones.cpp:140`)
- Глубина гор: BFS по cell_step внутрь горных клеток; буст = 0.08 + min(0.37, 0.04·depth) (`macro/zones.cpp:163`)
- Сборка: fbm − civ + горы | лес 0.04·count/16384, + вода 0.05, clamp01, floor(z·256) → байт (`macro/zones.cpp:199`)
- fBM: 5 октав с базовым периодом 128 (степень двойки, делит 1024); value_noise_wrap сворачивает период шума через wrapi (`macro/zones.cpp:50`)
- Зоны не сохраняются: это производное поле, его пересобирает rebaker (`macro/world_fields.h:12`)
- РАСХОЖДЕНИЕ: на генезисе зоны пекутся ДО грунтовок и полей (world_gen.cpp:209 против :298/:356), а rebake_world читает полный слой фич (app/main.cpp:1484). Опасность нового мира меняется на первом перепекании на civ-пулл грунтовок (0.22); шпили и руины поставлены по генезисному полю (`macro/world_gen.cpp:209`)
- Закон подбора: вес строки = row.weight × danger_match_weight(spawn_strength(row), danger) (`macro/fauna.cpp:175`)
- spawn_strength = log2(hp·dps), нормированный по таблице в 0..255; danger_match: вес делится пополам каждые kDangerHalfLife=25 байт, не больше 10 раз, минимум 1 (`macro/fauna.cpp:118`)
- Гейт выхода из субмира: только враг в радиусе kDangerProximityM, байт зоны не читается; kSafeExitDanger — сирота (`sub/engine.cpp:2447`)
- Угли опасности на карте: smoothstep(166, 255, zone) (`../shaders/macro.frag:628`)
- upload_zone_field переписывает только binding 2; из rebake_world без uploadNow заливка уходит на флаш zoneFieldDirty (app/main.cpp:3651-3654) (`macro/vk_macro_renderer.cpp:340`)
- kZoneCount=10 жив как полосы отображения (zone_band); его читает шаг ослабления шпилей 256/kZoneCount (spires.cpp:68) и смоук (app/smoke.cpp:1374-1377) (`macro/zones.h:18`)
## Выжить (4)
- Вердикт владельца 2026-08-24: опасность — континуум байта 0..255, мехника читает байт, а не полосу. Закон подбора спавна: вес = строка × ареал × danger_match; сила выводится из боевой строки; симметрия «ад населён демонами, а не демонами и кроликами»; хвосты «исчезающе малая вероятность», никогда не ноль → CANON S12: CANON.md:3901 до сих пор пишет «поле на клетках (0–9)», а CANON.md:3910 — «до появления весов контекста зона на спавн не влияет вообще». Оба утверждения устарели с 2026-08-24, и после сноса дока правда останется только в коде.
- Урок шва: период решётки шума обязан ДЕЛИТЬ сторону мира, значит быть степенью двойки (96 → 128). Замер: скачок на шве был 10.9× обычного шага соседей, полоса менялась на 39.9 % строк шва против 3.7 % в остальном мире; после правки шов 0.17× → problems.md (кейс-стади) + ссылка из AGENTS ЗАКОН АДРЕСА п.4: Числа живут только в комментарии zones.cpp:38-50. Ядро ссылается на value_noise_wrap как на законный wrapi, но не говорит, почему период обязан быть степенью двойки.
- Выведенные пороги байта: полоса строки шпиля и шаг tier (фактически 252 у tier 5), угли smoothstep(166,255), сирота kSafeExitDanger под снос, kZoneCount как единица полос, которую читает механика шпилей → SKELETON (слой поля, РАСХОЖДЕНИЯ): В SKELETON про опасность одна строка о типе (SKELETON.md:131). Сирота и шаг по полосам — находки ревью, и им нужна строка с file:line.
- Расхождение генезиса и перепекания: генезис печёт зоны без грунтовок и полей, перепекание — с ними → SKELETON (РАСХОЖДЕНИЕ) + вопрос владельцу: Док обещает «honestly RE-baked», а на деле поле нового мира не совпадает с полем после первого перехода. Шпили стоят по генезисному полю.

# economy.md — ВРЁТ, последняя дата внутри файла 2026-09-20 (СЕССИЯ 3; git 2026-09-22). В коде уже нет хлеба и зерна, коридора цены, контекстных множителей, дани по позициям, transfer_value и кошелька, стройки артелей (мосты, шахты, плуг), конечного железа с рождением жил, провианта на рейс и 1/8 ростера. Живые остатки: закон сохранения «кредит до дебета», бартер пакетом по слоту, ведомость, минт через craft_item, sp_price, законы роста без потолка. Всё это уже есть в CANON S10/S14.1/S25. Файл дублирует CANON и отстал от кода на две недели вердиктов
## Вердикты НЕ в ядре (1/32)
- [ru-дословно; ; владелец (атрибуция в коде: agent_memory.h:35-36, events/quests/quest_engine.cpp; :168; → S10] кто-то должен кому-то столько-то
## Ложь (40)
- commodity.h (the 14 nouns) — src/macro/commodity.h:99-103: В словаре 15 строк: static_assert(kCommodityCount == 15). Шапка commodity.h:111 сама говорит «14 rows», и это тоже ложь
- the bread a city bakes and the bread in the player's bag are the same row (items.cpp); seed larder of bread; bread = kGatherPerWorkerDay per baker-day — src/macro/commodity.h:79-83; src/macro/econ_day.h:143-146, 108-131; src/macro/items.cpp:107: Хлеба и зерна в каталоге нет, их вырезали 2026-09-20. Есть одна строка "food" (Provisions). Голодная строка лестницы = food, а рецепта выпечки в kRecipes нет
- one truth of MASS (weights match commodity.h verbatim — link law in econ_v1_test) — src/macro/commodity.h:43-46: Колонка weightKg в CommodityDef вырезана 2026-09-22 как второй словарь. Масса живёт только в ItemDef::weight
- Commodities tier as Raw / Vital / Instrument / Luxury — src/macro/commodity.h:28-34: CommodityTier умер 2026-09-18. «Что это за вещь» отвечает категория каталога items.h ItemType (Material/Goods/Food)
- `transfer_value` moves coin stack-by-stack … `transfer_value` remains the settlement half of scripted payments (recruit, rest, penalties); currency.h = coins + wallet math — src/macro/currency.h:6-8, 147-172; src/ui/overlays.cpp:2201: Трио кошелька (wallet_value/transfer_value/wallet_spend_up_to) вырезано 2026-09-18. Платёж контрагенту делает transfer_value_dense: стаки идут по плотности value/kg, приёмник типа Depot сразу гасит долг
- `econ_gather_day` books the take into the store before scarring the deposit — tests/econ_v1_test.cpp:181; rg econ_gather_day по src = 0: Функции нет. Добыча идёт только через агента ai_gatherer (npc_ai.cpp:1188-1195)
- The one known stray: `deliver_bag_home` (npc_ai.cpp) still debits the bag before asking the store — a full home store burns the haul — src/macro/npc_ai.cpp:145-150: Сначала store->add(id, n), и только при успехе bag->inv.remove(id, n). Кредит идёт до дебета, при полном складе груз остаётся в сумке
- an unpayable gold reward becomes a debt fact, not minted coin — src/events/quests/quest_engine.cpp:73-80, 90-101: Недоплаченная награда выдаётся тем, что есть у дающего (transfer_value_dense), плюс сообщение «The treasury runs thin». Debt-факт пишется только при нехватке на ШТРАФ
- Grain = FT_Field cells — src/macro/npc_ai.cpp:736-737: FT_Field (ResourceFieldId::Wheat) даёт строку "food". Строки grain нет
- deposit layer: the living cells ride the save WHOLE since v37, one sparse map per KIND — src/macro/deposit_layer.h:76-93: ResourceGrid cells[kDepositKindCount] — плоское поле над тором на каждый род, хеш снят (problems §52). Шапка deposit_layer.h:14 про «sparse map PER KIND» тоже стейл
- Iron is FINITE — and as the world's iron runs out, the discovery chance rises (depletion × 1/8 a day, now a Geology row of the ONE growth law) until a stone quarry strikes a new vein — src/macro/resource_field.h:114-135; src/macro/macro_stock.cpp:378-406: Домен Geology вырезан 2026-09-22 («НЕ БУДЕТ РОЖДАТЬСЯ ЖИЛ В ТЕЧЕНИЕ ПАРТИИ»). Все жилы идут законом HealToBaseline: ёмкость не убывает, взятое заживает за kWheatSeasonsToRegrow
- goal table (kGathererDefs rows: grain/wood/ore/clay/stone/silver) — src/macro/npc_ai.cpp:733-766: Строк девять: food/HomeField, food/HomeFauna (темп ×1/4), fibre/HomeFlaxField, silver, wood, iron, stone, clay, horses (rosterYield=Horse, темп 1)
- Caravan snapshots the home market at departure (AgentMemory MarketSnapshot — stock classes, a trader's memory) — src/macro/agent_memory.h:33-39; src/macro/npc_ai.cpp:1845, 1869: Ординал MarketSnapshot пуст с 2026-09-22. Что везти домой, решает ведомость дома (LandmarkLedger)
- capacity = rt.carryCap = get_carry_capacity × the row's haulMult (Caravan ×32 — the mules) — src/macro/npc.h:276, 1810; src/macro/squad.h:126-134, 151-167: haulMult задан только у лошади (8.0), у торговца по умолчанию 1.0, строки ×32 нет. carryCap = carryPerSoul × сумма спин ростера (refresh_squad_carry)
- the city crafts — today's table first (needs-ladder demand), then fair shares — src/macro/econ_day.cpp:87-99, 148-189: Три прохода «стол/доли/остатки» умерли. Каждый рабочий день уходит в рецепт с максимальной стоимостью выхода по текущей цене склада, после каждого назначения цена пересчитывается. Стейл-комментарий: world_tick.cpp:205
- `econ_consume_season` — each need covered WHOLE or not debited at all; an uncovered season is a hungry season — src/macro/econ_day.cpp:233-302; rg econ_consume_season = 0: Функции нет. econ_debt_boundary ведёт потребление как ДОЛГ: взыскивает смертью пропорционально непогашенной еде (remaining / kDaysPerSeason), выставляет новый счёт и сразу гасит его со склада
- the garrison eats and is PAID (both needs ×32, either miss = 1/8 of the roster once) — src/macro/roster_window.h:84-113: Взыскание пропорционально: доля ушедших = max(доля неоплаченного харча, доля неоплаченной платы). Доля 1/8 умерла 2026-09-21
- ONE continuous wellbeing … drives both the MOOD band and the population law daily: dP = r·P·drive — src/macro/world_tick.cpp:58-62; src/macro/econ_day.h:393, 443-470: Настроения в мире нет: в src ноль некомментарных упоминаний mood. Рост = kPopGrowthRatePerDay·P·wellbeing с wellbeing ∈ [0,1], отрицательного роста нет, убыль делает только смерть на границе
- every realm mints its own light coin … value 1 each until exchange rates arrive — src/macro/items.cpp:40-62; src/macro/faction.h:84-94: У каждой державы ТРИ монеты с номиналами 1/10/100 (copper/silver/gold), каждая бьётся из своего металла
- `FactionDef::mint` names the row's currency and `currency_for_faction_id` is its one reader … now they mint `coin_barbar` — src/macro/faction.h:94, 213-226; rg currency_for_faction_id = 0: mint — массив из трёх id, читает его faction_coins(). Монеты зовутся coin_barbar_copper/_silver/_gold
- Coin is a ware IN the package, always at FACE value on both sides — src/ui/trade_widgets.h:322-327: Ветка «coin is ALWAYS face value» умерла 2026-09-18, монета оценивается value_of как любой товар. Шапка trade_widgets.h:11-12 стейл
- scarcity = (demand + 1)/(supply + 1) po2-clamped to [1/4, 4] — src/macro/economy.cpp:46-63; src/macro/economy.h:55: Коридора нет с 2026-09-18: float(d+1)/float(s+1) без клампа, у цены только пол 1
- Charisma and context (mood / temperament) stay the one `trade_price` law on top … CHA enters via THE one `cha_trade_discount` formula; a generous merchant genuinely overpays — temperament — src/macro/economy.h:21-48; src/macro/economy.cpp:16-44, 175-177; src/macro/attributes.h:609: Контекстные множители (настроение, нрав) вырезаны 2026-09-19, cha_trade_discount вырезан 2026-09-22. Наценка = разница tradeDiscountPct двух сторон
- `FeatureDef::buildsPerDay` for raising a parcel / a span / a shaft — src/macro/features.h:160; src/macro/npc_ai.cpp:1129-1133: У feature_builds_per_day ноль вызовов: артели больше ничего не строят (вырезано 2026-09-22), колонка осиротела
- Crews are transient (rotate_worker_squads) — src/macro/npc_ai.cpp:3673-3676: Артели бессрочны с 2026-09-17: вернувшаяся домой не исчезает, распускает только суд перекомплекта
- Demand is derived: daily_demand_for flows recipe-output demand down to inputs, gated by the SITE that can run the recipe — src/macro/economy.h:100-102; src/macro/economy.cpp:85-135: Функция называется season_demand_for. Прямая часть читается из долга места, гейт рецепта — ранг ремесла в анкете (recipe_known), а не сайт
- every squad a landmark raises is loaded with bread = soldiers × (roundtrip days + the work day) (provision_squad); провиант вылазки считался по ХОРДЕ — починено — src/macro/npc_ai.cpp:3439-3448, 3866-3885, 4655-4659; rg 'provision_squad(' = только декларация и определение: У provision_squad ноль вызовов. Артель при подъёме получает груз содержания на сезон (load_season_upkeep: недостающее по needDebt + плата)
- The living plough: a crew whose parcel gave out spends its remaining cycle stamping a NEW field (plough_cell_ok) — macro-registry.md:919; src/macro/spawners.cpp:675-697: Распашка артелью вырезана 2026-09-22. plough_cell_ok/plough_field_cell зовёт только генерация мира
- the tribute is BY POSITION now: `titheOwedGoods[15]` + `titheOwedCoin` … carriers deliver them IN KIND — src/macro/state.h:540-551; src/macro/world_tick.cpp:157-170: Долг дани — одна стоимость titheOwedValue += memory_value(titheAvgValue) >> 3 от среднего inventory_value за сезон. Платится по плотности через transfer_value_dense (вердикт 2026-09-22)
- Mines: the crew's first day builds it and consolidates the connected vein cluster into the cell — src/macro/npc_ai.cpp:1129-1133; src/macro/deposit_layer.h:60; tests/deposit_reach_test.cpp:178: Стройку шахт вырезали 2026-09-22, у колонки mineFeature ноль читателей. consolidate_deposit_cluster зовётся только из теста
- The reach wave (npc_ai.cpp ReachWave) — src/macro/npc_ai.cpp:849-859; rg ReachWave по src = 0: ReachWave нет: рабочая зона = своя округа ∩ kNavHandReach, мостовой случай вырезан
- Crews build bridges … timber lays FT_WoodBridge — src/macro/features.h:64-67; src/macro/npc_ai.cpp:857-859: FT_WoodBridge и мостостроение артелей вырезаны 2026-09-22
- Wages and tithes are SEASONAL — each landmark pays on its own day of the 32-day season (ordinal % season) — src/macro/world_tick.cpp:159-162, 268: Размазка по ординалу умерла 2026-09-17. Начисление и суд — на единой границе season_boundary(day) (seasons.h:81-85, день % 32 == 1)
- Rest and quest penalties still burn through `wallet_spend_up_to` — src/macro/currency.h:6-8; src/events/quests/quest_engine.cpp:94: wallet_spend_up_to вырезан. Штраф сжигается через pay_value_dense без контрагента (по сути долг чести открыт, имя двери ложно)
- Loot-gold double mint (generate_loot_gold on top of the spawn purse) remains open — src/sub/engine.cpp:3447-3455: Кошелёк генерится только выведенному телу с пустым инвентарём (inv.used_slots()==0); tracked-тело роняет свой кошелёк. В субмире B2 закрыт. Путь сквадов squad.h:697-704 не проверял
- A single `refund_inputs()` door puts them back, and all three failure paths go through it — src/macro/econ_day.cpp:175-186; rg refund_inputs = 0: refund_inputs нет. Производство зовёт craft_item — всё-или-ничего внутри двери; отказ склада гасит кандидата на день
- Tithe rounding — ruled, not fixed … average via signed `>> 5` floors … Pre-scaling the stored average would, and was declined — src/macro/world_tick.cpp:135-157; src/macro/state.h:558-570: v104 ПРЕДМАСШТАБИРОВАЛ память (WorldMemory хранит значение × горизонт, читается memory_value), база одна — стоимость склада. Прежний «вердикт» в коде назван дефектом
- Slot hygiene: past half occupancy (>128 slots) … wired in `econ_consume_day` (fact Scrapped) and in `feed_squads_daily` — src/macro/items.h:194, 556; src/macro/econ_day.cpp:304-315; src/macro/world_tick.cpp:605: Контейнер на 1024 слота, порог kAutoScrapSlots = 512. Двери называются econ_store_hygiene и squad_bags_hygiene_daily. CANON.md:1922 с «128 из 256» тоже стейл
- the coin rows say {silver 1} → 32 — src/macro/items.cpp:256-276: yield = 128 на всех 12 монетных строках (вердикт 2026-09-18 «128 норм»), металл стоит 128×номинал
- Melting coin … floor(K×count/(2×yield)): 64 coins → 1 silver — src/macro/items.cpp:801 + 265: При yield 128: 64×1/(2×128) = 0, одно серебро дают 256 монет. Комментарий items.cpp:798-799 стейл
## Правда для SKELETON (15)
- barter_swap: пакет проверяется по слотам до сделки, расчёт на КОПИЯХ, коммит всё-или-ничего (`src/macro/currency.h:47`)
- Строка сделки = СЛОТ полки + def, а не id (BarterLine) (`src/macro/currency.h:31`)
- inventory_value = Σ value_of(экземпляр) × count (`src/macro/currency.h:228`)
- econ_produce_day делает всё, и товар, и монету, через craft_item; руки ранжируются по стоимости выхода в рабочий день, КПД = log2(pop)/4 (`src/macro/econ_day.cpp:180`)
- Потребление места = долг на границе сезона: смерть пропорциональна непогашенной еде, новый счёт, немедленное гашение (`src/macro/econ_day.cpp:233`)
- Суд границы один на всякий ростер (место, армия игрока, артель): взыскание пропорционально, плата сгорает в lootPoolValue через pay_value_dense (`src/macro/roster_window.h:72`)
- haul_between: кредит до дебета, приход в место сразу гасит needDebt (`src/macro/npc_ai.cpp:1335`)
- Добытчик ранит поле только после того, как сумка приняла груз; тейк = min(руки, наличие, влезающее по carryCap) (`src/macro/npc_ai.cpp:1152`)
- sp_price(maxSp, rate) = max(1, maxSp/rate) — одна цена действия (`src/macro/econ_day.h:277`)
- kGatherPerWorkerDay = 32 — якорь темпа (`src/macro/econ_day.h:98`)
- population_delta_per_day = (1/8 за сезон)/32 · P · wellbeing; нет потолка и отрицательного роста (`src/macro/econ_day.h:464`)
- Ведомость: publish_landmark_ledgers пишет stock_price по каждой строке с season_demand_for и неттингом; зовётся при генезисе (world_gen.cpp:387) и на границе сезона рядом с описью (world_tick.cpp:585-591) (`src/macro/econ_day.cpp:393`)
- stock_price = base × (demand+1)/(supply+1), без коридора, пол 1 (`src/macro/economy.cpp:46`)
- Минт ценностно нейтрален: static_assert(mint_is_value_neutral()) yield × номинал == стоимость состава (`src/macro/items.cpp:353`)
- Найм: hire_price_for = hireGold × soldier_level_factor (1+(L−1)/3); плата уходит в склад места transfer_value_dense, при недовозе — откат (`src/ui/overlays.cpp:2201`)
## Выжить (9)
- Замер «THE EMERGENT HAUL» 2026-09-16: seed 12345 × 256 дней против родительского коммита. Население +44 %, голодавших −32 %, запас зерна +1854 %, хлеба +1795 %, сделок +65 %. Вывод: «THE CAUSE IS THE ROAD, not the rate» → problems.md (новый §, кейс-стади), плюс перенаправить ссылку CANON.md:4077: CANON.md:4077 прямо ссылается «замер в economy.md: запас зерна ×19 за 256 дней». Со сносом файла ссылка повиснет, а таблицы нигде больше нет
- Числа СЕССИИ 3 (64 дня, сид 7, три состояния: население 255 626/255 608/257 217, хлеб в городах 398 360 → 206 034 и т.д.). Ведомость против старого мира на двух сидах: хлеб в городах −20 %/−67 %, ткань в деревнях +889 %/+447 %. Оговорка: «длинного горизонта (512 дней × несколько сидов) не мерили ни разу» → problems.md (рядом с §, где problems.md:3265 «мир качал хлеб ВВЕРХ»): Таблица есть только в NEXT_SESSION (живой промт, стирается). Оговорка про непромеренный горизонт — открытый долг метода (AGENTS §5 п.4)
- Замер 4 сида × 4 года (2026-09-01): рождено 220-228k, провал −8..−19 %, хвост растёт. Минт впервые жив — 89 568 монет на сиде 1. Инфляционный кран +57 % денежной массы за 4 года от стартовых кошельков артелей (×1400 деревень) → problems.md §34: В ядре этих чисел нет. Инфляционный кран — названный, но не закрытый хвост
- Денежный тупик 2026-08-30, замерено: день 190 — у 64 городов ~100 монет, у деревень ~230k; казна генезиса ×4 лишь оттягивает. Диагноз: у деревень нет стока монеты, у городов нет источника → problems.md (кейс-стади к S24/S10): Это обоснование феодального налогового графа и минта. Числа замера в ядре отсутствуют
- Отложенные долги экономики: отрисовка жил на карте и навык разведки, сезонный пульс урожая и истощение поля, курсы валют и биржа, вложение игрока в свой город, налоги в казну фракции → macro-registry.md (только после подтверждения владельцем, что они живы): Ни одного из этих пунктов нет в macro-registry. По закону AGENTS §0 п.4 «нет наряда там — нет наряда», и они исчезнут молча
- Открытые долги честности: штраф квеста и отдыха сжигается без контрагента (quest_engine.cpp:94 pay_value_dense). Для пути лута сквадов (squad.h:697-704) не проверено, закрыт ли двойной минт B2 → macro-registry.md: В реестре нет ни B1–B3, ни стоков без контрагента
- Процедура прибора баланса: tests/balance_run.cpp, запуск `balance_run [seedsCsv] [days] [outDir]` (по умолчанию 12345 256 balance_out). Пишет world_<seed>.tsv и landmarks_<seed>.tsv, exit 1 = сломан закон мира ([law] … FAIL). Таргет `check` его не собирает → README.md (процедура), CANON S10 уже зовёт его «дубль-прогоном»: README не упоминает balance_run ни разу, а это единственный арбитр баланса. Usage взят из шапки tests/balance_run.cpp:1-13
- Ссылки на economy.md из ядра и кода: problems.md:1930, 1945, 2244 («подробности механики — в economy.md, раздел 2026-09-09»), src/macro/npc_ai.cpp:146, src/macro/map_subject.h:62 → problems.md §40 (впитать суть «The shape now»), остальные ссылки перенаправить на CANON S10: После сноса ссылки повиснут. Механика ECON-1/2 и так описана в problems §40, но §40 отсылает за деталями сюда
- Стейл-шапки кода, повторяющие ложь этого дока: deposit_layer.h:14-21 («sparse map PER KIND», Geology), world_tick.cpp:205-207 («today's table first»), world_tick.cpp:556 (Geology), trade_widgets.h:11-12 (монета по номиналу), items.cpp:798-799 («64 coins → 1 silver»), commodity.h:111 («14 rows»). Сюда же расхождения CANON с кодом: CANON.md:1922 (порог 128/256), CANON.md:3779-3789 (предмасштабирование «отклонено»), CANON.md:3477 (мосты артелей) → SKELETON.md как строки РАСХОЖДЕНИЕ: AGENTS §0 п.2(4): «увидел в коде то, чего нет в скелете, — сперва впиши как РАСХОЖДЕНИЕ». Шапки врут ровно тем же, что и док

# nav.md — ВРЁТ частично, последняя дата внутри файла 2026-09-20. Ядро живое: округа, порталы по сегментам, граф, три чтения, nav_path_cost, событийный navEpoch, картина памяти. Всё мостовое и морское вырезано 2026-09-22, а в доке живо: мостовой сигнал, перепёк по мосту, aboard/dock/route_dry_unreachable, парусный профиль. Суть дублирует CANON S7 (CANON.md:1532-1549) и шапку nav_field.h
## Вердикты НЕ в ядре (4/8)
- [ru-дословно; 2026-09-02; стресс владельца (форма из nav.md; полная цитата владельца в tests/nav_region_te; :10; → S7] телепортни агента в глушь — выберется
- [ru-дословно; 2026-09-02; владелец (атрибуция nav_field.h:65, 77); :134; → S7] корабль делает воду дешёвой
- [ru-дословно; 2026-09-02; владелец; :147; → S4] сквады живут, где им нравится
- [ru-дословно; 2026-09-02; владелец (кап порталов 64 = ×2 над измеренным максимумом 32); :159; → S7] на всякий случай
## Ложь (8)
- Мостовой сигнал жив: жила в ничейном водяном кармане через РОВНО ОДИН разрыв от моей округи зовёт мостовую артель (CANON S10) — src/macro/npc_ai.cpp:857-859; src/macro/features.h:64-67; macro-registry.md:918: Мостовой случай вырезан 2026-09-22 вместе со стройкой артелей. Стейл-комментарий о «мостовом случае» остался в npc_ai.cpp:896
- nav_ensure перепекает при МОСТОВОМ Built; navEpoch поднимают двери add_landmark / set_landmark_type и мостовой штамп — src/macro/state.h:1023-1046; rg navEpoch: писатели только state.h:1025, 1044: navEpoch растёт только в add_landmark и set_landmark_type, мостового штампа в рантайме нет. Шапки nav_field.h:26, 90-91 (kill_landmark, мостовой штамп) стейл
- Жадный шаг остался закону двух случаев: мокрые ноги и мир без запечённого слоя — src/macro/npc_ai.cpp:483-492, 494-548: nav_step зовётся только при !flying && standingDry && baked. Жадным шагом ходят ещё летуны (v93) и всякий шаг, где nav_step вернул false (морское плечо viaSea, недостижимость)
- Профили движения: пеший читает routeNext, парус — routeNextSea; посадка/швартовка = смена стихии СОСТОЯНИЕМ (rt.aboard, счётчики корпусов у фичи), дисциплина dock — src/ecs/components.h:472-475; src/macro/nav_field.cpp:631-636, 671-674: aboard/dockX/dockY вырезаны 2026-09-22, паруса нет. routeNextSea читает только пеший nav_step как запасной путь, и на морском плече он отдаёт false
- Стихия ОБИТАНИЯ — свойство строки: пират роамит по воде, левиафан — морская фауна — rg -i 'pirate|leviathan|пират|левиафан' src/macro = 0 строк таблиц: Ни пиратской, ни морской строки в реестре существ нет. Морского хода нет
- Отказ моря fail-closed: морской рейс предлагается только когда морская таблица маршрут ЗНАЕТ (route_dry_unreachable) — rg route_dry_unreachable = 0; macro-registry.md:917: route_dry_unreachable вырезан вместе с кораблями 2026-09-22
- Прямая по тору перестала быть мерой решений — src/macro/npc_ai.cpp:1563-1575, 4011-4024: march_days_ и road_days_ в запечённом мире при kNavFar (пути нет) молча падают на хорду torus_dist_sq. Это противоречит обещанию nav_field.h:200-201 «молчаливой подмены геометрией здесь не бывает». Честно выбывает только pick_next_station_ (npc_ai.cpp:1440-1450)
- провиант вылазки считался по хорде … (починено ценой пути) — src/macro/npc_ai.cpp:3439-3448; rg 'provision_squad(' = только декларация и определение: Дверь провианта provision_squad мертва — ноль вызовов. Артели грузятся сезонным содержанием (npc_ai.cpp:3866-3885)
## Правда для SKELETON (15)
- Разбиение: одна мультиисточниковая Дейкстра от всех ландмарков по становимым клеткам пишет regionOf/distHome/stepHome (`src/macro/nav_field.cpp:145`)
- Предикат стояния один: вода становима только с FT_Bridge; марш делегирует сюда (npc_ai.cpp:3057-3060) (`src/macro/nav_field.cpp:78`)
- Водный ярус: waterRegionOf/waterDist/waterStep засеяны береговыми клетками округ, прайсер kNavSeaWeight (`src/macro/nav_field.cpp:184`)
- Порталы — лучший переход каждой 8-связной компоненты (сегмента) общей границы пары округ, отдельно сухие и водные (`src/macro/nav_field.cpp:264`)
- Кап kNavMaxPortalsPerRegion = 64; переполнение пишет в stderr И считается в portalOverflows, срез оставляет дешёвые (`src/macro/nav_field.cpp:324`)
- Планы порталов: planes = planeCount × cells × u16, поле обрезано ярусом своей стихии (`src/macro/nav_field.cpp:348`)
- Вес ребра графа = distHome[from] + distHome[to] + 16 (водное: waterDist + 32); Дейкстра по графу на каждый узел в routeDist/Next и routeDistSea/NextSea (R×R) (`src/macro/nav_field.cpp:408`)
- nav_ensure сравнивает seed, navEpoch, mapW, mapH — без прохода по миру (`src/macro/nav_field.cpp:498`)
- nav_path_cost = distHome[A] + routeDist[rA][rB] + distHome[B], в одной округе da+db, kNavFar при отсутствии пути (`src/macro/nav_field.cpp:511`)
- nav_step: своя округа — цепочка родителей цели навстречу, иначе stepHome; чужая — routeNext, потом routeNextSea, спуск по плану портала (`src/macro/nav_field.cpp:605`)
- quant16 молча зажимает цену в u16 (кванты 1/16) — потолок distHome ≈ 4095 клеток (`src/macro/nav_field.cpp:21`)
- navEpoch поднимают ровно две двери: add_landmark и set_landmark_type (у второй в src нет вызывателей) (`src/macro/state.h:1023`)
- Радиус рук kNavHandReach = 16 == kGathererReach под static_assert (`src/macro/npc_ai.cpp:854`)
- try_move зовёт nav_step только для сухих ног, не летуна, в запечённом мире; иначе жадный шаг «строго ближе» с тем же предикатом стояния (`src/macro/npc_ai.cpp:483`)
- Арбитр nav_region_test: 30 упорядоченных пар, 160 телепортов, переход set_landmark_type, portalOverflows == 0 (`tests/nav_region_test.cpp:180`)
## Выжить (5)
- Картина памяти навигации. Поля по клеткам 10 Б/клетку (regionOf 2 + distHome 2 + stepHome 1 + water×3 = 5) = 10 МБ при 1024². Четыре таблицы маршрутов 12·R² = 40.7 МБ при R≈1886. Планы 2·planeCount·1M = 26 МБ при 13 планах и 78 МБ при 39. Порталы ~112 КБ. Итого 80-135 МБ. Потолки: R = 10 000 → 1.2 ГБ, distHome — 4095 клеток по дороге (~819 по горе), зажим молчит → SKELETON.md (слой «места/навигация», со ссылками nav_field.h:107-141, nav_field.cpp:21-24, 348, 396-399): AGENTS §4 п.10 требует картину памяти системы. В SKELETON навигация представлена одной строкой (SKELETON.md:96, 132); M-52 в macro-registry называет потолки без раскладки байтов
- Замер запекания сид 7: R=1649 округ, 8178 порталов, 13 планов, полный перепёк — секунды; измеренный максимум порталов одной округи — 32 (обоснование капа 64). Маршрут до рынка у 1540/1575 деревень → problems.md (кейс-стади с горизонтом и сидом) и SKELETON как число рядом с капом: Кап 64 выведен из этого замера (ЗАКОН КОНСТАНТ). Вне шапки кода число 32/R=1649 нигде в ядре не записано
- Оговорка двери цены пути: из МЕСТА (distHome == 0) ответ точен; для двух случайных клеток одной округи завышен на дорогу через общий ландмарк, цена клетки до самой себя = «сходить домой и вернуться». Плюс РАСХОЖДЕНИЕ: march_days_/road_days_ молча падают на хорду при kNavFar (npc_ai.cpp:1563-1575, 4011-4024), у nav_path_days ноль вызовов → SKELETON.md (ПРАВДА nav_field.cpp:511-521 + РАСХОЖДЕНИЕ по хорде): Это контракт, по которому решения читают дверь; нарушение (хорда в запечённом мире) — живой дефект, которого нет ни в скелете, ни в реестре
- Полный перечень случаев жадного шага: мокрые ноги, незапечённый мир, летун, отказ nav_step (морское плечо, недостижимость). Беглецу разрешён брод, погоне нет → SKELETON.md (слой энтити/марш, npc_ai.cpp:483-548): Док называет два случая, код знает четыре. Скелет должен держать правду
- Стейл-шапки навигации: nav_field.h:25-28 и 88-93 (kill_landmark, мостовой штамп), nav_field.h:77-83 (носитель, dock, верфь), npc_ai.cpp:896 («остался ровно мостовой случай»). CANON.md:1545-1546 и CANON.md:3440-3470 всё ещё описывают мост и корабли как живые → SKELETON.md как РАСХОЖДЕНИЕ; вопрос владельцу, чинить ли CANON S7/S10 (снос 2026-09-22 зафиксирован только в macro-registry.md:917-919): Шапки и канон повторяют то, что код вырезал; после сноса nav.md это единственные носители ложного утверждения

# chronicle.md — ВРЁТ + ДУБЛЬ: законы и решения — почти дословный дубль CANON S20.1 (ведьмак, два яруса, слава, фильтр анналов, журнал, лента, зоны, запреты, ?27); таблицы дверей и запросов живые, но в SKELETON их нет. Ложь: закон аннигиляции жил и «железо родится от скудности» (оба отменены 2026-09-22), писатели revolt и Discovered (в коде их нет), контроль Robbed, EventBus::record как дверь (ноль вызовов), chronicle_recent как вид журнала. Ссылка на work_vector.md мертва (файл в history/). Последняя дата внутри файла — 2026-08-29 (git — 2026-09-22).
## Вердикты НЕ в ядре (2/16)
- [ru-дословно; 2026-08-29; owner, 2026-08-29; :159; → S20.1] «торговля — маленькое дело»
- [en-пересказ; ; owner's gameplay call (открытый вопрос); :284; → S20.1 / S11] Rumours — the journal door is built; the ACQUISITION mechanic (tavern? an encounter's word of mouth?) is the owner's gameplay call.
## Ложь (7)
- chronicle_test проверяет «the `victimShare` column with its `Robbed` control» — macro/chronicle.h:81-83 — Robbed ВЫРЕЗАН (владелец 2026-09-03); tests/chronicle_test.cpp:336-337 — контроль victimShare держит Killed; rg «Robbed» по tests/ — 0 совпадений: Рода Robbed нет; контроль столбца victimShare — FactKind::Killed против Traded
- `EventBus::record(fact)` — дверь в летопись, которой пользуются; «the frame's facts are a RANGE of sequence numbers» — events/event_bus.cpp:62-65 — метод есть; rg по src/ и tests/ — ни одного вызова EventBus::record и ни одного чтения frame_first_seq/last_frame_first_seq; все писатели зовут chronicle_record напрямую: macro/squad.h:440, macro/state.h:1186, sub/engine.cpp:1786: Дверь на шине — мёртвая мебель: факты подшиваются мимо шины, через record_deed/record_landmark_fact → chronicle_record
- «A worked-out vein LEAVES the deposit map the moment it runs dry… the «железо родится от скудности» law prospects against it. Discovery can push the live stock above the born level» — macro/deposit_layer.cpp:229-233 — ЗАКОН АННИГИЛЯЦИИ ОТМЕНЁН вердиктом 2026-09-22, ноль остаётся в клетке; macro/macro_stock.cpp:212-233 — выработанная клетка заживает через create_deposit до ёмкости deposit_virgin_at; macro/resource_field.h:130-135 — домен Geology вырезан, жилы не рождаются: Клетка жилы не уходит с карты: держит шрам и заживает к ёмкости из терраина и сида; новых жил в партии не рождается; живой запас выше родового подняться не может (deposit_apply клампит к cap)
- `chronicle_recent(sinceDay, limit)` — «the journal's view: backwards through the ring» — app/main.cpp:5231-5255 — панель Journal читает gs.player.journal (копии) через fact_sentence; chronicle_recent зовётся только из app/smoke.cpp:1272 и :3657: Журнал рисуется из собственных копий игрока (macro/journal.h), кольцо назад не обходится; у chronicle_recent нет продакшен-читателя
- Подключённые писатели: «famine and revolt transitions» — macro/world_tick.cpp:237-243 и :434-441 — пишутся только Starved (голод) и Died (вымирание места); rg -i «revolt» по src/ — только комментарий macro/chronicle.h:7: Механики бунта и писателя факта бунта нет; у мест только голод и вымирание
- Подключён писатель «a struck vein (`Discovered`, subject = the CELL — the land found it)» — rg «FactKind::Discovered» по src/ — только строка таблицы macro/chronicle.h:166; писатель ушёл вместе с доменом Geology (коммит d3f23f6, macro/resource_field.h:130-135): Род Discovered не пишет никто; строка таблицы осталась без писателя (macro-registry M-81 это предсказывал и числится «НЕ НАЧАТО», хотя в коде уже сделано)
- `chronicle_near(x, y, radius, sinceDay)` — «3×3 index cells, newest first» — macro/chronicle.cpp:107-133 — обходит (2r+1)² индекс-клеток по oy/ox, в каждой — от новых к старым; sub/engine.cpp:1743-1744 зовёт с radiusCells=0 (одна клетка): Окно задаёт радиус (3×3 — только при r=1); порядок «от новых» соблюдается внутри каждой индекс-клетки, а не по всей выдаче
## Правда для SKELETON (15)
- WorldFact — 32 байта, все поля числа, static_assert на sizeof (`macro/chronicle.h:248`)
- kFactKinds — одна строка на род (interestDays, baseRenown, victimShare), порядок строк прибит guard'ом rows_in_enum_order (`macro/chronicle.h:137`)
- Цена деяния = baseRenown + слава объекта / kRenownShareDivisor(10), если victimShare; порог фигуры kRenownToBeNamed выводится как максимум baseRenown (`macro/chronicle.h:204`)
- Капы: кольцо kChronicleFacts = 2^16, анналы kChronicleAnnals = 2^20, индекс-клетка 8×8 макро-клеток (`macro/chronicle.h:287`)
- chronicle_record: слот (seq-1)%2^16, линк в голову индекс-клетки, счётчик factsToday, в анналы — если interestDays > kChronicleWeatherDays(8) и есть именованный участник, при капе — громкий annalsFull (`macro/chronicle.cpp:50`)
- Цепочки самоусекаются: link_alive проверяет seq >= oldest_seq и seq < fromSeq, без отвязки (`macro/chronicle.cpp:25`)
- record_deed — единая макро-дверь: переводит энтити в MacroSpawnId, метит named-биты по до-деяльной славе, подшивает, платит славу (`macro/squad.h:422`)
- renown_slot/renown_of/grant_renown: слава сквада в MacroNpcRuntime::renown, места — в Landmark::renown через landmark_renown_slot; у Cell и Faction — nullptr (`macro/squad.h:366`)
- record_landmark_fact — близнец для мест: фигурность по славе места, факт, оплата base + 1/10 славы объекта (`macro/state.h:1164`)
- SubworldEngine::record_world_fact: субъект = kPlayerSquadOrdinal, клетка = содержащая макро-клетка, подшивка через record_deed (`sub/engine.cpp:1756`)
- Зоны субмира — плоский массив SubZone на 32 слота; факт пишется на ПЕРЕСЕЧЕНИИ (битмаска subZonesEntered_), дедуп — вопрос chronicle_near_kind к летописи (`sub/engine.cpp:1713`)
- player_journal_capture: резерв всего kJournalFactsCap (2^20) на первом захвате, участие (своя ординаль или носимого лорда) + локальность, nextInCell обнуляется, journalFull громкий; зовётся раз на шаг (`macro/journal.h:29`)
- SessionFeed — POD-кольцо из 8 строк, в сейв не пишется (`macro/state.h:655`)
- Сейв летописи: кольцо пишет только живые факты, линки пересобираются chronicle_rebuild_links; анналы при загрузке отвергают seq 0/из будущего и неизвестный kind (`macro/save.cpp:632`)
- Панель Journal рисует fact_sentence через app_fact_naming и показывает красные [journal full] / [world annals full] (`app/main.cpp:5230`)
## Выжить (6)
- Замер темпа летописи: фон ~80–100 фактов в день, волна голода 240–340; худший день × 64 дня интереса ≈ 21.6k, значит kChronicleFacts = 2^16 подтверждён; фильтр анналов снизил приток молодого мира с ~150 в день до ~0. Прибор — смоук chronicle_rate (app/smoke.cpp:4951) и Chronicle::factsToday. Шапка macro/chronicle.h:289-295 до сих пор пишет «never been measured» — её надо поправить → SKELETON (строка ПРАВДА у macro/chronicle.h:296 со ссылкой на smoke) + README (смоук-прибор): Числа кольца выведены из замера; в ядре есть только формула (CANON.md:5292), а шапка кода врёт
- Решение владельца 2026-08-29: Traded не платит долю славы партнёра (victimShare=false), «торговля — маленькое дело»; до этого столбца торговля славила сильнее войны → CANON S20.1: Решение есть только в комментарии кода macro/chronicle.h:126-134; в ядре нет
- Карта дверей и запросов летописи с file:line: chronicle_record, record_deed, record_landmark_fact, record_world_fact, player_journal_capture, chronicle_near / near_kind / recent / annals_of, SessionFeed; плюс продакшен-читатели кольца (tick_zones, macro/threat_field.cpp:44-58, журнал) → SKELETON (слой взаимодействия/памяти): В SKELETON о летописи только строка про wrapi (SKELETON.md:37), а таблица дока — единственная карта системы
- Открытые долги летописи: у тега LandmarkChangeOwner нет эмиттера (events/event_types.h:35); у фракции нет стора славы (macro/squad.h:378-380); GameEvent всё ещё несёт std::string s1/s2 и shared_ptr-пейлоады (events/event_types.h:73-82); NodeContext не видит мира (events/logic_nodes.h:40-47); у FactKind::Discovered ноль писателей, а M-80/M-81 в реестре числятся «НЕ НАЧАТО», хотя код уже сделан (коммит d3f23f6) → macro-registry: «Что осталось» живёт только в реестре; этих пунктов там нет, а статусы M-80/M-81 отстали от кода
- Открытый вопрос владельцу: механика получения слуха (таверна? молва встречи?); дверь журнала под неё уже построена → CANON S20.1 (как ?-вопрос): В ядре есть только «слух войдёт той же дверью» (CANON.md:5378), а сам вопрос не записан
- CANON S20.1 «УНИЧТОЖЕНИЕ ВЫРАБОТАННОГО ✓» (CANON.md:5362-5367) держит тот же отменённый закон, что и §Annihilation дока: жила не уходит с карты, Geology снят (macro/deposit_layer.cpp:229-232, macro/resource_field.h:130-135). Спиральная часть «истощённый шпиль — смена ландмарка» жива как замысел → CANON S20.1 / S10 (правка по M-87): После сноса дока ядро останется единственным носителем устаревшего закона

# map.md — ЖИВОЙ + ДУБЛЬ: архитектура тумана и карты почти целиком подтверждена кодом; законы (№1 карта — изображение, №2 память — утопленный кадр, только примитивы) — дубль CANON S11. Мелкая ложь: «сегодня v42» (сейчас v110), сейв-блок в save.cpp (сейчас world_fields.cpp), рецепт слуха «reveal_area с нулевым бюджетом» не работает, первый свип после загрузки запирает пролог. Последняя дата внутри файла — 2026-08-15 (git — 2026-09-22).
## Вердикты НЕ в ядре (1/5)
- [en-пересказ; 2026-08-15; обсуждено с владельцем и отложено (без явной атрибуции); :219; → S11] Landmark rumours — "every city known as a rumour" (start-knowledge option discussed and deferred)
## Ложь (4)
- Explored персистентен «с v40 (сегодня v42)»; «The save carries the grid whole с v40 (сегодня v42…)» — macro/state.h:356 — kSaveVersion = 110: Формат давно ушёл вперёд; «сегодня v42» — протухшая версия (повтор на строке 80)
- Таблица Files: «Save block (с v40) | src/macro/save.cpp» — macro/world_fields.cpp:46-83 (knowledge_write/knowledge_read) и :257 — строка реестра полей; macro/save.cpp:1071-1075 только зовёт write_world_fields: Знание сохраняется строкой реестра мировых полей в world_fields.cpp, отдельного блока в save.cpp нет
- Слух о ландмарке — «through the same reveal_area door with a zero budget» — macro/optics.h:92 — optical_sweep выходит сразу при budget <= 0, не посетив даже источник; macro/knowledge.cpp:59 зовёт именно его: reveal_area с нулевым бюджетом не открывает ничего; открыть только клетку ландмарка можно бюджетом > 0 меньше шага к соседу (источник посещается на d=0, соседи режутся по nd >= budget)
- Свип идёт «once after every boot/load — the sight anchor starts invalid»; окрестность спавна открывает «the first frame's sweep» — app/main.cpp:3414 — update_player_sight зовётся только при !app.revealMapOn && !app.sceneHoldsMap; sceneHoldsMap ставит пролог (app/main.cpp:1542): Пока сцена пролога держит карту, свипа нет — первый свип идёт после отпуска сцены, а не на первом кадре
## Правда для SKELETON (15)
- KnowledgeLayer — байт на клетку, три уровня 0/1/2; at() — единственная дверь чтения, fail-closed: без слоя отвечает Unknown (`macro/knowledge.h:37`)
- update_player_sight: одно сравнение клетки, иначе распад прошлого Visible в Explored и новый свип; ревизия бампается раз на пересечение (`macro/knowledge.cpp:22`)
- reveal_area метит только Explored (никогда Visible) той же оптикой (`macro/knowledge.cpp:52`)
- Один ограниченный оптический Дейкстра на всё: свечение ночи (macro_lighting.cpp:98), зрение (knowledge.cpp:40,59), поле опасности (zones.cpp:145); кроны +kCanopyOpticalCost·плотность, подъём +kClimbOpticalCost·rise, спуск бесплатен (`macro/optics.h:88`)
- Оптическая цена дорог — колонка реестра фич: FT_Road 0.65, FT_DirtRoad 0.85 (`macro/features.h:130`)
- Бюджет зрения выводится: √(2·R·kBodyEyeM)/kCellSize ≈ 4.6 клетки открытой земли (`app/main.cpp:1417`)
- Сейв знания: Visible схлопывается в Explored при записи; n==0 — слой не строили; ненулевой n обязан равняться карте, иначе r.ok=false (`macro/world_fields.cpp:52`)
- Закон утопления в конце macro.frag: drowned = mix(col, lum, 0.62)·(0.70,0.76,0.86); col = mix(drowned, col, visible)·explored; u_knowledgeMap — binding 5, u_zoneMap — binding 2, байт опасности, smoothstep(166,255) (`../shaders/macro.frag:1174`)
- upload_knowledge_field переписывает тексели на месте через update_region; вайт-айдл и пересоздание только при смене размеров; харнесс без слоя получает 1×1 Visible-тексель (`macro/vk_macro_renderer.cpp:264`)
- Гейты оверлея мира: ходоки рисуются только в Visible, пины требуют не-Unknown, ландмарки прячутся в Unknown и бледнеют в Explored, ховер отвечает «Uncharted», счёт деревьев — только в зрении (`ui/macro_overlay.cpp:380`)
- kMarkerSurface: quest/poi/danger — мир и карта, waypoint — только карта (`macro/markers.h:44`)
- Страница карты: перепись Charted % только при смене knowledge.revision; легенда перечисляет только открытые игроком роды (`ui/map_screen.cpp:235`)
- revealmap: on — все клетки Visible и в списке зрения; off — якорь сброшен, штатный распад, карта остаётся начерченной (`app/main.cpp:4045`)
- Миникарта субмира пересобирается при сдвиге центра окна или раз в >2 с (`ui/overlays.cpp:3269`)
- Каждый квест-пин открывает свою окрестность через reveal_area при смене набора маркеров (`app/main.cpp:3362`)
## Выжить (5)
- Архитектура тумана строками ПРАВДА: слой и дверь at, ритм (свип на пересечении клетки, распад — единственное забывание), reveal_area, строка сейва в world_fields.cpp, закон утопления в macro.frag, биндинги 2/5, 1×1-тексель харнесса, гейты оверлея → SKELETON: В SKELETON о знании одна строка (SKELETON.md:94), а все эти файлы и строки подтверждены кодом
- Семантика дев-команды revealmap (on/off, почему не нужен un-reveal) и того, что она подвешивает свип → README (раздел дев-консоли/проверки): Процедура проверки тумана руками; в README о консоли одна строка (README.md:137)
- Урок пера карты: береговая линия как изолиния гладкой высоты осциллировала вокруг канала в одну клетку и печатала волнистые тройные полосы; лечение — полоса чернил внутри водной клетки по каждому ребру вода↔суша, классификация по центру клетки → problems.md (кейс): Шрам дефекта рендера, в ядре не записан (есть только комментарий shaders/macro.frag:949-952)
- Будущие треки знания, которых нет в ядре: слух о ландмарке (reveal_area с бюджетом > 0 меньше шага, не нулевым), общее знание отряда и разведчиков как новые источники update_player_sight, множители восприятия/подзорной трубы в одной двери player_sight_budget_cells → CANON S11 (как замысел, после подтверждения владельцем): CANON S11 (CANON.md:3987) несёт только «виж от атрибутов» и «следопыты»; остальное пропадёт вместе с доком
- Субмир не имеет своего слоя знания по замыслу; его инструменты (миникарта HUD, карта окна 3×3) — отдельная система, которая при желании берёт язык карты, но не заводит второй туман → SKELETON (слой субмира) + CANON S11 одной фразой: Иначе следующий агент заведёт второй туман в субмире

# quests.md — ВРЁТ: раздел Known defects и строка про деревни мертвы — деревни дают квесты (app/main.cpp:490), золото платит казна дающего (events/quests/quest_engine.cpp:63-80); «engine pure» неверно; модель, глаголы, награды и производные маркеры — правда. Ссылка на ARCHITECTURE.md мертва (файл в history/). Последняя дата внутри файла — 2026-08-29 (git — 2026-09-22).
## Вердикты НЕ в ядре (0/0)
## Ложь (4)
- «Villages still give none — generate_quests_for_village … exists but is called from nowhere; the only caller path is generate_quests_for_settlement» — app/main.cpp:486-491 — при LandmarkType::Village зовётся generate_quests_for_village; строка Village в реестре объявляет kMapActQuests (macro/landmark_registry.h:295-296): Деревни выставляют доску контрактов, 1–2 квеста в день (content/quests/procedural.cpp:378-380)
- Квесты городов генерятся «from economy/distance/mood» — content/quests/procedural.cpp:119-130 (запас склада), :189-197 и :292-298 (дистанция), rng; поля настроения генераторы не читают: Контекст генерации — запас склада места, дистанция и сид; настроения в нём нет
- «The Gold reward is minted from thin air… coin is `add`ed straight into the player's inventory even though the giver settlement is known» — events/quests/quest_engine.cpp:63-80 — процедурная награда идёт transfer_value_dense из giver->inventory в сумку, тонкая казна платит сколько есть; чеканка только для авторской награды без дающего (:81-83): Дефект закрыт 2026-08-31 («деревня наняла — деревня платит»)
- «The engine is pure: it mutates quest/player state and the event bus only — never the map overlay» — events/quests/quest_engine.cpp:74-75 — списывает монету со склада ландмарка-дающего; :131 add_player_reputation двигает матрицу отношений; :77, :85, :125 пишут gs.sessionFeed: Тик движка мутирует и склад места, и матрицу отношений, и ленту сессии; маркеры он действительно не трогает
## Правда для SKELETON (11)
- Шесть глаголов целей: VisitCell, FindLocation, DeliverItems, DestroyNpc, WaitAt, InteractCell (`events/quests/quest_types.h:18`)
- Пять родов награды: Gold, Xp, Item, Reputation, Event (`events/quests/quest_types.h:48`)
- QuestEngine::tick: истечение → eval_objective по каждой цели → награды → QuestComplete/QuestFail (`events/quests/quest_engine.cpp:229`)
- Личность квеста — ординал из gs.nextQuestOrdinal при accept; оффер до принятия именуется тройкой {giver, offerSlot, bornDay} (`events/quests/quest_engine.cpp:290`)
- objective_target_cell повторяет поля eval_objective; DestroyNpc пина не получает (`events/quests/quest_engine.cpp:30`)
- rebuild_quest_markers снимает весь префикс quest_ и ставит MarkerStyle::Quest на каждую незакрытую пространственную цель (`events/quests/quest_engine.cpp:330`)
- Сигнатура quest_marker_signature (FNV, без аллокаций) гейтит перестройку в process_world_events; сбрасывается в 0 на новой игре и загрузке (main.cpp:1305, :1817) (`app/main.cpp:3320`)
- Штраф при нехватке монеты пишет факт долга в AgentMemory игрока (`events/quests/quest_engine.cpp:94`)
- Objective::subX/subY читают и пишут только строки сейва (`macro/save.cpp:913`)
- Генераторы — таблица kGenerators из 6 функций; город 2–4 оффера, деревня 1–2, пустой день — фолбэк gen_visit (`content/quests/procedural.cpp:364`)
- Пин квестов гейтится и масштабируется элементом UI-настроек QuestMarkers (`app/main.cpp:5775`)
## Выжить (4)
- Рецепт расширения: новый глагол = значение ObjectiveKind + case в eval_objective + case в objective_target_cell (если пространственный); новая награда = RewardKind + case в emit_reward; новый генератор = строка в kGenerators → SKELETON (слой событий/квестов): Структура расширения подтверждена кодом; в ядре о квестах этого нет
- Дефект: Objective::subX/subY — мёртвые координаты микромира в макро-сейве, у них нет ни генератора, ни читателя (events/quests/quest_types.h:36; macro/save.cpp:913-914, 935-936; держит их только tests/save_roundtrip_test.cpp:526) → macro-registry (наряд на снос): Нарушение S21 и ЗАКОНА ДВУХ МИРОВ; наряда в реестре нет
- Механика производного слоя маркеров (продюсер, сигнатура, сверка стейл-пинов после загрузки, reveal_area пинов) → SKELETON: Проверено по коду, в SKELETON строк о квестах нет
- Решение владельца 2026-08-31 «деревня наняла — деревня платит» (процедурная награда идёт из казны дающего, чеканит только авторская без дающего) живёт лишь в коде (events/quests/quest_engine.cpp:63-69), а док держит его как открытый дефект → CANON S20: Иначе после сноса дока закон будет записан только комментарием

# progression.md — ДУБЛЬ CANON S14 (5-5-5 / 1-1-1, закон выучивания, заглушка перков) + ВРЁТ: тег PlayerLevelUp удалён, «pure-data LogicNode[]» неверно, «ONE path» опыта неверно. Ссылка на ARCHITECTURE.md мертва (history/), «parity ledger lore.md §11» — на деле §12; «после этого стреляет дефолтное пророчество» противоречит CANON S20 ?26 (открыт). Последняя дата внутри файла — 2026-09-19 (git — 2026-09-22).
## Вердикты НЕ в ядре (0/3)
## Ложь (3)
- «the event tag survives in `event_types.h` for whoever wires this up for real» — events/event_types.h:15-18 — PlayerLevelUp в списке 16 удалённых тегов (перепись 2026-08-05); tests/quest_lifecycle_test.cpp:618 — «That tag is DELETED now»: Тега PlayerLevelUp нет; фидбеку уровня не на что опираться, его придётся заводить заново
- Уровень растёт внутри `award_exp`, «the ONE path that grants and consumes experience» — ui/overlays.cpp:826-831 — кнопка «Level Up» зовёт try_level_up напрямую; app/main.cpp:4480-4483 — консоль levelup тоже; macro/squad.h:540-553 — award_leader_xp качает безлистового лидера своим циклом по rt->xp: Грант опыта на лист идёт одной дверью award_exp (4 вызова), но расходует опыт ещё и UI-кнопка, дев-команда и отдельный цикл транзиентных NPC
- «Plot: L4 pure-data `LogicNode` factories…»; «Add plot content → one content/plot/*.cpp exporting `LogicNode[]`» — events/logic_nodes.h:28-36 — LogicNode несёт std::string id/label/next/tags и замыкания SmallFunction; content/plot/intro.cpp:189-192 — регистрация функцией register_intro_story_nodes, массивов LogicNode[] нет; content/plot/chapter_1.h:11-25 — глава 1 — заглушка, её условие всегда false: Сюжет — фабрики-функции с лямбдами и строками, которые регистрируются вызовом; «глава» пока плейсхолдер
## Правда для SKELETON (10)
- award_exp — одна дверь гранта: крутит try_level_up до порога; перегрузка с expMultPct даёт дивиденд мудрости (`macro/attributes.h:720`)
- try_level_up даёт +1 атрибут, +1 скилл, +1 перк-поинт (`macro/attributes.h:699`)
- LevelData по умолчанию: 5 атрибут-поинтов, 5 learnPicks, 5 perkPoints (`macro/attributes.h:456`)
- Двери трат: spend_attribute_point, learn_skill (0→1 и только из незнания), spend_learn_pick, spend_skill_point (отказ на ранге 0 и на kMaxSkillRank) (`macro/attributes.h:485`)
- PerkMask — 256 бит, static_assert 32 байта, has_perk/set_perk; лежит в CharacterSheet::perks (macro/character_sheet.h:43), едет в сейв v100 (macro/save.cpp:456-457) (`macro/attributes.h:399`)
- Закон скилла: множитель = ранг × pctPerRank строки, ранг зажат kMaxSkillRank = 100 (attributes.h:164) (`macro/attributes.h:348`)
- Четыре вызова award_exp: quest_engine.cpp:116, effect_applicator.cpp:58, macro/squad.h:597 (award_kill_xp), консоль addexp (`app/main.cpp:4465`)
- Узел sys_level_up удалён; реестр системных узлов держит один узел (settlement_dialog_node) (`events/node_registry.cpp:39`)
- Год = 2^20 тиков: kTicksPerDay 8192 × kDaysPerYear 128 (macro/seasons.h:48,60); kTicksPerRealSecond = 64 (`core/time.h:29`)
- Сюжетные узлы регистрируются в LogicNodeEngine на старте: register_builtin_nodes, затем register_intro_story_nodes (`app/main.cpp:1614`)
## Выжить (4)
- У прогрессии нет фидбека уровня: нет ни события, ни строки ленты, ни диалога при левелапе (тег PlayerLevelUp удалён); урок «тест, который сам подаёт событие, доказывает обработчик, а не продюсера» уже в problems.md:598-604 → macro-registry (наряд «фидбек уровня»): Долг «что осталось» живёт только в реестре; там его нет
- content/plot/chapter_1.h — плейсхолдер-узел с условием, которое всегда false (нарушение ZERO MOCKS); сюжет глав не построен → macro-registry: Док выдаёт «chapters» за построенное; реальность — заглушка
- Конфликт замысла: док говорит, что через 10 лет «стреляет дефолтное пророчество, если игрок не пошёл путём ведьмы или бунта», а CANON S20 ?26 (CANON.md:5244) держит концовку часов ОТКРЫТОЙ («пока ничего, надо думать») → CANON S20 ?26 (вопрос владельцу): Одно из двух неточно; решать владельцу, а не агенту
- Раскладка прогрессии с file:line (award_exp, try_level_up, двери трат, PerkMask, закон скилла, вызовы award_exp) → SKELETON: В SKELETON строк о прогрессии нет, хотя всё подтверждено кодом

# monsters.md — ВРЁТ, последняя дата внутри файла 2026-09-11 (git 2026-09-22). Закон одной таблицы тел и одного лута жив, и это ДУБЛЬ CANON S16/S12. Врёт в счёте рядов (пишет 52 и 30, в коде 46). Описывает снесённые спутники (kSpawnHabitats, kNpcPurse, kNpcLootId, is_creature_row), старый честный счёт фауны (faunaOverrides вместо плоского поля) и снятый дефект двойной чеканки. Строка Woodcutter и городская смесь 55/21/21/3 в коде мертвы. Ссылка на ARCHITECTURE.md битая: файла нет.
## Вердикты НЕ в ядре (1/3)
- [en-пересказ; 2026-08-07; владелец (owner's ruling); :355; → S2] a cleared ruin STANDS cleared, owner's ruling — no special cases (returning to a culled cell does NOT resurrect the culled; the repopulate-on-recenter XP/loot farm is dead)
## Ложь (14)
- kNpcTypeDefs holds fifty-two rows: sixteen roles and thirty-six creatures (kNpcTypeDefs[52]) — macro/npc.h:27-80 (enum NPCType), macro/npc.h:467 (kNpcTypeDefs[NPCType::Count]): В таблице 46 рядов. Людских родов 9 (Peasant, Merchant, Bandit, Guard, Witch, Sorceress, Adventurer, TaxCollector, RoadAmbusher), существ 37: 12 зверей, 7 монстров, Dragon, 16 рядов бестиария, Horse.
- Rows (30 today): Roles (11) peasant woodcutter merchant caravan bandit guard witch sorceress miner quarryman claydigger — macro/npc.h:27-80. Перечислителей Woodcutter/Miner/Quarryman/ClayDigger/Caravan нет. macro/npc_ai.cpp:1720: Caravan снесён 2026-09-21. CANON.md:965: роды-профессии снесены: Рядов 46, и этих пяти ролей среди них нет. kWoodcutterCombat остался только шаблоном боя, его носит TaxCollector (macro/npc.h:403, 1201).
- Row field `factionId` — faction registry id string; nullptr = the LAND decides — macro/npc.h:219-227, macro/npc.h:354: Колонки фракции в NpcTypeDef нет, так сделано намеренно. Для открытой земли есть колонка wildFaction, для места — landmark spawnFaction (macro/fauna.cpp:170, 194). Сама фракция лежит в экземпляре: ecs::NPCKind.factionIdx (ecs/components.h:147).
- `ai` — AIBehaviour vocabulary (Gatherer, Trader, Aggressive, Patrol, Flee, …) — macro/behaviour.h:18-48, macro/npc.h:558-563: Значения Patrol нет: снесено 2026-09-22, Guard переведён на Aggressive. VendorTrade и Nomad тоже снесены (macro/behaviour.h:40-44).
- creature_def_from_kind(kind) — ordinal -> row (nullptr if not a creature) — macro/fauna.cpp:282-294: Для любого валидного ординала возвращает ряд, людские включительно; природу не спрашивает (комментарий 2026-09-21). nullptr бывает только для невалидного числа.
- is_creature_row(t) (npc.h) — t >= NPCType::Rabbit, the last vestige, temporary — macro/npc.h:1883-1887 (надгробие), macro/npc.h:322 (колонка nature), macro/npc.h:1927-1934 (is_folk_kind/is_fauna_kind): Предикат снесён 2026-09-21. Природу рода говорит колонка NpcNature строки: Human/Fauna/Void.
- Registered loot profiles: 11 NPC roles (peasant woodcutter miner quarryman claydigger merchant caravan bandit guard witch sorceress) + 3 faction defaults + world props: `tree` — macro/items.cpp:416-418, macro/items.cpp:500-513: Профилей 13: 8 ролевых (peasant, woodcutter, merchant, caravan, bandit, guard, witch, sorceress), 3 фракционных (wildlife, demons, bandits) и 2 пропа (tree, crop). Профили miner/quarryman/claydigger умерли 2026-09-18.
- yield scales against a reference trunk constant `kPropYieldRefHeightM = 14 m` — sub/map_data.h:523, sub/map_data.h:607, sub/engine.cpp:2306: Такой константы нет. Опорная высота — колонка StructureKindRow::yieldRefHeightM ряда рода пропа: у Tree 14.0, у Crop своя.
- Gold is rolled by `generate_loot_gold(level, faction)` with `gold_faction_mult` (wildlife 0.1 / demons 0.6 / bandits 0.8); Known defect B2: mints coin into every corpse on top of the spawn purse — macro/items.h:671-673, macro/items.cpp:975-990, sub/engine.cpp:3449-3457: Сигнатура сейчас generate_loot_gold(npcType, level, CorpseLootContext, rng). Кошелёк берётся из колонок строки через npc_purse, gold_faction_mult в коде нет. Кошелёк чеканится только у выводимого тела с пустым инвентарём (inv.used_slots()==0), так что двойная чеканка B2 закрыта.
- Habitat is a bitmask COLUMN `kSpawnHabitats` (fauna.cpp), the sister enum-ordered table; add a creature → state its ground in its kSpawnHabitats row — macro/npc.h:340-354, macro/fauna.cpp:66-76, macro/fauna.cpp:175: Таблицу-спутник влили в строку 2026-09-22 колонками habitat / depositGate / wildFaction. roll_spawns читает npc_def(t).habitat. Устаревшие шапки врут до сих пор: macro/fauna.h:6 и :51.
- On the kHabTown stripe the old 55/21/21/3 mix stands as row weights (Peasant 55, Merchant/Woodcutter 21, Witch 3, each mining trade 21) — macro/npc.h:499 (Peasant habitat kHabTown), macro/npc.h:604 (Guard kHabTown, weight не задан = 0), macro/npc.h:514 (Merchant weight 21, habitat 0), macro/fauna.cpp:213-215: Полосу kHabTown несут только Peasant и Guard, а у Guard weight 0. Merchant, Witch и TaxCollector полосы не несут, рядов шахтёров и лесоруба нет. Поэтому pick_crowd_row на городской полосе всегда даёт Peasant.
- NPCType::Woodcutter survives as a row of the one body table (crowd flavour, street post, 60/25/15 garrison cut) — macro/npc.h:27-80, macro/npc.h:403: Такого рода нет. От него остался только шаблон боя kWoodcutterCombat, его носит TaxCollector.
- What persists is the sparse scar map `GameState::faunaOverrides` (saved since v33); wilds heal +1 head per scarred cell every `fauna_regrow_period_days()` (32 days) — macro/macro_stock.cpp:666-671, macro/state.h:977, macro/macro_stock.cpp:252-274, macro/macro_stock.cpp:362-364: Ряд FaunaCount читает и пишет поле ресурса ResourceFieldId::Fauna, шрам хранится в плоском ResourceGrid resourceScarCells. Рост идёт по fauna_growth_at: +1 за визит эпохи (kGrowthEpochDays = сезон), и только если в окрестности 3×3 жива хотя бы половина ёмкости. Выбитая долина иначе не оживает. faunaOverrides и fauna_regrow_period_days в коде нет, осталась лишь ложная шапка macro/macro_stock.h:65.
- `kNpcLootId` is a typed, enum-order-guarded table since 2026-08-29 — macro/items.cpp:517-541: kNpcLootId влит в колонку NpcTypeDef::lootId 2026-09-22. Свидетель every_npc_loot_id_resolves (static_assert) проверяет, что профиль каждой строки есть в реестре.
## Правда для SKELETON (15)
- Одна таблица тел kNpcTypeDefs[NPCType::Count]; порядок рядов = enum, прибит static_assert rows_in_enum_order (`macro/npc.h:1823`)
- FaunaEntry — псевдоним NpcTypeDef, второй таблицы существ нет (`macro/fauna.h:43`)
- Закон спавна: вес ряда = row.weight × (habitat & бит контекста) × danger_match_weight(spawn_strength(row), danger); weight 0 = никогда вслепую (`macro/fauna.cpp:174`)
- spawn_strength выводится из самой таблицы: log2(hp × dps), нормировка 0..255 по всем рядам; колонки силы нет (`macro/fauna.cpp:118`)
- danger_match_weight: половинится на каждые kDangerHalfLife = 256/10 байт промаха, пол 1/1024 (`macro/fauna.cpp:147`)
- Фракция тела: колонка места spawnFaction, иначе колонка wildFaction строки (`macro/fauna.cpp:170`)
- pick_crowd_row: полоса = landmark_def().crowdHabitat, гейт depositGate по живой жиле, фолбэк Peasant (`macro/fauna.cpp:203`)
- Открытая клетка: roll_spawns предлагает, бюджет = живой FaunaCount клетки, каждое тело рождается с BodyLoan FaunaCount (`sub/spawn.cpp:1177`)
- Ряд FaunaCount ведомости = поле ResourceFieldId::Fauna (read/apply) (`macro/macro_stock.cpp:666`)
- Рост фауны: +1 за визит эпохи, только если в 3×3 жива ≥½ ёмкости (`macro/macro_stock.cpp:256`)
- Смерть в субмире: lootId = колонка строки → npc_loot_id → фракция, ролл только в пустой инвентарь, кошелёк только у выводимого тела (`sub/engine.cpp:3429`)
- Реестр лута kLootProfiles (8 ролей + wildlife/demons/bandits + tree/crop); static_assert: каждый lootId строки резолвится (`macro/items.cpp:500`)
- grant_prop_loot: RNG от worldSeed ^ абсолютного тайла (детерминизм на место), выход × height / structure_kind_row.yieldRefHeightM (`sub/engine.cpp:2280`)
- Рубка дерева пишет в макро: macro_stock_apply(TreeCount, −1) по клетке (`sub/engine.cpp:2241`)
- Одно рождение тела emplace_body: лист записи или make_character_sheet → effective_sheet(аура сквада) → project_combat; консольный spawn идёт через spawn_npc_body → npc_type_from_token (`sub/spawn.cpp:293`)
## Выжить (9)
- Дословная цитата владельца 2026-08-20 о сносе второго каталога монстров (лорд может быть драконом/демоном/гоблином; сохраняемые NPC только в макромире) → CANON S16 (рядом с «Одна таблица монстров/NPC»): В ядре есть только закон, а авторский текст владельца по AGENTS §1 переносится дословно. Других носителей цитаты нет.
- Вердикт: зачищенная руина/клетка остаётся зачищенной, ре-центр окна не воскрешает убитых (ферма XP/лута мертва); выгрузка окна — не смерть → CANON S2 (запись-назад) или S5 (поле фауны): Вердикта нет ни в одном ядерном документе, а без него следующий агент может «оживить» спавн на ре-центре.
- Процедура «добавить существо»: один перечислитель NPCType в хвост и одна строка с именованными полями (combat, weight, habitat, depositGate, wildFaction, lootId, purseMin/Max, nature, radius). Спутников больше нет, спавн/лут/XP/спрайт/консоль подхватывают строку сами → README (раздел о контенте) и SKELETON СЛОЙ 2: В ядре нет рабочей процедуры расширения таблицы тел. Нынешний текст документа ведёт в снесённый kSpawnHabitats.
- Свидетели закона спавна и честного счёта: subworld_spawn_parity_test (опасность меняет состав, пик при совпадении, 1/1024 при полном промахе, rabbit < wolf < troll по силе), fauna_registry_test (толпа шпиля 14 видов), fauna_stock_test, смоук fauna_kill_writeback → SKELETON (строки ПРАВДА слоя 3 со ссылкой на тест): fauna_registry_test, fauna_stock_test и инвариант порядка силы в ядре не названы. Их знает только этот документ.
- Почему кошельки бестиария тонкие: кошелёк ЧЕКАНИТ монету в момент смерти, шпиль выставляет сотни тел, поэтому деньги несут только kobold 1–6, orc 2–12, cultist 2–14, lich 8–40; звери и конструкты пусты (problems.md §34) → CANON S10 (эмиссия): Обоснование чисел строки (ЗАКОН КОНСТАНТ) в ядре не записано.
- Именные дропы (филактерия лича, клинок вождя орков) принадлежат таблице артефактов. Выдумывать lootId в строке нельзя: это был бы второй словарь лута → CANON S16: Граница между реестром лута и артефактами в ядре не проведена.
- Открытые вопросы выплаты пропа: куча (путь трупа) против прямой выплаты в сумку; шкала выхода в строке лута; ключ профиля по виду (дуб/сосна); PropRow → macro-registry.md (вопрос владельцу): Нерешённый дизайн без наряда потеряется при сносе документа.
- Отложенные треки: бродячие макро-партии монстров из той же таблицы; золото как колонка лут-профиля вместо generate_loot_gold → macro-registry.md: По AGENTS §0 «нет наряда — нет наряда».
- Ложные шапки кода: macro/macro_stock.h:65 (GameState::faunaOverrides), macro/fauna.h:6 и :51 (kSpawnHabitats), macro/npc.h:376 (kNamedKinds «rows are positional aggregates» при именованных полях), macro/npc.h:1880-1882 (осиротевший «last remnant… predicate dies») → SKELETON (РАСХОЖДЕНИЕ): SKELETON п.4: увиденное в коде расхождение сначала вписывается. Шапки врут в тех местах, которые описывал этот документ.

# rpg.md — ВРЁТ, последняя дата внутри файла 2026-09-20. Каркас (лист, кубы, рекавери, аффиксы, доставка, CAST/SHOT) подтверждён кодом и ДУБЛИРУЕТ CANON S13/S14/S15. Устарели: где живёт лист игрока (сейчас компонент сквада, v91), PlayerTag (в сцене теперь AvatarTag), число скиллов (38, а не 33), закон Trade/CHA (одно производное), Foraging (читатель снесён), броня под землёй, ItemRef 40 Б. Леджер спящих строк почти весь неверен.
## Вердикты НЕ в ядре (1/21)
- [en-пересказ; 2026-09-06; владелец (owner ruling); :296; → S14] 1%/rank today — owner ruling 2026-09-06, the inline 2% died (Trade row's percent is the bargaining edge)
## Ложь (17)
- Universal CharacterSheet shared by the player (embedded in PlayerState, the only STORED sheet) — macro/state.h:691-698, macro/player_entity.h:194-202: Поля sheet в PlayerState нет с v91 (посадка Б). Лист игрока — обычный компонент CharacterSheet на его сквад-сущности, дверь player_sheet(). Именованные НПЦ тоже хранят лист.
- The player's own sheet still lives in PlayerState until landing Б moves it onto his squad's component — macro/state.h:691-698: Посадка Б проведена: лист едет в MacroNpcRecord сквада игрока (hasSheet), второй копии нет.
- `ecs::Health` is integer {hp, maxHp} (phase 4г, save v80) — ecs/pools.h:44-51: Компонента ecs::Health нет. Три бара и их carry живут в ecs::Pools {hp,maxHp,mp,maxMp,sp,maxSp,hpCarry,mpCarry,spCarry}, sizeof 36.
- Possession: the player is a movable `PlayerTag` flag … вселение = перенос флажка (sub/spawn.h) — ecs/components.h:114-127, sub/possess.h:58: В сцене тело игрока помечает AvatarTag, а PlayerTag — только макро-флаг «кем я на карте». Дверь переноса — possess_entity в sub/possess.h, её зовёт спелл possession.
- CHA: `tradeDiscountPct = CHA`; Trade: price law takes the edge from `skill_mult_of(Trade, rank) − 1`, caravan vendor via `leader_trade_rank_`, CHA's 1%/point rides by its own door — macro/attributes.h:643-652, macro/economy.cpp:24-31, macro/npc_ai.cpp:1622-1629: Производное одно: tradeDiscountPct = cha_trade_discount_pct(CHA) × skill_mult_pct(Trade)/100. Цена считается как trade_buy/sell_price(base, myTradePct, theirTradePct). bargaining_edge и leader_trade_rank_ удалены.
- Skills — 33 rows in the 64 envelope — macro/attributes.h:35-62, macro/attributes.h:316-325: Строк 38: к прежним добавлены пять ремёсел (Cooking, Blacksmith, Tailoring, Masonry, Alchemy, 2026-09-18). У ремёсел pctPerRank 0, они только открывают рецепты.
- The panel's skill tooltip is ARITHMETIC off the row, so a retuned percent cannot leave a stale sentence behind — ui/overlays.cpp:894-896, macro/attributes.h:274-277, macro/attributes.h:634-635, macro/attributes.h:672-679: Тултип печатает колонку row.effect. У Armsmaster там «ALL physical damage per rank», у Spellcraft «ALL spell power per rank», хотя их ранг с 2026-09-07 делит только ТЕМП (recovery_steps), а урон берёт сырой атрибут. Устаревшая фраза стоит в игре.
- `recompute_combat_maxima(eff)` runs ONLY when `player_standing_bonuses` changes … maxima recompute PRESERVES current pools — app/main.cpp:3467-3472, macro/player_entity.h:185-189, macro/player_entity.h:209-212, app/main.cpp:1111-1115: При смене стоячей суммы зовётся refresh_player_body, и он сохраняет ДОЛЮ каждого бара («доля у всех»), а не текущие пулы. recompute_combat_maxima нет. player_standing_bonuses — обёртка в app над universal standing_bonuses_of (squad.h).
- Worn armour protects underground by the body with PlayerTag fetching BodyEquipment off the PlayerSquadTag entity — sub/damage.cpp:48-66: Игроцкая ветка схлопнута. Любое тело читает надетое своей записи через state_of<ecs::BodyEquipment>(дверь зеркала sub/record.h) и считает worn_armor(gear, skills).
- Spellcraft adds its 5% generic layer on top via `rawSpellDamage` — macro/attributes.h:635, content/spells/spell_book.cpp:40-57: rawSpellDamage — сырой INT. Spellcraft влияет только на темп каста (recovery_steps). Процент удара спелла даёт только ранг его школы, для тега без школы это 100.
- Foraging — `feed_squads_daily`: daily bread need = roster × (100 − rank)/100 — macro/npc_ai.cpp:3450-3465, macro/roster_window.h:47-63: Скидка Foraging снесена 2026-09-22 вместе с squad_season_needs, feed_squads_daily нет. Счёт один: roster_bill по колонке рациона boardPerDay. У Foraging ноль механических читателей.
- The march heals nothing (`kMarchRecoveryPct = 0` gates all three bars) — macro/movement_cost.h:65-68: Константы kMarchRecoveryPct нет: марш просто никогда не зовёт закон отдыха.
- The instance ItemRef (save v82): affixRow[8] u8 + affixValue[8] i16 — 36 B/slot; still sleeping `material`/`quality` bytes — macro/items.h:204-232, macro/items.h:268: sizeof(ItemRef) == 40: добавлен entityId u32. Байт quality переименован в level — общую колонку уровня предмета и существа (2026-09-24).
- Ledger: the four armour skills — defense_of reads creature row + worn armour; the skills await their mitigation hook — sub/damage.cpp:28-46, sub/damage.cpp:59-66, macro/anatomy.h:283, macro/anatomy.h:296: Броня ряда умножается на sheet_armor_mult_pct(skills), носимая — через worn_armor(gear, skills) по скиллу самой вещи. Heavy/Light/Shield читаются, спит только Unarmored (0%/ранг).
- 7 of 8 weapon skills are unreachable: one weapon row exists (wpn_dagger) — macro/items.cpp:154-229: Оружейных рядов 7: wpn_dagger, wpn_sword, wpn_spear, wpn_axe, wpn_mace, wpn_staff, wpn_bow.
- Earth and Void schools have no spell in the 8-row catalog — macro/spells.h:306-573, macro/spells.h:69, macro/spells.h:514-516, macro/spells.h:553-555: Спеллов 10. possession и void_arrow с тегом Dark дают школу VoidMagic. Земли по-прежнему нет.
- Player squad foraging sleeps: Adventurer upkeep kNpcUpkeepNone — the player's roster eats no bread at all — macro/roster_window.h:47-63, macro/npc_ai.cpp:3468-3490: Рацион считается колонкой boardPerDay независимо от жалованья («зверь ест, но не получает»). roster_season_window судит каждый сквад, армию игрока тоже.
## Правда для SKELETON (15)
- PerkMask — 256 бит / 32 Б под static_assert; LevelData.perkPoints = 5 на создании, +1 каждый уровень (`macro/attributes.h:399`)
- Дверь способности: body_is_free / charge_ability / try_ability; kHarvestActSeconds 1.5 с (`sub/ability.h:37`)
- 8 атрибутов в конверте 16 слотов, все рождаются с 1, кап 255 у одной двери (`macro/attributes.h:74`)
- Потолки баров: maxHp=(база+END·10)×Bodybuilding, maxMp=(база+WIL·10)×Meditation, maxSp=база+((END+WIL)>>1)·10 (`macro/attributes.h:584`)
- quickness_pct(spd) = 100 + 100·spd/(spd+50) (`macro/attributes.h:620`)
- recovery_steps = база·10⁴/(quickPct·tempoPct generic-скилла), пол 1 шаг (`macro/attributes.h:672`)
- weapon_swing_seconds = kHandSwingS 1.5 + вес_кг × 0.5 (`macro/anatomy.h:346`)
- calculate_derived: rawPhys = STR, rawSpell = INT (сырые); tradeDiscountPct = CHA × Trade% (`macro/attributes.h:634`)
- exp_to_next_level = 1000·L·(0.1·L+1); try_level_up даёт +1/+1/+1; award_exp × expMultPct с округлением половины вверх (`macro/attributes.h:700`)
- Ранг скилла капится kMaxSkillRank = 100, конверт kMaxSkills = 64 (`macro/attributes.h:164`)
- effective_sheet: атрибуты клампятся в [1,255], ранги в [0,100], возвращает копию (`macro/character_sheet.h:408`)
- project_combat: CAST (кубы и тип спелла + INT), SHOT (без добавки атрибута), SWING (+STR); multPct = sheet_strike_mult_pct (школа / Bow / лучший оружейный или Unarmed) (`macro/character_sheet.h:514`)
- defense_of = броня ряда × sheet_armor_mult_pct + worn_armor(gear, skills) через state_of записи (`sub/damage.cpp:27`)
- grant_affixes: число = coin_run((16+power)/512, кап 8), единицы = 1 + coin_run((128+power)/512, кап 15); affix_power = 8·(lvl−1) + danger + min(64, (wealthMul−1)·64), кап 255 (`macro/items.cpp:855`)
- ItemRef = 40 Б: def u16, material u8, level u8, count i32, seed u32, entityId u32, affixRow[8] u8, affixValue[8] i16 (`macro/items.h:268`)
## Выжить (4)
- Леджер спящих колонок на сегодня: скиллы без читателя — Learning, Acrobatics, Prospecting, Quartermaster, Foraging (читатель снят 2026-09-22, macro/npc_ai.cpp:3459-3465); Unarmored 0%/ранг; пять ремёсел 0%/ранг (только открывают рецепты); у школы Земли нет спелла → SKELETON (РАСХОЖДЕНИЕ: колонки без читателя, AGENTS §4 п.9): Список нужен против «колонок-сирот», в ядре его нет, а текущий текст документа неверен по четырём пунктам.
- Долг: XP из quest_engine и effect_applicator считается по БАЗОВОМУ листу, и шапка +WIS его не масштабирует. Подтверждено: app/main.cpp:2975-2978 и :3348-3351 передают player_sheet (база), events/effect_applicator.cpp:58-60, events/quests/quest_engine.cpp:116-118 → macro-registry.md (наряд): Живой дефект против двери эффективного листа (CANON S14), наряда на него нет.
- Консольные двери осмотра: spawn <type> [level] [count] [faction], roll <itemId> [power], loot <profileId> [rolls] [power], loots, levelup [count], learn <spellId> (app/main.cpp:4125, 4376, 4642, 4633, 4471, 4497) → README (раздел «Проверка»): Это единственный ручной способ проверить аффиксы и лут-профили, а в README консоли нет.
- Фиксация: CANON S14:4571 до сих пор описывает Форажинг как cost-down дневного хлеба, хотя код снял этого читателя вердиктом 2026-09-22 (macro/npc_ai.cpp:3459-3465, «оба снести — один закон без исключений») → CANON S14 (правка неточного канона): Канон противоречит работающему коду, и по AGENTS §0 это надо назвать вслух.

# combat.md — ЖИВОЙ по сути, ВРЁТ в деталях, последняя дата внутри файла 2026-09-20. Закон двери урона, кубов, гибрида, досягаемости, враждебности, XP и согласия двух масштабов подтверждён кодом; по замыслу это ДУБЛЬ CANON S13/S14. Детали неверны: NpcDeath охраняется AvatarTag, а не PlayerTag; kNpcPurse влит в строку; sight уже авторится; kDamageKinds лежит в sub/damage.h. Документ говорит 9×9 и ссылается на CANON S13, а канон уже 8×8 (в коде 9). Ссылка на work_vector.md битая.
## Вердикты НЕ в ядре (3/10)
- [ru-дословно; 2026-09-06; владелец (owner 2026-09-06): заблокированный бронёй удар не молчит (HitFlash + D; :49; → S13] «пусть пишет всё равно»
- [ru-дословно; 2026-09-06; без явной атрибуции (ощущение плейтеста, которое снял BLOCKED); :49; → S13] «как будто не попадаю»
- [ru-дословно; 2026-08-27; владелец (Owner's design); :60; → S12] «контекст от таблицы мобов × зоны сложности (0..255) × богатство ландмарка/экономика, и расширяемо»
## Ложь (5)
- The door's fixed order ends with `NpcDeath` under the ONE `PlayerTag` guard — sub/damage.cpp:144: NpcDeath не эмитится, если на цели AvatarTag, то есть флаг тела в сцене. PlayerTag с 2026-09-10 — только макро-флаг (ecs/components.h:114-127).
- The kinds `kDamageKinds` live in the same file as the strike assembly (macro/damage_types.h) — sub/damage.h:55-81: DamageKind и kDamageKinds (колонки attributesKiller, armourApplies) лежат в sub/damage.h.
- THE purse — macro/npc.h `kNpcPurse`: one table, read by both worlds — macro/npc.h:310-335, macro/npc.h:1863: Отдельной таблицы kNpcPurse нет: влита в строку колонками purseMin/purseMax 2026-09-22. Читается дверью npc_purse(t).
- `Health` … the bar is int{hp, maxHp} (save v80) — ecs/pools.h:44-51: ecs::Health нет. Бары hp/mp/sp с их carry — поля ecs::Pools, а дверь урона пишет в pools_of(записи) (sub/damage.cpp:105).
- `sight` stays unauthored — every row answers 200 and only the alert chain reads it — macro/npc.h:439, macro/npc.h:458, macro/army.h:30, macro/army.h:84: Значение по умолчанию kNpcSightDefaultM = 200, но у RoadAmbusher sight 1000, у Dragon 60.
## Правда для SKELETON (15)
- apply_damage: пул записи (pools_of) → гард мёртвого → крит минует митигацию → mitigate → BLOCKED (HitFlash+DamageFx{blocked}) → вычесть, lethal = hp<=0 → LastHit по колонке вида → Dead один раз → NpcDeath, если нет AvatarTag (`sub/damage.cpp:97`)
- kDamageKinds: melee/spell/fall/script/dev с колонками attributesKiller и armourApplies (`sub/damage.h:75`)
- mitigate_amount: срез = max(A, dmg·A/(A+10)), kArmorHalving = 10, целочисленно (`macro/damage_types.h:112`)
- roll_strike = (NdM + flatAdd)·multPct/100 + вердикт крита; strike_mean_x2 — его матожидание ×2 для авторезолва (`macro/damage_types.h:133`)
- Кубы: roll_dice (ровно n бросков, Nd1 без RNG), crit_procs = luck·5 промилле, dice_mean_x2 = n·(m+1) (`core/dice.h:51`)
- DamageType — 9 значений (Slash…Void); ArmorProfile = uint8 × 9 колонок; uniform_armor — механический перевод скаляра (`macro/damage_types.h:35`)
- Враждебность одним порогом: factions_hostile / player_hostile_to; kHostileThreshold −64, kAllyRepThreshold 32, kKillRepPenalty −1 (`macro/state.h:1236`)
- melee_pick_target: зазор = расстояние − body_radius(цели), ранжирование по зазору (`sub/targeting.cpp:24`)
- Ширина тела — одна колонка NpcTypeDef::radius + npc_body_radius; у CombatTemplate bodyRadius нет (`macro/npc.h:1855`)
- CombatTemplate::castSpell — ординал реестра спеллов, −1 = не кастует; ряды авторят constexpr spell_ordinal("…") (`macro/army.h:135`)
- Одна цена тела в XP: npc_xp_reward = xpReward + (lvl−1)·5; им платят убийство в субмире и xp_for_fallen авторезолва (`macro/npc.h:1966`)
- fighter_power: броня = ряд × sheet_armor_mult_pct, эффективное HP ×(10+A)/10, удар — strike_mean_x2 с multPct листа (`macro/auto_battle.h:114`)
- Авторезолв платит лут теми же дверями (roll_fallen_spoils) и репутацию колонкой kill_is_no_crime (`macro/squad.h:673`)
- BattleFactSink — канал фактов авторезолва наружу без шины (L1 не видит L3) (`macro/macro_world.h:39`)
- Один гейт тела: tick_combat_recovery сливает Combat.recoverySteps каждый шаг (`ecs/systems.cpp:35`)
## Выжить (5)
- Два сознательных смещения авторезолва против очного боя: пороговая ветвь гибрида и крит НЕ кредитуются, потому что зависят от противника, а скаляр бойца его не знает. Тяжёлая броня в очном бою держит чуть больше, крит пробивает чуть больше. Свидетель — auto_battle_test (очные удары смягчаются через сам mitigate_amount) → CANON S13 (закон согласия двух исполнений): Без этого расхождение чисел двух масштабов прочтут как второй закон боя. В ядре смещения не названы.
- Свидетели двери: dice_door_test (11 проверок: границы, Nd1 без бросков, ровно n бросков, частоты крита, детерминизм, dice_mean_x2 = матожидание), damage_door_test (протокол, колонка атрибуции, два гарда, форма гибрида), melee_reach_test (пара лягушка/тролль, паритет сетки и полного скана), auto_resolve_speaks_test (факты, цена убийства, трофеи с негативными контролями), rpg_loot_test (ожидания из строки) → SKELETON (новый слой «бой», строки ПРАВДА со ссылкой на тест): Слоя боя в SKELETON нет вовсе, а dice_door_test/melee_reach_test/auto_resolve_speaks_test в ядре не упомянуты.
- Кейс «что убила дверь»: пять протокольных расхождений до одной двери (спелл-убийство игрока слало NpcDeath; бескиндовое тело — ничего; у спелла не было гарда мёртвого и труп перебивал атрибуцию; гравитация и дев-чит ставили DamageFx без HitFlash; летальность была ПРЕДСКАЗАНИЕМ hp−damage<=0) → problems.md (кейс-стади): Шрам, который оправдывает одну дверь. В problems.md его нет.
- Открытый вопрос: должна ли гибель гарнизона в авторезолве прореживать население места, которое его подняло (трек честного займа, CANON S5) → macro-registry.md (вопрос владельцу): Нерешённое без наряда потеряется.
- Расхождение канона и кода: документ и код держат 9 типов урона (Slash есть, ArmorProfile — 9 байт), а CANON S13 (CANON.md:4040-4047, вердикт 2026-09-14) требует 8×8 без Slash; перевод НЕ ПОСТРОЕН → SKELETON (РАСХОЖДЕНИЕ) + macro-registry.md (наряд перевода): Без строки в скелете следующий агент сочтёт 9×9 законом, потому что так пишет этот документ.

# spells.md — ДУБЛЬ CANON S15, местами ВРЁТ, последняя дата внутри файла 2026-09-09. Устройство книги, ординалов, эффектов, шпилей и гейта подтверждено кодом. Неверно: реестр из 8 рядов (в коде 10: possession, void_arrow), Spellcraft внутри rawSpellDamage, гарнизон шпиля как дичь клетки, Spire.spellId. Закон активных способностей живёт только здесь и в lore.md. Ссылка на ARCHITECTURE.md битая.
## Вердикты НЕ в ядре (2/6)
- [en-пересказ; 2026-08-14; владелец; :66; → S15] Living in the world layers per ARCHITECTURE.md Rule 13 (owner 2026-08-14): the spell registry is askable by worldgen, the subworld and the event applicator alike
- [en-пересказ; 2026-08-06; владелец (owner's ruling); :82; → S15] Every active ability in the game is a spell — owner's ruling, 2026-08-06: cleric miracles, magebane dispels, black-energy powers are rows in THIS registry; at most one extensible `kind` field, never a second registry
## Ложь (4)
- Registry kSpellDefs: fireball, ice-shard, magic-bolt, lightning-chain, energy-beam, armageddon, haste, flight (8 rows); chainCount filled in 8 rows — macro/spells.h:306-573, content/spells/effects.cpp:262-275: Рядов 10: добавлены possession (Dark/Void, Targeted) и void_arrow (Dark/Void, 26d1). kSpellEffects тоже 10 рядов, void_arrow летит через spawn_magic_bolt.
- the row index rides saves as `Spire.spellId` — macro/state.h:527, macro/spires.cpp:104-112: Отдельного типа Spire нет. Шпиль — Landmark типа LandmarkType::Spire в gs.landmarks, ординал лежит в Landmark::spellId.
- The spire is guarded by the demon family of its own cell's headcount — macro/spires.cpp:114-122, macro/fauna.h:75-76: Гарнизон шпиля — его НАСЕЛЕНИЕ (§42 Инк 5): landmark_born_population из колонок реестра × tier спелла. Линия дикой фауны шпиля снята.
- Spellcraft stays the generic half inside `rawSpellDamage`; Body/Mind spells read Spellcraft alone — macro/attributes.h:635, content/spells/spell_book.cpp:40-57: rawSpellDamage — сырой INT. Spellcraft делит только темп каста (recovery_steps), а у тега без школы процент удара 100: Spellcraft урон не множит вовсе.
## Правда для SKELETON (13)
- SpellBook: learned[4]×u64, activeSpell i32 (−1 = нет), sustained[4]×u64, sustainedDrainCarry; sizeof == 80 под static_assert (`macro/spell_book_state.h:34`)
- kSpellBookCapacity = 256, static_assert громко ловит kSpellCount > 256 (`macro/spell_book_state.h:29`)
- Битовая математика только в двух дверях spellbook_bit / spellbook_bit_set (`macro/spell_book_state.h:58`)
- spellbook_rule_active(book, SpellRuleId) — правило по колонке rule строки, без строковых сравнений (`macro/spell_book_state.h:93`)
- spell_ordinal — constexpr ординал строки kSpellDefs, −1 для неизвестного (`macro/spells.h:587`)
- kSpellEffects параллелен kSpellDefs по ординалу; static_assert на счёт и совпадение id (`content/spells/effects.cpp:277`)
- kSpellTagDefs: тег → dmgType + school (Ice→Water, Lightning→Air, Dark→Void, Light→Arcane, Body/Mind → SkillId::Count) (`macro/spells.h:66`)
- spell_mult_pct = ранг школы спелла, 100 без школы; spell_strike зовёт roll_strike с кубами строки, добавкой INT и LCK кастера (`content/spells/spell_book.cpp:53`)
- spellbook_cast заряжает Combat.recoverySteps тела-кастера через recovery_steps(row.recovery, Spellcraft); пер-спелловых таймеров нет (`content/spells/spell_book.cpp:157`)
- Цепь молнии мертва (H5): снаряд рождается с chainDecayPct/chainRadius/chainRemaining = 0, и apply_spell_chain выходит на первом гарде (`sub/spell_effects.cpp:340`)
- generate_spires: по шпилю на ряд kSpellDefs, spellId = ординал, ворота зоны по tier с релаксацией (`macro/spires.cpp:56`)
- SpireDepleted → аппликатор учит книгу и отдаёт SpellLearned фоллоуапом вызывающему (`events/effect_applicator.cpp:90`)
- spell_registry_test прибивает первые 8 ординалов (fireball…flight) и отображение id↔ординал (`tests/spell_registry_test.cpp:17`)
## Выжить (6)
- ЗАКОН АКТИВНЫХ СПОСОБНОСТЕЙ: всякая активная способность — строка реестра спеллов (чудеса клириков, развеивания, силы тёмной энергии), максимум одна расширяемая колонка kind, без второго реестра; чудо клирика ЕСТЬ ересь, которую жжёт его орден → CANON S15: Вердикт владельца 2026-08-06 в ядре не записан, есть только в lore.md.
- Процедура «добавить спелл»: дописать строку kSpellDefs (порядок вечен, шпили и книги носят ординал) и строку kSpellEffects (static_assert не пропустит расхождение; nullptr для селф-баффа). Шпиль, его место и этажи выводятся из строки → README (раздел контента) и SKELETON (слой магии): В ядре нет рабочей процедуры.
- Цепь молнии мертва (canon-audit H5): поля цепи рождаются нулями (content/spells/effects.cpp:111-112), apply_spell_chain выходит сразу (sub/spell_effects.cpp:340), SpellDef::chainCount никто не читает; spell_casting_effects_test сознательно ничего не утверждает (tests/spell_casting_effects_test.cpp:652-662, в комментарии устаревшее имя registry.cpp) → SKELETON (РАСХОЖДЕНИЕ) + macro-registry.md (наряд): «H5» и apply_spell_chain в ядре не упомянуты ни разу; без записи мёртвая механика читается как рабочая.
- Шов обучения: движок эмитит SpireDepleted (id шпиля + ординал), аппликатор учит книгу и возвращает SpellLearned (квесты следят за спеллами, не за шпилями; эмит после обхода span). Три двери в идемпотентный spellbook_learn: орб шпиля, консоль learn, награда квеста → SKELETON (слой событий): Порядок «emit после span» — знание, которое нужно для работы, а в ядре его нет.
- Правило полёта снаряда, которое наследует каждый новый спелл: вылет из глаза кастера, свип отрезка за тик, первое тело на пути, фракционная слепота, смерть о терраин, кладку и грань окна 3×3; скорость не ухудшает попадание (так звался баг сквозных выстрелов в упор) → SKELETON (слой боя/магии, после проверки кодом sub/spell_effects.cpp): Эти инварианты полёта описаны только здесь и в microcombat.md, который тоже под сносом.
- Правило 13: реестр спеллов лежит в слоях мира (macro/spells.h), поэтому его спрашивают worldgen (generate_spires), субмир и аппликатор. Сам документ ARCHITECTURE.md, на который ссылаются spells.md/monsters.md/rpg.md, отсутствует → CANON S26 (или AGENTS §11 слои): Источника правила в репозитории больше нет, все три ссылки на ARCHITECTURE.md битые.
