# СКЕЛЕТ — СИСТЕМЫ МИРА КАК ОНИ ЕСТЬ В КОДЕ

> **ЭТОТ ФАЙЛ ОПИСЫВАЕТ РЕАЛЬНОСТЬ, А НЕ ЗАМЫСЕЛ.** Замысел живёт в `CANON.md`,
> планы — в `macro-registry.md`. Здесь только то, что ИСПОЛНЯЕТСЯ, со ссылкой
> `file:line` на точку исполнения. Каждая строка помечена:
>
> · **ПРАВДА** — код делает ровно это;
> · **РАСХОЖДЕНИЕ** — замысел говорит одно, код делает другое; названо, чем
>   именно, и какой наряд реестра это закрывает.
>
> Владелец, 2026-09-22: «надо какой подход системный чтобы мы всегда
> действовали в рамках системы без шапок чтобы у меня был полный контроль что
> делаем… прям озвучиваем все системы КАК ЕСТЬ ВНУТРИ В КОДЕ а не как шапки
> названия и замыслы… чтобы это всегда было ЯВНО».
>
> **ПРАВИЛО ФАЙЛА:** сюда нельзя записать то, что не проверено греп-ссылкой.
> Утверждение без `file:line` — не строка этого файла. Шапка модуля
> доказательством НЕ является: шапки в этом проекте врали, и не раз.

---

## СЛОЙ 0 — СКЕЛЕТ: СВЯЗНЫЙ ТОР ИЗ КЛЕТОК

**Всё опирается на это. Ничего ниже нет.**

**ЗАКОН АДРЕСА (владелец, 2026-09-23):** «мир абсолютно всегда степень двойки и
он всегда связный тор клеточный ЭТО ЯДРО ЯДРИЩЕ САМОЕ»; «мир всегда степень
двойки и всегда квадратный и дефолт 1024х1024 и всегда связный тор».

| что | как в коде | статус |
|---|---|---|
| мир — решётка клеток 1024 × 1024 | `mapW = 1024, mapH = 1024` (`state.h:847`); ползунок `1 << mapSizeLog2`, 7…12 (`ui/screens.cpp:387-392`) — всегда квадрат, всегда степень двойки | **ПРАВДА** |
| инвариант формы мира проверяется ОДНОЙ функцией | `world_shape_ok` (`core/torus.h:43`) | **ПРАВДА** (с 2026-09-23) |
| мир отказывает незаконной форме ГРОМКО и в точке РОЖДЕНИЯ | `generate_terrain` (`map_generator.cpp:736-750`) печатает отказ и возвращает пустой терраин | **ПРАВДА** (с 2026-09-23) |
| адрес клетки — ОДНО число, заворот МАСКОЙ | `cell_of` / `cell_x` / `cell_y` (`core/torus.h:65-75`) | **ПРАВДА** (с 2026-09-23) |
| соседство есть арифметика индекса, и у неё есть ДВЕРЬ | `cell_step(idx, dx, dy, side)` (`core/torus.h:88`) | **ПРАВДА по форме**; жильцов пока двое (`deposit_layer.cpp river_adjacent`, свидетель) — **остальные 30 обходов соседей макромира ещё расписывают шов руками**, наряд **M-97** |
| `wrapi` — только для периодов, которые миром НЕ являются | `map_generator.cpp river_lattice` переведён на маску 2026-09-23; остаток — **128 вызовов в `src/macro/`, не разобранных поштучно** | **РАСХОЖДЕНИЕ**, наряд **M-97** |

**Почему это скелет, а не деталь.** Плоский массив над тором И ЕСТЬ связный
мир: соседняя клетка — соседняя память. Хеш `клетка → значение` хранит значения
и выбрасывает связность, и цена этого уже заплачена: геология была хешем, и в
день, когда у неё спросили «есть ли жила рядом», ответом стал скан 69 624 жил
на клетку — 4.3 мс из 6.8 мс пересечения шва (`problems.md §52`).

### ⚠ ЗДЕСЬ СТОЯЛА ЛОЖЬ, И ЕЁ СТОИТ ПОМНИТЬ

До 2026-09-23 эта таблица утверждала: «мир СВЯЗНЫЙ — тор, краёв нет | **одна
свёртка на весь проект: `wrapi`**; шесть рукописных копий снесены | **ПРАВДА**».
Перепись того дня нашла в МАКРОМИРЕ: **семь** разных механизмов заворота, **пять**
именованных дверей-алиасов, **две** рукописные свёртки, **шесть** тел половинного
размаха (четыре из них — внутри самого `core/torus.h`), **девять** дверей индекса
с двумя ОМОНИМАМИ `cell_index` (`ecs/components.h:50` с маской и
`map_generator.cpp:130` вообще без свёртки) и **30** обходов соседей.

Строка была написана по НАМЕРЕНИЮ — шесть копий действительно снесли однажды, и
автор решил, что этим вопрос закрыт. **«Дверь одна» — утверждение, которое
проверяется переписью, а не помнится.**

### ЦЕНА ДЕЛЕНИЯ, НАЗВАННАЯ ЧИСЛОМ

`wrapi` заворачивает через `std::int64_t %` по РАНТАЙМ-делителю, то есть
аппаратным целочисленным делением (~20-40 тактов против одного у маски), и
звался в `src/macro/` **143** раза. Два ответа на вопрос «где эта клетка» —
`ecs::cell_index` (маска) и `ResourceGrid::index` (деление) — давали одно и то
же ТОЛЬКО потому, что мир квадратный. Совпадение по случаю — не согласие.

Свидетель — `tests/torus_wrap_test.cpp`, 15 792 проверки, с негативным
контролем: наивный `idx + 1` на шве ОБЯЗАН врать.

---

## СЛОЙ 1 — ПОЛЯ НАД СКЕЛЕТОМ

**ЗАКОН (AGENTS.md, владелец 2026-09-22): любое число, привязанное к клетке
мира, есть ПОЛЕ над миром.** Буквально число в каждой клетке, а не переносное
значение.

### Форма поля — одна на все

`struct ResourceGrid` (`resource_field.h:277`): `width`, `height`, плоский
`vector<uint16>` по клеткам, счёт живых клеток, диск досягаемости, ревизия.
**ОДНА ДВЕРЬ ЗАПИСИ** — `ResourceGrid::write`, и всё производное (счёт живых,
диски, ревизия) двигается внутри неё, а не рядом. — **ПРАВДА**

### Двери к клетке — все ведут в один адрес (2026-09-23)

| слой | дверь | статус |
|---|---|---|
| `ResourceGrid` | `index` → `cell_of` (`resource_field.h:312`) | **ПРАВДА** |
| `DepositLayer` | `wrap_index` → `cell_of` (`deposit_layer.h:130`) | **ПРАВДА** |
| `FeatureLayer` | `at`/`set` → `cell_of` (`features.h:280,286`) | **ПРАВДА** |
| `TreeLayer` | `at` → `cell_of` (`tree_layer.h:119`) | **ПРАВДА** |
| `ZoneLayer` | `at` → `cell_of` (`zones.h:46`) | **ПРАВДА** |
| `KnowledgeLayer` | `at` → `cell_of` (`knowledge.h:61`) | **ПРАВДА** |
| `LandmarkGrid` | `at` + сборка → `cell_of` (`landmark_grid.h:46`) | **ПРАВДА** |
| `NavWorld` | `cell` → `cell_of` (`nav_field.cpp:74`) | **ПРАВДА** |
| `ecs::cell_index` | → `cell_of` (`ecs/components.h:55`) | **ПРАВДА** |
| `PathCostData` | **двери НЕ БЫЛО ВОВСЕ**; заведена 2026-09-23: `live`/`cost_at`/`water_at`/`height_at` (`pathfinding.h:61-76`) | **ПРАВДА** |
| `TerrainData` | три канала НЕ ЗАВОРАЧИВАЛИ; заведён заворот 2026-09-23 (`map_generator.h:77-88`) | **ПРАВДА** |

**УРОК, ОПЛАЧЕННЫЙ ДВУМЯ ЧАСАМИ МАШИННОГО ВРЕМЕНИ.** Проверка формы мира НА
ЧТЕНИИ умеет только соврать потише: fail-closed у терраина значит «нулевая
высота», то есть «вода», а мир целиком из воды не отказывает размещателю
поселений — он заставляет его искать сушу ВЕЧНО. `settlement_placement_test` на
мире 96×96 провисел два часа и не упал ни разу. **Отказать умеет только
проверка в точке РОЖДЕНИЯ, и она обязана говорить вслух.**

### ⚠ ГЛАВНОЕ РАСХОЖДЕНИЕ СЛОЯ: ОДНА СИСТЕМА — ТРИ КОНТЕЙНЕРА

Закон полей единый (`kResourceFields` — десять рядов), а данные лежат в ТРЁХ
структурах: шрамы в `GameState::resourceScarCells[10]` (`state.h:966`), жилы в
`DepositLayer::cells[6]` (`deposit_layer.h:101`), лес в `TreeLayer`. Реестр
ходит между ними указателями `carrierRead`/`carrierApply`.

Из десяти заголовков массива шрамов работают ТРИ — семь стоят пустыми, потому
что их данные живут в двух других контейнерах. — **РАСХОЖДЕНИЕ**, наряд **M-91**.

Найдено ПЕРЕПИСЬЮ ШТАБЕЛЕЙ (`src/core/stacks.h`) в день её появления, по
числам в её выводе, а не по чтению шапок.

### Какие поля сегодня лежат над миром

| поле | где | что в клетке | статус |
|---|---|---|---|
| терраин (высота, влажность, температура) | `TerrainData::rgba` | 4 байта на клетку | **ПРАВДА** |
| лес | `TreeLayer` | счёт деревьев | **ПРАВДА** |
| ДЕСЯТЬ ресурсных рядов, из них своё поле шрамов держат ТРИ | `GameState::resourceScarCells[10]` (`state.h:966`) | шрам; у семи рядов-носителей заголовок пуст, данные в двух других контейнерах | **РАСХОЖДЕНИЕ** — M-91 |
| ШЕСТЬ рядов жил | `DepositLayer::cells[6]` (`deposit_layer.h:101`), `uint16` | текущие юниты | **ПРАВДА сегодня, ПОДЛЕЖИТ СВЁРТКЕ в три:** `ores` + `stone` + `clay`, род металла уезжает в байт фичи шахты — наряд **M-94** |
| фичи | `FeatureLayer::data` (`features.h`) | 1 байт вида | **ПРАВДА** |
| стоимость пути + вода | `PathCostData` | вес шага | **ПРАВДА** |
| опасность | `ZoneLayer` | байт 0..255 | **ПРАВДА** |
| округи и порталы навигации | `NavWorld` | ординал округи | **ПРАВДА** |
| числа фич | `GameState::worked` (`state.h:975`), `uint16` | смысл задаёт ФИЧА на той же клетке: у порта корабли, у шахты залежи, у парцеллы урожай, **у города — ВСЯ ПАСТВА** | **РАСХОЖДЕНИЕ** — поле есть, а чисел в нём нет: единственный жилец (корабли) вырезан 2026-09-22. Ждёт своих чисел. Наряды **M-78**, **M-88** |

### Поле чисел фич — закон смысла

Число агностично, смысл ему задаёт фича, стоящая РОВНО на той клетке; второго
числа там быть не может, потому что клетка разрабатывается ровно одним способом
(`CANON.md:1323`). — **ПРАВДА по форме**, носителей пока нет.

**НАСЕЛЕНИЕ — ЖИЛЕЦ ЭТОГО ПОЛЯ, И ЭТО ВСЯ ПАСТВА, А НЕ ДУШИ ДОМА** (владелец,
2026-09-22: «ВСЯ ПОПУЛЯЦИЯ ГОРОДА ЛЕЖИТ В ЭТОМ ЧИСЛЕ ТО ЕСТЬ НЕ ТОЛЬКО РОСТЕР
НО И СКВАДЫ ГОРОДА В ПОЛЕ»). Три величины, одна арифметика:

| величина | носитель |
|---|---|
| вся паства места | число поля под клеткой места |
| души дома | ростер места |
| души в походе | паства − ростер |

Сегодня носителя у паствы нет, и оттого `souls_home` и `souls_flock`
(`labour.h:46-47`) возвращают одно поле байт в байт: город, у которого
полпаствы в артелях, РАСТЁТ как маленький и РИСУЕТСЯ маленьким. Шапка
`labour.h:36-45` описывает закон верно и сама признаёт, что он не исполнен. —
**РАСХОЖДЕНИЕ**, наряды **M-8**, **M-9**, **M-88**.

Потолок проверен числом: `uint16` против наблюдаемого максимума 6 300 на
четырёх сидах и проектного потолка столицы 8 192 (`settlement_score.h:154`) —
восьмикратный запас.

### Закон количества в клетке — ОДИН, и редкость в него не входит

`deposit_virgin_at` (`deposit_layer.cpp`): сколько в клетке = насколько шум
перевалил за свой гребень, растянутое на ширину клетки — **одинаково для всех
родов**. — **ПРАВДА** (с 2026-09-22)

**⚠ ФЕРТИЛЬНОСТЬ СЛЕПА К РЕЛЬЕФУ.** `wheat_baseline` (`macro_stock.cpp:164-172`)
читает ТОЛЬКО канал влажности; высота в неё не входит, поэтому мокрая ГОРА
несёт полный потенциал пшеницы. Запрет на гору в `parcel_ground_ok_` снят
2026-09-23 по вердикту «единственный запрет мира — вода», и тем обнажил дыру:
парцелл стало +21…+34 % на четырёх сидах, вся дельта на скалах. —
**РАСХОЖДЕНИЕ**, наряд **M-101**: лечится ВЕСОМ (`f(влажность, высота)`), а не
возвратом запрета.

Редкость живёт в двух колонках, и третьей не бывает: **порог** решает, сколько
клеток вообще несут род, **цена каталога** — сколько он стоит. Замерено на
сиде 7: золото 66 клеток, глина 281, серебро 634, железо 1182, медь 1760,
камень 36 632.

### Закон роста — ОДИН

`resource_fields_daily_growth` (`macro_stock.cpp`), две ветки:

* **`HealToBaseline`** — ёмкость клетки есть чистая функция терраина и сида, она
  не убывает никогда; убывает и заживает только взятое. Пшеница, лён, зверь,
  табун и **вся руда**. — **ПРАВДА** (с 2026-09-22)
* **`CarrierSpread`** — **ЕДИНСТВЕННОЕ ИСКЛЮЧЕНИЕ, названное вслух:** лес.
  Базовой нет, потому что нетронутая земля зарастает ГУЩЕ девственной, и
  прибавка пропорциональна 3×3. — **ПРАВДА**

Мир навещает клеток/32 в день (`kGrowthEpochDays = kDaysPerSeason`), поэтому
дорогой шум ёмкости спрашивается только на сезонном срезе, а горячий путь
читает сохранённое число.

### Добыча — ОДНА ДВЕРЬ

`resource_field_apply` зовётся вне реестра ровно из двух строк, и это один и тот
же цикл `ai_gatherer` (`npc_ai.cpp`): выход в ростер и выход в сумку. Все
ДЕСЯТЬ рядов берутся там. — **ПРАВДА**

Два законных пути к клетке, и оба живут (`enum class Worksite`, `npc_ai.cpp`):
**напрямую из поля** (лес, зверь) либо **из поля через ФИЧУ** (пашня, лён,
пастбище, жилы). Фича НЕ ХРАНИТ запас — она метка «здесь работают», а число
лежит в поле той же клетки. — **ПРАВДА**

---

## СЛОЙ 1½ — ВРЕМЯ

**ЗАКОН (AGENTS.md, владелец 2026-09-22): калибровать только в тиках, никогда в
реальных секундах.**

| что | как в коде | статус |
|---|---|---|
| одна величина времени, остальное выводится | `WorldTime::tick` — ОДНО поле (`core/time.h`); день, час, минута читаются с него | **ПРАВДА** |
| сутки 8192 тика, сезон 32 дня, год 128 дней | `kTicksPerDay = 8192` (`time.h:30`) | **ПРАВДА** |
| ни один закон мира не читает реальных секунд | единственное вхождение в `src/macro/` — `kAiPeriodSeconds` (`npc_ai.h:35`), и оно помечено «Presentation only; the AI itself never reads it» | **ПРАВДА** — проверено грепом 2026-09-22 |
| микромир тоже шагает квантом, а не кадром | `constexpr float dt = sm::kStepSeconds` (`app/main.cpp:3398`) — ARPG получает ПОСТОЯННЫЙ dt | **ПРАВДА** |
| один оборот цикла = один тик | `frame(app, simSteps)` (`main.cpp:5561`); CANON:285 | **ПРАВДА, и следствие названо:** на 144 Гц мир проживается в 2.25 раза быстрее по настенным часам. Симуляция ИДЕНТИЧНА (тиков в сутках всё равно 8192) — это неодинаковый ТЕМП на разном железе, а не дефект мира. Мелкий хвост, не крупная работа |

## СЛОЙ 2 — ЭНТИТИ

**ЗАМЫСЕЛ (владелец, 2026-09-22):** «ЕДИНАЯ СИСТЕМА ЭНТИТИ ОНИ ВСЕ ОДИНАКОВЫЕ у
них ДАЖЕ РАЗМЕР СТРУКТУРЫ В ПАМЯТИ ОДИНАКОВЫЙ… если мы преаллоцируем память для
16к энтити сквадов в макромире то неважно будет что они там кто-то город кто-то
артели кто-то лорды кто-то корованы… ЭТО ДОЛЖНА БЫТЬ ГЛАДКАЯ ПАМЯТЬ
ФИКСИРОВАННОЙ ДЛИНЫ ПУСТОТА НЕ СТРАШНО МЫ ПЛАТИМ ОСОЗНАННО ЦЕНУ».

| что | как в коде | статус |
|---|---|---|
| у всех сквадов одинаковый набор компонент | `kMacroSquadBytes` (`npc_spawn.cpp`) перечисляет 13 компонент, `static_assert` сторожит сумму | **ПРАВДА** |
| размер сквада известен числом | 12 745 Б; 16384 таких = 199 МиБ | **ПРАВДА** |
| ядро субъекта = инвентарь + ростер | 12 360 Б, и `static_assert` сторожит, что у МЕСТА оно то же самое | **ПРАВДА** |
| род сквада — колонка, а не класс | `SquadType` (`squad_type.h`) | **ПРАВДА** |
| память ГЛАДКАЯ, фиксированной длины, преаллоцированная | `entt::registry` (`ecs/world.h:9`) — разрежённые множества, аллокация по мере рождения, у разных сквадов РАЗНЫЙ набор компонент (лист и приказы — только части) | **РАСХОЖДЕНИЕ.** Форма верная по составу, но не по раскладке: гладкого массива фиксированной длины нет. Наряда в реестре ПОКА НЕТ — это строка, которую надо завести |
| существо и предмет — один контейнер | таблицы существ и предметов раздельны | **РАСХОЖДЕНИЕ** — пункт 2 плана владельца, наряды **M-71**, **M-73** |

---

## СЛОЙ 3 — МЕСТА

| что | как в коде | статус |
|---|---|---|
| место есть неподвижный сквад | `std::vector<Landmark>` (`state.h:855`), отдельная структура с собственными `x`, `y`, `population` (`:470-471`) | **РАСХОЖДЕНИЕ, и самое крупное.** Канон сам признаёт: «ЗАКОН ЗАПИСАН, СТРУКТУРЫ НЕ СЛИТЫ» (`CANON.md:987`). Наряд **M-90** — уничтожить полностью |
| число клетки лежит в поле | `population` лежит записью списка рядом с `x`/`y`; «что в этой клетке» отвечается перебором, и перебор стоит в коде 19 раз | **РАСХОЖДЕНИЕ** — прямое нарушение ЗАКОНА ПОЛЯ. Наряд **M-88** |
| гарнизон — тот же ростер, что у сквада | `Landmark::garrison` — тип `Roster`, тот же | **ПРАВДА** |

---

## СЛОЙ 4 — ВЗАИМОДЕЙСТВИЕ С МИРОМ

**ЗАМЫСЕЛ:** одна система — ПЕРЕМЕЩЕНИЕ / ЦЕЛЬ / ПРЕДИКАТ.

| что | как в коде | статус |
|---|---|---|
| ноги одни на всех | `try_move` (`npc_ai.cpp`) | **ПРАВДА** |
| цель есть `cell` / `what` / `onArrival` | цели нет как формы: `targetX/Y`, `targetSettlementId`, `lairX/Y` лежат порознь (`ecs/components.h`) | **РАСХОЖДЕНИЕ** — наряды **M-1**, **M-3** |
| марш и прибытие идут диспетчером | 14 машин зовут ноги и ритм каждая сама | **РАСХОЖДЕНИЕ** — наряд **M-2** |
| у каждой машины есть хвостовой `else` | у девяти из четырнадцати нет: сквад в незнакомом состоянии стоит вечно | **РАСХОЖДЕНИЕ** — наряд **M-4** |

---

## СЛОЙ 5 — ПРОИЗВОДСТВО

| что | как в коде | статус |
|---|---|---|
| производство идёт крафтом | один `craft_item`, состав/выход/труд — строка каталога | **ПРАВДА** |
| выпуск отвечает цене труда | `run_recipe(i, 1<<30, …)` — потолок-заглушка; коридор цены `0.25…4.0` | **РАСХОЖДЕНИЕ** — наряд **M-31** |

---

## ЧТО ВЫРЕЗАНО НАСОВСЕМ (2026-09-22, сессия 14)

Не «отложено», не «заглушено» — **отсутствует в коде**. Если следующий агент
видит упоминание — это мусор, и его надо снести, а не восстановить.

корабли · порты · брошенные корпуса · морской ход · верфь · деревянный мост ·
мостостроение артелью · строительство шахт артелью · строительство загонов ·
плуг · рождение новых жил · обратная связь «родится там, где скудно» · закон
аннигиляции выработанной жилы · колонка `worksRow` · слой-счётчик кораблей ·
колонка `unitScale` («насколько жирно гнездо этого рода»)

---

## ПОРЯДОК, В КОТОРОМ СКЕЛЕТ ОДЕВАЕТСЯ

Владелец: «потом мы начинаем работать надевать НА СКЕЛЕТ осмысленно, каждая
вещь новая должна быть системная универсальная элегантная и минимум систем без
всяких каких-то чисел рандомных непонятно где, каких-то служебных векторов и
прочего кала».

0. **АДРЕС КЛЕТКИ** — ✅ закон, дверь и девять слоёв сделаны 2026-09-23
   (`cf74fd93`, `89e16b02`, `135791b0`); ОСТАЛОСЬ: 30 обходов соседей на
   `cell_step` и 128 вызовов `wrapi` разобрать поштучно (**M-97**)
1. **Поля ресурсов** — ✅ закон роста и закон количества сделаны 2026-09-22
   (`d3f23f6b`, `463e9165`, снос `unitScale`); ОСТАЛОСЬ: один штабель вместо
   трёх контейнеров (**M-91**) и свёртка шести полей жил в три (**M-94**)
2. **Объекты: существо == предмет, контейнер 32×32** — M-71, M-73
3. **Сквады: слияние мест и сквадов ТЕХНИЧЕСКИ** — M-90, M-88
4. **Перемещение: цель / предикат / место** — M-1…M-6
5. **Производство через крафт** — M-31

**ПРАВИЛО ОДЕВАНИЯ.** Ни один шаг не считается сделанным, пока в этом файле не
появилась строка со статусом ПРАВДА и ссылкой `file:line` на точку исполнения.
Статус «сделано» по намерению не ставится никогда.
