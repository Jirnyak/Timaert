# Codex Progress

## Long-Run Self Prompt

Work only in `C:/Timaert` and its folders. Ignore Hecton instructions for this repo. Source authority order: TypeScript/Svelte in `C:/Timaert/src` is gameplay authority, `C:/Timaert/proto_c` is UX/reference authority when relevant, `C:/Timaert/timaert_c` is the C++ port to ship. Follow `C:/Timaert/AGENTS.md`, `C:/Timaert/timaert_c/AGENTS.md`, and `C:/Timaert/timaert_c/matwej.md`.

Do not ask the user for permission during normal work. Pick the next highest-priority visible regression or TS-parity gap from `matwej.md`, read the matching TS source before editing, then make the smallest C++ change that closes the gap. Do not invent gameplay behavior when TS already defines it. Do not refactor unrelated code. No exceptions, no RTTI, no `std::rand`, no per-frame allocation in hot paths, no per-file CMake edits for normal source files.

Work loop:
1. Re-read this progress file and the relevant `matwej.md` section.
2. Read the TS authority and current C++ implementation.
3. Compare against `5b16b69160e4621517292bbdca773fb04b47ea2c` when fixing regressions.
4. Patch only the narrow files needed.
5. Add or update one focused test/smoke when possible.
6. Run rare targeted builds/tests only after a coherent patch, not continuously.
7. Record concise progress here with commands and results.

Current priority after the shoreline placeholder patch: continue P0/P1 items in order. Prefer visible player regressions first: roads, macro shorelines, then TS-parity gameplay surfaces. Full builds and runtime screenshots are useful, but do them sparingly because the machine may be under load.

## 2026-05-17

- Task: P0.1 subworld shoreline/water regression from `timaert_c/matwej.md`.
- Local rules read: `C:/Timaert/AGENTS.md`, `C:/Timaert/timaert_c/AGENTS.md`, `C:/Timaert/timaert_c/matwej.md`.
- Current finding: TS `src/game/subworld/base-generator.ts` treats the heightmap as the underwater source of truth and rewrites water/shore/land tiles after height generation. Current C++ clamps heights from pre-existing `TILE_WATER` instead, so water-biome cells cannot expose above-water shore/land created by neighbour blending.
- Change in progress: added a C++ heightmap-to-water-tile sync before the water-plane clamp, with a focused coastal water-cell generator test. Also changed async seam placeholders from flat grass `WATER_LEVEL + kLandMargin` to a cheap macro water/land approximation, with a water-placeholder async test case.
- Verification:
  - Targeted MSVC build: `subworld_generator_parity_test` and `subworld_async_seam_test` PASS. CMake emitted the existing SDL2_mixer deprecation warning; no compiler warnings in the rebuilt targets.
  - `subworld_generator_parity_test.exe` PASS.
  - `TIMAERT_SUBWORLD_ASYNC_CASE=water_placeholder subworld_async_seam_test.exe` PASS.
  - Full `subworld_async_seam_test.exe` PASS. Reported water plane: `water=5015484 land=4421700 badWater=0 badLand=0 maxWater=0.40000 minLand=0.42000`.

- Task: P0.2 road regression audit/fix.
- Finding: current native road tracing still used the documented A* road model, but the large-map search budget had been cut to `4096` expansions. That caused same-land detours to be pruned before routing around water barriers. Seed 42 boot smoke before the fix: `cities=66 attempted=153 kept=23 pruned=130 componentPruned=45 expansions=388367`.
- Change: restored the large-map road A* budget to the last known-good native baseline `200000` without adding a direct-line fallback or changing water blocking/component pruning. Updated `road_river_generation_test` so the synthetic same-land detour crosses the old `4096` cap and must now survive while still not stamping rejected water cells.
- Verification:
  - Targeted MSVC build: `road_river_generation_test` PASS.
  - `road_river_generation_test.exe` PASS.
  - Targeted MSVC app build: `timaert` PASS; no compiler warnings in the rebuilt output.
  - Runtime boot smoke: `TIMAERT_BOOT_TRACE=1 TIMAERT_SMOKE_SEED=42 TIMAERT_SMOKE_SCRIPT=new_game,wait_boot_done,wait_visible,quit build-msvc/timaert.exe` PASS. Road stats after fix: `cities=66 attempted=153 kept=89 pruned=64 componentPruned=45 expansions=6854018`.
  - Not yet done: same-seed screenshot A/B against `5b16b69160e4621517292bbdca773fb04b47ea2c`; macOS Ninja build is not available from this Windows workspace.

- Task: P0.3 macro shoreline audit.
- Finding: compared TS/C++ macro shoreline data flow and rejected the tempting river-cell exception patch. TS derives macro shoreline visuals from master height (`master.r < seaLevel`) and does not classify post-carve river cells as a separate shoreline water mask in the renderer. Temporary C++ `macro_renderer.cpp` river/shore patch was reverted; no final macro shoreline code change is recorded for this item yet.
- Current suspect list for future work: native `map_generator.cpp` alpha/mask semantics still diverge from TS (native alpha is land/water; TS alpha is road/city mask), and native river generation/terrain mask order still needs a narrow same-seed visual A/B before changing renderer behavior.

- Task: P1.1 visible settlement/city subworld generation.
- TS authority: `SubworldScreen.svelte` marks any `gameState.settlements` cell as `landmark='city'`, villages as `landmark='village'`; `spawn.ts::populateCell()` spawns city/village population before fauna. Native generators already stamped roads/houses/walls/fields, but the 3D renderer only uploaded tree billboards, so houses/walls were invisible; city/village fauna tables are empty and native had no replacement citizen spawn.
- Change:
  - `sub/engine.cpp` and `sub/gens/dispatch.cpp`: settlement cells now resolve as `City` without a population threshold, matching TS.
  - `sub/renderer_3d.{h,cpp}`: added a cheap instanced opaque box pass for generated `Structure::House` and `Structure::Wall`, using existing `mgr.structures()` data. No old synchronous city generator reintroduced.
  - `sub/spawn.cpp`: added bounded city/village population projection with visible `Peasant`, `Merchant`, `Woodcutter`, `Witch`, and `Guard` roles before fauna return; city/village fauna remains empty like TS but settlement NPCs now exist separately.
  - `tests/subworld_spawn_parity_test.cpp`: added a city projection invariant.
  - `app/main.cpp`: added smoke action `enter_first_settlement` to move to the first settlement, enter subworld, and assert structures plus citizens exist.
- Verification:
  - Targeted MSVC build: `subworld_spawn_parity_test` and `timaert` PASS.
  - `subworld_spawn_parity_test.exe` PASS: `fauna=6 seed=324478056 zone=5 water_squad_blocked=1 city_projection=1`.
  - Runtime smoke: `TIMAERT_BOOT_TRACE=1 TIMAERT_SMOKE_SEED=42 TIMAERT_SMOKE_SCRIPT=new_game,wait_boot_done,enter_first_settlement,wait_visible,capture_frame,quit build-msvc/timaert.exe` PASS.
  - Runtime evidence: `settlement_subworld id=0 pop=5042 houses=380 walls=69 citizens=63 center=122,143`.
  - Screenshot artifact: `C:/Timaert/timaert_c/smoke_04_ui.png` shows visible 3D settlement structures.
  - Not yet done: macOS Ninja build; same-seed TS/proto visual comparison; richer textured building parity.

- Task: P1.3 stats/attributes/skills point spending.
- Finding: C++ already had TS-mirrored `Attributes`, `Skills`, `LevelData`, formulas, and save/load. The native `Character > Stats` tab only displayed values; it had no point pool display, `+` controls, level-up control, or perk selection path. This is an action/UI gap, not a save-schema gap.
- Change:
  - `macro/attributes.h`: added `AttributeId`, `SkillId`, `spend_attribute_point`, and `spend_skill_point` helpers.
  - `ui/overlays.cpp`: `Character > Stats` now displays attribute/skill/perk point pools, has `+` buttons for attributes and skills, recomputes combat stats after successful spending, exposes level-up when enough EXP is present, and offers basic perk selection from `kPerkList`.
  - `ui/screens.{h,cpp}` and `app/main.cpp`: bottom toolbar now has a Stats entry that opens `CharacterPanelTab::Stats`.
  - `app/main.cpp`: added smoke actions `open_stats`, `spend_attribute_vit`, and `spend_skill_bodybuilding`.
  - `tests/save_roundtrip_test.cpp`: roundtrip now asserts `attributePoints` and `skillPoints`, not only `perkPoints`.
- Verification:
  - Targeted MSVC build: `timaert` and `save_roundtrip_test` PASS.
  - `save_roundtrip_test.exe` PASS.
  - Runtime smoke: `TIMAERT_BOOT_TRACE=1 TIMAERT_SMOKE_SEED=42 TIMAERT_SMOKE_SCRIPT=new_game,wait_boot_done,open_stats,spend_attribute_vit,spend_skill_bodybuilding,save_game,quit build-msvc/timaert.exe` PASS.
  - Runtime evidence: `attrPts=8 skillPts=3 perkPts=1 vit=1 bodybuilding=0 hpMax=110`; after spending, `VIT 1->2 HP 110->120`, then `Bodybuilding 0->1 HP 120->125`; save completed.
  - No save version bump: no serialized fields were added.
  - Not yet done: macOS Ninja build; manual UI click screenshot of the Stats panel.

- Task: P1.2 settlement info lacks settlement map.
- TS authority: `src/screens/SettlementOverlay.svelte` exposes a `map` tab. It lazily calls `generateSubworldMap(worldSeed + settlement.id * 123, CELL_SIZE, CELL_SIZE, city/village, settlement.population)`, crops the center `256x256`, and shows the image plus seed/population.
- Change:
  - `ui/overlays.h`: added `SettlementPanelTab::Map`.
  - `ui/overlays.cpp`: added a lazy cached native settlement preview texture. It uses the current C++ subworld dispatcher (`sub::dispatch_generate`) with the same TS preview seed rule, Meadow default biome, city landmark mode, and the settlement population. The UI displays the 256x256 center crop with seed, population, house count, and wall count; Refresh invalidates the cache.
  - `app/main.cpp`: added smoke action `open_settlement_map`.
- Verification:
  - `git diff --check -- timaert_c/src/ui/overlays.cpp timaert_c/src/ui/overlays.h timaert_c/src/app/main.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC app build: `timaert` PASS.
  - Runtime smoke: `TIMAERT_BOOT_TRACE=1 TIMAERT_SMOKE_SEED=42 TIMAERT_SMOKE_SCRIPT=new_game,wait_boot_done,open_settlement_map,wait_visible,capture_frame,quit build-msvc/timaert.exe` PASS.
  - Runtime evidence: `settlement_map open id=0 name="Kosulmum" seed=0x0000002A pop=5042`; screenshot `C:/Timaert/timaert_c/smoke_04_ui.png` shows the Map tab with a non-empty city preview (`Houses: 380`, `Walls: 59`).
  - Not yet done: macOS Ninja build; TS pixel A/B. The native preview intentionally uses the current C++ subworld generator for parity with the shipped native city, not a new hand-drawn schematic.

- Task: P1.4 river naturalization, first safe pass.
- Finding: TS and native both generate rivers from coarse climate biome edges and cardinal A* along `edgeDist`, so the Voronoi/cell-edge look is mostly inherited design, not a broken C++ port. A first attempted data-level change to meander/width in `map_generator.cpp` changed seed-42 world topology (`cities=66` became `55`), so it was reverted immediately; `map_generator.cpp` has no retained content diff for this item.
- Change:
  - `macro/macro_renderer.cpp`: added `riverVisualValue()` in the shader. It samples `u_riverMap` with a tiny deterministic noise warp and 8-neighbour taps, widening/softening the rendered river without changing `TerrainData::riverData`.
  - `riverOverlay()` now uses the softened visual value, adds subtle bank variation, and keeps the existing height gate so ocean/coast cells are not blindly haloed.
  - Cell grid darkening is attenuated over the softened river mask, so high-zoom grid lines no longer cut across rivers. The shader string was split into `kFS3` to stay under MSVC literal-size limits.
- Verification:
  - `git diff --check -- timaert_c/src/macro/macro_renderer.cpp timaert_c/src/macro/map_generator.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC build: `timaert` and `road_river_generation_test` PASS.
  - `road_river_generation_test.exe` PASS.
  - Runtime smoke: `TIMAERT_BOOT_TRACE=1 TIMAERT_SMOKE_SEED=42 TIMAERT_SMOKE_SCRIPT=new_game,wait_boot_done,complete_story_overlay,wait_visible,capture_frame,quit build-msvc/timaert.exe` PASS.
  - Runtime evidence after data revert: seed 42 returned to previous topology, `cities=66 attempted=153 kept=89 pruned=64 componentPruned=45 expansions=6854018`; screenshot `C:/Timaert/timaert_c/smoke_04_ui.png` renders the macro map after the shader patch.
  - Not yet done: fixed-location TS/C++ river screenshot A/B at river-heavy coordinates; GPU perf capture for the extra shader taps; deeper data-generation redesign for true meanders/source-to-mouth continuity.

- Task: P1.5 SP decay from macro travel.
- TS authority: `src/game/movement-cost.ts` defines `MACRO_BASE_SP = 10`, biome fallback weights, and feature overrides. `src/screens/GameScreen.svelte::drainPlayerSpForCell()` subtracts `getCellSpCost(destinationBiome, destinationFeature) + overload`, then applies compounding HP damage if SP becomes negative. The TS comment says Travel skill reduces terrain SP cost, but the actual drain code does not apply that reduction, so native does not apply it either.
- Native finding: `macro/movement_cost.h` and `pathfinding.cpp` already had the TS cost table and cost grid. The live auto-walk route in `ui/macro_overlay.cpp::step_macro_walk()` and `app/main.cpp::poll_movement()` moved the player and emitted a move event, but never charged SP.
- Change:
  - `macro/travel.{h,cpp}`: added shared macro travel cost/drain helpers. They resolve destination biome from macro height/temp/moisture, apply destination feature override, add carry overload, subtract SP, and apply TS-style negative-SP HP damage. Native `CombatStats` are integer POD, so sub-1kg overload is rounded up instead of being lost.
  - `ui/macro_overlay.{h,cpp}`: `step_macro_walk()` now returns reached-cell count and accepts a zero-allocation callback fired exactly when a path cell is entered.
  - `app/main.cpp`: auto-walk now uses that callback to drain SP per reached macro cell; added smoke action `macro_travel_sp`.
- Verification:
  - `git diff --check -- timaert_c/src/macro/travel.h timaert_c/src/macro/travel.cpp timaert_c/src/ui/macro_overlay.h timaert_c/src/ui/macro_overlay.cpp timaert_c/src/app/main.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC app build: `timaert` PASS. CMake re-ran because `src/macro/travel.cpp` was newly globbed; no compiler warnings in rebuilt targets. Existing SDL2_mixer CMake deprecation warning remains.
  - Runtime smoke: `TIMAERT_BOOT_TRACE=1 TIMAERT_SMOKE_SEED=42 TIMAERT_SMOKE_SCRIPT=new_game,wait_boot_done,macro_travel_sp,quit build-msvc/timaert.exe` PASS.
  - Runtime evidence: `macro_travel_sp steps=3 pos=122,143->122,140 sp=110->80 cost=30 lastBiome=4 lastFeature=1 lastCell=10`, matching three road-cell entries at 10 SP each.
  - Not yet done: macOS Ninja build from a macOS host; broader rest/regeneration parity, because current P1.5 scope was travel decay and native `world_tick` still has no TS minute-level HP/MP/SP rest recovery.

- Task: P1.6 player loses HP in subworld without visible cause.
- TS authority: `src/screens/SubworldScreen.svelte` uploads every non-player NPC as a 3D billboard, using color fallback when no sprite UV exists, and shows combat feedback through HP flash/combat log. `src/game/subworld/engine.ts` records player-involved hits with attacker labels. Native C++ could spawn fauna/monster entities with `Position + Health + Combat + SubworldTag + SubworldAi + Sprite` but no `NpcCharacter`; the 3D renderer only drew `NpcCharacter` paper-dolls, so those Sprite-only hostiles could attack while invisible.
- Change:
  - `sub/renderer_3d.cpp`: added a colored billboard fallback for alive `SubworldTag + Sprite + Health` entities that lack `NpcCharacter`. This keeps ambient fauna/monster attackers visible even before they get paper-doll art.
  - `sub/engine.cpp`: direct hostile-to-player melee damage now sets a visible status line: attacker label, damage, compass bearing, and range.
  - `app/main.cpp`: added smoke action `subworld_enemy_feedback`. It clears blocking story/dialog overlays, enters an open non-landmark subworld cell, injects a Sprite-only hostile near the player, ticks combat once, and asserts a Sprite-only visible candidate, HP drop, and feedback string.
- Verification:
  - `git diff --check -- timaert_c/src/sub/engine.cpp timaert_c/src/sub/renderer_3d.cpp timaert_c/src/app/main.cpp timaert_c/src/macro/travel.h timaert_c/src/macro/travel.cpp timaert_c/src/ui/macro_overlay.h timaert_c/src/ui/macro_overlay.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC app build: `timaert` PASS, no compiler warnings in rebuilt targets.
  - Runtime smoke: `TIMAERT_BOOT_TRACE=1 TIMAERT_SMOKE_SEED=42 TIMAERT_SMOKE_SCRIPT=new_game,wait_boot_done,subworld_enemy_feedback,wait_visible,capture_frame,quit build-msvc/timaert.exe` PASS.
  - Runtime evidence: `subworld_enemy_feedback spriteOnly=8 hp=110->103 status="Hit by Hostile for 7 (E 5m)"`.
  - Screenshot artifact: `C:/Timaert/timaert_c/smoke_04_ui.png` shows the first-person subworld with the colored fallback hostile in front of the player and the damage status line visible.
  - Not yet done: native 2D subworld ECS overlay; richer sprite/label/HP bars for fauna; macOS Ninja build from a macOS host.

- Task: P1.7 subworld time rate.
- Finding: native `tick_playing_runtime()` advanced `tick_world_time_only()` with the same real `dt` used by macro runtime. That made the subworld clock run at full macro rate. The lead/user requirement is subworld time passing 10x slower, while keeping subworld movement/combat and macro NPC AI budgets on real frame `dt`.
- Change:
  - `app/main.cpp`: added `kSubworldTimeScale = 0.10f` and applies it only to the subworld `tick_world_time_only()` call. `app.subworld.tick(dt)`, spellbook, and macro NPC AI continue using unscaled `dt`.
  - Existing `subworld_time` smoke now asserts the expected scaled minute count, not just "time advanced".
  - The smoke clears blocking story/dialog overlays, enters an open non-landmark subworld cell, and neutralizes temporary subworld `Combat` actors inside the smoke so the clock-rate test is not randomly converted into a survival test.
- Verification:
  - `git diff --check -- timaert_c/src/app/main.cpp` PASS, only existing CRLF warning.
  - Targeted MSVC app build: `timaert` PASS, no compiler warnings in rebuilt target.
  - Runtime smoke: `TIMAERT_BOOT_TRACE=1 TIMAERT_SMOKE_SEED=42 TIMAERT_SMOKE_SCRIPT=new_game,wait_boot_done,subworld_time,quit build-msvc/timaert.exe` PASS.
  - Runtime evidence: `subworld_time frames=1000 dt=0.100 scale=0.10 minutes=144 expected=144 days=0`, ending with `subworld_time OK` and `PASS`.
  - Not yet done: visual before/after time screenshots; macOS Ninja build from a macOS host.

- Task: P1.8 mouse interaction in subworld menus.
- TS authority: `src/screens/SubworldScreen.svelte` only rotates the camera while the canvas has pointer lock; menu overlays are normal DOM UI. Native used SDL relative mouse mode for the 3D subworld camera, but the toggle lived inside `poll_movement()` and ignored native ImGui panels. If a menu opened while relative mode was active, SDL could keep the cursor captured and UI clicks became unreliable.
- Change:
  - `app/main.cpp`: added `gameplay_panel_open()`, `wants_subworld_relative_mouse()`, and `sync_relative_mouse_mode()`.
  - Relative mouse mode is now synchronized at frame level, not only while movement input is allowed. It releases for pause/non-playing state, story/dialog/event modals, map/character/quest/codex/settlement/diplomacy panels, and debug UI; it restores when subworld 3D resumes with no panel open.
  - Subworld camera motion now uses the same capture predicate, and subworld movement input is suppressed while a gameplay panel or ImGui capture is active.
  - Added smoke action `subworld_mouse_release` to assert SDL capture -> release on map panel -> release on character panel -> restore after closing panels.
- Verification:
  - `git diff --check -- timaert_c/src/app/main.cpp` PASS, only existing CRLF warning.
  - Targeted MSVC app build: `timaert` PASS, no compiler warnings in rebuilt target.
  - First smoke attempt failed with `captured=0` because I incorrectly tied SDL capture itself to ImGui `WantCaptureMouse`; normal HUD ImGui can set that flag without a gameplay menu. I corrected the predicate so `WantCaptureMouse` remains an event filter, not the SDL capture owner.
  - Runtime smoke: `TIMAERT_BOOT_TRACE=1 TIMAERT_SMOKE_SEED=42 TIMAERT_SMOKE_SCRIPT=new_game,wait_boot_done,subworld_mouse_release,quit build-msvc/timaert.exe` PASS.
  - Runtime evidence: `subworld_mouse_release captured=1 releasedMap=1 releasedCharacter=1 restored=1`.
  - Not yet done: true manual click verification on a local focused window; the smoke verifies SDL relative-mode state transitions, which is the narrow bug surface.

- Task: P1.9 strange macro NPC movement.
- TS authority: `src/game/npc-ai.ts` restores NPC SP while `Idle` or `Resting`, exits `Resting` once SP is at least half max, moves logic one cell per 0.5s tick, and `src/screens/GameScreen.svelte` renders NPCs through `visualX/visualY` interpolation instead of raw logical `x/y`.
- Native findings:
  - The C++ AI drained `MacroNpcRuntime::sp` but never regenerated it or exited `Resting`; travelling NPCs could stop permanently after a few steps.
  - Native macro overlay rendered `ecs::Position` directly. That made NPCs visibly jump every 0.5s instead of sliding between logical cells.
  - Walk animation was keyed from stale `visualSpeed`, so an NPC could keep walking in place after the visual position had caught up.
  - A read-only explorer independently confirmed the visual layer mismatch and stale `visualSpeed` animation condition as the likely player-visible cause.
- Change:
  - `macro/npc_spawn.cpp`: macro NPCs now get `ecs::VisualPos` initialized at spawn.
  - `macro/npc_ai.cpp/.h`: added TS-parity macro NPC SP recovery/rest exit and `tick_macro_npc_visuals()`, which interpolates `VisualPos` toward `Position` and snaps long seam/teleport jumps like TS.
  - `app/main.cpp`: macro NPC visuals tick in macro and subworld runtime paths; added smoke action `macro_npc_trace`.
  - `ui/macro_overlay.cpp`: macro NPC drawing uses `VisualPos` when present; walk/idle now follows actual logical-vs-visual delta, with direction derived from that delta.
- Verification:
  - `git diff --check -- timaert_c/src/app/main.cpp timaert_c/src/macro/npc_ai.cpp timaert_c/src/macro/npc_ai.h timaert_c/src/macro/npc_spawn.cpp timaert_c/src/ui/macro_overlay.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC app build: `timaert` PASS, no compiler warnings in rebuilt targets.
  - First `macro_npc_trace` smoke attempt overran the rest check by one AI tick: the NPC correctly left `Resting`, then immediately selected a new travelling goal on the next tick. The smoke was corrected to stop at the first `Idle` transition.
  - Runtime smoke: `TIMAERT_BOOT_TRACE=1 TIMAERT_SMOKE_SEED=42 TIMAERT_SMOKE_SCRIPT=new_game,wait_boot_done,macro_npc_trace,quit build-msvc/timaert.exe` PASS.
  - Runtime evidence: `macro_npc_trace entity=811 maxSp=84 rest=45:0 recovered=1 move=512.0,512.0->513.0,512.0 visual=512.00->512.50->513.00`.
  - Not yet done: before/after gameplay video; broader long-run trace for caravans across water/roads.

- Task: P1.10 tree billboards float / all trees orange autumn.
- TS authority: `src/game/subworld/renderer-3d.ts` sinks billboard bases by `spriteSize.y * 0.08` in the vertex shader. `src/game/subworld/base-generator.ts::temperatureToTreeType()` chooses tree species from macro temperature plus per-tree hash, not from a generic Meadow fallback.
- Native findings:
  - Native tree placement only sank the billboard anchor by a fixed `1.0m`; current tree heights are about `22m..44m`, so transparent atlas padding could still leave trunks floating, especially on slopes.
  - C++ tree species selection used `tree_type_for(Biome, hash)`, and most current forest subworlds arrive as Meadow-like local biomes. That made species drift from TS and could over-select autumn/orange variants.
- Change:
  - `sub/map_data.h`, `sub/engine.cpp`, `sub/seamless_manager.h/.cpp`: carried `macroTemperature` from macro terrain context into loaded cells and async completed cells.
  - `sub/tree_atlas.h/.cpp`: added `tree_type_for_temperature(float temperature, float hash)` matching the TS temperature bands: Pine, Birch, Autumn, Oak, Willow, Cherry, Jungle.
  - `sub/renderer_3d.h/.cpp`: tree instances now use temperature-based species and sink the anchor by `max(1.25m, height * 0.08f)`.
  - `app/main.cpp`: added smoke action `subworld_tree_anchor`, which enters a forested subworld cell, counts trees/species, checks anchor sink range, rejects all-autumn forests when there is enough sample size, and aims the 3D camera at a nearby tree before screenshot capture.
- Verification:
  - `git diff --check -- timaert_c/src/app/main.cpp timaert_c/src/sub/map_data.h timaert_c/src/sub/seamless_manager.h timaert_c/src/sub/seamless_manager.cpp timaert_c/src/sub/engine.cpp timaert_c/src/sub/renderer_3d.cpp timaert_c/src/sub/renderer_3d.h timaert_c/src/sub/tree_atlas.cpp timaert_c/src/sub/tree_atlas.h` PASS, only existing CRLF warnings.
  - Targeted MSVC app build: `timaert` PASS, no compiler warnings in rebuilt targets.
  - Runtime smoke: `TIMAERT_BOOT_TRACE=1 TIMAERT_SMOKE_SEED=42 TIMAERT_SMOKE_SCRIPT=new_game,wait_boot_done,subworld_tree_anchor,wait_visible,capture_frame,quit build-msvc/timaert.exe` PASS.
  - Runtime evidence: `subworld_tree_anchor cell=512,512 trees=53911 sink=1.76..3.52 types=[0,0,0,0,0,0,53911] focus=1540.5,1536.5`.
  - Screenshot artifact: `C:/Timaert/timaert_c/smoke_04_ui.png` shows a close first-person forest shot with green jungle atlas and trunks meeting/sinking into the terrain.
  - Not yet done: macOS Ninja build from a macOS host; TS/C++ same-cell screenshot A/B; broad climate sweep screenshot set for all species bands.

- Task: P2 first pass on M-key subworld map content quality.
- TS authority: `src/screens/SubworldScreen.svelte` opens a large `SubworldRenderer` canvas for the M-map, centered on the current subworld cell. `src/game/subworld/map-renderer.ts` draws tile classes, roads, walls, houses, fields, and tree/ground differences instead of only changing the shape of the map window.
- Native findings:
  - The accepted circular HUD minimap should stay circular. The bad surface is the `Subworld Map` M-key overlay.
  - Native `draw_subworld_map_overlay()` reused the always-on 384px HUD downsample. At 620px display size this produced coarse stretched blocks.
  - The old sample used one source tile per output pixel and only weak absolute-height shading, so roads could disappear, city details relied on chance sampling, and local relief was barely readable.
- Change:
  - `ui/overlays.cpp`: split the map cache into cheap HUD `384x384` and detailed M-map `768x768`, built only while the overlay is open.
  - Added block-aware tile downsampling for the detailed map so roads, houses, walls, water/shore mixes, rocks, fields, and forest patches survive minification.
  - Added hillshade plus subtle contour bands from `SeamlessSubworldManager::heightmap()` for local relief.
  - Added explicit overlays for composite road-mask indices and sparse structures (`House`, `Wall`, `Bridge`, `Rock`) so settlement/city structure is readable even after downsampling. Trees stay density-based through tile color to avoid turning forests into black noise.
- Verification:
  - `git diff --check -- timaert_c/src/ui/overlays.cpp` PASS, only existing CRLF warning.
  - Targeted MSVC app build: `timaert` PASS.
  - Runtime smoke 1: `TIMAERT_BOOT_TRACE=1 TIMAERT_SMOKE_SEED=42 TIMAERT_SMOKE_SCRIPT=new_game,wait_boot_done,attack_first_npc,open_map,wait_visible,capture_frame,quit build-msvc/timaert.exe` PASS.
  - Screenshot artifact 1: `C:/Timaert/timaert_c/smoke_05_ui.png` shows the M-map with readable roads and local forest/open-ground patches.
  - Runtime smoke 2: `TIMAERT_BOOT_TRACE=1 TIMAERT_SMOKE_SEED=42 TIMAERT_SMOKE_SCRIPT=new_game,wait_boot_done,enter_first_settlement,open_map,wait_visible,capture_frame,quit build-msvc/timaert.exe` PASS.
  - Runtime evidence 2: `settlement_subworld id=0 pop=5042 houses=380 walls=69 citizens=63 center=122,143`.
  - Screenshot artifact 2: `C:/Timaert/timaert_c/smoke_06_city_map.png` shows the M-map with central settlement roads, walls/ring, building markers, and 3x3 grid.
  - Not yet done: macOS Ninja build from a macOS host; same-location screenshot A/B against TS/browser or `5b16b69`; water/shore-focused M-map screenshot.

- Task: Effect applicator TS parity audit for the flagged quest/level-up suspects.
- TS authority:
  - `src/game/effect-applicator.ts` handles only `PlayerGoldChange`, `ApplyEffect`, `ReputationChange`, and `CodexUnlock`; `grant_xp` only adds EXP and does not level the player or emit `PlayerLevelUp`.
  - `src/game/quests/quest-engine.ts` records expired/failed quests in `completedQuestIds` as done/failed before emitting `QuestFail`.
  - `src/game/node-registry.ts` treats `PlayerLevelUp` as a presentation event for the level-up dialog.
- Native findings:
  - A read-only explorer independently confirmed that C++ `PlayerLevelUp` mutation and `queue_player_level_up_if_needed()` were C++-only plumbing, not TS effect-applicator behavior.
  - Native `QuestFail` had a useful `failedQuestIds` ledger, but no longer matched TS if it failed to also mark the quest as done.
- Change:
  - `events/effect_applicator.cpp/.h`: removed the C++-only automatic level-up queue helper and stopped mutating `LevelData`/combat stats on `PlayerLevelUp` inside `apply_events()`.
  - `events/effect_applicator.cpp`: `QuestFail` now appends to both `completedQuestIds` (TS done ledger) and `failedQuestIds` (native explicit failed ledger), deduped.
  - `app/main.cpp`: event application no longer re-emits automatic `PlayerLevelUp` after `grant_xp`.
  - `tests/quest_lifecycle_test.cpp`: updated parity coverage so `grant_xp` can cross the threshold without levelling, `PlayerLevelUp` is presentation-only in the applicator, the logic node still shows the level-up dialog when such an event is explicitly present, and failed quests mirror TS done-ledger semantics.
- Verification:
  - `git diff --check -- timaert_c/src/events/effect_applicator.cpp timaert_c/src/events/effect_applicator.h timaert_c/src/app/main.cpp timaert_c/tests/quest_lifecycle_test.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC build: `timaert` and `quest_lifecycle_test` PASS.
  - `build-msvc/quest_lifecycle_test.exe` PASS with summary including `effects=ok xp_effect=ok level_event=ok level_dialog=ok quest_failed=ok`.
  - Not yet done: full test suite; macOS Ninja build from a macOS host; broader audit of every effect dispatch arm beyond the flagged quest/level-up suspects.

- Task: EventBus subscriber-order parity audit.
- TS authority: `src/game/event-bus.ts::emit()` pushes the event into `tickEvents_`, then iterates the current listener array directly with `for..of`. This means same-tag listeners run in insertion order, a listener appended during emit is visible to the same emit, and a listener removed before its turn is skipped.
- Native finding:
  - C++ `EventBus` previously snapshotted the initial subscriber count and deferred subscription adds/removes until after dispatch. The existing native test explicitly locked in that deferred behavior, but it does not match the TS listener array semantics.
  - Verified JS array behavior with Node snippets before changing C++: append-during-iteration runs in the same pass; removing the next listener skips it.
- Change:
  - `events/event_bus.cpp/.h`: removed `dispatchDepth_`, `pendingAdds_`, `resetPending_`, and deferred compaction. `emit()` now iterates the live subscriber vector; `on()`, `unsubscribe()`, and `reset()` mutate immediately.
  - `tests/quest_lifecycle_test.cpp`: replaced the native-deferred mutation assertions with TS-parity assertions for append-during-emit and removal-during-emit.
- Verification:
  - `git diff --check -- timaert_c/src/events/event_bus.h timaert_c/src/events/event_bus.cpp timaert_c/tests/quest_lifecycle_test.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC build: `quest_lifecycle_test` PASS. Later shared-runtime check: `timaert` PASS after the EventBus/logic-node edits.
  - `build-msvc/quest_lifecycle_test.exe` PASS with `event_bus=ok`.
  - Follow-up completed later: per-tag container refactor closed the unrelated-tag unsubscribe gap. Remaining: full suite; macOS Ninja build from a macOS host.

- Task: Logic node id-string parity audit.
- TS authority:
  - `src/game/node-registry.ts` registers built-ins `enc_random`, `sys_level_up`, `sys_settlement` and activates the same three in `INITIAL_ACTIVE_NODES`.
  - `src/game/plot/intro.ts` registers/activates `intro_main`.
  - `src/game/plot/chapter-1.ts` defines dormant placeholder `plot_chapter_1`.
- Native finding:
  - C++ node id strings and user-facing dialog/story payloads already matched the TS names checked by current tests, but `LogicNodeEngine` had no direct `has(id)` / `is_active(id)` inspection. Tests were mostly proving counts/effects, not byte-identical id registration.
- Change:
  - `events/logic_nodes.h`: added small const inspection helpers `has(id)` and `is_active(id)`.
  - `tests/quest_lifecycle_test.cpp`: added `test_builtin_node_ids_match_ts_registry()` and strengthened intro/chapter checks to assert exact `intro_main` / `plot_chapter_1` registration and activation state.
- Verification:
  - `git diff --check -- timaert_c/src/events/logic_nodes.h timaert_c/tests/quest_lifecycle_test.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC build: `quest_lifecycle_test` PASS. Later shared-runtime check: `timaert` PASS after the EventBus/logic-node edits.
  - `build-msvc/quest_lifecycle_test.exe` PASS with `node_ids=ok intro_story=ok chapter_placeholder=ok`.
  - Not yet done: full suite; macOS Ninja build from a macOS host; deeper parity for logic engine iteration order (`std::unordered_set` vs TS `Set` insertion order) if multiple active nodes emit order-sensitive effects in the same tick.

- Task: LogicNodeEngine tick order/interleaving parity.
- TS authority: `src/game/logic-nodes.ts::tick()` iterates `activeIds_` as a JS `Set` in insertion order and executes a node effect immediately after that node's conditions pass. It does not first collect all nodes into a separate fire list. A later node's pure `check` can therefore observe events emitted by an earlier node in the same logic tick.
- Native finding:
  - C++ used `std::unordered_set` for `active_`, then populated `pendingFire_` before running any effects. This made active-node order unstable and hid same-tick effects from later pure checks.
- Change:
  - `events/logic_nodes.h/.cpp`: replaced `std::unordered_set active_` with insertion-ordered `std::vector active_`; `activate()` is now Set-like append-if-absent, and `tick()` checks/executes nodes inline, collecting `toRemove_`/`toAdd_` for the final TS-style delete/add phase.
  - Kept node definitions in `unordered_map`; this patch targets active Set semantics, not debug enumeration order.
  - `tests/quest_lifecycle_test.cpp`: added `test_logic_node_tick_order_matches_ts_set()`. It proves an active node inserted second can see a `Custom` event emitted by the first node during the same `tick()`, and that event order is `first, second`.
- Verification:
  - `git diff --check -- timaert_c/src/events/logic_nodes.h timaert_c/src/events/logic_nodes.cpp timaert_c/tests/quest_lifecycle_test.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC build: `quest_lifecycle_test` and `timaert` PASS.
  - `build-msvc/quest_lifecycle_test.exe` PASS with `logic_order=ok`.
  - Not yet done: full suite; macOS Ninja build from a macOS host; edge-case parity for TS `Set` behavior when a node removes/re-adds the currently executing id inside its own effect.

- Task: World tick TS parity guard.
- TS authority:
  - `src/game/world-tick.ts::advanceWorldMinute()` advances one minute at a time, emits hourly time advancement, and runs settlement/village/economy/player daily work on day rollover.
  - Settlement/village histories keep the newest 30 samples.
  - Player daily tick clamps gold at zero after army upkeep and increments `ageDays` by one.
- Native finding:
  - C++ intentionally adapts the TS midnight work into `WorldTickRuntime::pendingDailyTicks` plus `process_world_daily_ticks()` so the daily simulation can be budgeted across frames, especially while subworld is active.
  - The adaptation preserves the important TS state transitions only if midnight queues the exact day, daily processing drains in order, player upkeep/age happen only when the queued day is processed, and history remains capped at 30.
- Change:
  - Added `tests/world_tick_parity_test.cpp`.
  - Added `world_tick_parity_test` target in `timaert_c/CMakeLists.txt`, linked only to `src/macro/world_tick.cpp` and `src/macro/economy.cpp`.
  - Test coverage: one-minute hour rollover, one-minute midnight rollover with queued daily work, daily player upkeep/age processing, and settlement history 30-day retention.
- Verification:
  - `git diff --check -- timaert_c/tests/world_tick_parity_test.cpp timaert_c/CMakeLists.txt` PASS, only existing CRLF warning.
  - MSVC configure/build: `cmake -S . -B build-msvc && cmake --build build-msvc --target world_tick_parity_test` PASS.
  - `build-msvc/world_tick_parity_test.exe` PASS with `OK world_tick_parity_test clock daily_queue upkeep history`.
  - Follow-up completed later: app-level `TimeAdvance` event compression was replaced with one event per elapsed hour. Remaining: full suite; macOS Ninja build from a macOS host.

- Task: App-level `TimeAdvance` TS parity.
- TS authority:
  - `src/game/world-tick.ts::advanceWorldMinute()` emits one `TimeAdvance` event for each elapsed hour with `day` and `hour`.
  - `src/game/quests/quest-engine.ts` increments `wait_at.hoursWaited` once per `TimeAdvance` event; it does not read a compressed hour count from the payload.
- Native finding:
  - `emit_time_advance_if_needed()` compressed multiple elapsed hours into one event (`ix = hoursAdvanced`), and native `QuestEngine` treated that single event as multiple hours.
  - This was a C++ convenience, not TS behavior. It could skip exact hourly payloads for any subscriber that needs the intermediate `hour`.
- Change:
  - `app/main.cpp`: `emit_time_advance_if_needed()` now reconstructs elapsed absolute hours and emits one `TimeAdvance` event per hour. Payload convention is `a = day`, `iy = hour`, `ix = 1`.
  - `events/quests/quest_engine.cpp`: WaitAt now increments by one per event, matching TS.
  - `events/event_types.h`: documented the native flattened `TimeAdvance` payload convention.
  - `tests/quest_lifecycle_test.cpp`: WaitAt coverage now proves that one legacy event with `ix = 2` counts as exactly one TS hour; two separate events are required to finish the remaining two hours.
- Verification:
  - `git diff --check -- timaert_c/src/app/main.cpp timaert_c/src/events/quests/quest_engine.cpp timaert_c/src/events/event_types.h timaert_c/tests/quest_lifecycle_test.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC build: `quest_lifecycle_test` and `timaert` PASS.
  - `build-msvc/quest_lifecycle_test.exe` PASS with `wait_at=ok`.
  - Follow-up runtime smoke:
    - `app/main.cpp`: added smoke action `timeadvance_burst`.
    - Targeted MSVC app build: `cmake --build build-msvc --target timaert` PASS.
    - Runtime smoke: `TIMAERT_BOOT_TRACE=1 TIMAERT_SMOKE_SEED=42 TIMAERT_SMOKE_SCRIPT=new_game,wait_boot_done,timeadvance_burst,quit SDL_AUDIODRIVER=dummy build-msvc/timaert.exe` PASS.
    - Runtime evidence: `timeadvance_burst hoursAdvanced=3 count=3 events=[0:07,0:08,0:09]`.
  - Not yet done: full suite; macOS Ninja build from a macOS host.

- Task: QuestEngine accept/fail payload parity.
- TS authority:
  - `src/game/quests/quest-engine.ts::accept()` pushes the quest into `player.activeQuests` before emitting `onAccept` events and `QuestStart`.
  - TS `QuestStartEvent` carries `questId` and `title`; `QuestFailEvent` carries `questId` and `reason`.
- Native finding:
  - `events/quests/quest_engine.cpp::accept()` emitted `onAccept` and `QuestStart` before inserting into the native active list. That kept event order correct, but subscribers observing active quests during those events saw pre-accept state unlike TS.
  - Native `QuestStart` omitted the quest title, and expired `QuestFail` omitted the TS `reason='expired'` payload.
- Change:
  - `events/quests/quest_engine.cpp`: moved `active.push_back()` before `bus.emit_all(onAccept)` and `QuestStart`, then emits from `active.back()`.
  - `events/quests/quest_engine.cpp`: fills `QuestStart.s2` with quest title and expired `QuestFail.s2` with `expired`.
  - `tests/quest_lifecycle_test.cpp`: `test_ts_quest_tag_aliases()` now subscribes to both accept events and asserts active size is already `1` during each event, and checks `QuestStart` id/title payload. Expiry coverage now checks `QuestFail` id/reason payload.
- Verification:
  - `git diff --check -- timaert_c/src/events/quests/quest_engine.cpp timaert_c/tests/quest_lifecycle_test.cpp docs/notes/CODEX_PROGRESS.md` PASS, only existing CRLF warnings.
  - Targeted MSVC build: `cmake --build build-msvc --target quest_lifecycle_test` PASS.
  - `build-msvc/quest_lifecycle_test.exe` PASS with `quest_tags=ok quest_failed=ok`.
  - Not yet done: full suite; macOS Ninja build from a macOS host.

- Task: Quest reward direct-mutation parity.
- TS authority:
  - `src/game/quests/quest-engine.ts` mutates completed quest ledger before applying rewards, mutates gold/XP/item/reputation rewards directly, emits observable gold/reputation/custom/complete events afterward, and does not emit an XP `ApplyEffect` event for quest XP.
- Native finding:
  - Native previously deferred gold/XP/reputation/completed-ledger effects into `apply_pending_event_effects()`. That avoided duplicate code but meant event subscribers could observe old player state during quest reward events, unlike TS.
- Change:
  - `events/quests/quest_engine.cpp`: quest completion and expiry now update player ledgers directly; gold, XP, item, and reputation rewards mutate `PlayerState` directly before events. Quest XP no longer emits a synthetic `ApplyEffect(grant_xp)` event.
  - `events/event_types.h`: added internal `kEventEffectAlreadyApplied` marker for native events whose TS-side state mutation already happened.
  - `events/effect_applicator.cpp`: skips marked quest/result events so the runtime pending-event pass does not double-apply direct TS mutations.
  - `tests/quest_lifecycle_test.cpp`: reward-order coverage now asserts subscriber-visible direct state, event payload new totals, no XP event, and no duplicate state after pending application.
- Verification:
  - `git diff --check -- timaert_c/src/events/event_types.h timaert_c/src/events/effect_applicator.cpp timaert_c/src/events/quests/quest_engine.cpp timaert_c/tests/quest_lifecycle_test.cpp docs/notes/CODEX_PROGRESS.md` PASS, only existing CRLF warnings.
  - Targeted MSVC build: `cmake --build build-msvc --target quest_lifecycle_test` PASS.
  - `build-msvc/quest_lifecycle_test.exe` PASS with `reward_order=ok quest_failed=ok`.
  - Targeted MSVC app build: `cmake --build build-msvc --target timaert` PASS.
  - Not yet done: full suite; macOS Ninja build from a macOS host.

- Task: EventBus per-tag listener registry parity.
- TS authority:
  - `src/game/event-bus.ts` stores listeners in `Map<EventTag, EventHandler[]>`.
  - During `emit(event)`, only the array for `event.tag` is iterated. Mutating listeners for another tag must not affect the current tag's iteration.
- Native finding:
  - C++ had already been changed to live listener iteration, but still stored all tags in one vector. If a `Custom` handler unsubscribed an earlier `PlayerMove` listener during `emit(Custom)`, vector compaction could shift indices and skip the next `Custom` handler. TS cannot skip that handler because `PlayerMove` lives in a different listener array.
- Change:
  - `events/event_bus.h/.cpp`: replaced the single subscriber vector with a fixed per-tag array of subscriber vectors indexed by `EventTag`. Kept the existing `on(tag)`, `unsubscribe(id)`, `has_subscribers(tag)`, `reset()`, and `subscription_count()` API.
  - Preserved TS live-array behavior for same-tag append/remove during emit.
  - `tests/quest_lifecycle_test.cpp`: added a regression case where a `Custom` handler unsubscribes a `PlayerMove` handler and the following `Custom` handler still runs.
- Verification:
  - `git diff --check -- timaert_c/src/events/event_bus.h timaert_c/src/events/event_bus.cpp timaert_c/tests/quest_lifecycle_test.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC build: `quest_lifecycle_test` and `timaert` PASS.
  - `build-msvc/quest_lifecycle_test.exe` PASS with `event_bus=ok`.
  - Not yet done: full suite; macOS Ninja build from a macOS host.

- Task: QuestEngine accept/expiry order parity.
- TS authority:
  - `src/game/quests/quest-engine.ts::tick()` checks quest expiry before objective completion.
  - `QuestEngine.accept()` pushes the quest, emits all `onAccept` events, then emits `QuestStart`; it does not perform an internal duplicate guard.
- Native finding:
  - C++ checked expiry after objective completion, so a quest that was expired and complete in the same tick could complete instead of failing.
  - C++ emitted `QuestStart` before `onAccept`, and silently deduped accepted quest ids inside `accept()`. The UI already prechecks `is_known()`, but the engine method itself was not TS-equivalent.
- Change:
  - `events/quests/quest_engine.cpp`: moved expiry handling before objective evaluation.
  - `events/quests/quest_engine.cpp`: `accept()` now emits `onAccept` before `QuestStart` and no longer dedupes internally.
  - `tests/quest_lifecycle_test.cpp`: added coverage for `onAccept -> QuestStart` order, TS-like duplicate acceptance at the engine API level, and expired+complete quests failing before completion.
- Verification:
  - `git diff --check -- timaert_c/src/events/quests/quest_engine.cpp timaert_c/tests/quest_lifecycle_test.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC build: `quest_lifecycle_test` and `timaert` PASS.
  - `build-msvc/quest_lifecycle_test.exe` PASS with `quest_tags=ok quest_failed=ok`.
  - Not yet done: full suite; macOS Ninja build from a macOS host; separate reward-application parity pass because C++ currently routes some rewards through `effect_applicator`.

- Task: Quest abandon event parity.
- TS authority:
  - `src/screens/GameScreen.svelte::handleAbandonQuest()` removes the active quest, removes quest markers, and emits `QuestFail` with reason `abandoned`.
  - TS `QuestEngine.abandon()` itself only removes from `activeQuests`; the screen-level observable event is still `QuestFail`, not a separate abandoned tag.
- Native finding:
  - Native UI routes abandon through `QuestEngine::abandon()`, which emitted the C++-only `QuestAbandoned` tag. That meant the observable abandon event did not match the TS screen behavior and also bypassed the existing `QuestFail` failed-ledger path unless special-cased elsewhere.
- Change:
  - `events/quests/quest_engine.cpp`: `abandon()` now emits `QuestFail` with `s1 = quest id`, `s2 = "abandoned"`, and stable quest key in `a`.
  - `ui/overlays.cpp`: updated the hover tooltip to name `QuestFail(abandoned)`.
  - `tests/quest_lifecycle_test.cpp`: abandon coverage now asserts the TS-shaped `QuestFail(abandoned)` payload and no duplicate event for missing quests.
- Follow-up correction:
  - Because native `apply_events()` consumes all bus events, `QuestFail(abandoned)` would otherwise mark the abandoned quest as completed/failed. TS screen-level abandon does not add the quest to `completedQuestIds`, so abandoned quests remain re-acceptable.
  - `events/effect_applicator.cpp`: `QuestFail` with `s2 == "abandoned"` is ignored by the completed/failed ledgers.
  - `tests/quest_lifecycle_test.cpp`: abandon coverage now applies the emitted event and verifies the quest is not recorded as known/done.
- Verification:
  - `git diff --check -- timaert_c/src/events/quests/quest_engine.cpp timaert_c/src/ui/overlays.cpp timaert_c/src/events/effect_applicator.cpp timaert_c/tests/quest_lifecycle_test.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC build: `quest_lifecycle_test` and `timaert` PASS.
  - `build-msvc/quest_lifecycle_test.exe` PASS with `abandon=ok`.
  - Not yet done: full suite; macOS Ninja build from a macOS host.

- Task: Quest completion ledger push semantics.
- TS authority:
  - `src/game/quests/quest-engine.ts` appends to `player.completedQuestIds` with plain `push()` on both completion and expiry. It does not dedupe.
  - The native `failedQuestIds` array is an extra C++ classification ledger; TS has no equivalent.
- Native finding:
  - `effect_applicator.cpp` deduped `completedQuestIds` for both `QuestComplete` and `QuestFail`. After making `QuestEngine.accept()` TS-like and non-deduping, this hid duplicate completion entries that TS would preserve.
- Change:
  - `events/effect_applicator.cpp`: `completedQuestIds` now uses append semantics for `QuestComplete` and non-abandoned `QuestFail`.
  - `events/effect_applicator.cpp`: `failedQuestIds` remains unique because it is native-only classification state.
  - `tests/quest_lifecycle_test.cpp`: added duplicate `QuestComplete`/`QuestFail` effect coverage proving completed ids append twice while failed classification stays unique.
- Verification:
  - `git diff --check -- timaert_c/src/events/effect_applicator.cpp timaert_c/tests/quest_lifecycle_test.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC build: `quest_lifecycle_test` and `timaert` PASS.
  - `build-msvc/quest_lifecycle_test.exe` PASS with `effects=ok quest_failed=ok`.
  - Not yet done: full suite; macOS Ninja build from a macOS host.

- Task: QuestEngine reverse scan completion order.
- TS authority:
  - `src/game/quests/quest-engine.ts::tick()` scans `player.activeQuests` from the last index down to zero.
  - Completed quest indices are collected during the scan and completion rewards/events are emitted after the scan, in that reverse order.
- Native finding:
  - C++ scanned active quests forward and completed immediately. Multiple same-tick completions could therefore emit `QuestComplete` and rewards in the opposite order from TS.
- Change:
  - `events/quests/quest_engine.cpp`: `tick()` now scans active quests by descending index, removes expired quests immediately, moves completed quests into a temporary completion list, then emits rewards and `QuestComplete` after the scan.
  - `tests/quest_lifecycle_test.cpp`: added `test_quest_completion_order_matches_ts_reverse_scan()` with two simultaneous visit-cell completions; expected order is `q_high` then `q_low`.
- Verification:
  - `git diff --check -- timaert_c/src/events/quests/quest_engine.cpp timaert_c/tests/quest_lifecycle_test.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC build: `quest_lifecycle_test` and `timaert` PASS.
  - `build-msvc/quest_lifecycle_test.exe` PASS with `quest_order=ok`.
  - Not yet done: full suite; macOS Ninja build from a macOS host.

- Task: Quest reward dispatch/application parity guard.
- TS authority:
  - `src/game/quests/quest-engine.ts` applies quest rewards before emitting `QuestComplete`.
  - Gold, XP, item, and reputation rewards mutate player state; gold/reputation also emit observable events. Event rewards emit their embedded event.
- Native finding:
  - Native intentionally routes gold/XP/reputation through emitted data events consumed by `apply_pending_event_effects()` to avoid duplicating generic event-effect behavior. Item rewards still mutate inventory directly.
  - That adaptation is safe only if event order remains reward events before `QuestComplete` and the pending-event cursor prevents duplicate reward application.
- Change:
  - `tests/quest_lifecycle_test.cpp`: added `test_quest_reward_dispatch_order_and_application()`.
  - The test completes a visit-cell quest with Gold, XP, Item, Reputation, and Event rewards, asserts event order `PlayerGoldChange -> ApplyEffect(grant_xp) -> ReputationChange -> Custom -> QuestComplete`, verifies item is direct-only, then applies pending events and proves final state is correct and not re-applied.
- Verification:
  - `git diff --check -- timaert_c/tests/quest_lifecycle_test.cpp` PASS, only existing CRLF warning.
  - Targeted MSVC build: `cmake --build build-msvc --target quest_lifecycle_test` PASS.
  - `build-msvc/quest_lifecycle_test.exe` PASS with `reward_order=ok`.
  - Not yet done: full suite; macOS Ninja build from a macOS host.

- Task: Macro NPC AI behaviour contract.
- TS authority:
  - `src/game/npc-ai.ts` defines 8 reusable macro NPC behaviours: home wanderer, woodcutter, trader, nomad, aggressive, patrol, teleporter, and wanderer.
  - The visible movement complaint in `matwej.md` requires evidence on targets, state transitions, stamina/resting, and visual interpolation before changing behaviour further.
- Native finding:
  - `src/macro/npc_ai.cpp` had no dedicated test coverage for the 8 behaviour dispatch paths. Existing smoke coverage could show that NPCs exist, but not that Peasant/Woodcutter/Merchant/Caravan/Bandit/Guard/Witch/Sorceress still follow their TS-shaped state machines.
  - Native includes a deliberate resting recovery guard so macro NPCs do not stall forever after stamina drains; this is useful but needed a regression guard because TS itself has no recovery branch after `Resting`.
- Change:
  - Added `tests/macro_npc_ai_parity_test.cpp`.
  - Added `macro_npc_ai_parity_test` target in `timaert_c/CMakeLists.txt`, linked to `src/macro/npc_ai.cpp` and EnTT.
  - Test coverage: home wanderer returns beyond 20 cells; woodcutter targets nearest 30-cell tree via `TreeGrid`; trader targets non-home settlement; nomad excludes current target settlement; aggressive NPC chases/steps toward nearby player and drains SP; patrol returns when beyond 12 cells; teleporter cooldown counts down; wanderer enters wandering with a non-trivial target; resting recovers to idle; macro visual interpolation advances by `speed*dt` and snaps long jumps.
- Verification:
  - `git diff --check -- timaert_c/tests/macro_npc_ai_parity_test.cpp timaert_c/CMakeLists.txt` PASS, only existing CRLF warning.
  - MSVC configure/build: `cmake -S . -B build-msvc && cmake --build build-msvc --target macro_npc_ai_parity_test` PASS.
  - `build-msvc/macro_npc_ai_parity_test.exe` PASS with `OK macro_npc_ai_parity_test behaviours=8 resting=ok visual=ok`.
  - Runtime smoke: `TIMAERT_BOOT_TRACE=1 TIMAERT_SMOKE_SEED=42 TIMAERT_SMOKE_SCRIPT=new_game,wait_boot_done,macro_npc_trace,quit SDL_AUDIODRIVER=dummy build-msvc/timaert.exe` PASS.
  - Runtime evidence: `macro_npc_trace entity=811 maxSp=84 rest=45:0 recovered=1 move=512.0,512.0->513.0,512.0 visual=512.00->512.50->513.00`.
  - Not yet done: full suite; macOS Ninja build from a macOS host; visual video capture for the P1.9 complaint.

- Task: Macro travel SP drain parity guard.
- TS authority:
  - `src/screens/GameScreen.svelte::drainPlayerSpForCell()` derives biome from packed terrain RGBA, lets feature cost override biome cost, adds carry overload, subtracts SP, then applies negative SP remainder to HP.
- Native finding:
  - `src/macro/travel.cpp` already mirrored the TS path, but the behavior had no focused regression guard for high-cost water, road-over-water override, toroidal coordinate wrapping, overload, and HP spillover.
  - Native combat stats are integer POD; `overloadCost` intentionally uses `ceil(overload)` so sub-integer overload still hurts instead of being silently truncated away.
- Change:
  - Added `tests/macro_travel_parity_test.cpp`.
  - Added `macro_travel_parity_test` target in `timaert_c/CMakeLists.txt`, linked to `src/macro/travel.cpp` and `src/macro/items.cpp`.
  - Test coverage: bare water costs 100 SP; road over water costs 10 SP without rewriting biome; mountain feature costs 50 SP; negative coordinates wrap to dirt road at 15 SP; invalid terrain fails closed and clears stale output; 112 kg carried by default STR creates 2 kg overload; movement with 5 SP over a 10 SP road leaves SP at -5 and HP reduced by 5.
- Verification:
  - `git diff --check -- timaert_c/tests/macro_travel_parity_test.cpp timaert_c/CMakeLists.txt` PASS, only existing CRLF warning.
  - MSVC configure/build: `cmake -S . -B build-msvc && cmake --build build-msvc --target macro_travel_parity_test` PASS.
  - `build-msvc/macro_travel_parity_test.exe` PASS with `OK macro_travel_parity_test costs=ok overload=ok drain=ok invalid=ok`.
  - Runtime smoke after macro-recovery integration: `TIMAERT_BOOT_TRACE=1 TIMAERT_SMOKE_SEED=42 TIMAERT_SMOKE_SCRIPT=new_game,wait_boot_done,macro_travel_sp,quit SDL_AUDIODRIVER=dummy build-msvc/timaert.exe` PASS.
  - Runtime evidence: `macro_travel_sp steps=3 pos=122,143->122,140 sp=110->80 cost=30 lastBiome=4 lastFeature=1 lastCell=10`.
  - Not yet done: full suite; macOS Ninja build from a macOS host.

- Task: Effect applicator unclamped HP/gold parity.
- TS authority:
  - `src/game/effect-applicator.ts::applyEffects()` applies `PlayerGoldChange` as `player.gold += delta` without clamping.
  - `applyEffect_('damage_hp')` subtracts from `currentHp` without clamping to zero. Death/defeat handling is outside this pure effect dispatcher.
- Native finding:
  - `events/effect_applicator.cpp` clamped `PlayerGoldChange`/`Trade` through a shared branch and clamped negative HP after every `ApplyEffect`.
  - The shared branch made `PlayerGoldChange` diverge from TS for debt/cost effects. The HP clamp hid negative damage results that TS preserves for downstream handling.
- Change:
  - `events/effect_applicator.cpp`: split `PlayerGoldChange` from native `Trade`; `PlayerGoldChange` now keeps exact TS add semantics, while `Trade` retains its native clamp.
  - `events/effect_applicator.cpp`: removed the post-effect HP floor clamp.
  - `tests/quest_lifecycle_test.cpp`: strengthened `test_effect_applicator_ts_verbs()` with a negative gold delta and a lethal `damage_hp` case proving HP can go below zero like TS.
- Verification:
  - `git diff --check -- timaert_c/src/events/effect_applicator.cpp timaert_c/tests/quest_lifecycle_test.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC build: `cmake --build build-msvc --target quest_lifecycle_test` PASS.
  - `build-msvc/quest_lifecycle_test.exe` PASS with `effects=ok xp_effect=ok`.
  - Follow-up trade audit:
    - TS `src/screens/TradeOverlay.svelte` mutates `player.gold` and inventories directly; it does not emit a `Trade` effect event.
    - Native settlement/NPC trade UI also mutates gold and inventories directly.
    - `EventTag::Trade` had no current emitters and was only a stale native effect-applicator branch.
  - Follow-up change:
    - `events/effect_applicator.cpp`: removed the `EventTag::Trade` gold mutation branch.
    - `tests/quest_lifecycle_test.cpp`: added an ignored `Trade` event beside `PlayerGoldChange` so accidental Trade effects cannot mutate gold.
  - Follow-up verification:
    - `git diff --check -- timaert_c/src/events/effect_applicator.cpp timaert_c/tests/quest_lifecycle_test.cpp` PASS, only existing CRLF warnings.
    - Targeted MSVC build: `cmake --build build-msvc --target quest_lifecycle_test timaert` PASS.
    - `build-msvc/quest_lifecycle_test.exe` PASS with `effects=ok xp_effect=ok`.
  - Not yet done: full suite; macOS Ninja build from a macOS host.

- Task: LogicNode self-reactivation guard.
- TS authority:
  - `src/game/logic-nodes.ts::tick()` iterates the active `Set`, executes a fulfilled node, queues that id for removal, then applies `next` activations after the scan.
  - Activating the currently active id without deleting it is a JS `Set.add(existing)` no-op; using `next: [self]` is the intended one-shot-per-tick loop.
- Native finding:
  - The existing native self-remove test covered definition removal, but not the safe self-activation cases that plot/logic data can naturally use.
  - A Node probe confirmed a pathological `delete(current); add(current)` inside the same JS `Set` iteration can repeat in the same tick. That is not a pattern currently used by project data and should not be introduced casually.
- Change:
  - `tests/quest_lifecycle_test.cpp`: added `test_logic_node_self_reactivation_safe_cases()`.
  - The test proves direct `ctx.activate("self")` during the node effect does not keep the node active after the post-scan removal, and `next = {"self"}` remains active but fires only once per tick.
- Verification:
  - `git diff --check -- timaert_c/tests/quest_lifecycle_test.cpp` PASS, only existing CRLF warning.
  - Targeted MSVC build: `cmake --build build-msvc --target quest_lifecycle_test` PASS.
  - `build-msvc/quest_lifecycle_test.exe` PASS with `logic_self_reactivate=ok`.
  - Project-data scan: `rg "removeNode|remove_node|activate\\(|ctx\\.activate|next"` across TS/C++ plot/node code found normal `next=[self]` self-links and tests only; no production delete+readd-current-node effect pattern.
  - Not yet done: full suite; macOS Ninja build from a macOS host.

- Task: Macro-mode player recovery parity.
- TS authority:
  - `src/screens/GameScreen.svelte::advanceWorldMinute()` calls `advanceWorldMinuteTick()` and then, only when not in subworld/settlement subworld mode, restores HP/MP/SP each in-game minute.
  - Formula: `10 per hour * (1 + governingAttribute * 0.01) / 60`, where END governs SP, VIT governs HP, and WIL governs MP.
- Native finding:
  - `macro/world_tick.cpp` correctly mirrors clock/day progression, but native app runtime had no macro-mode per-minute player recovery after `tick_world()`.
  - Native `CombatStats` are integer POD while TS stats are JS numbers, so sub-integer minute recovery needs a runtime fractional accumulator to avoid either losing the regen or granting too much per minute.
- Change:
  - Added `src/macro/player_recovery.{h,cpp}` with `PlayerRecoveryAccumulator` and `apply_macro_minute_recovery()`.
  - `app/main.cpp`: stores a recovery accumulator on `App`, resets it on world destroy/boot, applies recovery only in the macro branch after `tick_world()`, and added smoke action `macro_recovery`.
  - Added `tests/player_recovery_parity_test.cpp` and `player_recovery_parity_test` target.
- Verification:
  - `git diff --check -- timaert_c/src/app/main.cpp timaert_c/src/macro/player_recovery.h timaert_c/src/macro/player_recovery.cpp timaert_c/tests/player_recovery_parity_test.cpp timaert_c/CMakeLists.txt` PASS, only existing CRLF warnings.
  - MSVC configure/build: `cmake -S . -B build-msvc && cmake --build build-msvc --target player_recovery_parity_test` PASS.
  - `build-msvc/player_recovery_parity_test.exe` PASS with `OK player_recovery_parity_test rate=ok clamp=ok zero=ok`.
  - Targeted MSVC app build: `cmake --build build-msvc --target timaert` PASS.
  - Runtime smoke: `TIMAERT_BOOT_TRACE=1 TIMAERT_SMOKE_SEED=42 TIMAERT_SMOKE_SCRIPT=new_game,wait_boot_done,macro_recovery,quit SDL_AUDIODRIVER=dummy build-msvc/timaert.exe` PASS.
  - Runtime evidence: `macro_recovery minutes=6 hp=1 mp=1 sp=1`.
  - Follow-up guard:
    - `app/main.cpp`: added smoke action `subworld_no_recovery`.
    - Runtime smoke: `TIMAERT_BOOT_TRACE=1 TIMAERT_SMOKE_SEED=42 TIMAERT_SMOKE_SCRIPT=new_game,wait_boot_done,subworld_no_recovery,quit SDL_AUDIODRIVER=dummy build-msvc/timaert.exe` PASS.
    - Runtime evidence: `subworld_no_recovery frames=42 dt=0.100 minutes=6 hp=5 mp=5 sp=5`.
  - Not yet done: full suite; macOS Ninja build from a macOS host.

- Task: TimeAdvance emission parity without subscribers.
- TS authority:
  - `src/game/world-tick.ts::advanceWorldMinute()` emits `TimeAdvance` on every hour rollover. It does not skip the event when there are no listeners or active `wait_at` quests.
- Native finding:
  - `app/main.cpp::emit_time_advance_if_needed()` skipped `TimeAdvance` when there were no native subscribers and no active wait objective. That preserved wait-quest behavior but made event history diverge from TS.
- Change:
  - `app/main.cpp`: removed the no-subscriber/no-wait early return so hourly `TimeAdvance` events are always emitted.
  - Existing `timeadvance_burst` smoke now also advances one hour after unsubscribing the only `TimeAdvance` listener and asserts the event is still written to history.
- Verification:
  - `git diff --check -- timaert_c/src/app/main.cpp docs/notes/CODEX_PROGRESS.md` PASS, only existing CRLF warning.
  - Targeted MSVC app build: `cmake --build build-msvc --target timaert` PASS.
  - Runtime smoke: `TIMAERT_BOOT_TRACE=1 TIMAERT_SMOKE_SEED=42 TIMAERT_SMOKE_SCRIPT=new_game,wait_boot_done,timeadvance_burst,quit SDL_AUDIODRIVER=dummy build-msvc/timaert.exe` PASS.
  - Runtime evidence: `timeadvance_burst hoursAdvanced=3 count=3 events=[0:07,0:08,0:09] noSubDelta=1 latest=0:10`.
  - Not yet done: full suite; macOS Ninja build from a macOS host.

- Task: Item use parity guard.
- TS authority:
  - `src/game/items.ts::useItem()` only consumes Potion/Food, clamps HP/MP/SP restoration to current max, still consumes and reports `+0` when already full, returns `Used <name>: ...`, and leaves missing/non-consumable items untouched.
- Native finding:
  - `macro/items.cpp::use_item()` matched the audited TS behavior, but there was no focused regression test protecting the exact messages, clamp, and consume rules.
- Change:
  - Added `tests/item_use_parity_test.cpp`.
  - Added explicit `item_use_parity_test` target in `timaert_c/CMakeLists.txt`.
  - Test coverage: HP potion partial clamp, full MP potion `+0 MP` consumption, food HP restore, non-consumable material ignored, missing item ignored.
- Verification:
  - `git diff --check -- timaert_c/tests/item_use_parity_test.cpp timaert_c/CMakeLists.txt` PASS, only existing CRLF warning.
  - MSVC configure/build: `cmake -S . -B build-msvc && cmake --build build-msvc --target item_use_parity_test` PASS. Existing SDL2_mixer CMake deprecation warning remains.
  - `build-msvc/item_use_parity_test.exe` PASS with `OK item_use_parity_test hp=ok full=ok food=ok non_consumable=ok`.
  - Not yet done: full suite; macOS Ninja build from a macOS host.

- Task: Spell casting parity audit, macro `canCast`, and Armageddon cap.
- TS authority:
  - `src/game/spells/spell-casting.ts::canCast()` allows world-map casting for any learned spell with a `macro` definition; it does not reject non-sustained macro spells just because a native executor is missing.
  - `src/game/spells/armageddon.ts::spawn()` uses `Math.max(16, Math.ceil(spread * 0.2))` for meteor count and has no upper cap.
- Native finding:
  - `content/spells/spell_book.cpp::spellbook_can_cast_ex()` had a native-only `World-map spell effect not implemented` rejection for non-sustained macro spells.
  - `content/spells/registry.cpp::spawn_armageddon()` capped meteors at 48, which diverged from TS at high scaled radii.
  - The app-level macro hotkey still blocks unimplemented non-sustained macro effects before spending MP; this is an honest runtime guard, not the pure `canCast` parity rule.
- Change:
  - `spell_book.cpp`: removed the non-sustained macro rejection from `spellbook_can_cast_ex()`.
  - `registry.cpp`: removed the Armageddon 48-meteor cap.
  - `tests/spell_casting_effects_test.cpp`: now asserts world-map `canCast` succeeds per TS for all learned macro spells (`fireball`, `ice_shard`, `lightning_chain`, `armageddon`, `haste`, `flight`), keeps no-effect native `spellbook_cast(..., inMicro=false)` from mutating state, and uses a high-INT Armageddon case that exceeds the old 48 cap.
- Verification:
  - `git diff --check -- timaert_c/src/content/spells/spell_book.cpp timaert_c/src/content/spells/registry.cpp timaert_c/tests/spell_casting_effects_test.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC build/run: `cmake --build build-msvc --target spell_casting_effects_test && build-msvc/spell_casting_effects_test.exe` PASS.
  - Runtime evidence: `PASS: projectiles=4 mp=0 cooldowns=2 sustained=0`.
  - Targeted MSVC app build: `cmake --build build-msvc --target timaert` PASS.
  - Not yet done: full suite; macOS Ninja build from a macOS host; actual non-sustained macro spell effects (`damage_region`, `buff_army`) remain unwired.

- Task: Energy Beam radius parity.
- TS authority:
  - `src/screens/SubworldScreen.svelte::tryCastSpell()` passes `projRadius = Math.max(1, rad > 10 ? 2.5 : 1.5)` into `engine.castSpell()`.
  - `src/game/spells/energy-beam.ts::spawn()` stores that radius on the visual beam descriptor, and `beamDamage()` uses `width: sc.radius * 2`.
  - `src/game/subworld/engine.ts::deferredDamage()` applies the beam damage on visual projectile expiry, not immediately on cast.
- Native finding:
  - Native correctly used a visual beam projectile with deferred expiry damage, but `registry.cpp::spawn_energy_beam()` wrote scaled spell radius 8.0 into `Projectile::radius`, making the damage corridor wider than TS.
  - `sub/spell_effects.cpp::apply_spell_beam()` used `p.radius + targetRadius`, while TS uses `p.radius * 2 + targetRadius`.
- Change:
  - `registry.cpp`: Energy Beam projectile radius now uses the TS spawn radius path (`1.5f` fallback / registry value) instead of scaled base radius.
  - `spell_effects.cpp`: beam hit width now uses `p.radius * 2.0f + target_radius`.
  - `spell_casting_effects_test.cpp`: asserts Energy Beam descriptor radius is `1.5f` and that a target at perpendicular distance 11 from the ray is a TS-style near miss.
- Verification:
  - `git diff --check -- timaert_c/src/content/spells/registry.cpp timaert_c/src/sub/spell_effects.cpp timaert_c/tests/spell_casting_effects_test.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC build/run: `cmake --build build-msvc --target spell_casting_effects_test && build-msvc/spell_casting_effects_test.exe` PASS.
  - Runtime evidence: `PASS: projectiles=4 mp=0 cooldowns=2 sustained=0`.
  - Targeted MSVC app build: `cmake --build build-msvc --target timaert` PASS.
  - Not yet done: full suite; macOS Ninja build from a macOS host.

- Task: Lightning Chain runtime parity.
- TS authority:
  - `src/game/spells/lightning-chain.ts` defines `chainCount` / `chainDecay` in data, but its `effect` only provides `renderProjectile`; it has no custom `spawn`.
  - `src/screens/SubworldScreen.svelte::tryCastSpell()` passes `speed = activeSpell.micro?.speed` and `projRadius = Math.max(1, rad > 10 ? 2.5 : 1.5)`.
  - `src/game/subworld/engine.ts::castSpell()` falls back to speed `300` when the passed speed is `0`, then the normal projectile hit path damages only the first hostile hit; it does not use `chainCount`.
- Native finding:
  - `registry.cpp::spawn_lightning_chain()` invented a native-only 420 px/s projectile with 0.6s lifetime, radius 7, `chainRemaining=4`, and `chainRadius=140`.
  - `spell_casting_effects_test.cpp` had been locking that native-only chain behavior instead of the current TS runtime.
- Change:
  - `registry.cpp`: Lightning Chain now spawns a normal TS-style projectile: fallback speed 300, radius 1.5, 3s lifetime, no chain damage fields on the projectile.
  - `spell_casting_effects_test.cpp`: descriptor assertions now reject native-only chain fields, and the hit test verifies only the first hostile is damaged while the nearby friendly and second hostile remain untouched.
- Verification:
  - `git diff --check -- timaert_c/src/content/spells/registry.cpp timaert_c/tests/spell_casting_effects_test.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC build/run: `cmake --build build-msvc --target spell_casting_effects_test && build-msvc/spell_casting_effects_test.exe` PASS.
  - Runtime evidence: `PASS: projectiles=4 mp=0 cooldowns=2 sustained=0`.
  - Targeted MSVC app build: `cmake --build build-msvc --target timaert` PASS.
  - Not yet done: full suite; macOS Ninja build from a macOS host.

- Task: Sustained spell zero-MP cancellation parity.
- TS authority:
  - `src/game/spells/spell-casting.ts::tickSpellBook()` checks each sustained spell every tick; if `combat.currentMp < spell.manaDrain * dt`, it sets MP to 0 and removes that spell.
  - With JS-number MP, a spell that exactly drains MP to 0 remains active for that tick, then is removed on the next positive drain tick.
- Native finding:
  - Native integer MP uses `sustainedDrainCarry` to preserve sub-integer drain over frames, but when MP was already 0 and the next `dt` accumulated less than 1 MP of drain, the spell could stay active longer than TS.
- Change:
  - `spell_book.cpp`: when sustained spells have positive drain and `currentMp <= 0`, the next tick removes all positive-drain sustained spells immediately and clears carry.
  - `spell_casting_effects_test.cpp`: added a boundary case where Haste drains 1 MP to exactly 0, remains active for that tick, then must be removed on a following `0.05s` positive drain tick.
- Verification:
  - `git diff --check -- timaert_c/src/content/spells/spell_book.cpp timaert_c/tests/spell_casting_effects_test.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC build/run: `cmake --build build-msvc --target spell_casting_effects_test && build-msvc/spell_casting_effects_test.exe` PASS.
  - Runtime evidence: `PASS: projectiles=4 mp=0 cooldowns=2 sustained=0`.
  - Targeted MSVC app build: `cmake --build build-msvc --target timaert` PASS.
  - Not yet done: full suite; macOS Ninja build from a macOS host.

- Task: Spell cooldown reason string parity.
- TS authority:
  - `src/game/spells/spell-casting.ts::canCast()` returns `Cooldown ${cd.toFixed(1)}s` while a learned spell is still cooling down.
- Native finding:
  - `spell_book.cpp::spellbook_can_cast_ex()` returned the native-only literal `Cooldown`, so UI/events/tests could not match the TS error reason.
- Change:
  - `spell_book.h`: `CastCheck::reason` now owns a `std::string`, allowing formatted reasons without dangling buffers.
  - `spell_book.cpp`: cooldown rejection now formats `Cooldown %.1fs`.
  - `app/main.cpp` and `ui/overlays.cpp`: reason consumers now pass/read `reason.c_str()` at the boundary.
  - `spell_casting_effects_test.cpp`: cooldown assertion now expects `Cooldown 2.0s`.
- Verification:
  - `git diff --check -- timaert_c/src/content/spells/spell_book.h timaert_c/src/content/spells/spell_book.cpp timaert_c/src/app/main.cpp timaert_c/src/ui/overlays.cpp timaert_c/tests/spell_casting_effects_test.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC build/run: `cmake --build build-msvc --target spell_casting_effects_test && build-msvc\spell_casting_effects_test.exe` PASS.
  - Runtime evidence: `PASS: projectiles=4 mp=0 cooldowns=2 sustained=0`.
  - Targeted MSVC app build: `cmake --build build-msvc --target timaert` PASS.
  - Not yet done: full suite; macOS Ninja build from a macOS host.

- Task: Micro spell projectile/blast radius runtime parity.
- TS authority:
  - `src/screens/SubworldScreen.svelte::tryCastSpell()` uses `rad = activeSpell.micro?.baseRadius ?? 1`, `blast = activeSpell.micro?.friendlyFire ? rad : 0`, and `projRadius = Math.max(1, rad > 10 ? 2.5 : 1.5)`.
  - `src/game/spells/armageddon.ts::spawn()` scales meteor count from the runtime `blastRadius`, not from `spellRadius(...)`.
- Native finding:
  - `spell_book.cpp::spellbook_cast()` passed scaled `spell_radius(...)` into the spawn context, so high-INT Fireball/Armageddon had wider runtime AoE/spread than TS.
  - `registry.cpp` kept hardcoded native projectile radii for Fireball/Ice Shard/Magic Bolt/Armageddon (`12/8/6/40`) instead of the TS runtime `2.5/1.5/1.5/2.5`.
  - The first targeted test attempt failed at `magic_bolt descriptor wrong`, proving the spawn function was still ignoring the corrected registry value.
- Change:
  - `spell_book.cpp`: micro cast context now passes `friendlyFire ? baseRadius : 0` as the blast/effect radius.
  - `registry.cpp`: Fireball/Ice Shard/Magic Bolt spawn paths now consume `SpellSpawnContext::projectileRadius`; built-in projectile radii match TS runtime.
  - `registry.cpp`: Armageddon runtime projectile radius is now `2.5`; meteor count now follows base `blastRadius` in runtime casts even when UI `spellRadius(...)` is larger.
  - `spell_casting_effects_test.cpp`: added descriptor assertions for Magic Bolt/Ice Shard/Fireball radii, Fireball blast radius `48`, and Armageddon high-INT ignoring scaled UI radius while still spawning `32` meteors from base radius `160`.
- Verification:
  - `git diff --check -- timaert_c/src/content/spells/spell_book.cpp timaert_c/src/content/spells/registry.cpp timaert_c/tests/spell_casting_effects_test.cpp` PASS, only existing CRLF warnings.
  - First targeted MSVC build/run after the initial patch: FAIL, `FAIL: magic_bolt descriptor wrong`; root cause was hardcoded radius inside `spawn_magic_bolt()`.
  - Targeted MSVC build/run after the spawn fix: `cmake --build build-msvc --target spell_casting_effects_test && build-msvc\spell_casting_effects_test.exe` PASS.
  - Runtime evidence: `PASS: projectiles=4 mp=0 cooldowns=2 sustained=0`.
  - Targeted MSVC app build: `cmake --build build-msvc --target timaert` PASS.
  - Not yet done: full suite; macOS Ninja build from a macOS host.

- Task: AoE blast hit-radius parity.
- TS authority:
  - `src/game/subworld/engine.ts::projectileExplode()` checks `dx * dx + dy * dy <= radius * radius`; it does not add the target entity radius to the AoE blast radius.
  - TS only uses entity radius for direct projectile overlap and beam width checks.
- Native finding:
  - `sub/spell_effects.cpp::apply_spell_blast()` used `p.blastRadius + target_radius(...)`, so Fireball/Armageddon hit targets whose centers were outside the TS blast circle.
- Change:
  - `spell_effects.cpp`: blast damage now uses `p.blastRadius` as a center-distance threshold, matching TS.
  - `spell_casting_effects_test.cpp`: added an edge test where a target centered at `48` is hit by a Fireball blast and a target at `53` with sprite radius `6` is not.
- Verification:
  - `git diff --check -- timaert_c/src/sub/spell_effects.cpp timaert_c/tests/spell_casting_effects_test.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC build/run: `cmake --build build-msvc --target spell_casting_effects_test && build-msvc\spell_casting_effects_test.exe` PASS.
  - Runtime evidence: `PASS: projectiles=4 mp=0 cooldowns=2 sustained=0`.
  - Targeted MSVC app build: `cmake --build build-msvc --target timaert` PASS.
  - Not yet done: full suite; macOS Ninja build from a macOS host.

- Task: Fireball timeout behavior parity.
- TS authority:
  - `src/game/spells/fireball.ts` has no custom `spawn`.
  - `src/game/subworld/engine.ts::castSpell()` default projectile does not set `explodeOnExpiry`; `projectileExplode()` runs only on collision for normal Fireball projectiles.
- Native finding:
  - `registry.cpp::emplace_projectile()` set `explodeOnExpiry = blast > 0`, so Fireball could explode after lifetime expiry even when it never hit anything.
- Change:
  - `registry.cpp`: normal bolt projectiles now set `explodeOnExpiry=false`; Armageddon and Energy Beam keep their explicit visual-first expiry behavior in their custom spawn paths.
  - `spell_casting_effects_test.cpp`: Fireball descriptor now rejects expiry explosion and a no-hit timeout test asserts a nearby target is not damaged after `3.1s`.
- Verification:
  - `git diff --check -- timaert_c/src/content/spells/registry.cpp timaert_c/tests/spell_casting_effects_test.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC build/run: `cmake --build build-msvc --target spell_casting_effects_test && build-msvc\spell_casting_effects_test.exe` PASS.
  - Runtime evidence: `PASS: projectiles=4 mp=0 cooldowns=2 sustained=0`.
  - Targeted MSVC app build: `cmake --build build-msvc --target timaert` PASS.
  - Not yet done: full suite; macOS Ninja build from a macOS host.

- Task: Spell projectile spawn-origin parity.
- TS authority:
  - `src/screens/SubworldScreen.svelte` creates the actual player entity with `radius: 1.5`.
  - `src/game/subworld/engine.ts::castSpell()` and `src/game/spells/energy-beam.ts::spawn()` offset default projectile/beam origins by `player.radius + 2`, so the runtime offset is `3.5`.
- Native finding:
  - `registry.cpp` used a fixed `4.0f` spawn/origin offset. A read-only audit initially suggested the `engine.addEntity()` default radius `4`, but that is not the player radius used by `SubworldScreen.svelte`.
- Change:
  - `spell_types.h`: added `kSpellCasterRadius = 1.5f` and `SpellSpawnContext::playerRadius`.
  - `spell_book.cpp` and `spell_types.cpp`: spawn context now carries the TS player radius.
  - `registry.cpp`: default projectile spawn position and Energy Beam origin now use `playerRadius + 2`.
  - `spell_casting_effects_test.cpp`: asserts Magic Bolt spawns at `x=103.5` from `(100,100)` along `+X`, and Energy Beam origin uses the same offset.
- Verification:
  - `git diff --check -- timaert_c/src/content/spells/spell_types.h timaert_c/src/content/spells/spell_types.cpp timaert_c/src/content/spells/spell_book.cpp timaert_c/src/content/spells/registry.cpp timaert_c/tests/spell_casting_effects_test.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC build/run: `cmake --build build-msvc --target spell_casting_effects_test && build-msvc\spell_casting_effects_test.exe` PASS.
  - Runtime evidence: `PASS: projectiles=4 mp=0 cooldowns=2 sustained=0`.
  - Targeted MSVC app build: `cmake --build build-msvc --target timaert` PASS.
  - Not yet done: full suite; macOS Ninja build from a macOS host.
  - Remaining spell audit item: Armageddon meteor placement still uses native hash RNG instead of consuming the live subworld RNG stream like TS.

- Task: Armageddon meteor RNG parity.
- TS authority:
  - `src/game/subworld/engine.ts::constructor()` seeds the subworld RNG with `config.seed`.
  - `src/screens/GameScreen.svelte` passes `seed={gState.seed + gState.player.x * 1000 + gState.player.y}` into `SubworldScreen`.
  - `src/game/spells/armageddon.ts::spawn()` consumes `rng()` three times per meteor: angle, distance, delay.
- Native finding:
  - `registry.cpp::spawn_armageddon()` used coordinate/spell-id hash values for meteor placement, so runtime meteor layout did not follow the TS subworld RNG stream.
- Change:
  - `SpellSpawnContext` now carries an optional `SpellRngFn` callback.
  - `SubworldEngine` owns a spell RNG seeded as `worldSeed + cellX * 1000 + cellY`, matching the TS screen seed formula.
  - `app/main.cpp::cast_active_spell()` passes the live subworld RNG into `spellbook_cast()`.
  - `registry.cpp::spawn_armageddon()` now consumes the callback for angle/distance/delay when present; hash fallback remains for direct low-level calls without a runtime RNG.
  - `spell_casting_effects_test.cpp`: fixed sequence `[0, 0.5, 0.25]` now proves the first TS-style meteor appears at `(180,100)` with lifetime `0.425s` for spread `160`.
- Verification:
  - `git diff --check -- timaert_c/src/content/spells/spell_types.h timaert_c/src/content/spells/spell_types.cpp timaert_c/src/content/spells/spell_book.h timaert_c/src/content/spells/spell_book.cpp timaert_c/src/content/spells/registry.cpp timaert_c/src/sub/engine.h timaert_c/src/sub/engine.cpp timaert_c/src/app/main.cpp timaert_c/tests/spell_casting_effects_test.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC build/run: `cmake --build build-msvc --target spell_casting_effects_test && build-msvc\spell_casting_effects_test.exe` PASS.
  - Runtime evidence: `PASS: projectiles=4 mp=0 cooldowns=2 sustained=0`.
  - Targeted MSVC app build: `cmake --build build-msvc --target timaert` PASS.
  - Not yet done: full suite; macOS Ninja build from a macOS host.

- Task: 3D spell projectile visual parity pass.
- TS authority:
  - `src/game/spells/spell-renderer.ts` delegates projectile rendering to per-spell renderers.
  - `magic-bolt.ts` renders a short elongated violet streak with trail/core/hot tip.
  - `ice-shard.ts` renders an elongated crystalline diamond with shimmer.
  - `fireball.ts` renders a pulsing fire glow with hot core.
  - `armageddon.ts` renders meteors that grow visually as lifetime expires.
- Native finding:
  - `sub/renderer_3d.cpp` rendered most spell projectiles as the same generic colored quad; only beams had a separate ribbon path.
- Change:
  - `renderer_3d.cpp`: spell instance `kind` now distinguishes beam/ribbon, arcane streak, ice shard, fireball glow, and Armageddon meteor.
  - `renderer_3d.cpp`: spell shader now receives local UV/kind/time and masks/tints projectile quads into streaks, diamond shards, circular fire glows, and growing meteor glows.
  - `renderer_3d.cpp`: Magic Bolt/Lightning Chain use elongated direction ribbons; Ice Shard uses a narrower diamond; Fireball and Armageddon use billboarded glow masks.
- Verification:
  - `git diff --check -- timaert_c/src/sub/renderer_3d.cpp` PASS, only existing CRLF warning.
  - Targeted MSVC app build: `cmake --build build-msvc --target timaert` PASS.
  - Runtime smoke: `TIMAERT_SMOKE_SCRIPT=new_game,wait_boot_done,cast_spell,capture_frame,quit TIMAERT_SMOKE_SEED=42 SDL_AUDIODRIVER=dummy build-msvc\timaert.exe` PASS.
  - Runtime evidence: shader path initialized, `spell_projectile active=magic_bolt projectiles=0->1`, captured `timaert_c/smoke_03_ui.ppm`.
  - Not yet done: full visual A/B against TS screenshots; full suite; macOS Ninja build from a macOS host.

- Task: Subworld damage flash feedback parity.
- TS authority:
  - `src/screens/SubworldScreen.svelte` tracks `lastPlayerHp`; when `engine.player.hp` drops, it sets `hitFlashTimer` to at least `0.3`.
  - While active, TS draws a red full-canvas overlay with alpha `min(0.45, hitFlashTimer * 1.6)`.
- Native finding:
  - Native subworld already showed a short status message on hit, but there was no screen-space damage flash layer, so incoming damage lacked the TS immediate red feedback.
- Change:
  - `app/main.cpp`: `App` now tracks `subworldLastPlayerHp` and `subworldHitFlashTimer`.
  - `tick_playing_runtime()` detects subworld HP drops and decays the flash timer.
  - Native subworld rendering now draws a translucent red background overlay while the flash timer is active.
  - `subworld_enemy_feedback` smoke now asserts `flash > 0` after a hostile melee hit.
- Verification:
  - `git diff --check -- timaert_c/src/app/main.cpp` PASS, only existing CRLF warning.
  - Targeted MSVC app build: `cmake --build build-msvc --target timaert` PASS.
  - Runtime smoke: `TIMAERT_SMOKE_SCRIPT=new_game,wait_boot_done,subworld_enemy_feedback,capture_frame,quit TIMAERT_SMOKE_SEED=42 SDL_AUDIODRIVER=dummy build-msvc\timaert.exe` PASS.
  - Runtime evidence: `subworld_enemy_feedback spriteOnly=8 hp=110->103 flash=0.100 status="Hit by Hostile for 7 (E 5m)"`, captured `timaert_c/smoke_03_ui.ppm`.
  - Not yet done: full suite; macOS Ninja build from a macOS host.

- Task: Subworld danger gem parity.
- TS authority:
  - `src/game/subworld/engine.ts::getDangerLevel()` returns `green`, `yellow`, or `red` from one hostile scan: red when a living hostile is within `max(playerRange, 40)`, yellow when within `DETECTION_RADIUS`, green otherwise.
  - `src/screens/SubworldScreen.svelte` maps that level to a green/yellow/red gem and tooltip.
- Native finding:
  - Native already blocked exit in high-danger zones when hostiles were nearby, but it did not expose the TS green/yellow/red danger level or render the gem feedback.
- Change:
  - `sub/engine.h/.cpp`: added `DangerLevel` and `SubworldEngine::danger_level()`.
  - `app/main.cpp`: draws a small subworld danger gem below the top HUD, with Safe/Caution/Danger colors and tooltip text.
  - `subworld_enemy_feedback` smoke now asserts the injected melee hostile produces `DangerLevel::Red`.
- Verification:
  - `git diff --check -- timaert_c/src/sub/engine.h timaert_c/src/sub/engine.cpp timaert_c/src/app/main.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC app build: `cmake --build build-msvc --target timaert` PASS.
  - Runtime smoke: `TIMAERT_SMOKE_SCRIPT=new_game,wait_boot_done,subworld_enemy_feedback,capture_frame,quit TIMAERT_SMOKE_SEED=42 SDL_AUDIODRIVER=dummy build-msvc\timaert.exe` PASS.
  - Runtime evidence: `subworld_enemy_feedback spriteOnly=8 hp=110->103 flash=0.100 danger=2 status="Hit by Hostile for 7 (E 5m)"`, captured `timaert_c/smoke_03_ui.ppm`.
  - Not yet done: full suite; macOS Ninja build from a macOS host.

- Task: Subworld combat log parity.
- TS authority:
  - `src/game/subworld/engine.ts` owns `combatLog`, pushes player-involved hit/kill messages, caps the log at 20 entries, and advances entry time every tick.
  - `src/screens/SubworldScreen.svelte` renders the last visible 5 entries for 4 seconds at top-center with fade.
- Native finding:
  - Native only had the short bottom `statusLine_`; incoming damage gave no rolling combat log entry like TS.
- Change:
  - `sub/engine.h/.cpp`: added fixed-size `CombatLogEntry[20]`, public read access, age advancement, and `push_combat_log()`.
  - Hostile melee damage now appends `"<attacker> hit/killed you for N"` while preserving the existing directional status line.
  - `app/main.cpp`: draws the latest visible 5 combat-log entries at top-center with TS-style warm text and fading black backing.
  - `subworld_enemy_feedback` smoke now asserts a visible combat-log entry in addition to sprite, HP, flash, danger, and status feedback.
- Verification:
  - `git diff --check -- timaert_c/src/sub/engine.h timaert_c/src/sub/engine.cpp timaert_c/src/app/main.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC app build: `cmake --build build-msvc --target timaert` PASS.
  - Runtime smoke: `TIMAERT_SMOKE_SCRIPT=new_game,wait_boot_done,subworld_enemy_feedback,capture_frame,quit TIMAERT_SMOKE_SEED=42 SDL_AUDIODRIVER=dummy build-msvc\timaert.exe` PASS.
  - Runtime evidence: `subworld_enemy_feedback spriteOnly=8 hp=110->103 flash=0.100 danger=2 combatLog=1 latest="Hostile hit you for 7" status="Hit by Hostile for 7 (E 5m)"`, captured `timaert_c/smoke_03_ui.ppm`.
  - Not yet done: full suite; macOS Ninja build from a macOS host.

- Task: Subworld combat log outgoing spell hit parity.
- TS authority:
  - `src/game/subworld/engine.ts::recordHit()` logs player-involved damage, including spell/projectile paths that call `recordHit(this.player, ent, damage)`.
- Native finding:
  - After the first native combat-log pass, hostile melee logged incoming player damage, but `tick_spell_projectiles()` still applied player spell damage without notifying `SubworldEngine` combat log.
- Change:
  - `sub/spell_effects.h/.cpp`: `tick_spell_projectiles()` now accepts an optional zero-allocation C callback for player-owned spell damage.
  - `sub/engine.cpp`: passes a private callback and formats `You hit/killed <target> for N` using the target NPC label.
  - `cast_spell` smoke now creates a deterministic target on the Magic Bolt trajectory and asserts that projectile damage appends a combat-log line.
- Verification:
  - `git diff --check -- timaert_c/src/sub/engine.h timaert_c/src/sub/engine.cpp timaert_c/src/sub/spell_effects.h timaert_c/src/sub/spell_effects.cpp timaert_c/src/app/main.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC build: `cmake --build build-msvc --target timaert spell_casting_effects_test` PASS.
  - Targeted test: `build-msvc\spell_casting_effects_test.exe` PASS with `PASS: projectiles=4 mp=0 cooldowns=2 sustained=0`.
  - Runtime smoke: `TIMAERT_SMOKE_SCRIPT=new_game,wait_boot_done,cast_spell,capture_frame,quit TIMAERT_SMOKE_SEED=42 SDL_AUDIODRIVER=dummy build-msvc\timaert.exe` PASS.
  - Runtime evidence: `spell_projectile active=magic_bolt projectiles=0->1 mp=100 cd=0 event=1 log="You killed Bandit for 13"`, captured `timaert_c/smoke_03_ui.ppm`.
  - Not yet done: full suite; macOS Ninja build from a macOS host.

- Task: Subworld entity hit flash parity.
- TS authority:
  - `src/game/subworld/engine.ts` defines `HIT_FLASH_DURATION = 0.15`, sets `entity.hitTimer` on melee, projectile, and beam damage, and decays it every tick.
  - `src/game/subworld/renderer.ts` overlays red flash on sprite/circle entities while `hitTimer > 0`.
- Native finding:
  - Native had player screen damage flash and combat log, but targets hit by melee/spells did not get the TS per-entity red flash feedback.
- Change:
  - `ecs/components.h`: added POD `HitFlash`.
  - `sub/engine.cpp`: melee target hits set `HitFlash{0.15}` and `SubworldEngine::tick_hit_flashes()` decays/removes it without heap allocation.
  - `sub/spell_effects.cpp`: spell projectile/blast/beam damage sets `HitFlash{0.15}` on targets.
  - `sub/renderer_3d.cpp/.h`: fallback colored sprite billboards and paper-doll character billboards now red-tint/flash-scale while `HitFlash` is active.
  - `cast_spell` smoke now keeps a deterministic Magic Bolt target alive and asserts the remaining hit flash timer after the projectile tick.
- Verification:
  - `git diff --check -- timaert_c/src/ecs/components.h timaert_c/src/sub/engine.h timaert_c/src/sub/engine.cpp timaert_c/src/sub/spell_effects.h timaert_c/src/sub/spell_effects.cpp timaert_c/src/sub/renderer_3d.h timaert_c/src/sub/renderer_3d.cpp timaert_c/src/app/main.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC build: `cmake --build build-msvc --target timaert spell_casting_effects_test` PASS.
  - Targeted test: `build-msvc\spell_casting_effects_test.exe` PASS with `PASS: projectiles=4 mp=0 cooldowns=2 sustained=0`.
  - Runtime smoke: `TIMAERT_SMOKE_SCRIPT=new_game,wait_boot_done,cast_spell,capture_frame,quit TIMAERT_SMOKE_SEED=42 SDL_AUDIODRIVER=dummy build-msvc\timaert.exe` PASS.
  - Runtime evidence: `spell_projectile active=magic_bolt projectiles=0->1 mp=100 cd=0 event=1 flash=0.050 log="You hit Bandit for 13"`, captured `timaert_c/smoke_03_ui.ppm`.
  - Not yet done: full suite; macOS Ninja build from a macOS host.

- Task: NPC missile attack parity.
- TS authority:
  - `src/game/subworld/engine.ts::resolveCombat()` branches `attackKind === 'missile'` into `spawnNpcMissile()` instead of instant melee damage.
  - `spawnNpcMissile()` creates a physical projectile with owner id, speed, radius 1.2, colour, damage, lifetime, optional blast radius, and `friendlyFire=false`.
- Native finding:
  - Native stored `Combat::Missile`, but `tick_subworld_combat()` still called the same instant strike path. Witch/Sorceress missile templates therefore lost their TS projectile behaviour and visual travel.
- Change:
  - `ecs/components.h`: added `MissileAttack` for speed/blast/colour data.
  - `sub/spawn.cpp` and `sub/engine.cpp::spawn_hostile_npc()`: missile NPC templates now attach `MissileAttack`.
  - `sub/engine.cpp`: missile attackers spawn physical `ecs::Projectile` entities; player collision is resolved in `SubworldEngine` because native has no ECS player entity in subworld.
  - NPC missiles now use the existing projectile renderer path, combat log, status line, and player damage flash on impact.
  - Added `subworld_missile_feedback` app smoke.
- Verification:
  - `git diff --check -- timaert_c/src/ecs/components.h timaert_c/src/sub/engine.h timaert_c/src/sub/engine.cpp timaert_c/src/sub/spawn.cpp timaert_c/src/sub/spell_effects.h timaert_c/src/sub/spell_effects.cpp timaert_c/src/sub/renderer_3d.h timaert_c/src/sub/renderer_3d.cpp timaert_c/src/app/main.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC build: `cmake --build build-msvc --target timaert spell_casting_effects_test` PASS.
  - Targeted test: `build-msvc\spell_casting_effects_test.exe` PASS with `PASS: projectiles=4 mp=0 cooldowns=2 sustained=0`.
  - Runtime smoke: `TIMAERT_SMOKE_SCRIPT=new_game,wait_boot_done,subworld_missile_feedback,capture_frame,quit TIMAERT_SMOKE_SEED=42 SDL_AUDIODRIVER=dummy build-msvc\timaert.exe` PASS.
  - Runtime evidence: `subworld_missile_feedback projectiles=0->0 hp=110->104 flash=0.200 combatLog=1 latest="Witch hit you for 6" status="Hit by Witch for 6 (here 0m)"`, captured `timaert_c/smoke_03_ui.ppm`.
  - Not yet done: full suite; macOS Ninja build from a macOS host.

- Task: Subworld player melee parity.
- TS authority:
  - `src/game/subworld/engine.ts::playerMelee()` runs every tick, decrements the player attack timer, uses `attackHeld`, range `playerRange ?? 5`, damage `playerDamage ?? 10`, cooldown `playerCooldown ?? 0.5`, records player-owned hit attribution, and sets target `hitTimer`.
  - `src/screens/SubworldScreen.svelte` binds `A` to `engine.attackHeld` and computes melee damage as `floor(10 + calculateDerived(...).rawPhysDamage)`.
- Native finding:
  - Native subworld had spell damage and NPC attacks, but no honest player melee loop. The player could not use the TS `A attack` combat path in native.
- Change:
  - `sub/engine.h/.cpp`: added `playerAttackHeld_`, `playerAttackTimer_`, and `tick_player_melee()`.
  - Native player melee now selects the nearest non-squad NPC within 5 tiles, applies `floor(10 + rawPhysDamage)`, sets `LastHit(playerOwned=true)`, `HitFlash{0.15}`, combat log, status line, cooldown `0.5`, and `NpcDeath` when lethal.
  - `app/main.cpp`: subworld input now maps `A`/left mouse to attack-held; `A` no longer doubles as left strafe in subworld. Left strafe remains on arrow-left and `Q`.
  - `ui/screens.cpp`: subworld hint bar now exposes `[A/LMB] attack` and `[Space] spell`.
  - Added `subworld_player_melee` smoke.
- Verification:
  - `git diff --check -- timaert_c/src/sub/engine.h timaert_c/src/sub/engine.cpp timaert_c/src/app/main.cpp timaert_c/src/ui/screens.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC build: `cmake --build build-msvc --target timaert spell_casting_effects_test` PASS.
  - Targeted test: `build-msvc\spell_casting_effects_test.exe` PASS with `PASS: projectiles=4 mp=0 cooldowns=2 sustained=0`.
  - Runtime smoke: `TIMAERT_SMOKE_SCRIPT=new_game,wait_boot_done,subworld_player_melee,capture_frame,quit TIMAERT_SMOKE_SEED=42 SDL_AUDIODRIVER=dummy build-msvc\timaert.exe` PASS.
  - Runtime evidence: `subworld_player_melee hp=40.0->29.0 expected=11.0 flash=0.100 playerOwned=1 log="You hit Bandit for 11" status="You hit Bandit for 11"`, captured `timaert_c/smoke_03_ui.ppm`.
  - Not yet done: full suite; macOS Ninja build from a macOS host.

- Task: Subworld movement SP drain parity.
- TS authority:
  - `src/screens/SubworldScreen.svelte` accumulates moved subworld distance after `engine.tick()`, charges `SUBWORLD_SP_PER_1000 = 10` per 1000 units, adds overload penalty, and converts negative SP into HP loss.
  - `src/game/movement-cost.ts` defines subworld movement as flat cost with no terrain penalty.
- Native finding:
  - Native had macro travel SP drain and a smoke proving subworld does not recover HP/MP/SP, but player input movement inside subworld did not spend SP.
- Change:
  - `app/main.cpp`: added `subworldDistanceAccum` and `charge_subworld_sp_for_distance()`.
  - Runtime subworld input now measures actual movement from `poll_movement()` and drains SP at the TS flat rate before the subworld tick/seam recenter path.
  - Overload uses native `inventory_weight`, `get_carry_capacity`, and `get_overload_penalty`, with integer ceil matching existing native macro travel handling.
  - Added `subworld_sp_drain` smoke.
- Verification:
  - `git diff --check -- timaert_c/src/app/main.cpp timaert_c/src/sub/engine.h timaert_c/src/sub/engine.cpp timaert_c/src/ui/screens.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC app build: `cmake --build build-msvc --target timaert` PASS.
  - Runtime smoke: `TIMAERT_SMOKE_SCRIPT=new_game,wait_boot_done,subworld_sp_drain,capture_frame,quit TIMAERT_SMOKE_SEED=42 SDL_AUDIODRIVER=dummy build-msvc\timaert.exe` PASS.
  - Runtime evidence: `subworld_sp_drain distance=100.0 charged=1 sp=100->99 hp=100->100 carry=0.0`, captured `timaert_c/smoke_03_ui.ppm`.
  - Not yet done: full suite; macOS Ninja build from a macOS host.

- Task: Subworld faction hostility and reputation-hit parity.
- TS authority:
  - `src/game/subworld/engine.ts` treats hostility to the player as reputation-driven: `tempHostileToPlayer` wins, otherwise faction reputation below `-50` is hostile.
  - `playerMelee()`, projectile damage, and beam damage apply `-1` reputation for hits on non-hostile faction NPCs, set temporary hostility for non-allied targets, and keep `ai === 'flee'` citizens from attacking in `resolveCombat()`.
  - `projectileShouldHit()` blocks normal non-friendly-fire player projectiles from hitting non-hostile faction targets.
- Native finding:
  - Native treated any `SubworldAi::Combat` NPC as hostile to the player, so settlement guards/combat-template NPCs could attack without reputation cause.
  - Native `SubworldAi::Flee` ran away from the player by distance alone, while TS only flees from a hostile threat.
  - Player melee/spells had hit feedback, but no faction reputation penalty/temp-hostile state.
  - Flee citizens could still strike after being marked hostile because native combat did not skip `SubworldAi::Flee`.
- Change:
  - `ecs/components.h`: added `TempHostileToPlayer` session tag.
  - `sub/engine.cpp`: added faction-id resolution for NPC vs fauna faction indices, reputation-driven `hostile_to_player_entity()`, hit/kill reputation penalties, temp-hostile marking, and TS-style Flee attacker skip.
  - `sub/ai.h/.cpp`: added an optional no-allocation player-threat callback so neutral Flee NPCs wander/idle normally and only sprint from the player when faction logic says the player is hostile.
  - `sub/spell_effects.h/.cpp`: `tick_spell_projectiles()` now accepts a no-allocation target filter callback; `SubworldEngine` uses it to enforce TS non-friendly-fire spell targeting.
  - `app/main.cpp`: added `subworld_reputation_hit` smoke and corrected smoke hostile faction indices for normal NPC types.
- Verification:
  - `git diff --check -- timaert_c/src/ecs/components.h timaert_c/src/sub/engine.h timaert_c/src/sub/engine.cpp timaert_c/src/sub/spell_effects.h timaert_c/src/sub/spell_effects.cpp timaert_c/src/app/main.cpp` PASS, only existing CRLF warnings.
  - Targeted MSVC build: `cmake --build build-msvc --target timaert spell_casting_effects_test` PASS after fixing the empty-tag `try_get` smoke bug.
  - Targeted test: `build-msvc\spell_casting_effects_test.exe` PASS with `PASS: projectiles=4 mp=0 cooldowns=2 sustained=0`.
  - Runtime smoke: `TIMAERT_SMOKE_SCRIPT=new_game,wait_boot_done,subworld_reputation_hit,capture_frame,quit TIMAERT_SMOKE_SEED=42 SDL_AUDIODRIVER=dummy build-msvc\timaert.exe` PASS.
  - Runtime evidence: `subworld_reputation_hit rep=0->-1 temp=1 ai=1 danger=2 neutralMove=0.000 friendlySpell=1 log="You hit Peasant for 11"`, captured `timaert_c/smoke_03_ui.ppm`.
  - Regression smoke: `TIMAERT_SMOKE_SCRIPT=new_game,wait_boot_done,cast_spell,subworld_missile_feedback,subworld_player_melee,capture_frame,quit TIMAERT_SMOKE_SEED=42 SDL_AUDIODRIVER=dummy build-msvc\timaert.exe` PASS.
  - Regression evidence: `spell_projectile ... log="You hit Bandit for 13"`, `subworld_missile_feedback ... latest="Witch hit you for 6"`, `subworld_player_melee ... log="You hit Bandit for 11"`, captured `timaert_c/smoke_05_ui.ppm`.
  - Not yet done: full suite; macOS Ninja build from a macOS host.
