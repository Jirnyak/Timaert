# Промт следующей сессии

Обновлён 2026-09-09 (конец сессии). Прочитай AGENTS.md; режим гайдед — код по
одобрению, вердикты в CANON.md. Стадия: ЦЕЛЕВОЙ БАГФИКС/ПОЛИШ перед контентом
демо (трек = memory bugfix-prerelease-track). Сценарий демо и имя — финальные
(см. CANON/release.md).

## ✅ СДЕЛАНО 2026-09-09 (вечер) — честная база смоуков + SAVE-1/SAVE-3

Работа по реестру `postdemoaudit.md` — берём id, правим, вердикт пишем В ФАЙЛ.

1. **БАЗА СНЯТА** (§9.0 реестра): весь смоук-набор × сиды 12345/1/7/999 =
   240 прогонов, `ctest` 95/95. Красных сценариев **пять, а не два**:
   `SMOKE-1` `prepare_spell_auras` (все сиды), `SMOKE-2` `cast_spell` (только
   12345), плюс новые `SMOKE-4` `toggle_flight` (1/7/999), `SMOKE-5`
   `dungeon_cave` (7), `SMOKE-6` `wait_visible` в одиночку (все).
   **Композиция набора записана** в §9.0 и в `debug.md` §7.05: пять токенов
   не самостоятельны (нужен `subworld_enter`), `spawn_squad_at_player` идёт
   ДО входа. Первый прогон стоил четырёх ложных красных ровно на этом.
2. **`SAVE-1` ПОЧИНЕН** (problems.md §38): свёртка загрузки вывернута —
   из 32 полей `GameState` файл владеет 30, генезис двумя (`politik`,
   `sessionFeed`), остальное одним `std::move`. Пять полей больше не
   теряются (шрамы ресурсов, лут-пул, корабли, следы, эмиттер id
   ландмарков). Свидетель — `save_payload_fingerprint`: тот же
   `write_payload` с фиксированной меткой → `checksum32`, считается до и
   после свёртки на двери загрузки; **поля перечисляет писатель, а не
   рука**. Сначала красный, потом зелёный; два негативных контроля сняты.
   Цена замерена: 69–74 мс при загрузке ~1.8 с (~4 %).
3. **`SAVE-3` ПОЧИНЕН** — сортировка блока кораблей; поднят из `P2` как
   условие честности свидетеля (иначе байты недетерминированы).

**Открыто после этого захода:**
- `SMOKE-7` — три макро-временны́х сценария (`macro_recovery`,
  `chronicle_rate`, `macro_travel_sp`) краснеют В СВИПЕ и зелены
  поодиночке; причина не найдена. Их красный не есть утверждение о коде.
- `SMOKE-4` (сид 999: тело на 46 см НИЖЕ опоры на промежуточной точке —
  допуск интегратора или нырок в грунт?), `SMOKE-5` (пещера не
  открывается на сиде 7, кандидат — `SUB-8`), `SMOKE-6` (пустой кадр
  макро-карты: гонка харнесса или карта правда не нарисована — демо).
- Предложение, НЕ сделано: список набора файлом в репо + флаг «прогнать
  всё», иначе следующий заход соберёт композицию заново и заново ошибётся.

**Дальше по §15 реестра:** тексты `TEXT-1..3` пачкой (чистый текст, риска
нет), затем `ECON-1..3` (денежная масса мира: двойное списание чеканки,
невозврат входов, храповик EMA дани). ГЛАЗАМИ владельца этот заход НЕ
проверен.

## ✅ СДЕЛАНО 2026-09-09 — рекавери, стрельба, торговля пер-стак, звуки

Восемь коммитов (0e3800e..ddce112), сейв v83, ctest 94/94, смоуки PASS
(cast_spell/self_fireball/player_melee/player_bow ×3 сида + console +
open_spells + missile_feedback + subworld_audio + loot_xp). Вердикты — CANON
S14 («ОДИН ГЕЙТ НА ТЕЛО», «СТРЕЛЬБА/МЕТАНИЕ» ✓, «ТОРГОВЛЯ ПЕР-СТАК» ✓);
трек = memory unified-recovery-track. ГЛАЗАМИ владельца НЕ проверено.

1. ЕДИНОЕ РЕКАВЕРИ: один гейт занятости на теле `Combat.recoverySteps`
   (бывш. cooldownSteps) — удар/каст/выстрел пишут, ЛЮБОЕ действие сперва
   проверяет (гейт агностик, тоггл sustained включительно); ходьба свободна;
   один `tick_combat_recovery`. Пер-спелловые кулдауны МЕРТВЫ
   (`SpellBook.cooldownSteps[]` вырезан из структуры и сейва — v83); ЗАМАХ
   МЁРТВ: каст стреляет на клик, castTime влит ЧИСЛОМ в колонку
   `SpellDef.recovery` (fireball 2.3, ice 1.7, chain 4.1, beam 2.9,
   armageddon 122 — суммарный темп не сдвинут), pendingCast*/
   resolve_active_cast/smoke_wait_cast_windup вырезаны. `spellbook_cast`
   находит Combat кастера по entity id и заряжает гейт дверью S14
   (Spellcraft); `can_cast_ex` берёт гейт ЯВНЫМ параметром. Вердикт: на
   МАКРО-карте боёв нет — касты там рекавери не платят (боевая система =
   единый движок микро/данжей). UI/консоль говорят «Recovery».
2. СТРЕЛЬБА (лук): `ItemDef.delivery` (Melee/Missile/Thrown) + `range`;
   `wpn_bow` аппендом (Bow, 1d8 Pierce, 1 кг = темп массой, двуручный,
   range 80). Урон стрелкового БЕЗ добавки атрибута (кубы + Bow% + LCK;
   worn DmgFlat остаётся — голос снаряжения); боеприпаса НЕТ нигде.
   Combat.kind/attackRange едут пер-тиковым рефрешем (лук в руки = следующее
   нажатие стреляет); выстрел = spawn_npc_missile, одна дверь с
   НПЦ-стрелками, прицел = кроссхейр. Thrown зарезервирован (после демо).
   Смоук subworld_player_bow (грабля сида 1: вход БЕЗ релокации —
   cast_spell-идиома, цель +8; рельеф/стволы съедали стрелу).
3. ТОРГОВЛЯ ПЕР-СТАК ОТ value_of: BarterLine = СЛОТ полки (+def-страж
   протухшей строки), barter_swap возит стак ЦЕЛИКОМ (seed+аффиксы через
   add_ref); обе панели ценят базу через value_of экземпляра (роллнутый меч
   продаётся за аффиксы, имя+тинт витрины на прилавке; дефицит/спрос
   считают ВИД); inventory_value (охота/лут-пулы/worth НПЦ) — той же
   дверью. barter_test переписан + 2 новых закона.
4. ЗВУКИ: SfxId SpellCast/PlayerHurt/Death аппендом с процедурными
   дефолтами; площадки: любой успешный каст, hp-дроп обитаемого тела рядом
   с хит-флешем, смерть тела на боевом тике (drainAll-свипы молчат).
   .wav в assets/sound перекроет автоматом.

## ✅ СДЕЛАНО 2026-09-07 (вечер) — ПРЕДМЕТНАЯ СИСТЕМА, демо-блокер закрыт

Шесть шагов, шесть коммитов (961c31d..HEAD), сейв v82, ctest 94/94, смоуки
console + subworld_player_melee ×3 сида PASS. Полная механика: rpg.md
«Affixes — one issuance door»; трек и вердикты: memory affix-system-track.

1. Реестр бонусов дотянулся до ПРОИЗВОДНЫХ законов: BonusTarget::Armor (9
   колонок 9×9) + Derived (DmgFlat/SwingPct/MovePct/CarryKg), 13 рядов в
   хвост; читатели в единственных дверях (defense_of, hand_strike_fields,
   перегрузки calculate_derived/get_carry_capacity с тоталами); один кламп
   %-рядов ×4 в обе стороны.
2. ItemRef: 8 аффиксов SoA (rows[8]+values[8], 36 Б/слот, static_assert),
   бамп v82.
3. Писатель: grant_affixes = ОДНА дверь выдачи (артефакты позже пишут те же
   ячейки без монетки); закон = coin_run p=(base+power)/512 (количество base
   16 / сила base 128, кап ролла 16 юнитов); таблица kAffixDefs с ВЕСАМИ БЕЗ
   НУЛЕЙ по типам; «of Mastery» = скилл самого предмета (новое оружие входит
   фактом существования); power = уровень×8 + danger + wealth до 64,
   собирается на трупе ДО роллов; параметр power без дефолта.
4. Плейсхолдеры кинжала/кожанки обнулены (колонка bonus ждёт уников);
   оружейные ряды: меч/копьё(2h)/топор/булава/посох(2h) аппендом, темп из
   массы; ЛУК сознательно отсутствует (ждёт закон стрельбы, см. очередь).
5. value_of(ItemRef) (цена аффиксов колонкой таблицы, пол 0), витрина одним
   написанием (draw_item_ref_name/bonuses, функциональный градиент
   белый→золото по n/кап), консоль roll <itemId> [power] + loot [power].
   ПОПУТНЫЙ БАГ: «Wear» списывал по ординалу (дюп роллнутого + потеря голого
   близнеца) — теперь Inventory::remove_at по слоту.
6. Тесты закона (монетка/дверь/цена/стакование/читатели удара и ног), доки.

## План следующей сессии (порядок предложен, владелец решает)

1. ГЛАЗА ВЛАДЕЛЬЦА — накопилось за три сессии, всё консолью/в игре:
   - аффиксы (give/roll/loot/equip/sheet/exec — инструкция в отчёте
     2026-09-07); крутилки в одном месте: items.h константы + kAffixDefs;
   - единое рекавери: меч↔каст на одном гейте, мгновенные касты, числа
     recovery спелл-рядов (`sheet` печатает все темпы);
   - ЛУК: `give wpn_bow` → equip → стрельба по кроссхейру (числа = ряд);
   - ПРИЛАВОК: роллнутая вещь на витрине с именем/тинтом и ценой value_of;
   - звуки: каст/входящий удар/смерть (процедурные — вердикт по ощущению);
   - хвосты прошлых треков: темп боя второй прогон; лужа смерти/капель/
     угли+дым/дождь (день 5 ~07-12ч); arrival-слайд (авторский текст).
2. Наполнение лут-профилей/магазинов новыми рядами (меч страже, топор
   лесорубу, лук охотнику...) — контент, числа гайдед.
3. Звуки-ассеты: .wav в assets/sound (melee-*/spell-cast/player-hurt/
   death.wav) — перекрытие синтеза автоматом.

## Очередь после демо — крупное и мелочь

0. СЛУЖБА ФРАКЦИИ — отложена за демо (вердикт 2026-09-06; memory
   faction-service-track).
1. МЕТАНИЕ: Delivery::Thrown зарезервирован (сам предмет летит) — оживить
   после демо; огнестрел = будущий Missile-ряд с жирным флэтом.
2. АРТЕФАКТЫ/УНИКИ: дверь готова (фикс-выдача через set_affix + врождённая
   колонка bonus ряда, до 8+8 строк) — нужен стол артефактов и где они
   лежат в мире (контент-стадия).
3. ЛУТ-КЛЮЧ НА ПЕЙЛОАД: сундук/труп с CorpseLoot без Structure не находится
   взглядом.
4. СМОУК-ПРИМИТИВЫ (п.5 трека реестров): goto_landmark / interact_aimed /
   take_stairs; идея «смоук-токен = консольная команда»; консоль-смоук
   расширить (skill/attr/equip/exec/roll).
5. UI-ТЕКСТУРЫ ЛОВУШКА: VK_ERROR_OUT_OF_POOL_MEMORY (портреты 48×48 без капа
   в дескрипторном пуле).
6. Класс бага «вход не сбрасывает состояние движка»: ревизия полей
   SubworldEngine, переживающих leave().
7. Стены канваса следов + сплэши дождя — за демо (трек частиц).

## Грабли (не наступать)

- «ИГРОКУ X, НПЦ Y» — ЗАПРЕЩЁННАЯ РАМКА (владелец капсом, дважды): игрок
  особен только камерой/инпутом/входами; тюнинг — по рядам и законам мира.
- kLootProfiles/kCatalog/kAffixDefs живут в АНОНИМНОМ неймспейсе items.cpp —
  новые sm-функции класть ПОСЛЕ его закрытия (ловится линкером).
- Каталог предметов: только АППЕНД — сейв несёт ординал ряда в ItemRef.def.
- roll_loot_profile: параметр power БЕЗ дефолта сознательно — новый сайт
  лута обязан назвать слово мира, компилятор найдёт забытые.
- Позиционные init при росте POD-структур — греп ВСЕ сайты; sizeof(ItemRef)
  на страже static_assert.
- ПЕР-ШАГОВЫЕ пересчёты дериватов — только с гейтом «что-то изменилось»;
  operator== BonusTotals читает И новые массивы (armor/derived).
- КЛАМП effective_sheet поднимает атрибут 0→1; Attributes{} = ЕДИНИЦЫ.
- int/int деление дробей = тихий ноль; printf %f с int = UB.
- Смоук-сцепки: гонять токены ПО ОДНОМУ; walk связкой subworld_enter,
  subworld_walk; smoke.sh ИЗ КОРНЯ репо (exit 127 из чужого cwd); compound
  `cd build && ...` из чужого cwd молча не собирает — абсолютные пути.
- Ординалы сейва навсегда: append-only; бампаем версию, миграций нет (v83).
- zsh: `===`-разделители в командах ломаются; echo-маркеры словами.

## Спящие ветки (сознательно, НЕ дефекты)

- material/quality байты ItemRef едут в сейве БЕЗ писателей и читателей —
  проснутся ВНУТРИ value_of вместе со своими таблицами (писатели раньше
  читателей; контент-стадия).
- Delivery::Thrown зарезервирован колонкой, поведения нет (после демо).
- Форажинг ИГРОКА спит: Adventurer.upkeep = kNpcUpkeepNone.
- XP-гранты quest_engine/effect_applicator на БАЗОВОМ листе — дверь приедет
  с переездом PlayerState (~550 мест).
- Body/Mind-теги спеллов спят до апдейта паладинов/клириков (S15).
- Рост тел в толпе: тела без Sprite берут человеческую колонну.
- HUD-индикатор занятости (одна полоска рекавери на всё — замаха больше
  нет) — отложен; звук каста ДОСТАВЛЕН 2026-09-09 (SfxId::SpellCast).

## ЭКОНОМИКА — отложенная очередь (НЕ ПОТЕРЯТЬ; вернуться после РПГ)

Хвосты трека баланса (эталоны: /tmp/guard512_*, /tmp/scent512_*, старший —
balance_out_navgraph.log; balance_run НЕ в check, гонять только при изменении
законов экономики/АИ):
1. ТЮНИНГ ПОПУЛЯЦИИ дубль-прогоном: popEnd 103-118k против navgraph 250-275k.
   Ручки: N строк ростера, labourShift, темп вендоров, веса скора аукциона,
   страх/патруль, крутилки следа/охоты (kScentDecayDays=4, kScentDiffusionShift=3,
   kScentQuantShift=2, kHuntBoldShift=1, kHuntScentFloor=8 — НЕ свипованы).
2. Минт 999/12345 = 0: геология к подножиям (вердикт записан).
3. Вселение vs растворение ротации — problems.md §35 (вердикт не снят).
4. Отказ ценой (провиант-порог), рейсовый паром, военно-полит слой (§34.6),
   кран спавн-кошельков (honest-spawn-wealth), перф §34.8: НАЧИНАТЬ С ПРОФИЛЯ
   7×32 (подозреваемые: deposit каждого think и дневная диффузия 34 планов —
   лечатся по-разному). Замер scent 4×512: минт сид 1 −26% (охота режет караваны
   серебра?) — посмотреть при тюнинге.
