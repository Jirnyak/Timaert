# СЕССИЯ 12 (СЛЕДУЮЩАЯ) — ПОЧИНИТЬ СБОРЩИКА, ПОТОМ ТЕГИ И НУЖДА В СТОИМОСТИ

Состояние: main = после сессии 11, сейв **v108**, ctest **104/104**, ноль
предупреждений. Смоуки не гонялись, глазами не смотрено.
База «до» сессии 11: `/tmp/bal_s11_base` (512 дней, сиды `7,1,12345,999`).

## ГЕЙТ №1 ВЗЯТ: СБОРЩИК ИДЁТ ВНИЗ И РАБОТАЕТ (разбор в problems §55-V)

Сид 7, 64 дня, против `/tmp/bal_s11_base`:
```
trades  +50.1 %   crewsSell +239 %   popCity +2.0 %   население +0.5 %
debtFood медианного города = 0 (как и в базе)   starvedPops = 0
deserters +77 %   ← единственный открытый минус
```
**Открытое дело сессии 12 — дезертиры.** Гипотеза (НЕ проверена): город
снаряжает почти четыре тысячи крю, и часть проваливает сезонное окно по
харчу. Разбирать замером.

## ДВЕ ОШИБКИ ЧТЕНИЯ, ОБЕ СНЯТЫ СЧЁТОМ — ЧИТАТЬ ПЕРЕД НОВЫМ ЗАМЕРОМ

1. **Машина без ног.** Сборщик не доставлял ничего, и я прочёл гистограмму
   состояний как «висит в Working». Подписи были сдвинуты: в `NPCState`
   порядок `Idle, Wandering, Traveling, Returning, Working`. Он стоял в
   **Traveling**, потому что в ветке пути **не было `try_move`**.
2. **Запас вместо нужды.** Объявил гейт проваленным по `food_city` −99.8 %.
   Пустой склад еды у города — это жизнь С КОЛЁС, а не голод: `debtFood` = 0
   и `wellbeing` 255 в обоих мирах, население города ВЫШЕ базы. Тот же класс,
   за который сессия 6 назвала прибор слепым (§54).

## ЧТО СДЕЛАНО В СЕССИИ 11 (5 коммитов + дань)

- `aaec9fc6` **КАНОН ДО КОДА: «ДОМОЙ» — ЭТО ПРОСТО ЕЩЁ ОДНА ЗАЯВКА В ТОМ ЖЕ
  АУКЦИОНЕ.** У поручения нет квоты и нет счётчика: сквад на каждой развилке
  спрашивает тот же аукцион, что его поднял, считая ИЗ СВОЕЙ ТОЧКИ. Таймер
  живёт в знаменателе, поэтому у дальности рейса нет потолка. Плюс два
  вердикта: горизонт принадлежит ТОЧКЕ СТОЯНИЯ (разведка на кольцо вперёд —
  отсюда дальность), у дальности нет потолка (рулетка даёт хвост).
- `68f88b26` **мембраны округи одной дверью** (`nav_region_portals`,
  `nav_regions_adjacent`) вместо двух ручных обходов. Негативный контроль
  посимвольный.
- `93422449` **ЯРУС 2**: мировое среднее (`WorldLedger`, 64 Б на мир) +
  `market_price_seen` + **заявка сбыта впервые считает ВЫРУЧКУ, а не
  настроение** (спред против прейскуранта партнёра вместо «дёшево ли мне это
  дома»).
- `046ce55f` **многоскачковый рейс**: «дальше» против «домой» на станции,
  `prevStationId` вышел из мёртвых.
- `db148c21` **ЕДИНЫЙ ЗАКОН РОЖДЕНИЯ СКВАДОВ**: строка реестра стала
  ШАБЛОНОМ, `outMask` (бит на строку, потолок 8) → счётчики экземпляров;
  спрос говорит СКОЛЬКО, пул рук УРЕЗАЕТ, размер = пул ÷ экземпляры; четыре
  одинаковые строки деревни схлопнуты в одну; **погрузка стала универсальной**
  (обход по плотности стоимости по всему инвентарю) — и казна впервые поехала
  в рейс, чем закрыт Х-13.
- **дань** (в дереве, см. выше): долг и его база свёрнуты в ОДНУ стоимость,
  шов вырезан, `ai_taxrun` → `ai_collector` вниз, `capital_of_` снесён.

## ЧТО РЕШЕНО ВЛАДЕЛЬЦЕМ И ЖДЁТ ПОСТРОЙКИ (канон уже написан)

1. **РОД ВЕЩИ — ТЕГ** (CANON S10): РЕСУРС / ПИЩА / ТОВАР / ДЕНЬГИ /
   ОРУЖИЕ-БРОНЯ-ЗЕЛЬЯ / МИСК. Всё из крафта — ТОВАР; потребность закрывается
   ЛЮБЫМ товаром.
2. **ОДНОРОДНАЯ НУЖДА В СТОИМОСТИ** (CANON S10): «душе нужно товара на N
   стоимости в день». Сносит `needDebt[15]` в ДВА числа (пища и товар).
3. **`kCommodities` сжимается**: колонка `weightKg` — второй словарь рядом с
   `ItemDef::weight`, умирает; `materialMask` уходит тегом материала.
4. **ПОДУШКА ГЕНЕЗИСА** (обсуждено, не решено окончательно): места рождаются
   с монетой и ресурсами пропорционально населению. Владелец: «монеты и
   ресурсы», сеять МЕСТУ. Мерить ПАРНЫМ прогоном (с ней и без), иначе
   отложенный провал примем за починку — Х-2 уже показал, что мир проедает
   генезисный засев.

## ЧИСЛО, КОТОРОЕ ОСТАЁТСЯ ВЕРНЫМ: ДЕНЕГ В МИРЕ НА ПОЛПРОЦЕНТА ЕГО ЕДЫ

`coinLandmarks` = **514 151 у ВСЕХ мест мира** против **108 млн единиц еды**.
Покупкой город прокормиться не может — кормит его ДАНЬ, и теперь она ходит.
(Прежняя запись «`food_produced` = 0, еду никто не производит» СНЯТА как
неверный вывод, вердикт владельца 2026-09-22: еда не варится рецептом, она
ДОБЫВАЕТСЯ артелями, и ноль в этой колонке — норма, а не дефект. Сама колонка
путает читателя и помечена под снос.)

## КАК РАБОТАТЬ В ЭТОЙ СЕССИИ — ПРАВИЛА, ВЫВЕДЕННЫЕ ЦЕНОЙ СЕССИИ 10

Сессия 10 стоила владельцу четырёх обрывов («ты не обосновываешь», «надо
факты», «это бред», «у нас же была куча обсуждений — всё пропало?»). Причина
каждый раз одна: агент проектировал, не сверившись с уже решённым. Правила:

1. **КАНОН ПЕРВЫМ, ДО КОДА И ДО МНЕНИЙ.** У владельца вся архитектура в голове
   и она ЗАПИСАНА. Разведка по коду даёт СОСТОЯНИЕ РЕАЛИЗАЦИИ, а не замысел, и
   без канона толкает изобретать хуже уже решённого. Заголовки секции искать
   `awk 'NR>1700 && NR<3450 && /^### /' CANON.md` — они говорящие.
2. **Архитектурное утверждение — гипотеза до ссылки `file:line`.** Проверять
   закон в ТОЧКЕ ИСПОЛНЕНИЯ, а не по шапке модуля (в сессии 10 шапка
   `behaviour.h` объявляла колонку общей для двух миров — и соврала).
3. **Факты отдавать субагентам, вердикт оставлять себе** (владелец разрешил
   прямо: «если надо проверь субагентами»). Разведка Р-1..Р-8 ниже снята так и
   стоит больше всех рассуждений агента вместе.
4. **Половина не остаётся в дереве молча** (§55): записывать что построено, что
   нет, что врёт читателю.
5. **Числа решают, а не мнения.** Крутилки живут в дубль-прогоне.

## ШАГ 0 — РАЗВЕДКА СУБАГЕНТАМИ, ДО ЕДИНОЙ СТРОКИ КОДА

Три параллельных агента, каждый возвращает ФАКТЫ со ссылками, без рекомендаций.

**Агент А — КАНОН.** Выписать дословно все законы, касающиеся торговли, цен,
округи и корована. Обязательно: `S10` целиком по заголовкам, особо «ЗНАНИЕ О
ЦЕНЕ — ТРИ ЯРУСА», «ЗАКОН ДЕШЁВОГО КОНЦА», «У СДЕЛКИ ДВА БОРТИКА», «РЕШЕНИЕ
ВЕСИТ ТОЛЬКО ТО, ЧТО МОЖНО ОПЛАТИТЬ», «РУКИ ИДУТ ТУДА, ГДЕ ВЫШЕ СТОИМОСТЬ НА
РАБОЧИЙ ДЕНЬ», «КЛАМП ДЕФИЦИТА СНИМАЕТСЯ», «ОБОЗ = СУММА СПИН», «ТРИ ПОТОКА
МАТЕРИИ»; плюс `S7` (пути и вес станции), `S19.2` (память, горизонт сезона),
`S25` (один обмен, разные контейнеры), `S26` (общий закон), `S4` («число
караванов — функция контекста места», «залипание лечится тремя уже
построенными вещами»).

**Агент Б — КОД.** Перепроверить актуальность Р-1..Р-8 ниже (файлы менялись) и
дополнить: где живёт **опись округи** (`survey_landmark_regions`) и в каком
такте она перепекается; граф **порталов/мембран** и как по нему брать «соседние
округи»; есть ли где-нибудь в коде **мировое среднее** по товару; что именно
делает `publish_landmark_ledgers` и чего в `LandmarkLedger` не хватает под
горизонт.

**Агент В — ХВОСТЫ.** Прочитать в этом файле «ЕДИНЫЙ РЕЕСТР ОТКРЫТОГО ПО
ЭКОНОМИКЕ» (Х-1..Х-16) и `problems.md` §55, §55-II, §56; выписать всё, что
касается торговли и цен, с пометкой «закрыто / живо / не построено».

**Свести в ОДНУ таблицу:** `закон канона → что в коде → что строить`. Только
после неё писать код.

## ШАГ 1 — ЗАМЕР ВЕСА СТАНЦИИ: ОТЛОЖЕН ДО КОРОВАНА (решено 2026-09-22)

**Постановка изменилась после разведки: квадрата нет НЕ В КОДЕ, А В КАНОНЕ.**
S7 (`CANON.md:447`) — место, где закон живёт, — говорит `1/(1 + дни пути)`
БЕЗ квадрата и выводит единицу замером (`CANON.md:462-466`: средний сосед
0.87 дня на 14 178 рёбрах, «колено веса стоит на среднем соседе»). Квадрат
стоит только в S10 (ярус 1) со ссылкой на S7, где его нет. Код
`pick_next_station_` совпадает с S7. Расхождение записано в CANON рядом с
ярусом 1; правится ОДИН из двух текстов, по числам.

**Почему отложен (вопрос владельца 2026-09-22: «что нам гонять, если корована
нет?» — он прав).** Вес станции сегодня зовёт заявка сбыта
(`npc_ai.cpp:4399`), а строят её ДЕРЕВЕНСКИЕ крю — те самые, которым S4
торговать запрещает. Замер сейчас измерил бы дальность плеча канала, который
мы сносим. **Мерить А/Б на рейсе корована, после порции 1.**

База «до» снята: `/tmp/bal_s11_base`, 512 дней × сиды `7,1,12345,999`.

## ПОЧЕМУ СРОЧЕН: В ДЕРЕВЕ ПОЛОВИНА, И ОНА ИЗМЕРЕНА (problems §55-II)

Скор приведён к одной размерности (`0b7ddbaf`) — и этим **открыл дыру, на
которой держался весь торговый канал мира**. Заявка сбыта обещала маржу ЦЕЛОГО
СКЛАДА при спине в одну душу: завышение в 50-100×. Без него аукцион честно
отвечает «возить невыгодно»:

```
crewsSell  3344 → 1036 (−69 %)    crewsGather 3666 → 5977 (+63 %)
trades     5783 → 1965 (−66 %)    food_vil +44 % (еда копится, не уезжает)
starvedPops 3291 → 6882 (+109 %)  popCity −3.3 %
```

Направление совпадает с замыслом владельца («ДЕРЕВНИ ДОБЫВАЮТ, ГОРОДА
ПРОИЗВОДЯТ, СОБИРАЮТ И ТОРГУЮТ») — деревни начали добывать, а торговать стало
НЕКОМУ. Это тот же барьер ×50 из наряда сессии 6, вошедший через другую дверь.

## НАСТОЯЩИЙ ДИАГНОЗ: ЧИСЛИТЕЛЬ СБЫТА — НЕ ВЫРУЧКА, А НАСТРОЕНИЕ

**Здесь стоял вывод агента «размер обоза дыру не закрывает, спасает только
обход станций». Он НЕОБОСНОВАН и снят владельцем 2026-09-21** («ты не
обосновываешь»; «возят же в инвентаре плюс лошади»). Проверка по коду
опровергла его дважды:

1. **ЛОШАДИ ДАЮТ ОБОЗ БЕЗ ДУШИ.** `refresh_squad_carry` (`squad.h:149`): вклад
   души = спина лидера × (`haulMult` её строки / `haulMult` лидера), и лошадь —
   «не особый случай, а строка с большим haulMult». Значит обоз на
   ЧЕЛОВЕЧЕСКУЮ душу РАСТЁТ с табуном, и масштаб per-soul помогает. Утверждение
   «N душ дают N× за те же дни, значит то же самое» — ложно.
2. **ЗАЯВКА СБЫТА НЕ ЧИТАЕТ ЦЕН ПАРТНЁРА ВООБЩЕ.** В блоке заявки нет ни одного
   обращения к прейскуранту `city`: весь `value` считается по СВОЕМУ складу.
   Значит рассуждение про «спред на спину» было про величину, которой в коде
   НЕТ, — а без цен партнёра спреда не существует ни на одном скачке, и
   «многоскачковость» ничего не объясняет.

**Диагноз, который остаётся после проверки:**

```
было:  местная «готовность сбросить» × ВЕСЬ склад  → гигантское число
стало: та же готовность × одна спина               → крошечное число
```

`value += fit * (base − homePrice)` — это «насколько дёшево моё излишнее добро
У МЕНЯ ДОМА», то есть мера ГОТОВНОСТИ СБРОСИТЬ, а не выручки рейса. Правки
`0b7ddbaf` (дни и спина) обе верны, но **числитель так и не стал тем, чем
должен быть**, и обвал торговли — артефакт этого, а не вывод экономики.

## РАЗВЕДКА ТОРГОВОЙ СИСТЕМЫ — ПРОВЕРЕНО СУБАГЕНТОМ 2026-09-21, СО ССЫЛКАМИ

Снята субагентом после того, как владелец дважды поймал агента на догадках,
выданных за вывод. **Это факты с адресами, а не рассуждение. Строить корован
только на них.**

**Р-1. СПРЕД МЕЖДУ ДВУМЯ МЕСТАМИ В МИРЕ УЖЕ ЕСТЬ — РОВНО ОДИН, И ОН В СДЕЛКЕ.**
Закупочный проход `trade_vendor_at_market` (`npc_ai.cpp:3611-3621`):
`worthHome` (цена ВЕДОМОСТИ ДОМА) − `buyHere` (цена рынка, где крю СТОИТ),
ранжирование лотов по `gainPerKg` убыванием (:3625-3627), потолок строки
`homeLedger->demand[i]`, кошелёк через `max_affordable_lot_`. Под гейтом
`homeLedger->published()` (:3590) — без опубликованной ведомости крю НИЧЕГО не
покупает. **То есть ярус 2 знания уже работает, но ТОЛЬКО по прибытии и только
в половине ЗАКУПКИ.** Половина ПРОДАЖИ читает лишь полку рынка (:3547-3562).

**Р-2. ВЕДОМОСТЬ ПУБЛИКУЕТСЯ РАЗ В СЕЗОН.** `publish_landmark_ledgers`
(`econ_day.cpp:393-420`) зовётся ровно дважды в продакшне: генерация мира
(`world_gen.cpp:384`) и `if (season_boundary(day))` в `world_tick.cpp:595`,
ПЕРЕД `rotate_worker_squads` (:597). Значит ярус 2 — это СЕЗОННЫЙ СНИМОК, а не
живая цена, и это ограничение всякого планирования по ведомости.
Читателей `ledger.price[]` в продакшне два: сделка (`npc_ai.cpp:2190` →
:3611) и заявка ДОБЫЧИ (:4335-4340).

**Р-3. ЗАЯВКА СБЫТА ЦЕН ПАРТНЁРА НЕ ЧИТАЕТ — ЭТО ОБЪЯВЛЕНО ЗАКОНОМ.**
Комментарий на месте (`npc_ai.cpp:4390-4391`): «Локальность закона: никакого
знания цен рынка — только СВОЙ склад». Партнёр входит трижды и только как
геометрия/ординал: предикат рода (:4403), `citySite` для `road_days_`/`fear_of`
(:4482), `city->id` как объект заявки (:4495). Весь `value` — домашняя цена
против БАЗЫ (:4446-4451, :4467-4478).

**Р-4. `ai_caravan` ТОЖЕ НЕ ЧИТАЛ ЦЕН НАЗНАЧЕНИЯ**, и его «обход станций» был
СЛУЧАЙНЫМ: `rt.stationsLeft = 2 + rand_int(ctx, 2)` (old:2014), станции —
`pick_next_station_` без цен, грузился на ПОЛОВИНУ трюма
(`load_cheap_at_home_(..., rt.carryCap / 2)`, old:1984), занимал кошелёк не
более половины стоимости склада дома (old:1998-2002), гейт «дом обязан быть
City» (old:1968). **Значит воскрешение `ai_caravan` КАК БЫЛО не даёт
информированной торговли — он возил наугад.**

**Р-5. `pick_next_station_` (`npc_ai.cpp:1825-1907`) цен не смотрит вовсе:**
вес `w = 1/(1 + days)` (:1860), дни — `nav_path_cost` в одну сторону, кандидаты
— жилые поселения соседних нав-округ; без `ctx.rng` рулетка вырождается в
первый вес (:1862).

**Р-6. `carryPerSoul` в аукционе — СИНТЕТИКА, не живой сквад**
(`npc_ai.cpp:4258-4269`): лист уровня 1 первой НЕ-solo строки крю реестра,
`get_carry_capacity × haulMult` рода. **Тягла из стойла в нём нет**, хотя
реальный обоз считает `refresh_squad_carry` как сумму спин ростера, где лошадь
— строка с большим `haulMult` (`squad.h:136-154`). То есть скор не знает про
табун, а рейс им пользуется.

**Р-7. Слиппедж — НА ЛОТ, а не на единицу** (`npc_ai.cpp:3563`, :3650-3653,
закон в `economy.h:63-70`): цена считается один раз на ПОСЛЕсделочной полке и
умножается на весь лот. Значит крупная партия оплачена целиком по худшей цене —
это давит большие обозы сильнее, чем маржинальное ценообразование.

**Р-8. Единственное сравнение цен двух мест вне сделки** — заявка ДОБЫЧИ берёт
МАКСИМУМ своей и сюзеренской цены, а не разницу (`npc_ai.cpp:4333-4341`), и
помечена «ПОД ГРИФОМ НЕ УВЕРЕНЫ» (:4312-4315).

### РАЗВЕДКА 2026-09-22 — Р-1..Р-8 ПЕРЕПРОВЕРЕНЫ, ПЛЮС ШЕСТЬ НОВЫХ ФАКТОВ

Все восемь **подтверждены** (строки сместились на единицы). Дополнение:

**Р-9. ОПИСЬ ОКРУГИ.** `survey_landmark_regions` (`npc_ai.cpp:3785`), ровно
один продакшн-вызов — `world_tick.cpp:590`, под `season_boundary`, СРАЗУ ПЕРЕД
публикацией ведомостей (:595). Считает только по ЖИЛЬНЫМ родам
(`resource_row_is_vein`), кладёт ближайшую клетку на род в `LandmarkSurvey`
(64 Б на место, ~120 КиБ на мир). Читателей два: `ai_gatherer` (`:1217`) и
заявка добычи (`:4348`).

**Р-10. ГРАФ МЕМБРАН — CSR, ДВЕРИ «СОСЕДИ ОКРУГИ» НЕТ.** `NavPortal`
(`nav_field.h:72`), хранение `portals` / `portalBegin` / `portalCount`
(`nav_field.h:125-127`), кап 64 портала на округу. Функции «перечислить
соседние округи» не существует — обход раскатан руками ДВАЖДЫ:
`npc_ai.cpp:1887-1906` (с дедупом O(deg²)) и `threat_field.cpp:70-84` (без
дедупа). **Это кандидат на одну дверь в порции 1.**

**Р-11. ОКРУГА = ЗОНА ВОРОНОГО ОДНОГО МЕСТА** (`nav_field.cpp:108-115`:
«каждый ландмарк — своя округа»). Замеры: R = 1 887 округ / 14 056 порталов
(сид 7), средняя степень **5.0-7.9**. Значит **горизонт яруса 2 — это ~7-9
мест из ~1 880: 99.5 % мира лежит за ним и виден мировым средним.**

**Р-12. МИРОВОГО СРЕДНЕГО НЕТ НИ ОДНОГО** — сплошной поиск по `src/`: ни цены,
ни запаса. Проходов по всему ростеру мест шесть, ни один не сводит экономику.
Единственные «всемирные» числа экономики — `ItemDef::value` и вес строки;
ценовой колонки в `kCommodities` нет НАМЕРЕННО (`commodity.h:39-42`).

**Р-13. ВЕДОМОСТЬ — ТРИ ПОЛЯ.** `publish_landmark_ledgers` (`econ_day.cpp:393`)
пишет `price[15]`, `demand[15]`, `day`; структура `state.h:435-447`, 124 Б на
место. Нет поля округи, нет мирового среднего, нет запаса, нет истории (на
каждой границе обнуляется целиком). День публикации ЕСТЬ и служит гейтом
`published()`.

**Р-14. ЧЕТЫРЕ ОТВЕТА НА «ПОРА ЛИ КОНЧАТЬ», И КОЛОНКА ПОРУЧЕНИЯ БЕЗ
ОБНУЛИТЕЛЯ.** Артель — три рукописных условия (`npc_ai.cpp:1595-1608`), рейс
сбыта — ПРИБЫТИЕ безусловно (`:2204`), сборщик — долг (`:2274`), кочевник —
никогда (`:2483`). Пара `squadType`/`errandObject` (`components.h:480-482`)
имеет двух писателей (`:4602`, `:4717`) и **ни одного обнулителя**;
«поручение исполнено» подменено предикатом ротации «стоит дома в Idle»
(`:3953`, `:3962`). Полевой сквад в аукционе не участвует и на границе сезона
НЕ распускается (роспуск трогает только `idleByRow`, `:4756`) — поэтому у
дальности рейса потолка нет, как того и требует канон.

### ЧТО ИЗ ЭТОГО СЛЕДУЕТ ДЛЯ НАРЯДА (и чего НЕ следует)

Обосновано: **у мира есть готовый спред (Р-1) и готовый ярус 2 (Р-2), но
планирование рейса ими не пользуется (Р-3, Р-4, Р-5).** Значит «корован» — это
не воскрешение старой функции (Р-4: она возила наугад), а **информированный
выбор станции и груза по ведомости партнёра**.
НЕ обосновано и решать ЗАМЕРОМ: нужен ли многоскачковый обход вообще; брать
«сколько влезет» по душе или по реальному обозу с тяглом (Р-6); не задавит ли
лотовый слиппедж (Р-7) крупные обозы.

## НАРЯД, ОДОБРЕННЫЙ ВЛАДЕЛЬЦЕМ 2026-09-22 — ПОРЯДОК И СВЯЗКА

Закон записан в канон ДО кода: **CANON S10 «ДОМОЙ» — ЭТО ПРОСТО ЕЩЁ ОДНА
ЗАЯВКА В ТОМ ЖЕ АУКЦИОНЕ»**. Читать его первым; ниже только порядок постройки.

**ПОЧЕМУ ЭТО ОДИН ИНКРЕМЕНТ, А НЕ ЧЕТЫРЕ.** Заявка «продолжить» обязана
оценить, что даст СЛЕДУЮЩАЯ станция — а это и есть ярус 2 (в горизонте её
ведомость, за горизонтом мировое среднее). Значит горизонт не «отдельная
порция перед корованом», а его ПРЕДУСЛОВИЕ; строить его раньше читателя
значит класть в дерево колонку без читателя (§55). И наоборот: запрет деревне
торговать (1.2) без городского корована (1.1+1.3) рушит канал — барьер ×50
измерен. Всё ниже собирается зелёным по шагам, но МЕРЯЕТСЯ целиком.

**ПОРЯДОК ПОСТРОЙКИ:**

1. **Ярус 2: горизонт и мировое среднее.** Горизонт = своя нав-округа плюс
   соседние по мембранам (тот же обход порталов, что у `pick_next_station_`;
   сегодня он раскатан руками ДВАЖДЫ — `npc_ai.cpp` и `threat_field.cpp`).
   Мировое среднее — `int worldPrice[kCommodityCount]` рядом с ростером мест,
   считается в том же проходе `publish_landmark_ledgers` (сумма / число
   опубликовавших), 60 Б на мир, ноль назначенных чисел (S26).
   **ЗАМЕРЕННЫЙ ФАКТ, КОТОРЫЙ НАДО ЗНАТЬ:** округа = зона Вороного ОДНОГО
   места, средняя степень графа мембран 5.0-7.9 → **горизонт это ~7-9 мест из
   ~1 880, то есть 99.5 % мира виден мировым средним.** Канон это и
   предсказывает («дальнее место выглядит обычным рынком»).
2. **Дверь скора «стоимость заявки ИЗ ТОЧКИ P».** Сегодня скор живёт внутри
   лямбды аукциона и считает только от дома. Два вызывателя: аукцион (P = дом)
   и машина рейса (P = текущая станция). Ноль новых констант.
3. **Заявка «домой»** в ту же урну + снятие поручения при выгрузке дома
   (колонка `squadType`/`errandObject` сегодня имеет двух писателей и НИ
   ОДНОГО обнулителя).
4. **Корован — коупл-связка 1.1 + 1.2 + 1.3 ниже**, машина рейса строится
   поверх шагов 2-3: переспрос **НА СТАНЦИИ**, груз из дома — излишком по
   дешёвому концу локального рынка.
5. **Прогон 512 × 4, гейты ниже.** База «до» уже снята: `/tmp/bal_s11_base`.
6. **ПОСЛЕ прогона** — А/Б квадрата в весе пути (шаг 1 выше).

**ЧЕГО НЕ ДЕЛАТЬ:** не начинать с памяти сквада (ярус 3 последний); не
воскрешать `ai_caravan` — он кончал рейс счётчиком станций, то есть отвечал не
на тот вопрос (приговор записан в канон); не заменять ведомость памятью.

## ПОРЦИЯ 0 — ДОСТРОИТЬ ЯРУС 2: ГОРИЗОНТ ВЕДОМОСТИ И МИРОВОЕ СРЕДНЕЕ

**ЗАКОН УЖЕ НАПИСАН — CANON S10 «ЗНАНИЕ О ЦЕНЕ: ТРИ ЯРУСА» (владелец,
2026-09-20). Ничего изобретать не надо, и порция 0 прежней редакции
(«числитель сбыта = спред против прейскуранта партнёра») была КУСТАРНОЙ
версией того, что канон задаёт точнее. Читать канон ПЕРВЫМ, до кода.**

Что канон говорит дословно и что из этого построено:

| ярус | закон (CANON S10) | в коде |
|---|---|---|
| 1. ЦЕНА ПУТИ | вес `1/(1+дни)²`; при нулевом знании корован всё равно встаёт и едет — «торговля работает стабильно, даже если ни у кого нет памяти» | ЕСТЬ, но **БЕЗ КВАДРАТА**: `pick_next_station_:1860` считает `1.0f/(1.0f+days)`. Расхождение канона и кода — проверить и свести |
| 2. ВЕДОМОСТЬ | место публикует состояние на границе сезона, **в тот же такт, что и опись округи**; **у знания есть ГОРИЗОНТ** — своя округа и соседние по мембранам; **за горизонтом — МИРОВОЕ СРЕДНЕЕ из той же ведомости** (число посчитано, не назначено, S26) | публикация ЕСТЬ (раз в сезон, `world_tick.cpp:595`), **ГОРИЗОНТА И МИРОВОГО СРЕДНЕГО НЕТ**; читателей два и оба точечные (Р-1, Р-2, Р-8) |
| 3. ПАМЯТЬ СКВАДА | личная поправка на ОДНО далёкое место — «его особое знание»; **опираться запрещено нигде**: пуста — работает ярус 2, врёт — цена вранья один рейс | не построено; и по канону это ПОСЛЕДНЕЕ, а не первое |

**ОТСЮДА НАРЯД, И ОН НЕ МОЙ, А КАНОННЫЙ:** корован выбирает станцию и груз по
ВЕДОМОСТИ В ГОРИЗОНТЕ, а за горизонтом видит мировое среднее. Значит строить
надо ровно две вещи, обе в ярусе 2:

1. **ГОРИЗОНТ ЗНАНИЯ** — своя нав-округа плюс соседние по мембранам. Дверь
   существует: тот же граф порталов, по которому ходит `pick_next_station_`
   (`npc_ai.cpp:1886-1905`), и тот же такт границы сезона, в который уже
   публикуется ведомость и перепекается опись округи.
2. **МИРОВОЕ СРЕДНЕЕ** — по той же ведомости, для мест за горизонтом. Ноль
   назначенных чисел: среднее считается из таблицы (S26). Следствие, названное
   каноном: дальнее место выглядит «обычным рынком» — туда ездят, но без
   предпочтения.

**ЧЕГО НЕ ДЕЛАТЬ:** не начинать с памяти сквада (ярус 3 — последний по канону,
и опираться на него запрещено); не воскрешать `ai_caravan` «как было» (Р-4: он
цен назначения не читал и возил наугад); не «заменять ведомость памятью» —
канон прямо фиксирует дефект, когда крю читало запас дома 4-битным классом
памяти и мир качал хлеб ВВЕРХ, в города, которые в нём тонут.

**ТОЛЬКО ПОСЛЕ ЭТОГО** имеет смысл вопрос «нужен ли многоскачковый обход и
какой» — и он решается замером, а не рассуждением.

## (СНЯТО) ПОРЦИЯ 0 ПРЕЖНЕЙ РЕДАКЦИИ — ЧИСЛИТЕЛЬ СБЫТА КАК СПРЕД

Дверь УЖЕ ЕСТЬ и уже используется: заявка ДОБЫЧИ читает прейскурант сюзерена
ярусом 2 знания (`suz->ledger.published()` → `suz->ledger.price[ci]`, тот же
аукцион). Заявка сбыта обязана делать то же против прейскуранта СВОЕГО
партнёра:

```
value = Σ (цена ТАМ − цена ДОМА) × сколько влезет в обоз
```

Ноль новых констант, ноль нового знания (ярус 2 уже законен, CANON S10).
Тогда числитель — выручка, размерность остаётся «монет на душу в день», и
только ПОСЛЕ этого замера можно судить, нужен ли корован и какой.
**Незакрытый вопрос формы:** брать «сколько влезет» по одной душе или по
реальному обозу крю (души + табун, `carryCap`). Обе заявки должны считать
одной базой; решать замером, а не мнением.

## ПОРЦИЯ 1 — КОРОВАН (коупл-связка, порознь НЕ СТРОИТЬ)

Три части, один инкремент, один прогон 512 × 4.

**ВНИМАНИЕ: порядок изменён.** Порция 0 выше идёт ПЕРВОЙ, и до её замера
порция 1 не обоснована — «нужен ли корован» станет вопросом с ответом только
после того, как числитель сбыта начнёт быть выручкой.

**1.1 Машина корована.** `ai_caravan` лежит целиком в истории:
`git show a39f42f3~1:src/macro/npc_ai.cpp` со строки 1955, **163 строки** (снесён
в v106 вместе с родом `NPCType::Caravan` — караван был сквадом, притворившимся
видом, и его обоз был вписан в породу лидера 32 спинами вопреки закону «обоз =
сумма спин ростера»). Возвращается **как машина ТИПА `SquadType::Caravan`**, не
как род: тип уже есть (`npc_ai.h`), `dispatch` уже спрашивает его первым.
Дверь станций `pick_next_station_` ЖИВА и со свидетелем (`npc_ai.h:341`) —
она и есть закон обхода (вес `1/(1+дни)`, рулетка с RNG, не argmax).
Что проверить при возврате: обоз берётся из РОСТЕРА, а не из породы лидера.

**1.2 Строка реестра объявляет ТИП, который поднимает.** `LandmarkCrewRow`
получает колонку `SquadType`: деревенские строки — только `Artel`, городская —
только `Caravan`. Аукцион предлагает строке ТОЛЬКО заявки её типа (заявка сбыта
перестаёт строиться деревне вовсе). Это и есть «строгое разделение» в форме
данных, а не ветки. **Порознь от 1.1 не строить** — иначе деревни перестанут
возить, а город не начнёт.

**1.3 Число корованов — функция контекста места** (CANON S4, вердикт владельца
«население и склада да»):
```
торговых крю = min( пул рук / душ на крю , стоимость излишка / стоимость трюма )
```
Первый член есть (`field_pool` / `perCrew`), второй — «нечего везти, обоз не
снаряжают». Ноль новых констант. Это же и есть названный владельцем рычаг
«больше корованов, которые меньше размером».

**ПРОГОН ОБЯЗАТЕЛЕН, 512 × 4.** Гейт: `trades`/`tradedValue` возвращаются к
порядку до `0b7ddbaf` ИЛИ выше, при этом `food_city` не падает и `starvedPops`
не растёт. Мерить города ОТДЕЛЬНО от деревень.

## ПОРЦИЯ 2 — ЛЕГАСИ НЕЩАДНО (владелец 2026-09-21), прогон не нужен

Вердикт: «всё через универсальную систему анкет; легаси уничтожаем нещадно».
Это ПРЕДУСЛОВИЕ смерти `SquadType::ByKind` — последней точки течи границы миров
(CANON S2). Пока одиночки живы, макромир продолжает читать каталог тел.

1. **Генезисные одиночки** (`npc_spawn.cpp:294-303`): `Merchant` (40 % на город)
   и `Guard` ×1-2 на город. Противоречат уже действующему §42 Инк 7 («уличная
   стража = ЗАПИСИ ГАРНИЗОНА места, никаких бесплатных тел»): именное идёт через
   анкеты, массовое через ростеры, а они ни то ни другое.
2. **`ai_trader`** (59 строк) — после сноса Merchant-одиночек недостижим.
   `AIBehaviour::Trader` теряет носителя ⇒ строка `Merchant` в каталоге тел
   получает **`Flee`** (это ПОСИМВОЛЬНО то же, что даёт сегодня
   `subworld_ai_for(Trader)`: не боец и не бродяга ⇒ беги), а значение умирает.
3. **`AIBehaviour::Gatherer`** — тем же ходом: строка `Peasant` получает `Flee`
   (снова байт-в-байт то же в сцене), значение и ветка в `dispatch` умирают.
   После 2 и 3 **в каталоге тел не остаётся ни одной макро-роли**, и колонка
   `ai` становится тем, чем её объявляет шапка `behaviour.h`.
4. **`npc_spawn.cpp:556`** — четвёртая точка течи: фракция при рождении
   выбирается по `npc_def(type).ai == Aggressive`. Должна приходить от того, кто
   сквад рождает.
5. `ai_nomad` ОСТАВИТЬ: он фолбэк, который машины зовут напрямую, и своего
   значения у него больше нет. `ai_patrol` оставить: квестовый спавн может
   назвать «guard», а строка `Patrol` нужна сцене.
6. Остаток Б-6: четыре копии выгрузки домой (`:2102`, `:2363`, `:2524`,
   `:4223`), `MarketSnapshot`, `lootPoolValue` (четыре писателя, ноль читателей).

## ПОРЦИЯ 3 — Б-4: СБОРЩИК ИДЁТ ВНИЗ

Наряд не изменился и лежит ниже (шапка сессии 10) + `problems.md §55`.
Что добавилось: тип `SquadType::Collector` берёт **свой** ординал (3 свободен
намеренно, патрульный не переиспользовать), и структурный блокер `outMask`
снимается порцией 1.3 — строка перестаёт быть слотом.

## ПОРЦИЯ 4 — ПЕРЕВОРОТ НАСЕЛЕНИЯ (Б-1)

Без изменений. Добавилось: закон распуска артели уже сведён к ОДНОЙ двери
(`dissolve_population_crew`, `41170995`), и владелец назвал целевую форму —
«буквально перенос между гарнизоном: весь гарнизон артели отдаётся в город, и
артель пустая удаляется». То есть переворот сливает обе половины двери в один
перенос ростер→ростер.

## ЧТО СДЕЛАНО В СЕССИИ 10 (6 коммитов)

- `41170995` **чистка**: патрульная механика была МЁРТВЫМ КОДОМ (~265 строк —
  урна, вылазка, `ai_patrol_errand`, `ErrandVerb::Patrol`, колонка `garrison`);
  **строка-призрак города** (`crewCount=3` при двух выписанных — дефолт занял
  место вырезанного патруля) держала **ТРЕТЬ мировой добычи дерева**, цена
  измерена перед сносом; правильный закон распуска стоял в НЕДОСТИЖИМОЙ ветке,
  а живая дверь превращала табун в горожан. Свидетель: `crew_rows_match_count`
  сверяет `crewCount` с числом авторских строк КОМПИЛЯТОРОМ.
- `87e37407` / `8d03c977` / `f81b2d00` **канон S2 «Протокол двух миров»**:
  макро = контекст, сцена берёт ДАТУ, симулится СВОИМИ законами, отдаёт ТОЛЬКО
  дельты; у тела наверх одна дверь — СТРОКА РОСТЕРА; макро и микро — РАЗНЫЕ
  РЕЖИМЫ (Civ/HoMM против M&B). Третий коммит — **исправление**: направление
  течи я назвал наоборот и успел записать ошибку.
- `aab5a5a6` **ТИП СКВАДА**: `ErrandVerb` УЖЕ был этой колонкой, не хватало
  закона «спрашивают первой». Четыре словаря в один, течь с 4 точек к одной
  названной (`ByKind`), снесены `VendorTrade`/`Nomad`. Негативный контроль
  посимвольный.
- `0b7ddbaf` **скор** — см. порцию 1 выше.

## УРОК МЕТОДА СЕССИИ 10

**Мои рассуждения врали дважды в одном разборе, и правду дал СЧЁТ.** Владелец
дважды сказал «надо факты». Ошибся в направлении течи границы (поверил шапке
`behaviour.h`, объявлявшей колонку общей для двух миров) и принял
`SubworldAi::Combat` за боевую систему (правда — закон «ВОЙНА ТЕЛА НЕ
СПРАШИВАЕТ», `sub/movement.cpp`). **Проверять закон в ТОЧКЕ ИСПОЛНЕНИЯ, а не по
шапке модуля.** Плюс третий раз того же класса, поймал сам: питон-парсер
таблицы существ выдал 7 значений вместо 46 — **транскрипцию обязана проверять
независимая контрольная сумма**.

И обратное наблюдение, тоже числом: **проект кристаллизуется.** От начала
сессии 9 исполняемый код 65 974 → 65 749 (**−225 строк**) при +329 строках
комментария, и за это окно ДОБАВИЛИСЬ две системы (реестр интересов, тип
сквада). Новое приезжает дешевле, чем уходит старое.

## КРИТЕРИЙ ПРИЁМКИ — БЕЗ ИЗМЕНЕНИЙ (CANON S4)

Гейты: (1) деревня кормит свой город; (2) население выходит на ПЛАТО
(`|ΔP| < 5 %` за четыре сезона); (3) город торгует по миру и производит.
Наблюдаемые: лестница комфорта не в нуле, `coinLootPool` не растёт монотонно.
Гейт №3 закрывает порция 1, гейт №1 — порция 3.

---

# СЕССИЯ 10 (СЛЕДУЮЩАЯ) — Б-4 НА РЕЕСТРЕ, ПОТОМ СКОР, ПОТОМ НАСЕЛЕНИЕ

Состояние: main = после реестра интересов, сейв **v107**, ctest **104/104**,
ноль предупреждений. Смоуки не гонялись, глазами не смотрено.
Негативный контроль переезда: `/tmp/bal_base` против `/tmp/bal_new`
(64 дня, сид 7) — оба TSV ПОСИМВОЛЬНО идентичны.

## ЧТО СДЕЛАНО В СЕССИИ 9 — ПРОЕКТ ПЕРЕВЕДЁН НА РЕЛЬСЫ СИСТЕМНОГО ПОДХОДА

Вердикт владельца, задавший всю сессию: «у нас системный подход, мы должны
всегда знать, как будет выглядеть финальная картина, когда все системы готовы,
ядро отполировано — как будут структуры в памяти; всё должно быть прозрачно».

**1. РЕЛЬСЫ (AGENTS п.10 + восемь ассертов).** Впервые посчитана память
макромира: **~105 МиБ в замеренном мире** (6 700 сквадов, 1 880 мест) и
**~602 МиБ по капам**, из них **96 % — два фиксированных массива по 256
слотов** (`Inventory` 9 216 Б и слоты ростера 3 076 Б). Форма намеренная
(AGENTS п.2), но числа не были названы ни разу, пока их не посчитали.
Поймано два врущих комментария: `MacroNpcRuntime` обещал «~36 bytes» при 96,
`SpellBook` — 72 Б при 80. Ведомость сквада стоит в `npc_spawn.cpp`, где он
рождается; ассерт «ядро субъекта у места и у сквада ОДНО» (12 360 Б) —
CANON S4 теперь проверяет компилятор с обоих концов.

**2. РЕЕСТР ИНТЕРЕСОВ (`macro/interests.h`, v107).** Феод перестал быть
системой и стал строкой: `Interest` = 8 Б (КТО / ТИП / СИЛА −127…127 / СРОК в
днях), 128 связей на субъекта = 1 024 Б. Снесены `suzerainLandmarkId`,
`vassalHead`, `vassalNext`, `vassalEpoch`, `ensure_vassal_edges` — и с ними
ПОЛОВИНА S24, которую год лечили второй половиной без читателя. Оба конца
ставит одна дверь `set_suzerain`. Четыре колонки отвергнуты с причинами
(`kind`, `detail`, `sinceDay`, `Friend/Enemy` как типы) — разбор в CANON S24.

**3. ОДНА ШКАЛА ОТНОШЕНИЙ (CANON S26 «Закон диапазона»).** Граница берётся по
типу, а не по круглому числу: кламп матрицы переведён на
`kRelationMin/kRelationMax`, `kAnyBand` (чьё имя значит «вся шкала») перестал
врать, диагональ матрицы стала верхом шкалы вместо `100`. `−128` зарезервирован
владельцем под особое значение, смысл не назначен.

**4. МЕДЬ И ЗОЛОТО СВЕРЕНЫ С КОДОМ.** Прежняя запись «их в мире НЕТ ВОВСЕ»
снята как неточная: жилы сеются, фичи шахт есть, руда в каталоге, рецепты
чеканки есть — нет только строки в `kGathererDefs` и товарного ординала.
Закон шахты (артель строит фичу, кластер жил всасывается, в мире обнуляется)
проверен и оказался УЖЕ универсальным по роду.

**5. ПОЛИТИКА ВЫРЕЗАНА ЦЕЛИКОМ** (вердикт владельца: «политики пока не будет,
иначе мы никогда не выпустим пререлиз демо»). Ушли: темпераментные банды и сам
темперамент, RNG-поток генезиса фракций, панель дипломатии со всей обвязкой
(keymap, настройки UI, кнопка), колонка `playerReputation`. Матрица 64×64
осталась и заполняется теперь АВТОРСКОЙ ТАБЛИЦЕЙ ненулевых пар («ко всем
прочим» — одна строка вместо восемнадцати). Пороги стали степенями двойки:
враждебность −64, союз 32. Замер: 76 из 122 колонок разошлись, причина названа
— враждебными были КОРОЛЕВСТВА между собой (случайные войны из `kAnyBand`);
голодавших 1854 → 744. Разбор в CANON S24.

## ПОРЦИЯ 1 — Б-4: СБОРЩИК ИДЁТ ВНИЗ, ТЕПЕРЬ НА ГОТОВОМ РЕЕСТРЕ

Наряд в `problems.md §55` (срез «что построено, что нет»), форма — в CANON S4
«Сборщик идёт вниз». Что изменилось после сессии 9: **обратное ребро больше не
надо строить** — у сюзерена лежат записи `Stance::Vassal`, обход бесплатный.

Вердикты владельца 2026-09-21, которых в прежнем наряде не было:
- **сборщик — ГЛАГОЛ, а не вид:** род `TaxCollector` УМИРАЕТ вместе с
  `AIBehaviour::TaxRun`, как умерли `Woodcutter`/`Miner`/`Vendor` и род
  `Caravan`. Сборщик = обоз `Peasant` с поручением `Tithe`;
- **все заявки через ОДИН аукцион по скору** — приоритета как сущности нет:
  заявка дани получает тот же скор «выработка в день рейса», что добыча и сбыт;
- **по сборщику на каждого вассала-должника**, пул только урезает;
- **грузит по нужде дома**, остаток — по `value_dense_order`.

**СТРУКТУРНЫЙ БЛОКЕР, НАЙДЕННЫЙ ЧТЕНИЕМ:** у места не может быть больше
сквадов, чем СТРОК реестра — `outMask` это один байт, бит на строку
(`static_assert`, `npc_ai.cpp:4251`), а у города `crewCount = 3`. «По сборщику
на 16 вассалов» в эту форму не влезает. Решение владельца: строка крю
становится ШАБЛОНОМ, число экземпляров говорят аукцион (сколько заявок с
положительным скором) и пул рук (на сколько хватает душ). `outMask` умирает
вместе с потолком 8. **Это же снимает потолок с числа караванов.**

## ПОРЦИЯ 2 — СКОР: ОДНА ВЕЛИЧИНА НА ВЕСЬ МИР

Разведка 2026-09-21 нашла ЧЕТЫРЕ величины, притворяющиеся одной:

| заявка | что считает | размерность |
|---|---|---|
| добыча (`npc_ai.cpp:4489`) | что привезёт ОДНА спина | монет / душу / день ✅ |
| сбыт (`:4584`) | ВСЮ маржу склада | монет / день |
| страх (`:4326`) | стоимость погибших на маршруте | монет (дней нет вовсе) |
| патруль (`:4652`) | угроза минус цена всей вылазки | монет (дней нет) |

Вердикт владельца: **чинить, одна размерность у обеих заявок.** Форма —
«монет на душу в день» на весь мир, три правки и ноль новых констант:
1. сбыт считает маржу по тому, что влезет в ОДНУ спину — той же дверью
   `value_dense_order()`, какой крю потом и грузится;
2. страх делится на `tripDays`;
3. патруль делится на `days × take` — и тогда патрульная урна сливается с
   общей, а `run_patrol_auction` перестаёт быть отдельным аукционом.
**Устаревший комментарий** `npc_ai.cpp:4342-4348` («`1 +` не выведено
ниоткуда») врёт: `1 +` уже снят, обе заявки уже делятся на дни. Переписать
вместе с ходом.

## ПОРЦИЯ 3 — ПЕРЕВОРОТ НАСЕЛЕНИЯ (Б-1), наряд не изменился

Лежит ниже, в шапке сессии 9. Добавилось одно: `Landmark::needDebt` —
ВТОРОЙ долг рядом с `garrison.needDebt`, и он умирает этим же переворотом
(problems §56).

## ЧЕТЫРЕ ДОЛГА, ЗАПИСАННЫЕ СЕССИЕЙ 9 (problems §56)

1. **`spawnFaction` — второй ответ на «чья это руина»**: колонка вида места
   говорит «демоны», колонка экземпляра — «ничья». Снести, дав руине и шпилю
   честный `factionIdx` (владелец: «руине надо дать фракцию»). Тогда лестница
   контекста сворачивается в две ступени.
2. **Пространства ординалов места и сквада должны слиться** — до тех пор сквад
   НЕ входит в реестр интересов (иначе молчаливая коллизия ординалов).
3. **Место расходится со сквадом на 1 552 Б**, и это список недоделок:
   `std::string name` (нарушение AGENTS п.1 и п.3), вторая память
   (`titheAvg*` против `AgentMemory`), второй долг (`needDebt`), `population`.
4. **Медь-золото**: четыре строки данных + бамп сейва (`kCommodityCount`
   15 → 17). Порядок — после Б-4 (вердикт владельца).

## КРИТЕРИЙ ПРИЁМКИ — БЕЗ ИЗМЕНЕНИЙ (CANON S4)

Гейты: (1) деревня кормит свой город; (2) население выходит на ПЛАТО
(`|ΔP| < 5 %` за четыре сезона); (3) город торгует по миру и производит.
Наблюдаемые: лестница комфорта не в нуле, `coinLootPool` не растёт монотонно.
Гейт №1 закрывает порция 1.

---

# СЕССИЯ 9 (СЛЕДУЮЩАЯ) — ПЕРЕВОРОТ НАСЕЛЕНИЯ, ПОТОМ ДОДЕЛАТЬ Б-4

Состояние: main = `a39f42f3`, сейв **v106**, ctest **104/104**, ноль
предупреждений. Смоуки не гонялись, глазами не смотрено.
Прогон сессии 8: `/tmp/balance_s8_base` (базовый) против `/tmp/balance_s8_pool`
(новый пул), 512 × 4, сравнивалка `/tmp/cmp_runs.py`.

## ЧТО СДЕЛАНО В СЕССИИ 8 (`a39f42f3`, один коммит — дуга не делилась зелёно)

- **ПУЛ РУК = ПОЛОВИНА ПАСТВЫ** (`macro/labour.h`). Доля реестра
  `pop >> labourShift` была числом с потолка И отвечала на два вопроса сразу
  (поле и станки города). Прокорм в счёт НЕ входит — вердикт владельца: рот
  ест одинаково дома и в поле. Умерли как следствия: гейт отлучки и третье
  прочтение пула в мере табуна; соло-курьер впервые платит душой из пула.
  Замер (все 4 сида): население **+19/+16/+17/+12 %**, сделок +2/+9/+27/+10 %,
  стоимость сделок +16/+4/+10/+16 %, рейсов сбыта +8/+6/+10/+5 %. Еда в городах
  как была случайной (+16/−11/−1/+47 %), так и осталась — её чинит дань, то
  есть Б-4.
- **ПРИРОДА РОДА** `kNpcNature` (Human/Fauna/Void) вместо границы ординала.
  Граница ВРАЛА: `Adventurer`, `TaxCollector`, `RoadAmbusher` считались
  зверьём (сборщик дани дома уходил в гарнизон, как лошадь), а нож излишка
  резал по тегу Mount — пойманный олень уходил в пул дезертиров человеком.
  Тег `Mount` остаётся РОЛЬЮ (вердикт владельца), природа — отдельно.
- **СНЕСЕНЫ СКВАДЫ, ПРИТВОРИВШИЕСЯ ВИДАМИ:** шесть мёртвых профессий и род
  `Caravan` вместе с `replenish_caravans` (ушёл один из двух литералов `20`),
  `ai_caravan` (163 строки), `AIBehaviour::CaravanTrade`, засев в генезисе.
- **У ЗАКОНА ТОРА ПОЯВИЛСЯ ПРЯМОЙ СВИДЕТЕЛЬ** — `pick_next_station_` в
  `npc_ai.h`: прежде закон пинался через ИИ каравана и умер бы с ним молча.
- **ДВЕ ДВЕРИ ДУШ** `souls_home` / `souls_flock` — обе пока дают одно число;
  читатели симуляции разведены по смыслу. Это подготовка порции 1 ниже.

## ПОРЦИЯ 1 — ПЕРЕВОРОТ НАСЕЛЕНИЯ (Б-1; решено владельцем 2026-09-21)

Дословно: «в фиче города есть число населения — и это бесплатное знание,
сколько сейчас в поле, потому что в ростере будет разница этого числа; если
умрут в поле — вычесть сразу из населения».

```
фича места:   population = ВСЯ паства (дома + в поле)
ростер места: души, которые стоят дома
в поле      = population − souls_home        ← ноль скана
```

Сегодня всё наоборот: `population` — это души ДОМА, и «сколько в поле»
считается **сканом всех сущностей мира каждый день** (`npc_ai.cpp`, вектор
`afield[]`). Движение душ внутри места (город поднял сквад, стража ушла в
него) население НЕ меняет — душа переехала из кармана в карман. Меняют только
вход и выход из мира: рождение, смерть дома, смерть в поле, и когда-нибудь
найм наружу через торговлю ростерами (владелец: «пока не переживай»).

Наряд:
1. **Одна дверь движения душ.** Мир двигает `population` ровно в 7 местах
   (`npc_ai` ×5, `npc_spawn` ×1, `world_tick`: голод, рост, рекрут). Все
   ходят одной дверью — иначе `население − ростер` начнёт врать молча.
2. **Души дома — в ростер места** (Б-1: гарнизон = ростер сквада-ландмарка).
   Контейнер не блокер: `SoldierSlot.count` — `int32`, слотов 256, город на
   74 000 душ влезает одной стаей.
3. **`souls_home` / `souls_flock` получают РАЗНЫЕ тела** — правка ровно в
   двух функциях, читатели уже разведены. Доразвести UI и смоуки (они на
   симуляцию не влияют, поэтому оставлены на этот шаг).
4. **Снести `garrison_target_strength`** (вердикт владельца) и с ним по
   цепочке `garrison_needs_recruit`, `garrison_recruit_` (второй литерал
   `20`), посев гарнизона в генезисе. НЕ сделано в сессии 8 намеренно: это
   тело переворота, отдельно от него — половина.
5. **Нож излишка ОСТАВИТЬ, перецелив** с `garrison_cap_` на закон упряжки:
   потолок табуна = пул рук места (спина на душу), излишек — в мясо. Голодом
   лошадей не резать (владелец: «они будут дармоедами, есть еду людей — это
   худшее решение»). Цена ножа названа: без него табун пух монотонно, 4 230
   голов за 128 дней.
6. **Снести ежедневный скан `afield[]`** — пул читает бесплатное число.
7. **ПРОГОН 512 × 4.** Поведение меняется честно и в двух местах: рост пойдёт
   по ВСЕЙ пастве (сегодня город с артелями в поле растёт как маленький), и
   смерть в поле впервые уменьшит население места.

## ПОРЦИЯ 2 — ДОДЕЛАТЬ Б-4 (половина в дереве с сессии 7, problems §55)

Наряд не изменился и лежит в `problems.md §55` (что построено, что нет, что
врёт читателю). Два уточнения владельца 2026-09-21, которых там ещё нет:
- **Сборщик — ОБОЗ, а не соло:** строка перестаёт быть `solo`, размер из
  пула, плюс тягло из стойла (`outfit_crew_mounts`). Соло-спина не вывезла бы
  дань деревни, и гейт №1 приёмки не закрылся бы.
- **Грузит ПО НУЖДЕ ДОМА,** остаток — по `value_dense_order`. Иначе сборщик
  везёт домой серебро, а меряем мы еду.
Остаются нерешёнными два вопроса, заданных владельцу и не отвеченных:
порядок траты пула между сборщиками и артелями, и сколько сборщиков место
держит одновременно (сегодня строку закрывает `outMask`).

## ХВОСТ ЗАМЕРА СЕССИИ 8 — ПЕРЕТОК РУК ИЗ ДОБЫЧИ В ВОЗКУ

Тем же прогоном: артелей ДОБЫЧИ −4/−4/−7/−3 % на всех сидах, рейсов сбыта
+8/+6/+10/+5 %, и железо −51/+25/−99/−40 % (сид 12345: 13 705 → **93**).
Пул вырос — аукцион разложил его по целям, и рейс оказался выгоднее копания.

Это НЕ дефект пула: это ответ аукциона, и он может быть верным. Но решать
надо замером, а не мнением. Кандидат на проверку: скор заявки добычи
размерно не сходится со скором рейса сбыта — у добычи числитель это
стоимость В ДЕНЬ, у сбыта полная сумма сделки, а делитель `1 +` не выведен
ниоткуда (признано вслух в `npc_ai.cpp`, комментарий над `road_days_`).
Честная форма названа там же: ВЫРАБОТКА В ДЕНЬ РЕЙСА для обеих заявок.

## ПОРЦИЯ 3 — ОСТАТОК Б-6 (сносы, прогон не нужен)

`ai_trader`, `ai_nomad`, четыре копии выгрузки домой, `MarketSnapshot`.

## КРИТЕРИЙ ПРИЁМКИ — БЕЗ ИЗМЕНЕНИЙ (CANON S4 «РАБОЧАЯ МИНИМАЛЬНАЯ ЭКОНОМИКА»)

Гейты: (1) деревня кормит свой город; (2) население выходит на ПЛАТО
(`|ΔP| < 5 %` за четыре сезона); (3) город торгует по миру и производит.
Наблюдаемые: лестница комфорта не в нуле, `coinLootPool` не растёт монотонно.
Гейт №1 закрывает порция 2; порция 1 работает на №2.

---

# СЕССИЯ 8 (СЛЕДУЮЩАЯ) — ДОДЕЛАТЬ Б-4, ПОТОМ СЛИТЬ РОЖДЕНИЕ СКВАДОВ

Состояние: main = `e3145d08`, сейв **v105 (бампа не было)**, ctest **104/104**,
ноль предупреждений. Смоуки не гонялись, глазами не смотрено.

## ЧТО СДЕЛАНО В СЕССИИ 7

- `77b51857` **ОДИН РОСТЕР, ОДИН СУД ГРАНИЦЫ**. `sm::Roster` (форма) +
  `roster_season_window` (закон) вместо двух копий поле-в-поле.
  Негативный контроль: 117 из 122 колонок совпали ПОСИМВОЛЬНО за 128 дней;
  разошлись пять приборных и только на границах сезона — `soulsUnpaid`
  стояла нулём при 1950 ушедших.
- `05878cdf` **канон: МАКРОМИР — ОДНА СИСТЕМА** (шесть вердиктов владельца).
- `e3145d08` **обратное феодальное ребро** + `owes_tithe` + канон формы
  сборщика + новый блок «рождение сквада — один закон, а не девять» +
  problems §55.

## ПОРЦИЯ 1 — ДОДЕЛАТЬ Б-4 (половина в дереве, см. problems §55)

**ВНИМАНИЕ: ребро построено, читателя у него НЕТ.** Это колонка без читателя
(AGENTS §9), легальная только пока порция не закрыта. Шов дани ЖИВ.

Наряд, по порядку, каждый шаг собирается зелёным:

1. **Гейт.** `CrewGate::Suzerain` → `Vassals` (`landmark_registry.h`).
   Предикат в `npc_ai.cpp` (искать `case CrewGate::Suzerain`): открыт, если
   у места есть хоть один вассал с `owes_tithe`. Обход — по `vassalHead` /
   `vassalNext` после `ensure_vassal_edges(gs)`.
2. **Претензия на вассала.** В переписи крю (`npc_ai.cpp:~4294`, цикл по
   `view<NPCKind, MacroNpcRuntime>`) собрать плоский `std::vector<std::uint8_t>
   titheClaimed(gs.landmarks.size())`: крю с `errandVerb == Tithe` метит
   `errandObject`. Проверять по ГЛАГОЛУ, не по `NPCType` — иначе хардкод рода.
3. **Рождение.** Соло-ветка (`npc_ai.cpp:~5117`): снять `if (!boardary)` —
   рождение в ЛЮБОЙ день (иначе «потом дородит» невозможно, один шанс в 32
   дня). Поднимать по сборщику на каждого вассала-должника, не занятого
   claimed, в порядке скора `стоимость долга / (1 + дни пути)`
   (`nav_path_cost`), СКОЛЬКО ПОЗВОЛИТ ПУЛ — и пул брать ТОТ ЖЕ, что у живых
   артелей (`(population + standingSouls[row]) >> labourShift`), НЕ заводя
   десятого ответа на «хватает ли» (CANON S4, блок про девять).
   Ставить `errandVerb = Tithe` (новое значение `ErrandVerb`),
   `errandObject = ординал вассала`.
4. **`ai_taxrun` → сборщик вниз.** Цель — `landmark_by_id(errandObject)`.
   В состоянии Working У ВАССАЛА: грузить `titheOwedGoods[]` в сумку
   (порядок `value_dense_order`, как сейчас) И ТАМ ЖЕ списывать долг —
   погрузка и погашение в ОДНОЙ точке, шва не остаётся физически. Домой —
   выгрузка в `depot_(*homeLm)`. Факт `FactKind::Taxed` — на возврате.
5. **Снос.** Блок дани из `ai_vendor` (~62 строки, искать `titheOwedGoods`
   выше `ai_taxrun`), `capital_of_` целиком и три гейта `== LandmarkType::City`
   в `ai_taxrun`.
6. **ПРОГОН ОБЯЗАТЕЛЕН** — первый ход, двигающий числа мира. Мерить
   `debtFood` городов ОТДЕЛЬНО от деревень: гипотеза Б-5 в том, что дань
   кормит город, а названный риск — что копивший долг вассал будет обобран
   до нитки. Рычаг, если деревни начнёт обдирать: долг СГОРАЕТ на границе
   (одна строка в `assess_tithe_`), но НЕ в том же инкременте.

## ПОРЦИЯ 2 — СЛИТЬ ДЕВЯТЬ ОТВЕТОВ «ХВАТАЕТ ЛИ ЛЮДЕЙ»

CANON S4 «РОЖДЕНИЕ СКВАДА — ОДИН ЗАКОН, А НЕ ДЕВЯТЬ» — таблица из девяти
точек там же. Строго ПОСЛЕ порции 1 и строго ДО переезда на фичи.

## ДАЛЬШЕ — ПЕРЕЕЗД НА ФИЧИ (Э-2 → Э-3 → Э-4)

Вердикты в CANON S9 «МАКРОМИР — ОДНА СИСТЕМА». `LandmarkType` умирает целиком,
под городом и деревней — `FT_City` / `FT_Village`, адрес места = клетка,
население = публикация ростера. Хвосты названы владельцем заранее: генераторы
субмира (спрашивают род места → будут спрашивать строку фичи) и городские
улицы (читают байт дороги под собой → будут читать контекст соседних клеток).

## КРИТЕРИЙ ПРИЁМКИ — ПРИНЯТ 2026-09-21 (CANON S4 «РАБОЧАЯ МИНИМАЛЬНАЯ ЭКОНОМИКА»)

Владелец задал его КАРТИНОЙ, а не числами; числа выведены из неё. 512 × 4.

**ГЕЙТЫ:** (1) деревня кормит свой город — Σ доставленной сборщиками еды > 0
и `debtFood` медианного города не растёт монотонно; (2) население выходит на
ПЛАТО — `|ΔP| < 5 %` за последние четыре сезона (не «не падает»!); (3) город
торгует по миру и производит — у медианного города ≥1 рейс корована за сезон,
ненулевой выпуск благ, охват не в одного соседа.
**НАБЛЮДАЕМЫЕ:** (4) лестница комфорта не в нуле; (5) `coinLootPool` не растёт
монотонно.

Гейт №1 закрывается порцией 1 ниже — она и есть его постройка.
Прежний гейт «население не падает монотонно» ОТМЕНЁН вердиктом владельца:
падение с посадкой на плато — здоровый мир (S25, потолок эмерджентен).
Бандиты возвращаются ПОСЛЕ базы и в названной роли — СТОК произведённых благ.

---

# СЕССИЯ 7 (СЛЕДУЮЩАЯ) — ЧИСТКА ДО СИСТЕМЫ

Состояние: main = `463170c6`, сейв **v105**, ctest **104/104**, ноль
предупреждений. Смоуки не гонялись, глазами не смотрено.
Прогон с ведомостью душ: `/tmp/balance_s6_souls` (512 дней × 4 сида),
сравнивалки `/tmp/cmp_runs.py`, `/tmp/cmp_debt.py`, `/tmp/cmp_souls.py`.

## ГЛАВНОЕ ЧИСЛО: ГОРОДА УМИРАЮТ ОТ ГОЛОДА РЯДОМ С 227 МЛН ЕДЫ

```
сид 7, день 128   мест   население   склад еды (МЕДИАНА)   пустых   УМЕРЛО
город               66      74 964                     0    46/66    65 629
деревня           1737      97 899                29 586   449/1737   7 037
```
90 % смертей мира — города. К дню 512 медианный город: население 0, еда 4.
Деревни при этом держат 227 млн еды при медианном населении 10.
Голод за горизонт: 155-166 тыс. душ на каждом сиде. Дезертирство — не
причина, а последнее звено: город не кормит себя → не гасит счёт своих
артелей → 1490-1970 артелей в сезон проваливают окно ПО ХАРЧУ (по плате:
0-6) → пул.

Баланс душ мира (новые колонки прибора): осёдлое население −84 %, а ДУШИ
МИРА −30/−25/−10/−13 %. Две трети «вымирания» — перелив, треть — настоящая
смерть. Закон сохранения: Δмир = рождено − голод − безымянная убыль
(73 492 / 46 009 / 12 206 / 14 747 душ; кандидат — потери боя).

## ЗАМЕР МИНИМАЛЬНОГО МИРА: БАНДИТЫ НЕ БЫЛИ ПРИЧИНОЙ ГОЛОДА

Прогон без бандитов и патрулей (`/tmp/balance_s7_min`, 512 × 4) против
прогона с ними (`/tmp/balance_s6_souls`), день 512:

```
сид        души мира   население    Σ ГОЛОД    пул лута    сделки
7             +30 %       +90 %       −1 %      −61 %      −24 %
1             +16 %       +40 %       −1 %      −42 %       −7 %
12345          +0 %        −1 %       +2 %       −8 %      −12 %
999            +2 %        +2 %       −1 %       −8 %      −14 %
```

**ГЛАВНОЕ: Σ голода не сдвинулся НИ НА ОДНОМ СИДЕ** (155-165 тыс. душ и до, и
после). Гипотеза «бандиты всех грабят, потому города и голодают» —
ОПРОВЕРГНУТА замером. Еду у городов не отнимают; её им НЕ ВЕЗУТ.

Что бандиты держали на самом деле: боевую смертность (души мира +30 %/+16 % на
двух сидах) и сжигание стоимости (пул лута −61 %/−42 %). Торговля просела на
7-24 % — вместе с бандитами ушли и встречные рейсы, которые их кормили.

Деревни в минимальном мире копят ЕЩЁ больше (`food_vil` +24 %/+8 %/+8 %/+33 %).
То есть сток еды не в бандитах, а в том, что излишек деревни не уезжает.

**Вывод для наряда:** порядок Б-4 → Б-5 (сборщик вниз, дань кормит город)
подтверждён числом как главный, а не предположением.

## ЧТО СДЕЛАНО 2026-09-21 (пять коммитов, все запушены)

- `3a122f82` **ведомость склада душ**: пять контейнеров + шесть потоков,
  негативный контроль прибора (108 колонок × 4 сида, ноль расхождений).
- `1f16a75e` **вырезаны трупы**: `FamineStarted`/`FamineEnded` (ноль
  отправителей — на этой колонке сессия 5 объявила «голода нет»),
  `stock_class_supply`, два врущих комментария.
- `463170c6` **вырезаны бандиты и патрули** (рождение; данные таблицы
  существ не тронуты) + два свидетеля, переживших свои механики.

## НАРЯД: ПРИВЕСТИ К СИСТЕМЕ (порядок — сверху вниз)

**Б-1. ГАРНИЗОН = РОСТЕР СКВАДА-ЛАНДМАРКА.** Сегодня `Landmark::garrison`
(`state.h:459`) — отдельный контейнер, и «единая система» написана дважды.
Из этого слияния следуют и еда, и патрули, и вердикт «всё в мире — сквад».
**Отсюда же вытекает элегантизация, названная владельцем:** население места
и его ростер (за вычетом животных) — одно и то же; `population` остаётся
производным числом для роста, но перестаёт быть вторым складом душ.

**Б-2. СМЕРТЬ СИГНАЛА `kNpcUpkeepNone`.** Вердикт: «у каждого апкип еды И
золота из таблицы НПЦ/мобов, она занимается этим и отвечает за это, и только
она». Форма: колонка живости (ест / не ест — нежить потом) + колонка
жалованья, где ноль значит ноль. Ростер смотрит в таблицу существ и оттуда
ставит долг. Размер игры от колонок НЕ растёт: `kNpcTypeDefs` — constexpr
таблица определений (53 строки на всю игру), экземпляр несёт только ординал.
Сегодня из-за сигнала корован не ест ВООБЩЕ, а строка игрока `Adventurer`
кормится бесплатно.

**Б-3. УБИТЫЙ ПОГИБАЕТ.** `drain_dead_leader_squads` льёт выживших в пул —
второй источник дезертиров, которого по канону нет (CANON S9 п.6). Восемь
точек вызова. Пока не сделано, пул — контейнер без оттока.

**Б-4. СБОРЩИК ИДЁТ ВНИЗ** (канон прав, код неправ): `ai_taxrun`
(`npc_ai.cpp:2394`) идёт ВВЕРХ, род места спрашивается ТРИЖДЫ. Умирает
`ai_taxrun` целиком (144 строки) + вторая копия уплаты дани (~62 строки).
Нужно обратное феодальное ребро (`vassalHead`/`vassalNext`, производное).

**Б-5. ЕДА В ГОРОД.** Замысел владельца: «дань от сборщика и корованы должны
обеспечивать городу еду — сборщик уходит в деревню вассала, забирает дань,
приносит на склад города; корован загружается товарами города и отправляется
по рынкам торговать». Арифметика сходится: деревня с 13 467 еды платит 1/8 =
1 683, шестнадцать вассалов дают городу сезон на 824 души. Значит дань
ДОЛЖНА кормить город — и не кормит из-за шва доставки (Б-4).

**Б-6. ЖИВОЙ, НО НЕПРАВИЛЬНЫЙ КОД** (не трупы — проверено): `ai_trader`
(59 строк, исполняется через Merchant, не торгует ничем), `ai_nomad`
(37 строк, 8 упоминаний — проверить перед сносом), четыре копии выгрузки
домой (`:2102`, `:2363`, `:2524`, `:4223`), `replenish_caravans` + генезисный
флот, `MarketSnapshot`, `lootPoolValue` (четыре писателя, читателей ноль,
16.3 млн стоимости мира заперты в числе).

**Б-7. НАЧАЛО СУТОК.** `season_boundary` = `(day-1) % 32 == 0`, то есть
граница на днях 1, 33, 65, 97 — и знаковый modulo с веткой на отрицательные.
При начале отсчёта с нуля это `(day & 31) == 0`, сезон = `day >> 5`, без
ветки и вычитания, и вся лестница времени сходится (день 2^13 тиков, сезон
2^5 дней, год 2^2 сезона). Это смена начала мировых суток: бамп сейва и все
потребители дня. Свой инкремент со свидетелем.

## ЕДИНЫЙ РЕЕСТР ОТКРЫТОГО ПО ЭКОНОМИКЕ (сведён 2026-09-21 из сессий 1-6)

Реестры сессий 2-6 ниже по файлу — ИСТОРИЯ. Этот список — единственный живой,
и каждая строка сверена либо с кодом, либо со свежим прогоном
`/tmp/balance_s7_min` (минимальный мир, 512 × 4).

### ЗАКРЫТО — не возвращать

| было записано | чем закрыто |
|---|---|
| «мир не голодает ни дня» | ОПРОВЕРГНУТО: 155-165 тыс. душ за горизонт на каждом сиде |
| «мир дезертирует, это причина» | ОПРОВЕРГНУТО: дезертирство — последнее звено цепи, начинающейся голодом городов |
| «бандиты грабят, потому города голодают» | ОПРОВЕРГНУТО замером: без бандитов Σ голода тот же (−1 %/−1 %/+2 %/−1 %) |
| **Табуны растут монотонно** (замер В.14 сессии 6) | ЗАКРЫТО: `horsesGarr` 0 → 1723 → 621, ссадка излишка работает |
| **Деревня не производит ничего** (дефект А.6) | ЗАКРЫТО ходом 5: ремесленные ранги деревни равны городским, разница эмерджентна от населения |
| **Длинный горизонт 512 × 4 не бежал ни разу** | СНЯТ трижды: `_base`, `_souls`, `_min` |
| `stock_class_supply`, `FamineStarted/Ended`, подъём банд, патрульная строка | ВЫРЕЗАНЫ 2026-09-21 |

### ЖИВЫЕ ХВОСТЫ — с числами свежего прогона

**Х-1. ДЕНЕЖНАЯ МАССА МИРА УМИРАЕТ, И ЭТО НЕ БАНДИТЫ.** Пул лута
775 → **6 480 795** БЕЗ единого бандита в мире, монета на складах мест
1 124 115 → **286 868 (−75 %)**. Значит главный кормилец пула — **сожжённое
ЖАЛОВАНЬЕ** (`gs.lootPoolValue += pay_value_dense(...)`, окно сезона и
`world_tick.cpp:329`), а не бой. Читателей у пула по-прежнему НОЛЬ. Прежняя
запись реестра («бой запирает стоимость») была неполной — теперь названо.

**Х-2. ЛЕСТНИЦА КОМФОРТА НЕ ПРОСТО В НУЛЕ — ОНА ПАДАЕТ ДО НУЛЯ.**
`tools` 347 191 → **700**, `cloth` 196 052 → **0 ровно, начиная со 128-го дня**,
`bricks` 114 470 → 25 341. Мир проедает генезисный засев и не воспроизводит
его. Прежняя формулировка «в мире 21 инструмент» описывала СТАТИКУ; правда —
ДИНАМИКА обвала.

**Х-3. ЧЕКАНКА МЕРТВА.** `minted` = 0 весь горизонт (единственный всплеск 128
на дне 256). Серебра нет в лестнице нужд, и производный спрос на монетный
металл равен нулю по построению.
**ФОРМУЛИРОВКА «МЕДИ И ЗОЛОТА В МИРЕ НЕТ ВОВСЕ» СНЯТА КАК НЕТОЧНАЯ** (сверено
с кодом 2026-09-21, CANON S10 «Спрос на ресурс существует только через
производство»): ординалы полей, геология с порогами 0.90/0.995, фичи
`FT_CopperMine`/`FT_GoldMine`, руда в каталоге (128 и 12 800) и рецепты
чеканки на четыре фракции — ВСЁ ЕСТЬ. Нет ровно двух вещей: строки в
`kGathererDefs` (некому копать) и товарного ординала в `kCommodities` (не в
чем возить). Мир сеет медь и золото и не умеет их поднять. Цена — четыре
строки ДАННЫХ, ноль кода, но `kCommodityCount` 15 → 17 = бамп сейва.
**ВЕРДИКТ ВЛАДЕЛЬЦА 2026-09-21:** спрос на монетный металл даёт ЧЕКАНКА, в
лестницу нужд он не входит; природа самой МОНЕТЫ (тег «чистое богатство»)
остаётся НЕ ФИНАЛИЗОВАННОЙ — разбор и отвергнутая форма в CANON S10.
**ПОРЯДОК:** после реестра отношений и Б-4 (вердикт владельца).

**Х-4. Шов дани + сборщик идёт ВВЕРХ** → порция Б-4.

**Х-5. Сторона сделки = КОНТЕЙНЕР** (найм за товары, обмен бойцами, выкуп,
дарение) — спроектировано, не построено.

**Х-6. Раздача пула лута** — одна агностичная дверь; замыкает контур Х-1.

**Х-7. `kCommodities` → КАТЕГОРИИ** (дань, `MarketSnapshot`, `materialMask`),
бамп сейва.

**Х-8. Базальное производство** — рулетка по скору вместо argmax в
распределении рук (тот же закон, что у поручений артелей).

**Х-9. Две сделки в одну дверь** — вердикт владельца в силе, исполнимо ТОЛЬКО
вместе с законом «нужда встречной стороны неприкосновенна с обеих сторон»
(CANON S10, четыре измеренных провала, голод от +246 % до +512 %).

**Х-10. Память рынка ОТЛОЖЕНА с доводом**, `MarketSnapshot` сносится в любом
случае (порция Б-6).

**Х-11. 53 из 88 умирающих мест совсем пусты** — помощь сюзерена / заём /
принять смерть мест как контент (дверь `set_landmark_type` готова).

**Х-12. Водные анклавы:** 35 деревень из 1575 без сухого маршрута к рынку.

**Х-13. Казна в закуп рейса не грузится** — названо вслух, не проверено.

**Х-14. Овцы** — ещё одна строка формы лошади (свой тег, в упряжку не идут).

**Х-15. ПОТОЛКИ (знать, не чинить):** таблицы маршрутов растут как R²
(40.7 МБ при R≈1886, 1.2 ГБ при R = 10 000); `distHome` — потолок 4095 клеток,
**зажим МОЛЧИТ**; скан ростера растёт квадратом.

**Х-16. `spire_climb` сид 12345** — известный красный смоук, вердикт владельца
«забей».

## КРИТЕРИЙ ПРИЁМКИ — ВЕРДИКТА ВСЁ ЕЩЁ НЕТ
Пять чисел (объяснены владельцу 2026-09-21); №1 переписан его же вердиктом:
мерить не осёдлое население, а ДУШИ МИРА (Σ населения всех мест + души
сквадов). Рекомендация агента: гейт — №1 и №5 (законы сохранения), остальные
три — наблюдаемые числа.

---

# СЕССИЯ 6 (СЛЕДУЮЩАЯ)

Состояние: main = `f5d1be65`, сейв **v105**, ctest **106/106** (известный
красный `rpg_loot_test` закрыт коммитом `f228880e`). Смоуки не гонялись.
Глазами не смотрено.

**ПРОГОНЫ СЕССИИ 5 ЛЕЖАТ В `/tmp`** (512 дней × 4 сида каждый, колонки
починенного прибора — `debtFood`/`debtComfort`/три потока):
`balance_s5_base` (база), `balance_s5_ema` (дань предмасштабирована),
`balance_s5_village` (деревня производит), `balance_s5_upkeep` (счёт
содержания v105). Сравнивалка — `/tmp/cmp_runs.py`, разбор долгов по родам
мест — `/tmp/cmp_debt.py`. **Если /tmp вычищен — прогон повторить, он стоит
~25 минут на четыре сида параллельно** (и ПЕРЕД ним собрать `--target
balance_run` явно: `check` его НЕ собирает).

## ВЕРДИКТЫ ВЛАДЕЛЬЦА 2026-09-21 — ЧТОБЫ НЕ ПОТЕРЯЛИСЬ

Реестр всего, что решено этой сессией. Построенное помечено, непостроенное —
это наряд.

| вердикт | статус |
|---|---|
| **EMA: горизонт всякой памяти = СЕЗОН**, орфография одна, хранение предмасштабированное | дань ✅ v104; **поля угрозы и запаха НЕ переехали** → ход 2 |
| **Закон двух тактов**: день дешёвый, граница — техническая дверь глобальной перемены, фриз там ЗАКОНЕН; вне границы — нештатное событие | ✅ CANON S19.2 |
| **Сезон = веха**: перенормировка И итог накопленного за 32 дня | ✅ CANON S19.2 |
| **Бюджет памяти**: десятки и сотни МБ бесплатны (до 8 ГБ), «поле удвоится» — не довод | ✅ AGENTS |
| **Дань — налог на ИМУЩЕСТВО** («чем обычно располагаешь»), ставка честная 1/8 | ✅ v104, замер: в шуме (облагать нечего) |
| **Деревня производит по той же модели**, меньше — эмерджентно от населения; не чеканит по РАНГУ, не по виду | ✅ замер: долг комфорта деревень −35…−37 %, городов +12…+37 % |
| **Память рынка**: снимок бьёт память на всех горизонтах — ведомость остаётся СНИМКОМ | ✅ измерено, CANON S19.2 |
| **Лестница самонормируется**: новый товар сам поднимает планку 100 % благополучия; вес вне пропорции ЗАПРЕЩЁН | ✅ CANON S10 |
| **У всякого, кто кормит, есть счёт**; 1/8 и пол «одна душа» — ошибка, взыскание ПРОПОРЦИОНАЛЬНО | ✅ v105, замер неоднозначен (см. ход 0) |
| **Снаряжать при выходе** — дом гасит счёт уходящей артели вперёд | ✅ v105 |
| **УБИТЬ ГАРНИЗОН: ландмарк = неподвижный сквад, гарнизон = его ростер** | закон ✅, **СТРУКТУРЫ НЕ СЛИТЫ** → ход 3 |
| **Игрок сам платит своему гарнизону** — окно выбора «чем и сколько», тем же бартером над контейнерами | ❌ UX, не построено, CANON S4 |
| **Порция 6** — занятие = строка ростера | ❌ наряд ниже |
| **Критерий приёмки экономики** (пять чисел) | ❌ предложен, вердикта нет |

## ХОД 0 — МИР УМИРАЕТ. ЭТО ГЛАВНОЕ ЧИСЛО ТРЕКА, И ОНО ПЕРЕБИВАЕТ ВСЁ ОСТАЛЬНОЕ

**Первый в истории проекта прогон на 512 дней (4 сида, `/tmp/balance_s5_base`)
говорит, что мир не переживает полутора лет.** Население падает МОНОТОННО на
всех четырёх сидах, одинаковой формой — значит это структура, а не сид:

```
день      сид 7    сид 1  сид 12345   сид 999
  2      332410   321878   317948    336748
128      184115   163320   166797    168152     −45 %
256      109511    95085   103196     96598     −67 %
512       82802    69888    75549     77941     −75 %
```

Все числа сессий 1-5 сняты на 64-128 днях — то есть В НАЧАЛЕ ОБВАЛА, и потому
читались как рост. «Население +13…+21 %» прежних дублей — это сравнение двух
траекторий падения на раннем участке.

**ГОЛОДА НЕТ НИ ОДНОГО ДНЯ** (`starvedPops` = 0 весь горизонт, `famineStarts`
= 0). Мир не голодает — он ДЕЗЕРТИРУЕТ:

```
день        pop  дезертиры   крю Gather   крю Sell
  2      332410          0            0          0
128      184115      51655         4545       2299
256      109511      96177         3727       3029
512       82802     154385         3611       3119
```

**Пул дезертиров 0 → 154 385 душ — это две трети всей убыли населения.**
Остальное держат ростеры живых крю (~6700 артелей) и гарнизоны.

**ГИПОТЕЗА ПРИЧИНЫ, названная по коду (проверить первым делом).** Окно сезона
(`squad_season_window`, `npc_ai.cpp:3971`) судит каждый ростер законом «покрыто
ЦЕЛИКОМ или не списывается», и непокрытая нужда стоит 1/8 ростера в пул
дезертиров. Арифметика сходится: 154 385 душ за 16 сезонов ≈ 9.6 тыс. за
сезон; при ~6700 крю это ~1.4 души с крю за сезон, то есть 1/8 от ростера в
~11 душ. **Похоже, что окно проваливает КАЖДОЕ крю КАЖДЫЙ сезон.**

Это согласуется с вердиктом 2026-09-19, снявшим предоплату сезона при создании
артели («худшее — если задержится, потеряет 1/8 по общему закону»): цена была
названа как «худшее», а оказалась НОРМОЙ.

**ЧТО МЕРИТЬ ПЕРВЫМ:** сколько крю проваливают окно за сезон и почему —
не хватает харча, платы или и того и другого; и сколько душ возвращает
`raise_deserter_bands` против того, сколько пул принимает. Сегодня зонда на
это нет: `balance_run` печатает РАЗМЕР пула, но не поток в него и из него.
Две колонки — и причина станет видна.

**ПОКА ЭТО НЕ ЗАКРЫТО, остальные ходы меряются на падающем мире**, и всякое
«+10 % торговли» читается на фоне −75 % населения.

## ХОД 1 — ПОРЦИЯ 6: ЗАНЯТИЕ = СТРОКА РОСТЕРА (разобрана до кода)

Вердикт владельца выбран главным ходом сессии 5 и НЕ построен — сессия ушла
в прибор, двери и память. Форма разобрана чтением кода 2026-09-21, ниже —
наряд, а не замысел.

**ДИАГНОЗ ОДНОЙ ФРАЗОЙ.** У мира нет понятия «занятие», потому что ГЛАГОЛ и
ОБЪЕКТ выпадают из ОДНОЙ рулетки: `run_auction` (`npc_ai.cpp:4438`) строит
заявки Gather И Sell вперемешку, а рулетка строки (`:4820`) тянет из общей
кучи. Отсюда всё остальное: деревня ездит торговать, город ездит закупаться,
дань уезжает попутно к случайному соседу, и 98 % торгового канала держат
крестьянские артели, которым канон S4 торговать запрещает.

**ЗАКОН:** глагол — КОЛОНКА строки ростера; объект — вердикт аукциона.

| строка | глагол (колонка) | объект (аукцион) |
|---|---|---|
| крестьянин деревни | `Gather` | строка `kGathererDefs` + площадка из описи |
| корован города | `Sell` | станция по весу `1/(1+дни)` — построена |
| стража | `Patrol` | горячая округа — построена |
| сборщик | `Tithe` (новый) | вассал с ненулевым долгом |

### И-1. Фильтр заявок по глаголу (минимальная правка, мир уже едет)

Аукцион продолжает считать ВСЕ заявки один раз на место в день (кэш
`bidCount` остаётся). Меняется только выбор: строка тянет рулетку **из
подмножества заявок своего глагола**. Точки: `LandmarkCrewRow`
(`landmark_registry.h:60`) получает колонку `ErrandVerb verb`; блок
`case CrewGate::Auction` (`npc_ai.cpp:4813-4838`) фильтрует `bids[]` по
`cr.verb` перед взвешенным броском; `bidCount <= 0` для своего глагола =
строка закрыта (отказ по цене — тот же закон).
**Цена:** деревенские строки становятся `Gather`, городская артель
покупателей — `Sell`. Рейсов деревень больше нет ⇒ **канал сужается в ~50
раз, если И-2 не сделан В ТОМ ЖЕ ИНКРЕМЕНТЕ.** Порознь не строить.

### И-2. Число торговых крю = функция контекста (в том же инкременте)

Вердикт владельца (CANON S4 «ЧИСЛО КАРАВАНОВ — ФУНКЦИЯ КОНТЕКСТА МЕСТА»)
и одобренная форма:
```
торговых крю = min( пул труда / душ на крю ,
                    стоимость излишка склада / стоимость полного трюма )
```
Первый член уже есть (`pool` / `perCrew`, `npc_ai.cpp:4964`); второй —
«нечего везти, обоз не снаряжают». Ноль новых констант.
**Что умирает:** `replenish_caravans` (`npc_spawn.cpp:692-733`, норма «один
караван на город» + барьер `pop < 20`, дубль барьера гарнизона) и
генезисный флот (`npc_spawn.cpp:271-287`, `0.3 × числа городов`). Караван
становится обычной строкой ростера, подсудной суду границы.
**Прикидка до кода:** чтобы 66 городов дали сегодняшние ~3040 крю, нужно ~45
крю по ~7 душ на город; пул труда города `pop>>3` = 315 душ при 2520
населения — руки есть, порядок числа макро-сквадов тот же, что сегодня.
**Главный риск-замер:** деревня перестаёт ПОКУПАТЬ (товар приходит только
если корован доехал). Мерить `debtComfort` и `debtFood` деревень отдельно от
городов — прибор это теперь умеет.

### И-3. Сборщик идёт ВНИЗ + обратное феодальное ребро

Канон S4 (2026-09-20) дословно: «сборщик универсально от феода к вассалу…
если к нему сборщик не дошёл, то вассалу норм». В коде ровно обратное:
курьер идёт ВВЕРХ (`ai_taxrun`, `npc_ai.cpp:2394`) и род места спрашивается
ТРИЖДЫ (`:2387`, `:2405`, `:2482`), все три — `== City`.

**Обратное ребро — это не новая сущность, а недостроенная половина S24.**
Канон: «каждый узел знает ТОЛЬКО прямых подчинённых и сюзерена»; в коде
построена одна половина (`suzerainLandmarkId`, `state.h:470`), и её же
комментарий цитирует канон урезанным. Форма: `vassalHead` + `vassalNext` —
односвязный список через ординалы мест, плоско, +8 байт на место, вставка
O(1) на УЖЕ СУЩЕСТВУЮЩИХ дверях `add_landmark` / `set_landmark_type`.
Производное: восстанавливается на загрузке из колонок сюзерена, в сейв не
едет. **Колонку заводить ТОЛЬКО вместе с читателем** (AGENTS §9).

**Что умирает:** `ai_taxrun` целиком (144 строки — это `ai_vendor` без
торговой половины с фиксированной целью), вторая копия уплаты дани
(`:2305-2336` и `:2483-2512`, ~62 строки), три City-гейта.
**Что чинится даром:** шов дани (`npc_ai.cpp:2297` — гейт получателя
«это поселение», долг сюзерену гасился доставкой случайному соседу).

### И-4. Снос мёртвого (без прогона)

`ai_trader` (59 строк) и `ai_nomad` (37) — скан городов + таймер, **не
торгуют ничем**: ни сумки, ни сделки. `NPCType::Vendor` не рождается нигде
(ветка диспетчера `:3627` недостижима). Четыре копии выгрузки домой
(`:2102`, `:2363`, `:2524`, `:4223`). Хардкоды `== LandmarkType::City` в
`ai_trader`/`ai_nomad` (`:2552`, `:2558`, `:2609`, `:2615`) — тот самый
класс ворот «рынок бывает только городом», снесённый 2026-09-20 везде,
кроме этих двух функций.

**Порядок:** И-1+И-2 одним коммитом со своим прогоном (ждать ПИЛУ, не ровную
линию) → И-3 своим прогоном → И-4 без прогона. Сейв-бамп: И-2 (строки крю) и
И-3 (если ребро всё-таки поедет в сейв).

## ХОД 2 — ПОЛЯ УГРОЗЫ И ЗАПАХА НА ДВЕРЬ ПАМЯТИ

Дверь построена (`macro/memory.h`, сессия 5), дань переехала, поля — нет.
Закон канона (S19.2): горизонт ВСЯКОЙ памяти = сезон, орфография одна.
- **Угроза** (`threat_field.{h,cpp}`): полураспад 8 дней → дневной шаг с
  горизонтом сезона; память 11.5 → 32 дня (×2.8). `threat` становится
  `WorldMemory`, +12 КБ на мир. Реплей (`:45-58`) переписывается: сегодня он
  восстанавливает распад сдвигом `age / kThreatDecayDays`, нужно затухание
  по новому закону (цикл по возрасту — реплей бывает на загрузке и перепёке
  графа, а это события КЛАССА ГРАНИЦЫ, где дорого — законно).
  **ОСТОРОЖНО:** все читатели (`threat_on_route`, `fear_of` в аукционе)
  обязаны читать `memory_value()`, иначе страх вырастет ровно в 32 раза.
- **Запах** (`scent_field.{h,cpp}`): `uint16` × 1024² × фракции × 2 плана.
  Предмасштабирование требует ширины; по вердикту владельца о бюджете
  (AGENTS: десятки и сотни МБ бесплатны, до 8 ГБ) это больше не возражение —
  назвать число и взять честную ширину.
  **СМЫСЛ МЕНЯЕТСЯ:** с сезонным горизонтом поле говорит «здесь живут», а не
  «здесь прошли». Читателей (охота по градиенту) перечитать под этот смысл,
  а не унаследовать молча.
- Обе миграции чинят СВОИ мёртвые зоны диффузии: `per = (t>>3)/deg`
  обнуляется при `t < 8·deg` (угроза), `per = (t>>3)>>3` — при `t < 64`
  (запах). То есть слабая угроза не растекается ВООБЩЕ, слабый след тоже.

## ХОД 3 — СЛИЯНИЕ ГАРНИЗОНА В РОСТЕР (вердикт: «гарнизон умирает»)

Закон уже применён (v105: гарнизон судится тем же счётом, поля на `Landmark`
зеркалят `ecs::SquadRoster`), структуры — нет. Слить:
`Landmark::garrison` читается набором (`garrison_recruit_`), ссадкой
(`garrison_trim_`), вылазками (строка крю с флагом `garrison`), авто-боем и
сейвом. После слияния у места остаётся ОДИН контейнер существ — тот же, что у
сквада, — и «гарнизон» перестаёт быть словом мира.
**Цена:** сейв-бамп, ~десяток точек кода, свой прогон.

## ХОД 4 — ИГРОК ПЛАТИТ СВОЕМУ ГАРНИЗОНУ (UX поверх построенного закона)

Из закона v105 следует даром: списание идёт в начале сезона и допускает
ЧАСТИЧНУЮ оплату — значит игроку это открывается ОКНОМ «чем и сколько
заплатить», тем же бартером над двумя контейнерами (S25). У НПЦ выбор делает
правило, у игрока — рука; закон и дверь одни. Не строить второй механики.

## ХОД 5 — ДЕРЕВНЕ ВЕРНУТЬ РАБОТУ — ✅ СДЕЛАНО 2026-09-21

После сноса хлеба у деревни единственное ремесло `Cooking = 10`
(`characters.h:205`), а **в `kRecipes` не осталось ни одной строки на
Cooking** — деревня не производит НИЧЕГО, и `econ_produce_day` крутится
вхолостую для ~1700 мест каждый день. Вердикт: дать деревне низкие ранги
ремёсел (кандидаты — Tailoring/Masonry ранга ~5: своё волокно в ткань, своя
глина в кирпич). Это ДАННЫЕ, одна строка `kLandmarkSheets`.
**Цена:** город теряет монополию на блага; лестница комфорта деревень
оживает; прогон обязателен.

## РЕЕСТР ОТКРЫТОГО (сверен с кодом 2026-09-21)

### А. Дефекты, подтверждённые чтением кода
1. **Пул лута 9.2 М, читателей НОЛЬ** (`state.h:851`): пишут трое
   (`world_tick.cpp:278`, `npc_ai.cpp:4004`, `squad.h:251`), читают сейв,
   TSV и тест. Единственный сток денежной массы мира.
2. **Шов дани** — лечится И-3.
3. **Сборщика вниз нет** — лечится И-3.
4. **Хроническая смертность 3-6 % населения каждую границу.**
5. **Чеканка — потребитель без спроса** (`economy.cpp:108-113` признаёт это
   в комментарии): серебра нет в лестнице нужд, `jewelry` варится из железа,
   производный спрос на монетный металл равен нулю по построению. **Меди и
   золота в мире НЕТ ВОВСЕ** — в `kGathererDefs` одна монетная жила
   (`npc_ai.cpp:883`), то есть две монетные строки из трёх чеканить не из
   чего. Формы вердикта по-прежнему нет.
6. **Деревня не производит ничего** — ход 3.

### Б. Спроектировано, не построено
7. Порция 6 — ход 1.
8. Сторона сделки = КОНТЕЙНЕР (найм за товары, обмен бойцами, выкуп).
9. Раздача пула лута — одна агностичная дверь.
10. `kCommodities` → КАТЕГОРИИ (дань, `MarketSnapshot`, `materialMask`).
11. Базальное производство (рулетка по скору вместо argmax).
12. Объединение двух сделок в одну дверь (четыре измеренных провала).

### В. Замеры
13. **Длинный горизонт 512×4 — СНЯТ 2026-09-21** (`/tmp/balance_s5_base`),
    впервые. Сравнение с `/tmp/balance_s5_ema` — замер предмасштабированной
    дани.
14. **Табуны**: `horsesGarr` растёт монотонно.
15. **Подвоз еды в города** против нужды — теперь измерим честно через
    `debtFood` по родам мест.

### Г. Вердикты не взятые
16. **Память рынка** — разобрана и ОТЛОЖЕНА с доводом (см. грабли ниже):
    память НАБЛЮДАТЕЛЯ отвергнута доказательно; кандидат — ведомость по
    СРЕДНЕМУ складу (`titheAvgGoods` уже лежит на каждом месте), но это
    ставка «дрожь > сноса», и её надо ПОСЧИТАТЬ по готовому TSV прогона
    прежде, чем строить. `MarketSnapshot` сносится в любом случае.
17. 53 из 88 умирающих мест совсем пусты.
18. Водные анклавы: 35 деревень без сухого маршрута.
19. **Критерий приёмки экономики** — предложен, вердикта нет (см. ниже).

### Д. Потолки (знать, не чинить)
20. Таблицы маршрутов растут как R²; `distHome` — потолок 4095 клеток,
    зажим МОЛЧИТ. Скан ростера растёт квадратом.

## КРИТЕРИЙ ПРИЁМКИ ЭКОНОМИКИ (предложен 2026-09-21, вердикта НЕТ)

«Готово» у экономики не определено, и потому недоказуемо. Предложение —
пять чисел на 512 днях × 4 сидах:
1. население не падает монотонно ни на одном сиде;
2. Σ голодавших душ-дней к концу горизонта не растёт;
3. у расслоения есть СЕРЕДИНА (медианная деревня не в 200 раз меньше
   медианного города: сегодня 12 против 2520);
4. лестница комфорта не в нуле у медианного места (сегодня на весь мир 21
   инструмент, 1278 кирпичей, 3187 ткани);
5. `coinLootPool` не растёт монотонно.
Сегодня не выполнен ни один, кроме первого, а первый до 2026-09-21 не
проверялся вовсе.

## ГРАБЛИ СЕССИИ 5 (все мои)

1. **Прибор, который печатает одну величину дважды под разными именами, не
   меряет — он успокаивает.** `bread` и `grain` были одним ординалом, `unmet`
   — литеральным нулём; на этих колонках сняты все числа сессий 4-5.
   Проверять прибор ДО того, как верить его числам.
2. **Мёртвая зона целочисленной памяти — это МОЛЧАНИЕ, а не падение.** Поле
   живёт, значения меняются, тесты зелёные, и при этом вся лестница комфорта
   не облагается данью. Поймано трижды за день в трёх системах.
3. **«Принятое следствие» в комментарии — это диагноз, а не вердикт.** Обе
   дыры дани были описаны в коде подробно и объявлены принятыми; описание
   дефекта не делает его решением.
4. **Ошибка в ОЦЕНКЕ времени стоит решений владельца.** Я назвал прогон
   «~3.5 часа», взяв цифры из записей старой сборки; замер дал 24 дня/мин,
   то есть ~21 минуту. Владелец на этом основании предлагал отложить
   baseline. Мерить, а не вспоминать.
5. **Свидетель ловит автора чаще, чем код:** «сходится за 8 горизонтов» —
   неправда, невязка гаснет как `e^(−дни/горизонт)`, и для миллиона нужно
   ~14 сезонов.

---

# СЕССИЯ 5 (СЛЕДУЮЩАЯ)

Состояние: main = `5747930e`, сейв **v103**, ctest **104/105** (единственный
красный — `rpg_loot_test`, известный красный параллельной сессии Е, к экономике
отношения не имеет). Смоуки не гонялись. Глазами не смотрено.

## ХОД 1 — ЧТО МЕРИТЬ ПЕРВЫМ

Волокно ПОСТРОЕНО и измерено (`5747930e`): мир здоров, торгует больше, голодает
меньше, копает втрое. Открытых блокеров экономики на входе в сессию НЕТ —
впервые за три сессии. Ближайшие кандидаты, по убыванию отдачи:

1. **ДЛИННЫЙ ГОРИЗОНТ 512 дней × 4 сида** — не бежал ни разу; все числа
   сессий 3-5 сняты на 64 днях. Прогон стоит часы, но это единственное, что
   покажет, держится ли мир за год.
2. **ЦЕНА СЮЗЕРЕНА — снять и перемерить.** Она стоит ПОД ГРИФОМ «не уверены»
   (слово владельца). Волокно могло починить мир и без неё; проверить дёшево —
   один A/B на два сида.
3. **ПОРЦИЯ 6** (занятие = строка ростера) — крупнейший несделанный свип, с
   названной ценой: без закона рождения караванов канал сузится в ~50 раз.

## ЧТО СДЕЛАНО 2026-09-20 (четыре коммита, все с числами в сообщениях)

- `2a59b904` **станция по весу** `1/(1+дни)`, бросок дня детерминированным
  потоком, кап мембран снят, City-гейт станции снесён. Главное: дверь звалась
  БЕЗ RNG, и 98 % канала мира возило товар одному и тому же ближайшему соседу
  (негативный контроль печатает 96/96). Цена: сделок −48 %/−51 %; куплено:
  деревень с хлебом +9 %/+12 %, население +3 %/+1 %.
- `980355f7` канон S4 исправлен по прибору (кап был «восемь БЛИЖАЙШИХ», а не
  порядок хранения; вариант с капом неотличим), S10 — новый блок «у сделки два
  бортика», где записана откачённая попытка свести две сделки в одну дверь
  (четыре варианта, все хуже, голод от +246 % до +512 %).
- `ee295c00` **три потока материи**: пища кормит сама, хлеб вырезан, пол
  спроса мёртв, арифметика сезонного счёта починена (умножение перед делением
  — до этого хутор в 30 душ не хотел ни ткани, ни кирпича), цена сюзерена.
  Ход ПЛОХОЙ по числам, причина найдена (см. ход 1).
- `4c496cf4` канон S10: блок «три потока», отмена вердикта о нулевом спросе,
  закон «у всякого потока должна быть СВОЯ работа».

## РЕЕСТР ОТКРЫТОГО (сверен с кодом 2026-09-20, дубли сняты)

### А. Дефекты с числами
1. **Пул лута 9.2 М стоимости, читателей НОЛЬ** (`state.h:851`; пишут трое,
   читает никто) — денежная масса утекает в число.
2. **Шов дани:** `ai_vendor` списывает `titheOwedGoods` дома, а товар кладёт в
   депо станции ИЗ ПОРУЧЕНИЯ — под диффузией это случайный сосед; долг гасится
   ЮРИДИЧЕСКИ (`npc_ai.cpp:2293-2299`). Канон S4/S7 считает этот дефект
   закрытым — он НЕ закрыт, и после снятия равного жребия стал шире.
3. **Сборщика вниз НЕТ:** платит должник вверх (`ai_taxrun`), и род места
   спрашивается на обоих концах (`npc_ai.cpp:2389`, `:2369`). Канон S4
   «сборщик идёт вниз» описывает несуществующий код.
4. **Хроническая смертность 3-6 % населения каждую границу сезона.**
5. **Хлеб в городах +167 % на сиде 7** от хода станции (на сиде 1 плоско).
6. **Чеканка**: до трёх потоков `minted = 0` за 256 дней; после — ожила
   (+228 %), но вердикта о форме («металл строкой лестницы / спрос двора /
   поручение артели») по-прежнему нет.

### Б. Спроектировано, не построено
7. **ПОРЦИЯ 6 — занятие = строка ростера.** Гейты по роду места живы:
   `npc_ai.cpp:1953` (дом каравана = City), `:2371`, `:2389`, `:2466`,
   `ai_trader`/`ai_nomad` целиком. **Цена сноса названа: без закона рождения
   караванов канал сузится в ~50 раз** (~3040 вендорских крю против 67).
8. **Число караванов = функция контекста** (население и склад), как потолок
   гарнизона; сегодня назначено спавном (`npc_spawn.cpp:275`, гейт `pop < 20`).
9. **Сторона сделки = КОНТЕЙНЕР** (найм за товары, обмен бойцами, выкуп,
   дарение); `hire_npc` переписывается через эту дверь.
10. **Раздача пула лута** — одна агностичная дверь (см. А-1).
11. **`kCommodities` → КАТЕГОРИИ** (дань, `MarketSnapshot`, `materialMask`,
    ведомость) с бампом сейва — ждало калибровки, теперь ждёт волокна.
12. **Базальное производство (Lac operon)** — рулетка по скору вместо argmax.
13. **Объединение двух сделок в одну дверь** — вердикт владельца в силе, но
    исполнимо ТОЛЬКО вместе с третьим законом «нужда встречной стороны
    неприкосновенна с обеих сторон» (см. CANON S10, четыре измеренных провала).

### В. Замеры не снятые
14. **Длинный горизонт 512 дней × 4 сида — не бежал НИ РАЗУ.** Все числа
    сессий 3-4 сняты на 64 днях.
15. **Табуны**: `horsesGarr` растёт монотонно (4 230 голов за 128 дней).
16. **Подвоз еды в города** против нужды: городу на 1200 душ нужно ~38 000
    пищи в сезон, дань от ~26 вассалов даёт ~13 000.

### Г. Вердикты не взятые
17. **53 из 88 умирающих мест СОВСЕМ пусты** — помощь сюзерена / заём /
    принять смерть мест как контент (дверь `set_landmark_type` готова).
18. **Водные анклавы:** 35 деревень из 1575 без сухого маршрута к рынку.
19. **Кромка «всё или ничего»** для гарнизона и сквадов — про место или про
    душу (для мест её снял долг).

### Д. Названные потолки (знать, не чинить)
20. Таблицы маршрутов растут как **R²**: 40.7 МБ при R≈1886, **1.2 ГБ при
    R = 10 000** («1 ГБ ещё терпимо»). `distHome` — потолок **4095 клеток по
    дороге**, зажим МОЛЧИТ. Скан ростера растёт квадратом.

### Е. Вычищено как устаревшее (не возвращать)
- «Снятие капа мембран хуже» — не воспроизводится (кап = восемь БЛИЖАЙШИХ).
- «Равновероятный выбор соседа уже построен» — его не было вовсе (нет RNG).
- «45 млн хлеба в городах» — число ДО ведомости; и хлеба в мире больше нет.
- `stock_class_supply` — мёртвая дверь (ни одного вызова).
- Гейт «торгую только в городе» — снесён `2a59b904`.

## ГРАБЛИ 2026-09-20 (все мои, все про измерение и про слова)
1. **«В каноне написано» ≠ «в коде исполняется».** Слово «случайный» стояло в
   законе с первого дня, а рулетки не было: дверь звали без RNG. У случайности
   обязан быть свидетель, который ВАЛИТСЯ при её отсутствии.
2. **Не принимай границу за назначенное правило, пока не понял, что она
   держит.** Два бортика сделки я счёл «воротами по роду места» и снёс — оба
   защищали по стороне, и мир умер четыре раза подряд.
3. **Снимая производственное звено, спрашивай «куда уйдут руки».** Хлеб был
   работой, которая кормит; без него городская артель ушла в ткань и спряла
   городскую еду.
4. **Категория каталога не растягивается на второй вопрос.** «Что это за
   вещь» и «чему эта материя служит» — разные оси; попытка сказать их одной
   колонкой уронила два закона разом (свидетели поймали в ту же минуту).

---

# СЕССИЯ 3 — ЗАКРЫТА 2026-09-20 (8 коммитов, запушено `75d03b1f`)

- `67648df5` канон: два слоя, цена пути, ядро рулетки, занятие = строка
  ростера, сборщик вниз, три яруса знания.
- `c2f0ca3e` **ординал и есть адрес** — `landmark_by_id` без скана ростера.
  Выигрыша НЕТ (214.3 → 210.5 с, в шуме); оставлено за масштаб — работа
  скана растёт КВАДРАТОМ, при 10 000 мест это ×28.
- `4e82b166` **ВЕДОМОСТЬ** (S10 ярус 2) — место публикует цену и сезонную
  нужду на границе сезона рядом с описью. **НАСОС ВЫКЛЮЧЕН:** крю читало
  запас дома 4-битным классом памяти с потолком 4096, и город с 45 млн хлеба
  выглядел голодным — мир качал хлеб ВВЕРХ. Два сида: хлеб в городах −20 % и
  −67 %, в деревнях +16 % и +15 %, зерно в городах +494 % и +145 %.
- `ba8ffc56` + `27e1ed79` **события мест** — двери `add_landmark` /
  `set_landmark_type`, опрос ростера снят. Цена в шуме; главное — тихая
  ошибка: сторож сравнивал ЧИСЛО живых мест.
- `86be1d87` **дверь цены пути** `nav_path_cost` + починен провиант (считался
  по хорде, ноги шли в обход — сквад выходил голодным).
- `3f35f379` **скор = выработка в день рейса**, назначенное `1 +` умерло.
  Лучший результат сессии: население 257 217, голодавших 4 215, сделок 8 977,
  хлеб в городах 206 034 — всё лучше обоих предшественников.
- `5006c5a6` доки: economy.md, nav.md (память пересчитана, потолки названы),
  промт сессии 4.
- `75d03b1f` **рулетка по всей карте — измерена и откачена**, пять вариантов,
  все хуже; причины числами (см. порцию 5 выше).

**ЧЕМ ЗАКОНЧИЛОСЬ ПО СУТИ.** Мир двигают ВЕДОМОСТЬ (цена на месте) и ФОРМА
РЕЙСА (выработка в день). Маршрут остаётся слепым: корован ходит случайно по
весу к соседям и расходится по миру цепочкой прыжков — вердикт владельца
подтверждён прибором, а предложение исправить его провалилось на замере.

**ЧЕГО НЕ ДЕЛАЛИ:** глазами ничего не смотрели, смоуки не гоняли, сейв не
бампали, длинного горизонта (512 дней × 4 сида) не было ни разу — ВСЕ числа
сессии сняты на 64 днях.

---

# Промт следующей сессии

Обновлён 2026-09-19 (вечер). Прочитай AGENTS.md; режим гайдед — код по
одобрению на каждую порцию, вердикты в файлы (CANON.md / canon-audit.md /
problems.md). Уточняй вопросами ДО кода, варианты — с ценой каждого.

---

# СЕССИЯ 2 (СЛЕДУЮЩАЯ) — ДОЛГ, ГАРНИЗОН, КОНТЕЙНЕР

Состояние на старте: **main = `df2f44c2`**, сейв **v98**, ctest **105/105**,
смоуки **65/66** (`spire_climb` сид 12345 — известный красный, вердикт
владельца «забей»). Глазами НИЧЕГО из сессии 1 не проверено.

## ХОД 1 — ПОТРЕБЛЕНИЕ КАК ДОЛГ (вердикт владельца, CANON S10 дословно)

**Это главный ход сессии: он отменяет ДВА действующих правила сразу и
меняет ядро потребления.** Форма согласована целиком, три уточнения владельца
получены — строить можно без нового обсуждения:
1. На границе сезона место получает ДОЛГ = сезонная нужда по каждой строке
   лестницы. Долг висит весь сезон.
2. Приход на склад гасит долг СРАЗУ, и товар СРАЗУ съедается. На складе
   лежит **только излишек** — значит всё видимое свободно для погрузки и
   оплаты.
3. На следующей границе непогашенный остаток взыскивается ПРОПОРЦИОНАЛЬНО:
   по ГОЛОДНОЙ строке доля долга × население уходит насмерть (новая честная
   смертность), по прочим строкам непогашенное гасит РОСТ (закон S25
   «сытость × комфорт»). **От нехватки ткани не умирают.** Старый долг не
   переносится.

**ЧТО СНОСИТСЯ ВМЕСТЕ С ЭТИМ (иначе правило будет написано дважды — S26):**
- вычитание сезонного амбара в погрузке (`load_cheap_at_home_`, npc_ai.cpp);
- НЕ строить защиту амбара при оплате стоимостью — дефект, найденный
  2026-09-19 (город платил за хлеб только что купленным хлебом и проваливался
  ниже своей сезонной нужды по дровам), лечится этим ходом и только им.

**СЛЕДСТВИЕ ДЛЯ ЦЕНЫ:** спрос кривой дефицита читается из ДОЛГА, а не из
«склад минус сезонная нужда» (`stock_scarcity`, economy.cpp).

**ЦЕНА ХОДА, названная до кода:** у мира исчезает «чёрный день» (склад
перестаёт быть подушкой), и появляется смертность, которой не было — главное
число мира поедет. **Свой baseline, 4 сида, мерить голод ОТДЕЛЬНО от
торговли.** Поле долга едет в сейве ⇒ бамп `kSaveVersion` 98 → 99.

## ХОД 2 — ПОТОЛОК ГАРНИЗОНА ОТ КОНТЕКСТА (вердикт, CANON S4 дословно)

Сегодня `garrison_target_strength` = `population >> garrisonShift` и это
только цель НАБОРА: если гарнизон вырос иначе (двухтактный обоз ставит туда
табун), обрезки сверху нет вовсе — лошади копятся без предела.
1. Потолок = функция контекста места: население И стоимость инвентаря
   (богатое логово стережёт большая банда) — новая колонка, не новая система.
2. Выше потолка — излишек снимается со СЛАБЕЙШИХ.
3. Куда девается снятый, решает его СТРОКА: человек — в пул дезертиров
   (мир поднимает из него разбойников), зверь по тегу `NpcTag::Mount` — ПОД
   НОЖ, прямой конвертацией в товар-еду по своей стоимости.
Замер после хода: `horsesGarr` в `balance_run` обязан перестать расти
монотонно (сейчас 4 230 за 128 дней и продолжает).

## ХОД 3 — СТОРОНА СДЕЛКИ = КОНТЕЙНЕР (S25, последний кусок наряда)

Форма цены под это уже построена (двусторонние анкеты, коммит `df2f44c2`) —
осталась сама дверь: сторона сделки есть КОНТЕЙНЕР, инвентарь или ростер,
безразлично. Цена существа — колонка его строки (`hire_price_for` уже есть),
цена предмета — `ItemDef::value`. Отсюда даром: найм за товары, обмен
бойцами, выкуп, дарение; `hire_npc` переписывается через эту дверь и
перестаёт быть отдельной функцией с `playerGold`.

## ОТКРЫТЫЕ ЧИСЛА, КОТОРЫЕ ЖДУТ ЭТИХ ХОДОВ

- **ТОРГОВЛЯ СЖАЛАСЬ** после единого бартера: сделок −38 %/−71 %, оборот
  −83 %/−90 % (128 дней × 2 сида). Причина не дефект: спред ×0.7 был скрытой
  СУБСИДИЕЙ ПОКУПАТЕЛЮ. Калибровать ПОСЛЕ хода 1 — он пересчитает весь спрос.
- **ПОДВОЗ ЕДЫ В ГОРОДА** — главное незакрытое число экономики (блок
  «СЕССИЯ 2 — ДОДЕЛАТЬ ЭКОНОМИКУ» ниже, пункт 1). Мерить ПОСЛЕ хода 1:
  долг меняет саму меру нужды.
- **ЛЕСТНИЦА КОМФОРТА** в нуле (21 инструмент, 1278 кирпичей, 3187 ткани
  против 16 млн хлеба) — пункт 2 того же блока.
- Хвост лошади: овцы — ещё одна строка той же формы (свой тег, в упряжку не
  идут).

---

# СЕССИЯ 1 — ЗАКРЫТА 2026-09-19 (6 коммитов, сводка для истории)

**Сделано одним заходом (вердикты владельца: «слоты сразу, без компата» и
«тянуть инк. 4+5 сейчас»), ctest 105/105 + смоуки 65/66 (spire_climb «забей»):**
- `SoldierSlot` 12 Б × 256 слотов; обход = СЛОТЫ, души — `souls()` с адресом
  (слот, индекс); `size()` = Σcount, кэша-двойника нет.
- Читатели количеств ×count; гарнизоны рождаются/добираются СТАКАМИ
  (`generate_garrison` без ординалов) — тысячные армии открыты.
- Лица/займы: генерик — из адреса; `MacroStockKey`/`MacroDebt` несут
  `detailKind`/`detailLevel` (дискриминатор `detailLevel > 0`, Peasant = род 0).
- Потери авто-боя = `SoldierRecord`; одна страйк-дверь `remove_one_soldier`.
- **Новый канон S4 (дословно в CANON):** все сквады именные, по карте ходят
  только лидеры; одиночка = лидер с пустым ростером.

**ВСЕ СЕМЬ ИНКРЕМЕНТОВ ЗАКРЫТЫ 2026-09-19, сейв v98, ctest 105/105 + смоуки
65/66 (spire_climb — известный, «забей»):**
- инк. 3 — сейв СЛОТ-строками, `kSaveVersion` 96 → 97; нормализация уровня
  переехала в двери push (сломанный уровень больше не может ЖИТЬ в
  контейнере, а не только не может сохраниться);
- инк. 6 — добор и создание артелей льют ГЕНЕРИКИ, ординал больше не
  крутится на толпу (поток имён остаётся лидерам и душам с историей);
- инк. 7 — ЛОШАДЬ-ЮНИТ, v98: поле `Horses`, фича `FT_Pasture`, колонка
  `rosterYield`, строка NPC `Horse` (haulMult 8 / прокорм 1 / найм 240),
  свидетель `test_the_catch_lands_in_the_roster`. Формы и числа замера —
  дословно в CANON S10.

**ДВУХТАКТНЫЙ ОБОЗ ПОСТРОЕН** (вердикт владельца из трёх форм, дословно в
CANON S10): приход — сдать домой ВСЁ ездовое, выход — место снаряжает по
коню на душу из стойла (рядом с `provision_squad`), потолка ловли нет
(тормоз — цена аукциона). Тег `NpcTag::Mount` = колонка строки существа по
образцу `ItemType`. Замер: стойла мест 1 → **4 230** голов, упряжка 11 848,
население в шуме. Овцы = ещё одна строка той же формы (свой тег, в упряжку
не идут).

**ЕДИНЫЙ БАРТЕР ПОСТРОЕН (S25, шаг 4 порядка работ).** Торговая сила
свёрнута в ОДНО производное (`tradeDiscountPct` = харизма × торговля, образец
`moveSpeedPct`) — дефект S26 «считается в двух местах» закрыт; сделка стала
ДВУСТОРОННЕЙ (анкета против анкеты, наценку берёт сильнейший, величина =
разница); умерли спред ×0.7, пол 0.5 и потолок 1.5 и `bargaining_edge`;
анкеты мест получили харизму и торговлю ДАННЫМИ (город 10/30, деревня 5/10).
Свидетели `price_law`, `caravan_deal`, `rpg_rules`, `squad_war` переехали
вместе с законом.

**ИЗМЕРЕННОЕ ЧИСЛО, ОТКРЫТОЕ ДЛЯ СЕССИИ 2 — ТОРГОВЛЯ СЖАЛАСЬ:** 128 дней ×
2 сида, сделок −38 % / −71 %, оборот −83 % / −90 %. Причина названа и не
является дефектом: спред ×0.7 был СКРЫТОЙ СУБСИДИЕЙ ПОКУПАТЕЛЮ (рынок платил
0.7 базы), и с честной ценой тот же кошелёк берёт меньше. Население и голод
расходятся по сидам (сид 1: +19 % населения, голод −94 %; сид 7: в шуме) —
траектории, как и прежде, расходятся. **Калибровать решено ПОСЛЕ долговой
модели** (вердикт владельца): она пересчитает весь спрос, потому что цена
начнёт читать ДОЛГ, и сегодняшняя крутилка была бы снесена ею же.

**НАЙДЕНО ПРИ ЭТОМ (дефект старше правки, лечится долгом):** оплата
стоимостью не спрашивает, не съедает ли платёж сезонный амбар плательщика —
город расплачивался за хлеб только что купленным хлебом и проваливался ниже
своей сезонной нужды по дровам. Отдельного лекарства НЕ строить: долговая
модель (CANON S10) отменяет само понятие амбара.

**СЛЕДУЮЩЕЕ ПО НАРЯДУ:** (1) ДОЛГОВАЯ МОДЕЛЬ ПОТРЕБЛЕНИЯ — первый пункт
сессии 2, вердикт записан в CANON S10 дословно; (2) ПОТОЛОК ГАРНИЗОНА от
контекста места + излишек под нож (CANON S4, вердикт 2026-09-19: человек в
пул дезертиров, зверь по тегу `Mount` — в еду); (3) сторона сделки =
КОНТЕЙНЕР (найм за товары, обмен бойцами, выкуп, дарение) — форма цены под
это уже готова, осталась сама дверь.

## ИСХОДНЫЙ НАРЯД (для истории)

**Повод и вердикт — в CANON S4 «РОСТЕР — ЭТО ИНВЕНТАРЬ СУЩЕСТВ» (дословно,
2026-09-19).** Дыру нашёл владелец: у каждой души ростера сегодня своя запись с
`entityId`, из которого субмир крутит ЛИЦО и `faceSalt`, — на тысячных армиях это
невозможно, а кап 1024 души на сквад делает «тысячные армии» недостижимыми
вообще. Форма гибрида (одобрена): **одинаковое стакуется, скрученное — свой
слот**, как у предметов.

```
слот = { kind u16, level i16, count i32, entityId u32 }
   entityId == 0 → генерик-стак (сливается по {род, уровень}), count любой
   entityId != 0 → душа с историей (лидер/именованный/вселённый), count == 1
слотов 256 — зеркало 256 стаков инвентаря
```
Числа: 1024×8 Б = 8 КиБ и кап 1024 → 256×12 Б = 3 КиБ и кап по int32.
Лицо генерика выводится из (ординал сквада, род, индекс в стаке); хранимая
табличка лиц — замысел владельца на потом, архитектуру не меняет.

## СЕМЬ ИНКРЕМЕНТОВ (каждый зелёный; НЕ отдавать субагенту — правило AGENTS)

1. **Форма + API `SoldierSquad`** (`macro/army.h`): слоты-стаки; `size()` = Σcount;
   `push` сливает генерик по {род, уровень}, уникальные — свой слот; отдельная
   дверь обхода ДУШ и отдельная — СЛОТОВ. Аддитивно, дерево зелёное.
2. **Читатели количеств** — на слоты × count: прокорм и жалованье
   (`squad_season_needs`), пул труда и суд состава (`rotate_worker_squads`),
   `total_soldiers`/`count_soldiers_of_kind` (`army.h`), гарнизонные вылазки
   (`world_tick`).
3. **Сейв** (`save.cpp` ~372-395): формат слота, **бамп `kSaveVersion`** —
   владелец разрешил («мне пох на версии сейвов»).
4. **Субмир-спавн** (`sub/spawn.cpp` ~1148-1155): лицо и `BodySpec` из
   (ординал сквада, род, индекс), а не из `entityId`; `BodyLoan` ключуется
   слотом+индексом.
5. **Авто-бой** (`macro/auto_battle.h`, `squad.h` ~686/751/999): потери приходят
   не списком `entityId`, а по слоту и числу; опыт — по {род, уровень} слота.
6. **Ротация/дезертирство/повышение** (`npc_ai.cpp` 3665, 4450-4458, `squad.h`
   142-159): добор/ссадка/1-8 работают со стаками; **после каждого изменения —
   `refresh_squad_carry`** (дверь уже построена 2026-09-19).
7. **ЛОШАДЬ КАК ЮНИТ** (CANON S10 «ЛОШАДЬ — ЮНИТ, А НЕ ПРЕДМЕТ», дословно):
   пастбище — строка фич, лошади — поле ресурса как руда, у строки цели добычи
   ОДНА новая колонка «выход ложится в РОСТЕР, а не в сумку». Обоз растёт сам —
   закон Σ haulMult уже построен. Овцы — той же строкой потом.

## ЧТО УЖЕ ПОСТРОЕНО К ЭТОМУ НАРЯДУ (не переделывать)
- `carryPerSoul` + `carryCap` + дверь `refresh_squad_carry` (squad.h): обоз =
  Σ спин по колонке `haulMult` каждой души, пересчёт в каждом месте изменения
  состава. Коммит `85473ecf`.

## ПОСЛЕ РОСТЕРА — ДВЕРЬ СДЕЛКИ НАД КОНТЕЙНЕРАМИ (S25, вердикт есть)
Сторона сделки = контейнер: инвентарь ИЛИ ростер. Игрок видит «Копейщик ур.1
×24» и торгует пачкой. Даром: найм за товары, обмен бойцами, выкуп, дарение.

---

# ХВОСТЫ ЭКОНОМИКИ (наряд большой сессии 2026-09-18/19) — читать после ходов 1-3

**Главное открытое ЧИСЛО:** сколько еды доезжает до города за сезон против его
нужды. Городу на 1200 душ нужно ~38 000 еды/сезон; дань 1/8 от ~26 вассалов даёт
~13 000; остальное — рейсы. До 2026-09-19 обоз врал (считался при спавне), теперь
он честный — **первым делом замер** `balance_run 7,1,12345,999 512` и сравнение с
`/tmp/balance_step4` (прогон с артелью горожан, БЕЗ честного обоза).

1. **ЗАМЕР ПОДВОЗА** и выбор одного из трёх выходов, если не сходится (все —
   данные, не спецпуть): (а) лошади в обоз (сессия 1, инкремент 7); (б) добывающая
   строка в ростере города — ближние поля его (не отмена доктрины «город
   производит»); (в) города рождаются меньше (S25: «потолок эмерджентен —
   дистанция, логистика, комфорт»).
2. **ЛЕСТНИЦА КОМФОРТА** — сегодня в мире 21 инструмент, 1278 кирпичей, 3187
   ткани против 16 млн хлеба. Причина НЕ аллокация: деревня умеет одно ремесло
   (кухня), у городов не было сырья. По S25 рост = сытость × комфорт, поэтому
   население и падало. Мерить после подвоза.
3. **БАЗАЛЬНОЕ ПРОИЗВОДСТВО (Lac operon)** — замысел владельца в CANON S10,
   ОТЛОЖЕН до замера п.2. Если лестница останется в нуле, честная форма — не пол
   производства, а **рулетка по скору вместо argmax** в распределении рук (тот же
   закон, что у поручений артелей), ноль новых констант.
4. **kCommodities → КАТЕГОРИИ** (шаг 3 сессии Г+Д, форма согласована): дань
   `titheOwedGoods[15]`, `MarketSnapshot` (4 бита на товар), `materialMask`
   переезжают на семь категорий; бамп сейва. Ждёт, чтобы не мешать калибровке.
5. **БАРТЕР АНКЕТ** (закон 4 канона, S25 «ЛАВКИ НЕ СУЩЕСТВУЕТ»): свернуть
   `SkillId::Trade` в `tradeDiscountPct` по образцу `moveSpeedPct`, убить
   `bargaining_edge` и второй аргумент подписи, сделать сделку двусторонней,
   заполнить харизму/торговлю в строках анкет мест. Поднимает выручку всех продаж
   мира — baseline обязателен.
6. **РАЗДАЧА ПУЛА ЛУТА** (CANON S10 «СТОКИ И ЦИРКУЛЯЦИЯ» + пять вердиктов
   формы): одна агностичная дверь — спавн грузит инвентарь ИЗ пула, сундуки,
   бандитские ландмарки хранят РЕАЛЬНЫЕ вещи (перезапись по value/kg), суд
   ландмарка = предохранитель от взрыва. Замыкает денежный контур; сегодня
   9-20 М стоимости заперты в числе.
7. **ЧЕКАНКА** — когда товар перестанет утекать, станет видно, нужна ли монета
   NPC-миру вообще (у игрока контракт отдельный).
8. **Мелкие хвосты:** казна в закуп рейса не грузится (названо вслух — если
   прогон покажет голод при живой казне, править); `spire_climb` красный на сиде
   12345 (вердикт «забей»); долг S5 — сумма кластера шахты и урожай парцеллы
   ещё живут в природном поле.

---

## БОЛЬШАЯ ЭКОНОМИЧЕСКАЯ СЕССИЯ (2026-09-18, вечер) — В ХОДУ

По «ПОРЯДКУ РАБОТ» ниже, решения владельца в начале сессии: зазор шагов 1→2
закрыт ПОТОЛКОМ ПОГРУЗКИ на один шаг; kCommodities → категории — ПОСЛЕ
шагов 1–3.

- **Шаг 0 BASELINE**: `balance_run` получил колонки `cloth`/`iron` в
  landmarks-TSV (медианы по местам — иначе расщепление горизонта не видно);
  4 сида (7/1/12345/999) × 512 дней бегут в `/tmp/balance_baseline`.
- **Шаг 1 ПОСТРОЕН** (ctest 105/105, смоуки 65/66 + spire_climb «забей»):
  один закон погрузки `load_cheap_at_home_` (артель И караван: дома дешевле
  базы, сверх сезонного амбара; дефицитное не грузится — прежнее «сверх
  ДНЕВНОЙ нужды» продавало зимний амбар); аукцион Sell ценит ОБА конца
  (дань + продать дешёвое + КУПИТЬ дефицитное — голодный хутор едет за
  хлебом). Казна в закуп НЕ грузится (названо вслух: value-dense мог бы
  увезти амбар; медианная монета мест = 0) — если прогон покажет голод при
  живой казне, отдельной правкой. Свидетели переехали: вендор-фикстура
  держит настоящий излишек (4000 зерна при сезонной нужде 1600),
  аукцион-фикстуре закрыты полки комфорта. Смоук `subworld_recovery` пинил
  КЛЕТКУ макро-сквада вопреки CANON S2 — теперь пинит только ВИЗУАЛ (SUB-1).
  **Дубль-прогон шага 1 — после baseline.**
- **ВЕРДИКТ ЗАПИСАН: пул лута = казна теневого мира** (CANON S10 «СТОКИ И
  ЦИРКУЛЯЦИЯ», дословно): одна агностичная дверь раздачи (спавн бандитов
  грузит инвентарь ИЗ пула, сундуки, ландмарки, потом культисты/дезертиры),
  интенсивность спавна от пула. Строить отдельным треком после шагов.
- **Шаг 2 ПОСТРОЕН** (ctest 105/105, смоуки 65/66 + spire_climb): коридор
  умер + `1<<30` умер + руки ранжируются по стоимости выхода на рабочий день
  (цена пересчитывается после каждого назначения — слиппедж производства) +
  спрос в кривой сезонный одной дверью (`stock_scarcity`). Потолок погрузки
  шага 1 оказался ЗАКРЫТОЙ ФОРМОЙ нового закона (цена < базы ⇔ склад >
  сезонной нужды). НАХОДКА: оценка afford была построена под коридор — без
  него голодный рынок не мог купить ничего; заменена точной границей
  (`max_affordable_lot_`, двоичный поиск по монотонной стоимости лота).
  Стены станции каравана переведены на сезонную нужду. Свидетели переехали:
  price_law («дрип щедрого купца» умер вместе с коридором — теперь это
  закон), caravan_deal, woodcutter (кошельки фикстур подняты до сезонных
  цен — тонкая казна платила вендору собственным хлебом), guard_patrol
  (полки закрыты — тест сторожит страх, не экономику дефицита).
- **BASELINE 512 дней уточнил гипотезы**: ткань копится той же горой, что
  хлеб (медиана города 94–179 тыс.) — дефект производства ОДИН, «в потолок»
  упирается только безсырьевое (железо: медиана 0). Расслоение жёсткое:
  медианная деревня 12 душ.
- **ОТКРЫТО ПОСЛЕ ШАГА 2 (вердикты вечера, строить после чисел дубля):**
  (а) производный спрос не гасится складом выхода — город с полным амбаром
  хлеба «хочет» зерна по полной рекурсии; при сезонной цене это раздувает
  покупной конец рейса до миллионов. Владелец: «надо тогда думать» —
  кандидат: спрос на вход = недопечённый остаток выхода (нетто против
  склада), см. сводку сессии. (б) кап покупного конца рейса = ПОКУПАТЕЛЬНАЯ
  СПОСОБНОСТЬ, вердикт владельца: «у нас бартер-система — покупательная
  способность это суммарная стоимость инвентаря» рейса.
- **ДУБЛЬ ШАГА 1 — 4 СИДА × 512 ДНЕЙ, ПОДТВЕРЖДЁН НА ВСЕХ:**

  ```
  сид   | популяция конца      | Σголодавших душ-дней | медиана деревни | голодавших деревень
  7     | 257 391 → 300 287    | 1 079 889 → 721 618  | 12 → 39         | 1405 → 862
  1     | 229 396 → 266 317    | 1 144 220 → 818 852  | 12 → 36         | 1498 → 930
  12345 | 236 928 → 268 531    | 1 119 599 → 837 376  | 12 → 39         | 1402 → 796
  999   | 227 064 → 274 269    | 1 244 689 → 862 619  | 12 → 38         | 1517 → 958
  ```
  Население +13…+21 %, голод −25…−33 %, сделок +49…+63 %, чеканка +5…+12 %,
  МЕДИАННАЯ ДЕРЕВНЯ ×3 НА ВСЕХ ЧЕТЫРЁХ МИРАХ (12 → 36-39), голодавших
  деревень −39…−43 %. Железо шумит (вверх на 7/12345, вниз на 1/999) —
  траектории расходятся, как и было измерено. Хлебная гора цела (дефект
  ПРОИЗВОДСТВА — мерило шага 2). Дефект РАСПРЕДЕЛЕНИЯ закрыт.
- **Шаг 3 ПОСТРОЕН** (сытость пропорциональна, S25) — и он ВСКРЫЛ смертельный
  гейт: мир умер (76 тыс. душ из 310 тыс., 5 сделок/день), потому что условие
  создания артели требовало сезон хлеба со склада ПОСЛЕ еды, когда склад пуст
  по построению. Прежний мир жил на НЕСПИСАННОМ хлебе голодавших — кромка «всё
  или ничего» была скрытым капиталом ворот. Гейт снят вердиктом владельца
  (мера остаётся, недостача = 1/8 в дезертиры).
- **ЗОНД ПОРУЧЕНИЙ построен** (`balance_run` колонки crewsGather/Sell/other +
  deserters) и сразу дал главное число сессии: **4035 артелей в рейсах сбыта —
  45 сделок в день на весь мир**. Это ЛОВУШКА ЛИКВИДНОСТИ: голодному городу
  первая буханка стоит база × сезонную нужду / 2 (≈185 000), и купить он не
  может ни при каком кошельке.
- **АРТЕЛЬ ГОРОЖАН ЗА ПОКУПКАМИ ПОСТРОЕНА** (вердикт владельца): строка
  ростера города, рынок берётся из ПОРУЧЕНИЯ, феодальное ребро читается в обе
  стороны (городу — ближайший вассал). Снят хардкод «рынок бывает только
  городом». Сделка теперь идёт НА ДЕШЁВОМ КОНЦЕ — закон записан в S10.
- **ДИАГНОЗ ЛЕСТНИЦЫ КОМФОРТА (числа):** в мире 21 инструмент, 1278 кирпичей,
  3187 ткани против 16 млн хлеба. Причина не аллокация: деревня умеет ОДНО
  ремесло (кухня), а у городов не было сырья из-за ловушки. По S25 рост требует
  комфорта — потому население и падало монотонно. Лекарство = артель горожан,
  замер 4 сида × 512 дней в `/tmp/balance_step4` (бежит).
- **Базальное производство (Lac operon)** — замысел владельца записан в S10 и
  ОТЛОЖЕН до этого замера; если лестница останется в нуле, честная форма —
  рулетка по скору вместо argmax (тот же закон, что у поручений), НЕ пол.
- Впереди: числа `/tmp/balance_step4`, потом категории / бартер анкет /
  чеканка; хвост — раздача пула лута (трек стоков).

## ЧТО СЛУЧИЛОСЬ В СЕССИИ В (2026-09-18) — СПЕЦПУТИ СНЯТЫ, СЕЙВ v96

Пять коммитов в main (4cf0f880, 002ccd86, fbfb4e46, ed48b80f, 4648b2f4),
**ctest 105/105 + смоуки 66/66 на каждом**, глазами НЕ проверено.

**ПОСТРОЕНО (вердикты второго суда, дословно в CANON S10/S5/S20.1):**
- **№1 монета = ПРОСТО ТОВАР.** 12 строк каталога (4 фракции × медь 1 /
  серебро 10 / золото 100), состав `{свой металл 1} → 128`. Снесены ворота
  `is_currency_item`, СПИСОК валют (`kCurrencyDefs`,
  `currency_for_faction_id`) и весь кошелёк над ним — список монет это тот
  же спецпуть, просто без `if`. Один закон: платёж по ПЛОТНОСТИ ЦЕННОСТИ
  (`pay_value_dense`/`transfer_value_dense`), покрытие судит
  `inventory_value`, выдача — размена `add_value_in_coins` по mint-колонкам.
- **Выход 128 обоснован МАССОЙ** (вопрос владельца → «128 норм»): монета
  0.01 кг, руда 4 кг ⇒ 32 % металла; свидетель `mint_yield_is_mass_honest`.
- **Медь и золото** — строки полей геологии; лестница редкости («медь чаще,
  серебро среднее, золото самое редкое») двумя колонками: порог = сколько
  клеток, масштаб = насколько жирно гнездо.
- **СЛОЙ РАЗРАБОТКИ ПОСТРОЕН** (`gs.worked`, одно поле; первый жилец —
  счётчик пришвартованных у порта). `gs.shipsAtCell` и его СКАН умерли;
  scar-строки стали полями. **В GameState не осталось ни одного
  unordered_map** — утверждение S5 «последние два хеша» стало правдой.
- **№2** профессии толпы снесены (буквально: «только стражники и пизанты» —
  Merchant и Witch тоже вон); **№4** SettlementHistory снесена; **№9**
  факт земли идёт через `record_deed`; посев капитала с «± четверть
  разброса от мирового сида».

**ДУБЛЬ-ПРОГОН (сид 12345, 256 дней, «до» = 6701e600 отдельным worktree):**
население 178 852 → **211 059**; денежная масса 9.99 М → 9.71 М
(стабильна); сделок 489 → **2 679**; оборот торговли 707 325 →
**59 114 545** (×84 — до сессии торговля УМИРАЛА к концу горизонта);
душ голодало 58 907 → **34 974**; зерно на руках 1.2 М → 2.2 М.

## ПЛАН КАЛИБРОВКИ ОТ ВЛАДЕЛЬЦА (2026-09-18, вечер) — ГОЛОД ГОРНЫХ ХУТОРОВ

Дословно: «деревни же покупают в городе? ну бартером? раз у них голод, что они
не покупают там хлеб? горные ресурсы же дорогие? надо кстати сделать их
побольше тоже, если ща их мало; ну либо подкрутить ещё охоту, чтобы больше
приносила — в общем обсудить, наверняка можно откалибровать».

**НАЙДЕННЫЙ КАПКАН (главный лид, из кода):** рейс сбыта поднимается только при
ИЗЛИШКЕ сверх сезонной нужды (`surplus = have − demand × kDaysPerSeason`) плюс
долг дани. У голодного хутора излишка НЕТ НИКОГДА — значит он не едет в город,
значит не покупает хлеб, хотя под ним лежит дорогая руда, которую надо ещё
сезон копать. Сюзерен при этом есть всегда (деревня рождается под городом), так
что дело не в отсутствии рынка.

**Что обсудить/сделать следующим заходом:**
1. **Право на рейс не от излишка, а от НУЖДЫ**: голодный дом должен ехать
   ПОКУПАТЬ (на что — на руду, на монету казны, на что угодно ценное), а не
   только продавать лишнее. Это тот же закон, что у купца (цена дома против
   цены рынка), просто применённый к решению «ехать ли вообще».
2. **Руды побольше** (слово владельца): текущая калибровка — медь 87k юнитов /
   6.2k клеток, серебро 5k/2.3k, золото 278/278 при выходе чеканки 128; ручки
   в `kDepositGen` (порог = сколько клеток, unitScale = насколько жирно гнездо).
3. **Охота щедрее**: темп 8 против 32 у пашни (колонка `perWorkerDay` строки
   добычи, цена цикла = бар/темп). Крутить числом.
4. Многосидовый дубль-прогон: один сид не доказывает металл — траектории
   расходятся (измерено: железо 0 против 1192 на двух прогонах одного сида).

**ДОБАВЛЕНО СЕССИЕЙ Г+Д (2026-09-18, чтением кода — НЕ прогоном):**

5. **ГИПОТЕЗА «НЕТ ПЕКАРЯ» — ОПРОВЕРГНУТА, не тратить на неё заход.** Деревня
   печёт: её анкета (`characters.h landmark_sheet`) держит cooking 10 против
   minRank 1 у хлебной строки, и `tick_villages_` гоняет тот же
   `econ_produce_day`, что город. Вердикт владельца того же дня: «не
   должно быть хуторов без пекаря, они через крафт-систему всё уметь
   производить должны, просто в деревне население меньше — поэтому менее
   эффективно».

6. **ГЛАВНЫЙ НОВЫЙ ЛИД — ЖЁСТКАЯ КРОМКА БЬЁТ ПО МАЛЫМ СИЛЬНЕЕ ВСЕГО.**
   `econ_consume_season` списывает нужду «целиком или не трогает вовсе»: не
   хватило ОДНОЙ единицы на сезон — склад НЕ трогается, и сыт НИКТО
   (`fed = 0`). Кромка намеренная (так и написано в коде), но она нелинейна по
   размеру места: городу с тысячами душ спрос сглаживает запас, а хутору на 30
   душ нужно 30 × 32 = 960 хлеба одним куском, и промах на единицу даёт
   **полный голод при почти полных закромах** — ровно ту картину, что владелец
   и наблюдал (94 645 душ-дней за 64 дня). Проверяется дёшево: у голодавшего
   хутора посмотреть `count_of(bread)` в день границы — если он около нужды, а
   не около нуля, дело в кромке, а не в торговле.

7. **И то же самое бьёт ВТОРОЙ раз, через КПД.** Закон эффективности
   `log2(популяции)/4` (CANON S10) даёт хутору на 32 души множитель ×1¼, а
   городу на 5488 — ×3: на душу хутор печёт вдвое хуже. Два штрафа за малость
   складываются, и оба сходятся в одной точке — сезонной кромке выше.
   ВОПРОС ВЛАДЕЛЬЦУ при заходе: кромка «всё или ничего» — это про МЕСТО или
   про ДУШУ? Если про душу (накормлено столько, на сколько хватило), хутор
   голодает пропорционально, а не поголовно, и новых систем это не заводит —
   меняется одна ветка.

8. **Проверить ДО торговли: есть ли в горах что печь.** «Пища» — сырьё, и
   растёт она из поля пашни (темп 32) и фауны (темп 8). В горах пашни нет.
   Может оказаться, что хутор голодает не потому, что не купил хлеба, а
   потому, что ему не из чего его испечь — тогда пункты 1-3 лечат симптом.
   Измерять: запас `food` горной деревни против низинной, до и до/после.

### НАЙДЕНО ПРИ ПРОБЕ СЕЗОННОГО ГОРИЗОНТА (2026-09-18, НЕ построено)

Владелец одобрил системную форму («заодно посмотрим… сразу и на деревни, и на
города, и вообще на все ландмарки универсально») и сам назвал риск: «если кто-то
продаст много в город, где этого нет, цена останется высокой и он начнёт абузить
и разорит город — или цена всё равно смотрит положение на складе и корректирует
по дельте, сколько на складе и сколько надо на этот сезон?».

**Ответ по коду:** цена смотрит склад и пересчитывается после КАЖДОЙ партии
(`stock_price(base, have + moved, …)`), поэтому каждая проданная единица дешевеет
для следующей, а равновесие при сезонном горизонте — ровно «склад = сезонная
нужда» (цена по базе, дальше ниже). Абуз самозатухает.

**НО ПРОБА ВСКРЫЛА ДРУГОЕ, И ЭТО БЛОКЕР ФОРМЫ:** при сезонном горизонте почти
любой склад оказывается НИЖЕ сезонной нужды, а коридор кривой сегодня всего
0.25…×4 — то есть мир целиком живёт в НАСЫЩЕНИИ верхнего клампа, где цена
перестаёт реагировать на склад. В насыщении слиппеджа между концами сделки нет,
и «щедрый купец» (покупает 0.9, продаёт 1.2) даёт прибыль на каждом круге —
именно тот абуз, которого владелец опасался, только приходит он от потери
чувствительности, а не от высокой цены. Поймал свидетель `price_law_test`
(«полная полка никогда не даёт прибыли»); вместе с ним покраснел
`caravan_deal_test` (его коридоры пинят ДНЕВНОЙ горизонт).

**ЧТО НУЖНО СДЕЛАТЬ ФОРМЕ, ЧТОБЫ ОНА ЖИЛА:** расширить коридор клампа вместе с
горизонтом (кандидат: симметричный po2-коридор порядка 1/32…×32, то есть тот же
сезон в обе стороны), затем дубль-прогон и перекалибровка (голод, торговля,
денежная масса). Свидетели `price_law_test` и `caravan_deal_test` переезжают
вместе с законом — их корридоры выведены из горизонта, а не назначены.

**Готовый черновик правки** (чтобы не выводить заново): `stock_scarcity` считает
нужду как `demandPerDay × kDaysPerSeason`; в рейсе сбыта правило «излишек сверх
сезонной нужды» снимается, ценность считается двумя концами
(`worthThere − worthHere`), а ЧТО грузить решает «дома дешевле базы».

### ВЕРДИКТ ВЛАДЕЛЬЦА ТОГО ЖЕ ВЕЧЕРА: УСЛОВИЕ РЕЙСА — ХАРДКОД, ПРИВЕСТИ К
### СИСТЕМНОМУ ВИДУ (обсуждалось, НЕ построено)

Дословно: «надо рейс сбыта поднимать не по нужде, а просто — на складе то всяко
что-то есть, это будет заодно универсально»; «сезонная нужда как условие
поднятия рейса торговли — это хардкод: если деревня добывает что угодно, она это
и продаёт, буквально живёт этим; в общем приведи к системному виду».

**Диагноз верен:** «излишек сверх сезонной нужды» — частное правило, торчащее из
общего закона цены. Плюс измеренное следствие: ценность рейса считалась как
«излишек × (база − домашняя цена)», то есть выгода ТОЛЬКО от затоваривания — у
хутора руды мало, домашняя цена высока, выгода ноль, рейс не поднимался, хлеб не
покупался, хутор голодал на дорогой руде.

**ПРЕДЛОЖЕННАЯ СИСТЕМНАЯ ФОРМА (к обсуждению перед кодом):** горизонт кривой
дефицита сделать СЕЗОННЫМ, потому что мир ест раз в сезон (S19.2). Тогда цена
дома говорит всё сама: склад ниже сезонной нужды — дорого (амбар не продают),
выше — дешевеет (излишек едет), а руда, которой дом почти не ест, дешева дома и
дорога в городе с первой единицы. Отдельное условие «излишек» исчезает; остаётся
одна кривая — та же, что у закупки (цена там против цены здесь).

**ЦЕНА:** меняет цены ВСЕХ товаров сразу ⇒ отдельный инкремент со своим
дубль-прогоном и вероятной перекалибровкой (голод, торговля, денежная масса).
Пробная правка (рейс по двум концам, без смены горизонта) уронила
`goal_auction_test` на проверке диверсификации рулетки — то есть свидетель
диверсификации завязан на старую меру и тоже переезжает вместе с законом.

## АРХИТЕКТУРА ЭКОНОМИКИ ЦЕЛИКОМ — СЛОВАМИ ВЛАДЕЛЬЦА (2026-09-18, обсуждение)

Дословно: «у нас есть 1024х1024 клеточный связный мир тор — у него есть рельеф и
климат на нём генерятся поля природных ресурсов руды деревья фауна и тд / потом
генерятся города и деревни соеденинные дорогами у городов и деревень есть
население и они порождают сквады — деревни порождают артели крестьян
универсльные которые могут добывать прирлдные рескры и строит фичи и города
корованы которые торгуют и патрули (крестьяне тоже могут торговать с
родительским городом деревни) — популяция требует товары для роста особенно еду
города и деревни производят товары из ресурсвов по единой системе крафта чем
больше населения тем больше производят по цене по нужде (в игре есть система
стоимости всего) но локальная цена может расти или падать относитьельно
стоимости если на складе нет товара а нужда есть он дорогй — спрос (поэтому
пришедшие корованы или крестьяне будут эмерджентно его продватаь и наоборот чего
много на складе будут покупать потому что оно дешево — все скарсиси хардкоды и
тд надо убрать потому что как я вижу модель что я описал должна исчерпывать весь
замысел — деревни добывают ресурсы немного производят сами товаров и избытки +
дань несут в город родительский (по феод системе) также привести всё к 1/8
никаких десятин нет и в городе дервня может наменять свои что принесла выше дани
на товары ну универсальный бартер».

**СВЕРКА С КОДОМ: из четырнадцати звеньев построено двенадцать.**
Построено: тор и поля (S1/S5), поля ресурсов, дороги, `pool = pop >> labourShift`,
универсальные артели, караваны и патрули, рейс крестьян к родителю (`ai_vendor`),
рост от товаров (`wellbeing = fedFrac × (0.5 + 0.5·comfortFrac)`), одна дверь
крафта (`craft_item`), КПД `log2(pop)/4`, `ItemDef::value`, дань `>> 3` = 1/8 и
для товаров, и для монеты (десятин в коде НЕТ — требование «всё к 1/8» уже
выполнено), бартер по номиналу (S25).
Не построено/расходится: **климат** — функция, а не поле (S19.1);
**производство по нужде** — см. ниже; **цена** — есть, но зажата коридором.

### ДВА ХАРДКОДА, И ЭТО ОДИН ПРОПУСК

1. `econ_day.cpp:230` — `run_recipe(i, 1 << 30, …)`. Pass 0 работает по нужде, и
   то по ДНЕВНОЙ (`if (demand <= have) continue` — склад на один день уже
   останавливает). Pass 1 «честные доли» и pass 2 «остатки» получают лимит
   `1 << 30`: ~300 из 315 городских рабочих делают товар, которого никто не
   просил.
2. `economy.cpp:51` — коридор `0.25 … 4.0` у `stock_scarcity`.

**Проверено специально: `run_recipe_out` НЕ спрашивает цену вообще** — ни
`value`, ни `stock_price`, ни сравнения с ценой труда; только «есть сырьё» и
«есть руки». Поэтому оба хардкода — следствие одного пропуска: цена обязана быть
сигналом «хватит», но производство к ней глухо. Раз сигнал никто не слушает,
цену пришлось зажать коридором — и склад всё равно растёт без предела, потому
что останавливать его нечем.

### НЕДОСТАЮЩЕЕ ЗВЕНО ЗАМЫСЛА (назвать вслух)

**У труда есть цена, и партия бежит только если выход дороже труда.**
Цена труда уже построена (S14.1, `item_labour`, цена цикла в SP) — её просто не
спрашивает `econ_produce_day`. С этим звеном оба хардкода ИСЧЕЗАЮТ, а не
заменяются другими:
- город печёт, пока хлеб дороже труда; склад растёт → цена падает → перестают.
  **Потолок выпечки не нужен — он выводится из цены;**
- цене можно падать сколь угодно низко: это не поломка, это стоп-сигнал.
  **Кламп не нужен;**
- сезон проедает склад → цена растёт → печь возобновляют. Цикл сам.
Это ровно S26: «константа обязана выводиться из свойства мира, а не
назначаться». Сегодня назначены все три: `0.25`, `4.0`, `1 << 30`.

**КАЛИБРОВОЧНАЯ РУЧКА, названная честно:** без клампа пустой склад даёт
`цена = база × (спрос + 1)` — у города на 2520 душ это ×2521. Голодные цены
реалистичны, но игрок сможет их доить. Не блокер; померить в дубль-прогоне.

### ЗАМЕР НА ЧЕТЫРЁХ СИДАХ (2026-09-18, `balance_run`, день 350) — ЭТО ЗАКОН

```
сид   | ГОРОД хлеб  p25 /   медиана /       max | ДЕРЕВНЯ хлеб med | монета med: город/деревня
7     |  974 551 / 1 422 511 /  9 765 366        |               26 |      0 / 0
1     |  752 138 / 1 128 019 / 13 330 265        |               24 |      0 / 0
12345 |  759 606 / 1 319 074 / 11 830 975        |               26 |      0 / 0
999   |  712 988 / 1 219 584 /  9 196 960        |               24 |      0 / 0
```
Медианный город: 1,2–1,4 М хлеба при сезонной нужде 80 640 (пол 2520 душ × 32) —
запас на ~17 сезонов; нижняя четверть городов держит 700 тысяч. Медианная
деревня: два десятка буханок. Отношение ~50 000×. Монета: ноль в медиане во всех
четырёх мирах, у ОБОИХ видов мест. Четыре независимых мира сошлись с точностью
до процентов — сид ни при чём.
(Сиды 1/12345/999 остановлены на днях 357–383 из 512; сравнение ведено на общем
дне 350. Прогон сида 7 полный.)

### СЛЕДСТВИЕ ДЛЯ СКЛАДА

`ItemRef::count` — `std::int32_t`, а `econ_store_hygiene` считает СЛОТЫ, не
объём, и прямым текстом «Plain stacks are never touched». **У склада нет предела
по объёму вообще:** 256 слотов по два миллиарда единиц. Миллион хлеба лежит в
одном слоте и не вызывает ничего. Если производство станет по цене, отдельный
предел склада не понадобится; если нет — понадобится, и это будет второй
назначенный потолок.

### ДВА ДЕФЕКТА, А НЕ ОДИН (важно для порядка работ)

- **Голод деревень = дефект РАСПРЕДЕЛЕНИЯ.** Рейс гейтится излишком (строки
  46–51 выше), голодный хутор не выезжает и не покупает дешёвый городской хлеб.
  Вердикт владельца снять этот гейт уже вынесен.
- **Хлебная гора = дефект ПРОИЗВОДСТВА.** Даже при починенном рейсе деревни не
  могут это выесть: 66 городов × 1,4 М ≈ 92 М хлеба против ~667 тыс. сезонного
  потребления ВСЕХ 1737 деревень — запас на ~138 сезонов (35 лет).
Чинить их надо порознь, иначе дубль-прогон не скажет, что подействовало.

### ГОРИЗОНТ РАСЩЕПЛЯЕТСЯ ПО ТОВАРАМ (снимает противоречие с пробой)

Проба сезонного горизонта видела «мир живёт в насыщении ВЕРХНЕГО клампа»
(строки 114–116), а замер показывает города в насыщении НИЖНЕГО (1,4 М против
нужды 80 640). Оба верны для РАЗНЫХ строк каталога: хлеб печётся без предела и
топит пол, остальное (ткань, кирпич, инструмент, руда) производится мало и
упирается в потолок. Мир сидит в обоих насыщениях одновременно.
**Следствие для формы:** симметричный коридор 1/32…×32 расширит оба конца, но
асимметрию не уберёт — хлеб просто уедет в новое дно. Коридор имеет смысл
двигать только вместе со звеном «производство по цене».
ОГОВОРКА: в TSV есть только `bread`/`grain`/`coin`; про ткань и кирпич это
гипотеза, объясняющая расхождение, а не замер. Проверяется парой колонок в
`balance_run`.

### ЛИД: «МИР СТОИТ У ПЕЧИ» (дёшево проверить, объясняет открытый хвост)

В pass 1/2 уходят ~300 из 315 городских рабочих. Те же руки не копают руду. В
открытых хвостах (ниже, п. 3): «металл добывается, но мало и шумно — железо 1192,
серебро 12 за 64 дня при 1860 местах». Возможно, это НЕ отдельная проблема: мир
не бедный на металл, мир весь стоит у печи. Проверка — распределение рабочих по
рецептам в городе за день.

### ИНВЕНТАРЬ КЛАМПОВ: ИХ ПЯТЬ, ТРЁХ РАЗНЫХ ВИДОВ (снимать — ОДИН)

Вердикт владельца «клампы наверное все надо снять» уточнён замером: под «скарсити
хардкодами» лежат три РАЗНЫЕ вещи, и снос всех пяти сломал бы торговлю.

| # | где | что зажимает | снимать |
|---|---|---|---|
| 1 | `economy.cpp:51` | `scarcity` в `[0.25 … 4.0]` | **ДА** |
| 2 | `economy.cpp:35` | спред лавки `× 0.7` | нет |
| 3 | `economy.cpp:29,35` | бонус харизмы `max(0.5)` / `min(1.5)` | нет |
| 4 | `econ_day.cpp:231` | `1 << 30` как «нужда» pass 1/2 | **ДА** |
| 5 | `econ_day.cpp:120` | партий за день ≤ `1 << 20` | нет |

- **Вид А — кламп ДЕФИЦИТА (снять):** №1 спорит с замыслом прямо («цена растёт и
  падает как угодно»). №4 формально не кламп, а его зеркало: врёт производству,
  что нужда бесконечна. Оба — назначенные числа, оба уходят вместе со звеном
  «производство по цене труда».
- **Вид Б — ТРЕНИЕ ЛАВКИ (оставить):** `× 0.7` это не про дефицит, а про то, что
  торговец берёт себе разницу (купил за 100, продал за 70). Снять — купля и
  продажа сравняются, товар можно будет крутить туда-сюда бесплатно. **Спред —
  то, что делает арбитраж трудом, а не кнопкой.** К «скарсити» не относится.
- **Вид В — ПРЕДОХРАНИТЕЛЬ (оставить):** `1 << 20` защищает от переполнения int.

**СНЯТИЕ №1 ЧИНИТ АБУЗ, НАЙДЕННЫЙ ПРОБОЙ (строки 118–119 выше).** Слиппедж
(цена по ПОСЛЕсделочному складу) убивает арбитраж всегда — кроме насыщения, где
цена не двигается. Значит кламп не защищает от «щедрого купца», а СОЗДАЁТ его;
после снятия `price_law_test` («полная полка никогда не даёт прибыли») станет
правдой сам собой, без отдельной починки.

**РАДИУС:** снятие №1 меняет ВСЕ цены в игре сразу; `price_law_test` и
`caravan_deal_test` переезжают вместе с законом (уже измерено пробой).

### ЕДИНАЯ СИСТЕМА БАРТЕРА: №2 И №3 СЛИВАЮТСЯ В СРАВНЕНИЕ АНКЕТ (канон S25)

Вердикт владельца 2026-09-18 — дословно в CANON S25 («лавки как таковой нет есть
два макросущности… просто сравнение харизм двух и у кого больше тот и
наценивает»). Отменяет строки №2 и №3 таблицы выше как ОТДЕЛЬНЫЕ числа.

**ОПОРА В КОДЕ (проверено):**
- `CharacterSheet { attributes; skills; levelData; }` — у ландмарка харизма уже
  есть КАК ПОЛЕ (`characters.h kLandmarkSheets`). Заполнены только ремёсла
  (`city.skills[Cooking]=30` и т.д.); харизма и `SkillId::Trade` — нули.
  **Заполнить = данные, не код.**
- `npc_ai.cpp:3364` уже называет спред «the house margin», которую скилл
  торговца «claw back», — дом мыслится контрагентом, просто без анкеты.
- В цену сегодня идёт харизма ОДНОЙ стороны: `trade_price(base, charisma,
  bargaining, …)` — четыре вызова в `overlays.cpp`, пять в `npc_ai.cpp`.

**РАБОТА:** подпись цены становится двусторонней (анкета А против анкеты Б),
`0.7` и `max(0.5)`/`min(1.5)` уходят вместе с ней; строки анкет городов и
деревень получают харизму и торговлю.

**РИСКИ, НАЗВАННЫЕ ДО КОДА:**
- равные стороны → наценка 0. Это НЕ поломка (прибыль из разницы цен мест,
  арбитраж гасит слиппедж), но при маленькой сделке в огромном складе слиппедж
  ≈ 0 и прокрутка становится бесплатной — не выгодной, просто бесплатной.
  Померить отдельно.
- **выручка всех продаж мира разом вверх** (сегодня продавец получает долю от
  цены, при равных анкетах будет полная). Денежная масса и оборот поедут —
  дубль-прогон обязателен, сравнивать с baseline ДО этой правки.
- **не завести кламп заново:** если у наценки появятся пол и потолок, мы
  вернулись туда же под другим именем. Граница выводится из анкет или её нет.

**ВОПРОС ЗАКРЫТ ВЛАДЕЛЬЦЕМ (2026-09-18):** наценка читает **финальное
производное** торговой силы — «уже после атрибута харизмы и скила эффективного
всего… она берёт финалочку». Не пара {атрибут, скилл}, а ОДНО посчитанное число,
то же, что на листе. Тогда спеллы, артефакты, аффиксы и перки входят в цену
бесплатно, без строки в торговом коде. Канон — S25.

**ЧТО ЭТО ЗНАЧИТ ДЛЯ РАБОТЫ — МЕНЬШЕ, ЧЕМ КАЖЕТСЯ: ДВЕРЬ УЖЕ ЕСТЬ.**
Производный блок листа (`attributes.h:412` `tradeDiscountPct`, считается там же,
где `moveSpeedPct`) — это и есть «финалочка». И в нём уже живёт нужный ОБРАЗЕЦ:
```
d.moveSpeedPct = quickness_pct(Spd) * skill_mult_pct(s, Athletics) / 100;
d.tradeDiscountPct = cha_trade_discount_pct(Cha);      // ← скилл НЕ сложен
```
Скорость сворачивает атрибут И скилл в одно производное; торговля — только
атрибут, а `SkillId::Trade` применяется ОТДЕЛЬНО и позже
(`economy.cpp:21 bargaining_edge`). То есть сегодня торговая сила считается в
ДВУХ местах — ровно дефект S26, который вердикт и закрывает.

Порядок: (1) свернуть Trade в `tradeDiscountPct` по образцу `moveSpeedPct` —
одна строка, второй закон не заводится; (2) `bargaining_edge` и второй аргумент
подписи умирают; (3) сделку сделать двусторонней (анкета А против анкеты Б);
(4) заполнить харизму и Trade в строках анкет городов/деревень — ДАННЫЕ.
Спеллы, артефакты и аффиксы после этого попадают в цену бесплатно: они меняют
производный блок, а сделка спрашивает только его.

### ЧЕГО ЭТА ПОЧИНКА НЕ КАСАЕТСЯ (не перепутать при прогоне)

Найдена причина ДВУХ симптомов — хлебной горы и мёртвой цены. Отдельными
дефектами остаются и этой правкой НЕ лечатся:
- **п.6 жёсткая кромка «всё или ничего»** — вопрос «про МЕСТО или про ДУШУ?» без
  ответа;
- **п.8 в горах нет пашни** — хутор может голодать не от торговли, а оттого, что
  печь не из чего;
- **рейс от излишка** — дефект распределения;
- **чеканка / решение «мир бартерный»**.
И главное: **прогон не решает, прогон меряет.** Он скажет, подействовала ли
починка, — поэтому шаги ниже разведены по одному.

### ФИНАЛЬНАЯ МОДЕЛЬ СОГЛАСОВАНА (2026-09-18) — ПЯТЬ ЗАКОНОВ, КАНОН S10/S25

Разбор закрыт вердиктами владельца; `?37` закрыт. Канон: «ФИНАЛЬНАЯ ФОРМА: ПЯТЬ
ЗАКОНОВ» в S10. Коротко, чтобы не листать:

1. **ЦЕНА** — `value × (спрос+1)/(запас+1)`, коридора нет, спрос СЕЗОННЫЙ, пол
   цены 1 = закон «ничто не бесплатно».
2. **ПРОИЗВОДСТВО** — руки в верхний рецепт по `стоимость выхода / рабочий день`.
3. **РОСТ** — `сытость × (0.5 + 0.5·комфорт)`, сытость ПРОПОРЦИОНАЛЬНА.
4. **ОБМЕН** — наценка от разницы финальных производных торговой силы.
5. **ДАНЬ** — 1/8, построено.

**ГЛАВНОЕ ОТКРЫТИЕ РАЗБОРА: ТРИ ПРАВКИ ОКАЗАЛИСЬ ОДНОЙ.** Снятие коридора,
ранжирование рук и сезонный горизонт спроса порознь НЕ РАБОТАЮТ:
- ранжирование без сезонного горизонта убивает хутор: склад в течение сезона не
  убывает (S19.2 списывает одним куском), при дневной мерке 400 хлеба у 30 душ
  читаются как «завались» (`31/401 = 0.077`), руки уходят в шахту, а нужда была
  960 — сигнал опаздывает ровно на сезон;
- сезонный горизонт без снятия коридора упирается в насыщение (уже измерено
  пробой, строки 115–123 выше);
- снятие коридора без ранжирования не останавливает выпечку.
**Делать одним инкрементом, дубль-прогон один — но с предварительным baseline.**

**ОТВЕРГНУТО, НЕ ПРЕДЛАГАТЬ СНОВА:** «партия бежит, если выход дороже труда» с
ценой труда в еде. Для товара, которым платят жалованье, цена сокращается с обеих
сторон (`yield × p > дней × паёк × p` ⇒ `yield > дней × паёк`) — условие
физическое, от склада не зависит, хлебная гора им НЕ лечится. Подробности в
каноне S10.

**ЧЕГО ЦЕНА НЕ ТРЕБУЕТ ДОПИСЫВАТЬ** (проверено в разборе): голод она понимает
сама — колонка спроса даёт еде множитель на порядок больше любого другого товара
(1 на жителя-день против 1 на 32…512); складом самого дорогого мир не забьётся —
чем мельче спрос строки, тем быстрее обваливается её цена при накоплении.

### ПОРЯДОК РАБОТ (каждый шаг — свой дубль-прогон, одна вещь за раз)

0. **BASELINE** от текущего HEAD: 4 сида × 512 дней, колонки `bread/grain/coin`
   плюс `cloth`/`iron` (последние две добавить — без них расщепление горизонта
   по товарам остаётся гипотезой). Всё дальнейшее сравнивать с ним.
1. **Рейс от нужды, не от излишка** — вердикт владельца уже есть; лечит голод
   деревень (дефект РАСПРЕДЕЛЕНИЯ). Свой дубль-прогон.
2. **ОДИН ИНКРЕМЕНТ: коридор снят + ранжирование рук + сезонный горизонт** —
   лечит хлебную гору и мёртвую цену (дефект ПРОИЗВОДСТВА). Порознь не работают,
   см. выше. Свидетели `price_law_test` и `caravan_deal_test` переезжают вместе
   с законом. Свой дубль-прогон; ждать ПИЛУ, а не ровную линию.
3. **Сытость пропорциональна** — закрывает заодно вопрос кромки «место или
   душа» (п. 6 выше) и голод горных хуторов при полных закромах. Свой прогон:
   ожидается, что «богато добывает → хорошо растёт» впервые станет измеримым.
4. **Единая система бартера** (двусторонние анкеты) — отдельным заходом, блок
   выше. Двигает выручку всех продаж мира: baseline обязателен.
5. **Чеканка / решение «мир бартерный»** — когда товар перестанет утекать в
   никуда, станет видно, нужна ли монета NPC-миру вообще (у игрока контракт
   отдельный: продать лут за золото и купить дорогую снарягу).

**ОТКРЫТО, БЕЗ ВЕРДИКТА — РАССЛОЕНИЕ МИРА.** Производство ∝ `pop·log(pop)`
(пул × КПД), потребление ∝ `pop` ⇒ излишек НА ДУШУ растёт без предела: больше
место → больше товаров на голову → быстрее рост → ещё больше место.
Положительной обратной связи ничто не демпфирует, и замер это подтверждает —
деревни p25=9 / медиана 12 / p75=16, города медиана 2520 / max 11 995. Это не
разброс, а РАССЛОЕНИЕ: середины нет, хотя замысел просит «богатые и бедные
деревни». Кандидат в демпферы — округа (место кормится с ограниченного радиуса);
проверить, затухает ли сбор по расстоянию, или город собирает со всего мира.

## ХВОСТЫ ВЕЧЕРА 2026-09-18 (сессия В+, коммиты 39193ab6, 769f0799)

Построено по вердиктам того же дня: КАРТА ОКРУГИ (место описывает свою
нав-округу раз в сезон, артель читает — поиска жилы в тике НЕТ), один закон
закупки караванов (белый список снесён; цена дома против цены рынка, выгода
на килограмм), ПИЩА одной строкой с двумя источниками (пашня 32 / охота 8,
различие через цену цикла SP), горное вето снесено (мест 1612 → 1860),
нулевого спроса больше нет (пол выведен из слабейшей нужды).

**ОТКРЫТО, с числами:**
1. **spire_climb КРАСНЫЙ** (сид 12345): шпиль на высоте — крыша ниже земли
   входа (dz = −25.6, onRoof = 0). Вердикт владельца: «забей, им потом
   отдельно займёмся». По памяти это тот же дефект, что был открыт на сиде 7,
   — снятие вето лишь вскрыло его на дефолтном сиде.
2. **Хронический голод 94 645 душ-дней за 64 дня** (было ~230/день до
   сессии, стало ~1500/день): горные хутора живут на грани — пашни нет,
   охота кормит хуже. Бисект: пол спроса НЕ виноват. Варианты к обсуждению:
   темп охоты выше, или мест на скале меньше (арабельный терм жёстче), или
   хутор рождается меньшим.
3. **Металл добывается, но мало и шумно**: железо 1192, серебро 12 за 64 дня
   при 1860 местах. Один сид ничего не доказывает — траектории расходятся;
   нужен многосидовый прогон.
4. Чеканка ожила (0 → 384), но денежная масса всё ещё утекает в пул лута
   (раздачи из пула нет — хвост сессии сезонов).

## ХВОСТ №1 НА СЛЕДУЮЩУЮ СЕССИЮ: ДВОР НЕ ЧЕКАНИТ ВООБЩЕ

`minted = 0` за 256 дней И ДО, И ПОСЛЕ сессии — это не регрессия, а
непостроенное звено: у городов на складе нет монетного металла, потому что
металлы не входят в лестницу нужд и караваны их не покупают. Пока это так,
денежная масса мира только УБЫВАЕТ (жалованье сгорает в пул лута: 9.2 М в
пуле к 256-му дню против 0.5 М в живом обороте). Вопросы владельцу ДО кода:
покупает ли город металл как нужду (строка лестницы), или артель везёт его с
шахты собственным поручением, или чеканка — отдельный спрос «двор хочет
металла на N дней вперёд».

## ОСТАЛЬНЫЕ ХВОСТЫ
- Пул лута растёт без РАЗДАЧИ (хвост сессии сезонов): 9.2 М стоимости
  заперты в числе, из которого мир ничего не выдаёт.
- Остаток долга S5: сумма кластера шахты и урожай парцеллы ещё живут в
  природном поле («фертильность минус шрам») — перенос ЗАКОНОВ в уже готовый
  слой разработки.
- `ai_mage_hunt` остался без амбиентной добычи (принятая цена сноса толпы).
- Нарезка `canon-audit.md`: **Г и Д ЗАКРЫТЫ 2026-09-18** (сессия Г+Д, ниже).
  Остались **Е** (РПГ: перки / NPC multPct=100 / спелл-множители tierPct и
  scalingPower / расклейка Missile) и **Ж** (навигация: nav_step «три
  чтения», допекатель цены моста). Хвост wrapf из Ж закрыт сессией Г+Д.

---

## СЕССИЯ Г+Д (2026-09-18, вечер) — ЗАКРЫТА. 21 КОММИТ, 105/105 НА КАЖДОМ

Механический наряд (дрейф-константы + комментарии-лжецы) выполнен ЦЕЛИКОМ:
**Г1-Г12 и Д1-Д12**. Д1 и Д8 оказались уже закрыты сессиями В и А. Г2 закрыт
вердиктом владельца тем же вечером. Реестр помечен в `canon-audit.md`.

Сверх наряда, по вердиктам владельца того же вечера:
- **CANON S2 — НЕПРЕРЫВНАЯ МАКРОСИМУЛЯЦИЯ КАК ОСНОВА И ИСТИНА ИГРЫ.** Записан
  дословно, с эталонным сценарием (сквад не ждёт игрока под землёй, уходит
  своей клеткой, может быть ВСТРЕЧЕН в субмире, убитый там — исчезает
  наверху) и с поимённым списком того, что закон запрещает. Повод: в коде
  нашлись ЧЕТЫРЕ копии прямо противоположной лжи — канон об этом молчал.
- **AGENTS правило 9 — ЗАКОН ЯВНОГО СМЫСЛА** («во всех строках должен быть
  заложен явный смысл, который ведёт к каноническим системам — это позволит
  избежать сраний»). Под ним записаны четыре класса дефектов, которые из его
  нарушения и выросли.
- **АНКЕТА ЕСТЬ У ВСЕГО В МАКРОМИРЕ.** Сквады это уже умели (`sheet_of`:
  владеемый лист у именованного, генерик-бросок у транзитного) — не умели
  МЕСТА. Заведены анкеты родов (`characters.h kLandmarkSheets`, constexpr,
  одна на род, в сейв НЕ едет).
- **Пять ремёсел** строками таблицы скиллов (Cooking/Blacksmith/Tailoring/
  Masonry/Alchemy), **`EconSite` снесён**: рецепт называет ремесло и ранг,
  место отвечает анкетой. Деревня не чеканит не потому, что деревня, а
  потому что её кузнечный ранг ноль — и его можно вырастить. Зелья варят.
- **КАТЕГОРИЯ ВЕЩИ — единственный словарь «что это такое»**: заведена
  `ItemType::Goods`, восемь товаров и двенадцать монет ушли из `Misc` (в
  «прочем» остался один честный `misc_gem`); **`CommodityTier` умер** —
  все три его читателя спрашивают категорию. Мёртвая колонка `econSite`
  реестра ландмарков снесена следом.

### ШАГ 3 ЭТОЙ РАБОТЫ — НЕ СДЕЛАН, ДЕЛАТЬ БОЛЬШОЙ ЭКОНОМИЧЕСКОЙ СЕССИЕЙ

Вердикт владельца: «вырежи товарное, убери товарные строки — у нас теперь
просто система итемов единая и точка», и отдельно: «бамп сейва вообще не
проблема, мне пох на версии сейвов».

От `kCommodities` осталась РОВНО ОДНА работа — **компактное ординальное
пространство**, и она держит три места:
1. `state.h` `titheOwedGoods[15]` и `titheAvgGoods[15]` — **на каждом
   ландмарке, едут в сейв**. Дань считается по-товарно.
2. `agent_memory.h` `MarketSnapshot` — **по 4 бита класса запаса на товар**,
   четырнадцать товаров в семи байтах из восьми; отсюда и
   `static_assert(kCommodityCount <= 16)`. Это НЕ маска «видел/не видел».
3. `commodity.h` `materialMask` — битовое пространство сырьевых строк.

**ФОРМА (согласована с владельцем, не построена):** всё три переезжают на
КАТЕГОРИИ. Категорий семь — нибблы влезают в 4 байта вместо семи, и ≤16
перестаёт быть ограничением: караван помнит «тут густо товарами, пусто
ресурсами», а не 132 предмета поимённо. Дань становится по-категорийной.

**ЦЕНА, НАЗВАННАЯ ВСЛУХ:** бамп `kSaveVersion` (владелец разрешил), плюс
переписываются дань (`world_tick assess_tithe_`/`deliver`), `EconFact.commodity`,
память агента и процедурные квесты. Каталог — 132 строки против 15 товарных,
так что по-предметная дань стоила бы 1056 байт на ландмарк (~34 МБ на 32k
мест) — по DOD-закону допустимо, но по-категорийная честнее ОТВЕЧАЕТ на
вопрос «сколько ты должен сюзерену».

**ВАЖНО ДЛЯ ТОГО ЖЕ ЗАХОДА — цена, уже заплаченная:** веса ремёсел у всех 52
ролей `kRoleWeights` — НУЛИ, то есть **ни один НПЦ ремесла не выучит**.
Местам это не мешает (их анкета авторская), но НПЦ-ремесленников не будет до
раздачи весов, а раздача пересоберёт генерик-лист КАЖДОГО транзитного сквада
(там сидит Foraging, который кормит отряды) — это отдельное решение с
дубль-прогоном, НЕ «заодно».

**И ГРАБЛЯ, СТОЯЩАЯ ОТДЕЛЬНОГО ВНИМАНИЯ:** комментарий у
`kRoleWeights.skill[SkillId::Count]` клянётся, что добавление скилла заставит
компилятор спросить КАЖДУЮ роль («a compile error is the right way to ask that
question, and a silent zero is the wrong one»). **Для АППЕНДА это неправда** —
проверено: пять скиллов добавлены, компилятор промолчал, brace-elision тихо
занулила. Сторож ловит только вставку в СЕРЕДИНУ. Либо строить настоящий
сторож, либо снять обещание.

---

## ИСТОРИЯ: ЧТО СЛУЧИЛОСЬ В СЕССИИ Б (2026-09-17/18) — СЕЗОНЫ НА ВЕКА

Гайдед-закладка системы сезонов. Все вердикты владельца — в CANON S19.1/S19.2
+ S10/S14.1 ДОСЛОВНО; реестр — canon-audit.md (вопросы 2/6/7 → построено,
нарезка Б → построено). Итог: **ctest 105/105, смоуки 66/66, сейв v95.**

**ПОСТРОЕНО (S19.2 — единое окно мира):**
- Все балансы — в день ГРАНИЦЫ сезона (`season_boundary`, day%32==1; размазка
  по ординалу умерла): еда ландмарков (`econ_consume_season` — сезон нужды
  ЦЕЛИКОМ или не списывается; благополучие сезона = байт на ландмарке),
  гарнизоны (хлеб+жалованье ×32, обе нужды, 1/8 один раз), сквады и игрок
  одной дверью (`squad_season_window`, суд по строке КАЖДОЙ души — гейт
  лидера/Adventurer.upkeep=0 мёртв), дань — на границе. Посевной амбар =
  СЕЗОН хлеба (static_assert).
- Сезонная ротация: артели БЕССРОЧНЫ (дом → пере-аукцион дня → новый рейс);
  суд границы = ДВА РЕГУЛЯТОРА (строки×гейты = сколько сквадов, пул = какого
  размера; база пула = население + души СТОЯЩИХ дома артелей): создать
  недостающий (условие создания = сезон содержания), распустить лишний без
  строки, привести составы (добор/ссадка — только дома; в поле состав
  неприкосновенен). Провиант-на-рейс остался только вылазкам гарнизона.
- Крестьяне работают ЗА ЕДУ (upkeep=0 колонкой); ОПЛАТА СТОИМОСТЬЮ
  (`pay_value_dense`: плотность value/kg, монеты выигрывают арифметикой
  веса, пустая казна платит натурой, сгорание в пул лута).
- Один закон нуля SP: `exhaustion_bite = долг² / 32` (крутилка), крафт и
  валка кусают ПРИ ТРАТЕ; «отказ ног» — политика ИИ, не второй закон.
- Излишек рейса сбыта — сверх СЕЗОННОЙ нужды (иначе деревня продавала бы
  свой зимний амбар).

**ДУБЛЬ-ПРОГОН 1а+1б+1в (4 сида × 256 дней):** мир переживает зиму с
запасом — Σголодающих 11M → 0.6M (−95%), популяция конца +22..25%,
aliveSettlements без потерь, торговля к концу ×2-3 (348→751/день), прогон
быстрее (1565→1152 с/сид). Прогон инкремента составов — /tmp/balance_after2.
Глазами НЕ проверено.

## СЛЕДУЮЩАЯ СЕССИЯ — ИНКРЕМЕНТ 2 ТРЕКА: КЛИМАТ-ПОЛЕ (CANON S19.1)

Спроектировано вердиктами, НЕ построено:
1. **Температура — ХРАНИМОЕ скалярное поле** int8 на клетку («можно будет
   потом кучу бесплатных крутых механик»); перепекается на границе сезона,
   в сейв НЕ едет (восстановимо из генезиса + дня). Осцилляция = гладкая
   годовая волна × вес широты (у полюсов качает, у экватора мягко), БЕЗ
   if-по-сезону. Существующие читатели ступенчатого season_temp_offset
   (листва субмира) перевести на волну.
2. **Живая фертильность = f(T, M)** — её читает закон роста (`growthAt`):
   зимой пашня глохнет САМА, аукцион сам гонит руки в шахты/лес (ноль нового
   ИИ). Генезисная фертильность остаётся якорем РАЗМЕЩЕНИЯ (среднегодовая
   того же закона, не вторая правда). Влажность v1 статична (отказ вслух).
3. **ДУБЛЬ-ПРОГОН — ГЛАВНЫЙ ГЕЙТ:** мир обязан пережить НАСТОЯЩУЮ зиму
   (рост полей глохнет!) — осенняя жатва + зимнее проедание склада; склады
   еды не в ноль структурно, смерть деревни только с фактом, масса
   стабильна, торговля жива. Сравнивать с /tmp/balance_after2 (или свежим
   baseline от HEAD).
4. Потом: **снег** (поле С ПАМЯТЬЮ, сейв-бамп) + **лёд** (эмерджентен от T<0,
   субмир: та же плоскость воды + шумовое покрытие, льдины у нуля) +
   **макро-визуал** (снежные клетки + tintRGB палитры); граница — законное
   место перепёка нава подо льдом («алгосы сложнее O(N) бесплатно»).

**ХВОСТЫ-НАХОДКИ прогона (кандидаты на свои заходы):**
- **Пул лута пухнет 2M → 9-10M за 2 года** (жалованье стоимостью жжёт со
  всех окон, а РАЗДАЧИ ПУЛА НЕТ — canon-audit блок 2: «вливы есть, раздачи
  НЕТ ни одной»). Трек «выход пула в руины/данжи/дропы» стал материальным.
- **Чеканка просела** (Σminted сид 1: 1.26M → 0.67M; сид 7 → 0): сезонные
  амбары сделали sell-руны реже — серебро реже едет в город. Крутилка/зонд.
- coinLandmarks сид 1 упал 1.2M → 0.56M — жалованье выжигает монету
  ландмарков; смотреть вместе с раздачей пула.

**Отказы v1, названные вслух (не строить без вердикта):** погода-поле
(осадки/ветер/циклоны/метель), переменная влажность, сезонные ростеры
спавна/миграции фауны, посев-жатва-машинерия.

Метод: вердикты дословно; варианты с ценой ДО кода; ctest+смоуки вместе на
-O3; дубль-прогон обязателен; git status перед git add.

---

# ХВОСТЫ СЕССИИ Е (РПГ И БОЙ) — закрыта 2026-09-19, 7 коммитов

Все семь пунктов наряда построены (Е1 перк-заглушка v100, Е2 честный урон
НПЦ, Е3 сила спелла = его кубы, Е4 расклейка каста и выстрела, Е5 броневые
скиллы, Е6 прицел 3D, Е7 пошаговый режим P). **Глазами принят ТОЛЬКО
пошаговый режим** («отлично работает»). Детали — CANON S13/S14/S15,
canon-audit блок 3, память `rpg-session-e-track-2026-09-19`.

**ОТКРЫТО, по убыванию важности:**

1. **ДОЛГ КОНТЕНТА: у школы Void нет спелла с уроном.** Лич (чей тип урона
   Void) временно кастует Arcane-луч `energy_beam`. Это ненаписанная строка
   реестра (S15 «школа = набор спеллов»), а не дыра закона. Тот же вопрос у
   ведьмы: она фиолетовая/тёмная, а кастует `magic_bolt` (Arcane).
2. **Сдвиг силы кастующих НЕ ЗАМЕРЕН В БОЮ.** Кубы теперь берутся из спелла
   (вердикт владельца), и ряды сдвинулись: ведьма 18→12 (−33%), культист
   10.5→12 (+14%), дракон 31.5→36 (+14%), лич 26→28 (+8%), сорка 22→22 (0%).
   Числа арифметические; как это играется — не смотрели.
3. ~~**Е7(в)**~~ — ✅ ЗАКРЫТО 2026-09-20 вердиктом и постройкой: зелья,
   интеракции и добыча платят тот же гейт, оговорка S14 «вне гейта» умерла.
4. **Смоук-сюита ФЛАКУЕТ** — work_vector §11.4 (заведено этой сессией).
   Постоянный красный один: `spire_climb` (§11.1, отложен владельцем).
5. ~~**§11.2 `subworld_recovery`**~~ — ✅ ПОЧИНЕН 2026-09-19 тем же днём:
   утверждение переписано на закон ЕДИНИЦ (визуал не уезжает от своей
   клетки), медленное мышление макромира легально, негативный контроль
   встроен, три сида зелёные. Строка ушла из реестра §11.
6. **Перк-маска никем не ЧИТАЕТСЯ** (по замыслу: графа нет). Когда придёт
   граф — узлы льют строки в `BonusTotals` через существующую дверь
   `squad_bonuses`, а не через новый код.
7. **Сейв: этот трек занял v100.** Эконом-треку — v101.

## ✅ ЕДИНАЯ СИСТЕМА СПОСОБНОСТЕЙ — ПОСТРОЕНА 2026-09-20 (наряд закрыт)
Дверь `sub/ability.h`, колонки длины у предметов/интеракций/добычи, свидетель
в `turn_based_cycle` (в застое рубка купила 93 шага, повторная отказала).
Подробности — CANON S13. Наряд ниже сохранён как ПАМЯТЬ о том, что строили.

### (исторический наряд)

Канон записан дословно — CANON S13, блок «В СУБМИРЕ ВСЁ — СПОСОБНОСТЬ, И У
ВСЕГО ЕСТЬ РЕКАВЕРИ». Форма уже вердиктована, спрашивать заново НЕ НАДО:

1. **ОДНА ДВЕРЬ над своими таблицами.** Никакого переезда данных: спелл-ряд,
   оружейный ряд и ряд предмета остаются на месте, каждый отдаёт
   `Ability{recovery, цена, эффект}`. Единственная дверь применения в субмире
   проводит любое действие через `Combat.recoverySteps`.
2. **В v1 через гейт: зелья/еда (`use_item`), интеракции (`interact`),
   добыча (`harvest_action`).** Атака и каст уже там.
3. **Длина — колонка строки через `recovery_steps`** (база в секундах ÷
   Spd-асимптота ÷ генерик домена). 0 — честный частный случай.

**Точки кода:** `use_item` (macro/items + ui/overlays «Use»), `interact` и
`harvest_action` (sub/engine), гейт `ecs::Combat::recoverySteps`, дверь
`recovery_steps` (macro/attributes.h). Колонка recovery нужна у `ItemDef` и
у строк интеракций/добычи.

**Свидетель:** в пошаговом режиме застывший мир НЕ должен позволять пить
зелья и обыскивать бесконечно — каждое действие обязано купить миру свою
длину (расширить `turn_based_cycle`, где сейчас это проверено только для
удара). Плюс негативный контроль на ноль: действие с recovery 0 мир не
двигает и это законно.

**ОСТОРОЖНО (грабля этой сессии):** `Missile`-колонка однажды уже ЗНАЧИЛА
не то, что называла. Проверить, что «способность» не станет вторым словом
для того, что уже называет `Delivery`/`attackKind`.
