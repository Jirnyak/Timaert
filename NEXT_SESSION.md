# Промт следующей сессии — ПРЕДМЕТНАЯ СИСТЕМА (ДЕМО-БЛОКЕР)

Обновлён 2026-09-07 (вечер). Прочитай AGENTS.md; режим гайдед — код по
одобрению, вердикты в CANON.md. Стадия: ЦЕЛЕВОЙ БАГФИКС/ПОЛИШ перед контентом
демо (трек = memory bugfix-prerelease-track). Сценарий демо и имя — финальные
(см. CANON/release.md).

## ✅ СДЕЛАНО 2026-09-07 — дев-консоль = единая тест-система (весь план §1)

Машинерия была (app/debug_console.h, реестр без свитча); добиты недостающие
столы тем же паттерном «таблица печатает свои id, команда пишет через
СУЩЕСТВУЮЩУЮ дверь» (полный справочник: debug.md §7.-1):

1. `skills`/`skill <key|all> <rank>`, `attrs`/`attr <key|all> <score>` —
   запись в БАЗОВЫЙ лист с клампами закона (ранг 0..100, ноль = невыучен —
   эмерджентно, по вердикту «не громоздить систем»; атрибут 1..255, пол =
   единица рождения), рекомпут максим сайтом траты поинтов И ТОЛЬКО при
   реальном изменении (гейт «что-то изменилось»).
2. `loots`/`loot <profileId> [rolls]` — броски через единый лут-реестр
   (roll_loot_profile — та же дверь, что смерть/сундук) в сумку на уровне
   игрока; в items.h добавлен принтер реестра loot_profile_count/id.
3. `gear`/`equip <itemId> [cell]`/`unequip <cell>` — зеркало UI-вкладки
   (закон сохранения: refusal, not drop); НОВАЯ дверь anatomy `equip_at`
   (вердикт: явная клетка = два кольца на разные пальцы; общий хелпер
   try_equip_cell с equip, старый путь бит-в-бит).
4. `sheet` — база→эффективный, дериваты, ТЕМПЫ через дверь восстановления:
   замах руки (масса-закон) + wind-up ВСЕХ известных не-sustained спеллов
   (вердикт) — окно в CANON S14 для тестов.
5. `exec <file>` — состояние = текстовый файл тех же команд; `#`-комменты,
   СТОП на первой ошибке с номером строки (вердикт), вложение запрещено
   (вердикт); Console::execute вернул вердикт успеха (свидетель =
   errorCount: c.error() хендлера роняет строку файла).

Проверено: Release -O3, 0 ворнингов, ctest 94/94, смоуки console +
subworld_player_melee ×3 сида PASS. Сейв не бампался.

## План следующей сессии (порядок предложен, владелец решает)

1. ПРЕДМЕТНАЯ СИСТЕМА — ДЕМО-БЛОКЕР (вердикт владельца 2026-09-07: «система
   не доделана, а она ключевая для геймплей-лупа — гринд предметов и
   экипировка/билды»). Найдено консолью: каждый `give wpn_dagger` даёт
   бит-в-бит одинаковый «Rusty Dagger +2 STR». Факты: бонус — не хардкод, а
   колонка ряда kCatalog; читатель аффиксов ЕСТЬ (worn_bonuses суммирует
   def->bonus + ItemRef.affix одним accumulate, сейв хранит), ПИСАТЕЛЯ НЕТ
   ВООБЩЕ; +2 STR/+2 END — плейсхолдеры эпохи доказательства двери
   (2026-08-27). Дорожка (гайдед, вердикт (а) владельца):
   - обнулить плейсхолдер-бонусы wpn_dagger/arm_leather + поправить их
     описания — ГОЛЫЕ предметы; колонку bonus ряда ОСТАВИТЬ (уники/артефакты
     = легитимный механизм врождённого бонуса);
   - ПИСАТЕЛЬ АФФИКСОВ — оживить взведённый трек РПГ-слоя (memory
     rpg-layer-track: процедурный предмет = запись стака с seed; ItemRef уже
     несёт material/quality/seed/affix): где роллится (лут-дверь? дроп?
     магазин?), закон силы/редкости — вопросы владельцу до кода;
   - оружейные ряды под скиллы: сейчас 1 оружие (wpn_dagger/Dagger) на 8
     оружейных скиллов — меч/топор/копьё/булава/лук/посох; масса сама даёт
     темп (weapon_swing_seconds), dice/dmgType/skill = колонки ряда;
   - консоль уже готова тестировать всё это (give/equip/sheet/loot/exec).
2. Хвосты глаз: второй прогон темпа боя (кулак ~1.4с, страж ~2.2с, звери
   быстрые) + замах кастов; лужа смерти/капель/угли+дым/дождь (день 5
   ~07-12ч) — из трека частиц; arrival-слайд (авторский текст владельца).
3. Звуки-ассеты: когда появятся .wav — просто положить melee-swing/hit/
   blocked.wav в assets/sound (перекрытие автоматом); очередь на новые
   SfxId-ряды: входящий удар по игроку, каст, смерть.

## Грабли (не наступать)

- «ИГРОКУ X, НПЦ Y» — ЗАПРЕЩЁННАЯ РАМКА (владелец капсом, дважды за
  2026-09-07): игрок особен только камерой/инпутом/входами; любой тюнинг
  формулируется по рядам и законам мира (memory: no-player-special-framing).
- kLootProfiles/kCatalog живут в АНОНИМНОМ неймспейсе items.cpp — новые
  sm-функции класть ПОСЛЕ его закрытия (ловилось линкером, не компилятором).
- Позиционные init при росте POD-структур — греп ВСЕ сайты (BodyDesc.height
  сдвинул бы speed у любого позиционного, ловилось ворнингом literal-conv).
- ПЕР-ШАГОВЫЕ пересчёты дериватов — ТОЛЬКО с гейтом «что-то изменилось»
  (безусловный recompute воскрешал spRegen — cde1ec5); консольные skill/attr
  ходят этим гейтом.
- КЛАМП effective_sheet поднимает атрибут 0→1 — ожидания тестов обязаны
  ходить ТОЙ ЖЕ дверью; Attributes{} по умолчанию = ЕДИНИЦЫ, не нули.
- int/int деление дробей ран = тихий ноль; printf %f с int-аргументом = UB.
- Смоук-сцепка macro_travel_sp,macro_recovery,subworld_recovery одной
  сессией красна и на базлайне — гонять токены ПО ОДНОМУ; walk — связкой
  subworld_enter,subworld_walk; smoke.sh запускать ИЗ КОРНЯ репо (относит.
  пути, exit 127 из чужого cwd).
- Рукотворный NavWorld в тестах: подгони bakedSeed/bakedLandmarks/mapW/H.
- Ординалы сейва навсегда: append-only; бампаем версию, миграций нет (v81).
- zsh: `===`-разделители в командах ломаются; писать echo-маркеры словами.

## Спящие ветки (сознательно, НЕ дефекты)

- Форажинг ИГРОКА спит: Adventurer.upkeep = kNpcUpkeepNone → сквад игрока
  хлеб не ест вовсе — вопрос экономики владельцу.
- XP-гранты quest_engine/effect_applicator на БАЗОВОМ листе — дверь приедет
  с переездом PlayerState (~550 мест, отдельный заход).
- Body/Mind-теги спеллов спят «до апдейта паладинов и клириков» (S15);
  с вердиктом «один рычаг» их спелл-прибавка = сырой INT (Спеллкрафт ушёл
  в темп) — сила без школьного множителя до пробуждения школ.
- Рост тел в толпе: тела без Sprite (голые фикстуры) берут человеческую
  колонну по умолчанию — честная дверь пер-тела уже есть (Sprite.height).
- HUD-индикатор замаха каста и звук каста — отложены сознательно (443f0f5).

## Очередь после демо — крупное и мелочь

0. СЛУЖБА ФРАКЦИИ — ОТЛОЖЕНА ЗА ДЕМО (вердикт владельца 2026-09-06: от неё
   ничего не зависит, сценарий демо её не использует); трек 2026-09-05
   (присяга → не враги → войны за жалованье) взведён, детали = гайдед-разбор
   при заходе (memory: faction-service-track).

1. ЛУТ-КЛЮЧ НА ПЕЙЛОАД: сундук/труп с CorpseLoot без Structure не
   находится взглядом — ключ интеракции должен быть на пейлоад, не на вид.
2. СМОУК-ПРИМИТИВЫ (п.5 трека реестров): goto_landmark / interact_aimed /
   take_stairs — недостроенные кирпичи смоуков. (+ идея 2026-09-07:
   «смоук-токен = консольная команда», один словарь — обсудить при заходе;
   консоль-смоук расширить на новые команды skill/attr/equip/exec.)
3. UI-ТЕКСТУРЫ ЛОВУШКА: segfault-риск VK_ERROR_OUT_OF_POOL_MEMORY
   (портреты 48×48 без капа в дескрипторном пуле) — короткий заход.
4. КЛАСС БАГА «вход не сбрасывает состояние движка»: ревизия полей
   SubworldEngine, переживающих leave(), не делалась (pendingSfx уже
   чистится с 2026-09-06).
5. Стены канваса следов + сплэши дождя — за демо (трек частиц).

## ЭКОНОМИКА — отложенная очередь (НЕ ПОТЕРЯТЬ; вернуться после РПГ)

Хвосты трека баланса (эталоны: /tmp/guard512_*, /tmp/scent512_*, старший —
balance_out_navgraph.log; balance_run НЕ в check, гонять только при изменении
законов экономики/АИ):
1. ТЮНИНГ ПОПУЛЯЦИИ дубль-прогоном: popEnd 103-118k против navgraph 250-275k.
   Ручки: N строк ростера, labourShift, темп вендоров, веса скора аукциона,
   страх/патруль, крутилки следа/охоты (kScentDecayDays=4, kScentDiffusionShift=3,
   kScentQuantShift=2, kHuntBoldShift=1, kHuntScentFloor=8 — НЕ свипованы).
2. Минт 999/12345 = 0: геология к подножиям (вердикт записан).
3. Вселение vs растворение ротации — problems.md §35 (вердикт не снят).
4. Отказ ценой (провиант-порог), рейсовый паром, военно-полит слой (§34.6),
   кран спавн-кошельков (honest-spawn-wealth), перф §34.8: НАЧИНАТЬ С ПРОФИЛЯ
   7×32 (подозреваемые: deposit каждого think и дневная диффузия 34 планов —
   лечатся по-разному). Замер scent 4×512: минт сид 1 −26% (охота режет караваны
   серебра?) — посмотреть при тюнинге.
