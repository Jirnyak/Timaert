# Промт следующей сессии

Обновлён 2026-09-12 (ночь). Прочитай AGENTS.md; режим гайдед — код по
одобрению на каждую порцию, вердикты в файлы (postdemoaudit.md /
problems.md / CANON.md). Стадия: §41 «игрок == НПЦ» ЗАКРЫТ; меню-сессия
2.5 ЗАКРЫТА ЦЕЛИКОМ; СИСТЕМА МАТЕРИИ/КРАФТА ПОСТРОЕНА 2026-09-11/12.
**ЗАКРЫТ ВЕСЬ БЛОК SUB ПОСТДЕМО-АУДИТА** (SUB-3..SUB-8, пять коммитов
`e1627f8b..34636283`) и построен КУСОК 1 ШВА АНКЕТЫ. ctest 98/98,
смоуков 63/63. НОВОЕ В CANON: система интеракции/диалога — читать перед
работой по диалогам.

## КАК РАБОТАТЬ (читать первым, это не формальность)

**Замысел и архитектура живут в голове владельца, а не в коде и не в доках.**

1. **Спрашивай владельца до правки.** Порция маленькая и согласуется:
   «беру X, причина, форма правки, чем докажу» — и ждём ответа.
2. **Не додумывай замысел.** Найденное поведение может быть АВТОРСКОЙ
   механикой; прочитать комментарий НА МЕСТЕ ВЫЗОВА и спросить.
3. **Развилки — вариантами с ценой и рекомендацией**, решает владелец.
4. **Вердикт пишется В ФАЙЛ**, иначе всплывёт заново.
5. **Свидетель раньше правки**; `cmake --build build --target check` +
   `sh smoke.sh all` гонять вместе; база 98/98 + 63/63.
   **ВАЖНАЯ ПОПРАВКА 2026-09-12: вердикт по ОДНОМУ полному прогону НЕ
   доказательство.** Пять прогонов подряд на одном запиненном сиде дали
   РАЗНЫЕ одиночные красные (`fauna_kill_writeback`; `prologue_road` +
   `trigger_battle_start`; `subworld_time` + `subworld_enemy_feedback`),
   каждый зелен при одиночном повторе, и два прогона чистых целиком.
   Скрипт сам это говорит («re-run each red ALONE before believing it»).
   В `prologue_road` причина видна в числах: засадник не успевает
   сократить 267 м — это ПЕРФ КАДРА, не логика. Значит: красный →
   повторить ОДИН токен → если зелен, это SMOKE-7; РЕЖИМ падения сверять
   с реестром ВСЕГДА.
   **ЛОВУШКА ДИАГНОСТИКИ (стоила половины сессии 2026-09-12):**
   `smoke.sh` печатает только строки, начинающиеся с `[smoke]` (фильтр
   `grep '^\[smoke\]'` на выходе). Временную печать писать префиксом
   `[smoke] probe …`, иначе вывода НЕТ и легко решить «эта ветка не
   исполняется» — я так трижды строил теории на пустом месте.
6. **Авторские тексты владельца — ДОСЛОВНО** (Varnava, King-Peasant,
   DragonN — его слова).
7. **«Игроку X, НПЦ Y» — запрещённая рамка.**

## ✅ ГДЕ МЫ (HEAD 4595c4b, сейв v93, глазами НЕ проверено НИЧЕГО с v85)

Сессия 2026-09-10 (полный день, 6 посадок запушено):

1. **Посадка Б листа (v91)** — PlayerState::sheet мёртв; двери
   player_sheet/player_effective_sheet(world); универсальная
   standing_bonuses_of/effective_sheet_of (squad.h); storedSheet и ветки
   PlayerSquadTag мертвы; events-слой берёт CharacterSheet* идиомом.
2. **Стол анкет (v92)** — macro/characters.h: строка = id/имя/тело/лист
   (бросок|авторский)/фракция/дом/behaviour/агенда; тег DesignCharacterTag
   → designOrdinal в записи; спавн только в генезисе (смерть навсегда);
   ЛЕСТНИЦА effective_behaviour = ЗАКОН (приказ > контекст-перекрытия
   будущего > анкета > строка типа, npc_ai.h). **Варнава** (Waypoints,
   город №0 ↔ ближайшая деревня).
3. **SMOKE-4 закрыт свойством** player_grounded() (все 4 сида зелёные);
   macro_npc_trace исключает носителей высших ступеней лестницы.
4. **King-Peasant** — Peasant level 70, фракция ОН САМ (строка
   king_peasant), дом = случайный варварский город (homeFactionPrefix),
   чистый роамер, модель ai_mage_hunt: бьёт ТОЛЬКО тела Witch/Sorceress
   фракции magika (крестьян магики НЕ трогает — слово владельца).
5. **Корень 5 §41 (LastHit.playerOwned) ЗАКРЫТ** — байт мёртв, жнец
   резолвит лидера по ТЕЛУ убийцы, одна дверь award_kill_xp (владеемый
   лист растёт как игрок: WIS-дивиденд, очки КОПЯТСЯ; транзиент —
   бросок); обе макро-точки на той же двери; репутация по «руке игрока»
   (аватар/его солдат). Попутно: свой удар игрока шёл attackerId=0 —
   починен на тело аватара. ?35 в CANON: РЕЕСТР НЕ РЕЖЕМ (не до демо).
6. **ПОЛЁТ СИСТЕМНО (v93) + драконы** — вердикт «субмир 3D, все
   воспринимают x/y/z; полёт честный как в M&M»: SubworldAi.wantVz =
   третья ось намерения (пишется только Flying-телам; ходока прижимает
   закон опоры), конверт/кламп общий с игроком, колонка
   CombatTemplate::cruiseM (дракон 10, орёл 6, ястреб 5), рождение через
   maybe_emplace_flying. На карте полёт = смена стихии в ОДНОМ законе
   марша (образец парусов): rt.flying — рельеф не платится, вода без
   корабля. Род Dragon (hp 500, 3d20 Fire, файрбол Missile-колонками,
   процедурный Avian) + 5 зеркальных таблиц; модель LairSorties (логово
   rt.lairX/Y = вершина массива, порог = kMountainBiomeLevel, радиус из
   агенды, бьёт слабейших, возвращается); Dragon1-3 по вершинам. Фракции:
   king_peasant (свой), dragons (общий, killIsNoCrime).

ctest 96/96, ноль ворнингов. Свидетель стола — design_characters_test.

## ⏭ ЧТО ДАЛЬШЕ (порядок владельца)

**0. ТРЕК «ЗЕРКАЛО» — ПОСТРОЕН ЦЕЛИКОМ 2026-09-12 (Инк 1–5), НЕ
ЗАКОММИЧЕН, ГЛАЗАМИ НЕ ПРОВЕРЕН.** Вердикты и разбор — в CANON («Вердикты
ТРУДА» → блок «ЗЕРКАЛО ЦЕЛИКОМ»), класс дефекта свидетельниц — problems.md
§45. ctest 99/99, смоуков 63/63 (чистый прогон), перф `spire_perf` 4.1 %
не сдвинулся, сейв НЕ бампался.

- **ВЕРДИКТЫ ВЛАДЕЛЬЦА.** (1) СВЁРТКИ ВВЕРХ НЕ БУДЕТ: тело = АДРЕС
  макро-записи, подобранное/потраченное/опыт происходят С ЗАПИСЬЮ, поэтому
  наверх ехать нечему и hp-дробь умирает вместе с копированием вниз.
  (2) Опыт за килл — ВСЕМ одной дверью; разведка: это УЖЕ закон (жнец
  резолвит любого убийцу через MacroOrigin / MacroDebt-Roster / AvatarTag),
  строить было нечего.
- **ЧТО СТАЛО ЗАКОНОМ.** `src/sub/record.h`: `record_of` / `state_of<C>` /
  `pools_of` / `StandingMirror`. У двери НЕТ ветки на игрока — оболочка
  героя носит `MacroOrigin` на свой сквад. Умерли:
  `reconcile_tracked_bodies_to_macro`, `reconcile_player_hp_to_macro` (от
  неё осталась `report_player_damage` — чистая обратная связь),
  `TrackedInheritance::carry`, дробь раны, ВЫБОРКА власти полос,
  рукописная ветка «снаряжение игрока на скваде» в митигации. Появились:
  `mirror_bodies_from_record` (верх тика; блок тела = копия СЦЕНЫ и
  одновременно снимок «что было в начале тика»), `combat_from_sheet` (одна
  дверь «лист → удар» у рождения и пересборки), `refresh_body_strike`
  (ворота `BonusTotals::operator==`). Попутно закрыта дыра: тело рождалось
  с БАЗОВЫМ листом записи — кольцо лорда защищало его, но не било вместе с
  ним; теперь эффективный.
- **Стражи, без которых закон ломает игру:** `macro_exit_cell_for_body`
  требует ЧУЖУЮ запись (иначе обычный выход телепортирует игрока в клетку
  входа); `refresh_body_strike` сохраняет `recoverySteps` (иначе занятая
  рука получает бесплатный удар).
- **Грабля конвейера сработала снова:** дверь потянула `.cpp`-символы
  (`standing_bonuses_of` → anatomy.cpp → items.cpp) и сломала линк двух
  чужих тест-таргетов; лечится строками в CMakeLists, это норма.
- **ВСЕЛЕНИЕ ВЫРЕЗАНО КАК МЕХАНИКА (вердикт владельца тем же днём, CANON).**
  «Одержимость — не более чем перенос флажка (эффект для будущих спеллов)».
  Удалены `possession.md`, `MacroExitCell`, `macro_exit_cell_for_body`,
  `adopt_possessed_macro_as_player`, `remap_macro_player_to_origin` — четыре
  сущности схлопнуты в `follow_flag_to_its_record` («ты тот, в чьём теле
  стоишь»); вычищен словарь D2/D3/body-native/revert target/Inc 5c-5e из
  комментариев и доков (MANIFEST/ARCHITECTURE/rpg/microcombat/macrosim/lore/
  microworld). Два юнит-кейса, сторожившие удалённые двери, сняты — закон
  сторожит смоук `subworld_exit_remap` из конца в конец.
- **ХВОСТ (не начато, мелкий):** каст и зелья из main.cpp всё ещё идут в
  `player_pools(app)` — в свою запись, а не в запись носимого тела. Под
  вердиктом «перенос флажка» ответ очевиден (платит запись того, кого носишь,
  через `pools_of`), но это UI-слой и отдельная порция.

**1. ГЛАЗА ВЛАДЕЛЬЦА — он играет по инструкции (выдана в чате в конце
сессии 2026-09-10; шаги: новая игра → марш/грани/encounter → Варнава у
первого города → сейв/лоад с листом и уровнем в HUD → левел-ап/траты →
одержимость → полёт над склоном → бой/автобой/лук/прилавок → консолью
goto к варварскому городу (царь) и к горе (дракон, глазами полёт тела)).
Фидбек + playtest.log разобрать; отложенные хвосты глаз: аффиксы,
рекавери, звуки, частицы (дождь день 5), arrival-слайд.**

**2. По фидбеку глаз — чинить найденное.** Ожидаемые шероховатости
тестового контента (не баги закона): драконий спрайт — процедурный Avian
без арта; меле-мобы летуна не достают (честно, пике придёт с первым
дерущимся меле-летуном); NpcDeath глушится только для смерти игрока.

**2.5 ПРОМТ ОТДЕЛЬНОЙ СЕССИИ — УНИВЕРСАЛЬНЫЕ МЕНЮ МАКРОМИРА**
(записан по слову владельца 2026-09-11; «дыра большая — отдельная
сессия»). Замысел владельца дословно: «у нас есть меню-попапы сквадов
соседних — это хорошая практика; на основе них мы сделаем такие
универсальные меню для всего, с чем можно взаимодействовать/торговать в
макромире (сквады/города/ландмарки и тд)».
- ПРООБРАЗ: панель соседних сквадов (ui/macro_overlay.cpp — ряды с
  Talk/Trade/Attack) + бартер-окно «NPC Trade» (внутри УЖЕ единая
  торговля: draw_barter_column/value_of, та же сделка, что прилавок).
- ДЫРЫ, которые сессия закрывает: PLAY-1 (панель = легаси-россыпь
  кнопок, а не система) и PLAY-2 (меню поселения хардкодит
  LandmarkType::City — main.cpp settlement_by_id/settlement_at_player;
  у деревень/шпилей/руин меню НЕТ).
- ФОРМА (согласовать с владельцем ДО кода): ОДНО универсальное меню
  ВЗАИМОДЕЙСТВИЯ для любого объекта карты — сквад, город, деревня,
  шпиль, руина, (клетка?): объект объявляет свои ДЕЙСТВИЯ строками
  (talk/trade/attack/enter/hire/rest/...), реестр решает какие доступны
  (колонки LandmarkDef / поведение сквада), рендер один. Родня лестницы
  effective_behaviour: данные объявляют, одна дверь исполняет.
- СВЯЗКИ: система интеракции/диалога владельца (CANON/work_vector —
  Talk станет её крючком); доска контрактов поселений (анкеты ↔ игрок);
  торговля ВЕЗДЕ через один бартер-виджет.
- ВЕРДИКТЫ ВЛАДЕЛЬЦА 2026-09-11 (старт меню-сессии):
  1) Иерархия дословно: «попапы едины, нажатие для взаимодействия на
     попап, а потом уже открываются специфичные меню — сквад/город/
     деревня со своими нюансами, но основа единая» (реф M&B: у деревни,
     лорда и города разные опции, система меню единая).
  2) Попап-ряд = ЧИСТЫЙ КЛИК → меню; три кнопки Talk/Trade/Attack с
     ряда умирают (уходят пунктами внутрь меню). PLAY-1 закрывается
     целиком.
  3) Попапы всплывают у ВСЕХ ландмарков (город/деревня/шпиль/руина/
     логово/шахта/...); у кого действий мало — минимум инфо/Enter,
     решает колонка действий.
  4) Деревня видит: прилавок + найм из гарнизона + доска контрактов.
     ОТДЫХ ТАВЕРНЫ ВЫРЕЗАН ВООБЩЕ (владелец, тот же день: «это мусор»)
     — вкладка Rest, rest_cost и колонка MoodRow::restCost удалены;
     отдых = только один макро-закон (Z). СДЕЛАНО.
  4б) Хранение: ВАРИАНТ А утверждён (единая ДВЕРЬ над двумя
     хранилищами). Вариант Б (ландмарк = entity) отклонён для этой
     сессии по замеру: 20 файлов / 75 касаний gs.landmarks, 38 вызовов
     landmark_by_id, сейв-миграция; «ENTT-природа»
     создания/уничтожения уже закрыта вердиктом S9 (kind-колонка:
     руина создаётся пушем записи, шпиль→руина = флип типа). К Б может
     вернуться ВОЕННЫЙ СЛОЙ (гарнизон как AutoBattleSide места).
  5) Меню города = ОБОБЩИТЬ нынешнюю панель поселения: вкладки гейтятся
     данными (колонка действий), хардкод City умирает; деревня видит
     своё подмножество вкладок.
- ПРОВЕРКА СИММЕТРИИ (владелец просил, код подтвердил): инвентарь и
  армия у сквада и ландмарка = БУКВАЛЬНО ОДНИ ТИПЫ (`Inventory` 256
  стаков: NpcInventory / Landmark::inventory; `SoldierSquad` 1024:
  SquadRoster / Landmark::garrison), одни функции и одни сериализаторы
  сейва; кошелёк = монетные стаки в инвентаре У ОБОИХ. Рвётся только
  ВЛАДЕНИЕ/АДРЕСАЦИЯ (ECS-компонент vs поле структуры) — лечится парой
  дверей store_of/roster_of над subject {entity | landmark id}.
- ВЕРДИКТ ВЛАДЕЛЬЦА 2026-09-11 (вдогонку, ЗАГЛАВНЫМИ): «КОРОЛЕВСТВ
  ТЕПЕРЬ НЕТ (ВЫРЕЗАТЬ), У НАС ТОЛЬКО ФРАКЦИИ — ОДНА СИСТЕМА».
  Разведка подтвердила: королевства УЖЕ съедены реестром фракций
  (2026-07-30, все 10 = обычные строки kFactionDefs; имя/цвет/
  темперамент Kingdom — мёртвые дубли, ownerKingdom мёртв, войны/
  дипломатии королевств не существует, Politik НЕ в сейве — только
  Landmark::kingdomIdx одним полем). Вырезка СРЕДНЯЯ: Landmark
  получает factionIdx (сейв v94, ретро-таблица 10 строк в ридере),
  suzerainLandmarkId поглощает capitalLandmarkId И nearestCityId
  (один столбец сюзеренитета вместо двух механизмов), KingdomDef →
  RealmSeedDef (гео-сид расселения фракции), язык per faction slot;
  резолвер faction_index_for_kingdom (37 упоминаний) схлопывается в
  чтение поля; ~16 src-файлов + 10 тестов (settlement_faction_test
  переписывается). Это же снимает вопрос попапа Инк 3: faction_of у
  сквада и ландмарка станет ОДНИМ пространством реестра.
- ПЛАН ИНКРЕМЕНТОВ (каждый — по одобрению): Инк 1 subject-двери
  store_of/roster_of — СДЕЛАН 2026-09-11 (macro/map_subject.h +
  map_subject_test: адресное равенство со старыми путями, kind-blind
  ландмарки, fail closed; 97/97 + 62/62); Инк 2 словарь глаголов +
  колонка действий — СДЕЛАН 2026-09-11 (map_actions.h биты,
  LandmarkDef.actions: город/деревня = Trade|Hire|Quests, Enter =
  walkable-колонка без бита-дубля, дверь actions_of; вердикты
  пришпилены литералами в тесте); Инк 3+4 — СДЕЛАНЫ 2026-09-11
  (8639176): попапы соседей = сквады И ландмарки (один закон Чебышев
  ≤1), ряд = ОДНА кнопка → меню взаимодействия (глаголы из actions_of,
  один рендер-цикл, один switch исполнения; сквад Talk/Trade/Attack,
  поселение Enter/Trade/Hire/Contracts, шпиль/руина минимум
  Enter+инфо), россыпь кнопок мертва — PLAY-1 ЗАКРЫТ; панель поселения
  де-хардкожена (landmark_has_settlement_panel = колонка действий,
  City-фильтры мертвы ×4, вкладки Build+Trade/Recruit/Quests гейтятся
  глаголами, деревня получила меню, доска — своим генератором,
  мёртвая «Enter City» вырезана) — PLAY-2 ЗАКРЫТ; Инк 5 — СДЕЛАН
  2026-09-11 (7c739e2): одна торговая обвязка (player_haggler /
  BarterWrapState / draw_barter_body в trade_widgets.h; у сайтов только
  прайс-лямбды — спрос = дело субъекта — и запись факта).
  ПОПУТНО ТЕМ ЖЕ ДНЁМ: КОРОЛЕВСТВА ВЫРЕЗАНЫ (a3e47b0, сейв v94, см.
  вердикт выше; CANON S24 обновлён). ВСЁ ЗАПУШЕНО, 97/97 + 62/62,
  ГЛАЗАМИ НЕ ПРОВЕРЕНО НИЧЕГО.
- ФИДБЕК ВЛАДЕЛЬЦА (тот же день) ОТРАБОТАН (c9989f7, 4682ebc):
  1) спрайты ландмарков в попапе — из kLandmarkDraw (строка карты);
  2) генерик-окно Interaction ВЫРЕЗАНО («плохо и костыльно») — клик по
     ряду открывает СРАЗУ панель субъекта: ландмарк = панель поселения
     (теперь для ЛЮБОГО вида, вкладки по колонке действий), сквад =
     окно Squad той же формы («меню города — хороший пример, его
     обобщить»: шапка + кнопки Talk/Attack, вкладки Info/Trade; окно
     «NPC Trade» умерло во вкладку);
  3) вердикт: попапы соседних клеток ОСТАЮТСЯ (Чебышев ≤1) вместе с
  4) ЗАКОНОМ ПЕРЕНОСА БОЯ: атакующий, объявляя бой, ДОХОДИТ — при
     нажатии Attack игрок встаёт в клетку защитника
     (player_jump_to_cell) ПЕРЕД PreBattle; вся встреча всегда одна
     клетка, рассинхрон позиций макромира закрыт по корню.
- ФИДБЕК-РАУНД 2 (тот же день) ОТРАБОТАН (d832945, f16d98d):
  1) «нахардкодил параллельных систем» — отдельное окно Squad УБИТО,
     сквад = ВЕТКА draw_settlement: одно окно ###Settlement (заголовок
     = имя субъекта), ОДНА пауза (pausing_panel_open), один таб-бар;
     macro_overlay только строит ряды и докладывает клик; смоук
     открывает панель через App::subjectSquad;
  2) КАРТА ГОРОДА чинена по корню: превью строилось из фейкового
     контекста (cx=0, Meadow, сид worldSeed+id*123 — параллельный
     закон сида); теперь kCellSeedX/Y общие (sub/map_data.h), превью =
     реальные факты клетки + 3×3 соседей (cell_facts), тот же сид —
     картинка = город, в который входишь;
  3) ИНВЕНТАРЬ-СЕТКА 16×16 СДЕЛАНА: draw_inventory_grid — один виджет
     для сумки персонажа и ОБОИХ прилавков; торговля по вердикту
     владельца: без третьей сетки, резерв прямо в стеке продающего
     (ЛКМ +Amount, ПКМ снять, ячейка count−staged + золотая рамка);
     списочная draw_barter_column удалена.
- ОСТАТОК блока 2.5 ЗАКРЫТ 2026-09-12 (блок 2.6): верстак = вкладка
  Craft панели персонажа + кнопка Scrap у стека. Talk = крючок S27.
  ГЛАЗАМИ НЕ ПРОВЕРЕНО НИЧЕГО из меню-сессии.

**2.6 СИСТЕМА МАТЕРИИ/КРАФТА — ПОСТРОЕНА ЦЕЛИКОМ 2026-09-11/12**
(вердикты владельца в CANON «Крафт/Скрап», разбор в economy.md
«matter merged»; сейв НЕ бампался — состав это статика каталога):
- Состав = до 4 пар {u16 ординал каталога, u8 счёт} на строке ItemDef
  (item_parts, авторство kPartsAuthoring в items.cpp); терминал =
  пустой состав; id материала = тот же каталог, третьего реестра нет.
- Двери: craft_item (белая база seed=0; монеты отвергнуты — чеканка =
  право двора), scrap_at (floor(n/2), seed+аффиксы сгорают,
  вариант-байт ItemRef.material = 1+raw-ряд подменяет слот 0),
  auto_scrap_overflow (>128 слотов, дешёвые нефунгибельные).
- «ВСЁ СЛИВАЕМ»: kRecipes-входы МЕРТВЫ — RecipeDef = только труд
  {output, темп-в-батчах, место}; деривативный спрос и закупка караванов
  читают ту же материю.
- ЧЕКАНКА СЛИТА ЦЕЛИКОМ (вердикт владельца 2026-09-12 ЗАГЛАВНЫМИ,
  раунд гнева — «БАРТЕРНАЯ ЭКОНОМИКА… ГОРОД ДЕЛАЕТ МОНЕТЫ ЧЕРЕЗ СИСТЕМУ
  КРАФТА ПО СВОЕМУ ИИ»): у состава есть ВЫХОД (item_yield; монета =
  {серебро 1} → 32, «состав монеты и есть монетный двор»);
  econ_produce_day делает ВСЁ — хлеб и монету — зовя craft_item на
  складе (спецветка чеканки, kMintMetal, ручные списания-возвраты
  МЕРТВЫ); переплавка монет = scrap_at (64 монеты → 1 серебро);
  ворота item_is_currency ВЫРЕЗАНЫ (подделка бессмысленна: реакция
  стоимостно-нейтральна, static_assert у таблицы); энтропия стала
  ПУЛОМ floor(K×состав/(2×выход)) — 2 кинжала → 1 железо.
- Авто-скрап вшит: econ_consume_day (факт Scrapped) + feed_squads_daily
  (мешки ИИ; PlayerTag не проходит — «игрок разбирает осознанно»).
- UI: вкладка Craft (каталог БЕЗ фильтров, монеты в списке «32x Imperial
  Crown», красная нехватка, полная цена) + Scrap 1/Scrap all с
  пул-превью возврата у стека Inventory.
- Свидетели: item_parts_test (no-arbitrage замкнутой формой
  Σсостав×цена ≤ 2×выход×цена строки, нейтральность двора, пул, плавка
  и ковка монет, all-or-nothing, подмена варианта, авто-скрап),
  econ_v1_test §7/§7б, смоук open_craft (раунд-трип + кадр — вкладка
  ГЛАЗАМИ АГЕНТА видна, владельцем НЕ проверена).
- УРОК СЕССИИ (владелец поймал ДВАЖДЫ за день): любой спецпуть у
  монет/товаров = «насрано»; если кажется, что нужен гейт или ветка —
  СНАЧАЛА спросить владельца, он предпочтёт обобщение закона
  (yield-колонка) любой проверке.
- Хвосты: рецепты-знания игрока (сейчас крафтится весь каталог —
  минимально универсально, по слову владельца «без рецептов»); гейт
  верстака/кузнеца (сейчас крафт доступен везде, лагерное ремесло);
  материалы-варианты (сталь) — байт готов, производителя нет;
  balance_run дубль-прогон после слияния чеканки не гонялся.

**2.7 ТРЕК ТРУД/SP — ПОСТРОЕН ЦЕЛИКОМ 2026-09-12** (вердикты + ПОСТРОЕНО
в CANON «Крафт/Скрап», блок «Вердикты ТРУДА»): labour-колонка каталога
(item_labour; эконом-темпы дословно, kRecipes = {output, site}, якорь
хлеба == kGatherPerWorkerDay свидетелем); вкладка Craft печатает и
списывает SP = бар/труд В МИНУС (хлеб 3 / меч 60 / статуя 120 при баре
120); харвест-с-удара ВЫРЕЗАН, действие Harvest (клавиша H, keymap)
платит бар/32 за объект; курс добычи ОДИН — kTreeLoot и kCropLoot = 1
(rpg_loot_test перепинен на закон курса). Свидетели: item_parts_test,
econ_v1_test, смоук console harvest_action (ОБЯЗАТЕЛЬНЫЙ, sp_paid=3),
кадр вкладки с SP-ценами смотрен. 98/98 + 63/63. ГЛАЗА ВЛАДЕЛЬЦА:
H-рубка работает, SP тратится (после фикса f7e7e46a — списание шло в
тик-зеркало тела; закон власти полос записан в CANON: действия игрока
платят в склад player_pools, одержимое тело — body-native). balance_run
ДУБЛЬ-ПРОГОН СВЕДЁН 2026-09-12: b0e13b5 (до слияния чеканки) против
HEAD, сид 12345 × 256 дней — world.tsv и landmarks.tsv ПОБАЙТНО
ИДЕНТИЧНЫ, оба exit 0 (все законы зелёные): слияние чеканки в craft_item,
labour-колонка и пул-энтропия не сдвинули экономику ни на единицу —
числа переехали дословно, а авто-скрап в самоигре не находит
нефунгибельного (лут с аффиксами живёт в лут-пуле, не в мешках). Числа труда ручных строк =
черновик (зелья 8, кинжал/посох 4, остальное оружие/кожанка 2) —
владелец правит данные. Скилл Harvest/Craft — будущее аппендом («ща не
надо»). Глазами НЕ проверено: Craft-вкладка с SP-ценами, минус-SP с
укусом, авто-скрап городов/сквадов, плавка монет.

**3. НОВАЯ СИСТЕМА ВЛАДЕЛЬЦА — ИНТЕРАКЦИЯ/ЕДИНЫЙ ДИАЛОГ** (заметки в
CANON.md/work_vector.md, реф Fallout/Underrail/Divinity). ЕМУ НУЖЕН
БОЛЬШОЙ ПРОМТ ДЛЯ НОВОЙ СЕССИИ — написать, когда скажет. Стол анкет
примет колонку-крючок диалога аппендом.

**4. Хвосты контента стола + ВЕРДИКТ ШВА АНКЕТЫ**: авторский лист
(authoredSheet) без свидетеля до первой авторской анкеты; конкуренция за
доску контрактов (анкеты ↔ игрок); архетипы = обобщение ротации (позже).
**НОВЫЙ ВЕРДИКТ ВЛАДЕЛЬЦА 2026-09-12 в СЕССИЮ ПО СКВАДАМ/НПЦ** (записан
и в CANON «Вердикты ТРУДА»): «HP-only реконсиляция субмирного тела —
ДЫРА; универсально макро→микро→макро должна идти АНКЕТА (стол анкет —
лорды/драконы/царь-крестьянин — уже есть)». Шов несёт запись персонажа
целиком (лист/полосы/идентичность) одной переносимой сущностью для
героя, одержимого тела и спроецированного лорда; выборка власти полос и
хвост «НПЦ платит за каст» умирают вместе с этим. Форму — согласовать
до кода.

**5. Реестр `postdemoaudit.md` §15**: TEXT-1..3 (TEXT-1 ЖДЁТ авторского
текста владельца — сцена ведьмы), UI-2→UI-1, режим C.

## Грабли (не наступать)

- Ссылка на ECS-компонент НЕ живёт через симулируемый тик; спавн
  negative-control СТАВИТЬ ДО сборки указателей (поймано в тесте стола).
- Тестовые таргеты линкуют ПОДМНОЖЕСТВО .cpp: inline-дверь → .cpp-символ
  ломает линк чужих тестов (anatomy.cpp пяти таргетам).
- Новый РОД реестра = строки в ПЯТИ зеркальных таблицах (kNpcPurse,
  kNpcMapColor, kNpcLootId, kRoleWeights, kSpawnHabitats) — сторожа
  поймают компилятором, это норма конвейера.
- Генезис-сканы карты обязаны гардить пустой terrain (headless-фикстуры
  зовут спавн без рельефа — SEGFAULT вершин пойман).
- Смоук-лаборатория с перекраской исключает носителей высших ступеней
  лестницы; «ничей» чит-кил (attackerId=0) не платит никому — киллы
  смоуков атрибутируются телу аватара.
- Мост-анестезия ЗАПРЕЩЁН; каталог/ординалы: только АППЕНД (v93);
  smoke.sh ИЗ КОРНЯ, токены ПО ОДНОМУ; -O3; zsh ломается об `===`;
  perl-массоправки не понимают порядка «ensure → дверь».
- **Смоук, зелёный по УДАЧЕ РЕЛЬЕФА, — не свидетель.** Две поймано
  2026-09-12: `cast_spell` искала линию огня только по рельефу и ставила
  мишень ЗА стеной; `subworld_walk` мерила темп ПОСРЕДИ СВИТЫ игрока,
  где тела расталкивают друг друга. Обе краснели от правки радиуса и обе
  были неправы. Свидетельница обязана спрашивать ровно то, что
  спрашивает механизм под тестом.
- **Не откатывать одобренную владельцем работу молча.** 2026-09-12 снёс
  готовый SUB-4 `git checkout`-ом, потому что он оставлял два красных
  смоука — надо было доложить и спросить. Красные оказались дефектом
  свидетельниц, а не правки.

## Спящие ветки и очередь после демо — без изменений

См. редакцию 365043a — «Очередь после демо», «Спящие ветки»,
«ЭКОНОМИКА» не менялись. Живые хвосты: ШОВ АНКЕТЫ (вердикт владельца
2026-09-12, §4 выше и CANON: через шов субмира едет АНКЕТА целиком, не
HP — это поглощает старый mp/sp-хвост «НПЦ платит за каст» и убивает
выборку власти полос),
MacroPos-скретч → int-марш, SUB-6 голый лорд, «тело без управления
ползёт» (вскрыто SMOKE-4), меле-пике летунов, z-сепарация стай,
кредит убийце игрока (фракционные killed-факты).
