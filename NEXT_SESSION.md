# Промт на следующую сессию

Обновлён 2026-08-29 (вечер, после канон-аудита + двух вечерних заходов).
Дерево собрано, `check` 87/87 зелёный, смоуки зелёные, сейв **v63**.
Запушено: `b8aa772` (фикс полос цвета земли в субмире — берег водной
клетки = её незатопленный климатический грунт, GROUND-кольцо `nbGround`,
водная ветка дизера снесена; microworld.md §cross-seam ground materials)
и `44c1e4e` (квестовая личность → ординалы + кодекс-реестр, см. таблицу
ниже). Сводка аудита — `canon-audit.md` 2026-08-29; хвосты —
`problems.md` §33.

## Кто ты и как здесь работают

Прочитай `AGENTS.md` целиком. Ключевое:

- **менять код только с явного разрешения владельца на каждое изменение**;
  предлагай варианты, не правь молча. Субагенты — только для РАЗВЕДКИ;
- **ЗАКОН КОНСТАНТ**: у числа рядом лежит вывод из свойства мира;
- **ЗАКОН ТИПА**: ширина по достижимому диапазону, знак по величине;
- вердикт тестов — только после `BUILD_OK`: зелёный ctest на несвежем
  бинарнике ловился не раз.

Смоуки: `sh smoke.sh <token>[,<token>] [seed]`.

## Что построено в этом заходе (не переделывать, читать доки)

| что | где читать |
|---|---|
| ЛАНДМАРКИ ОДНИМ РЕКОРДОМ: `struct Landmark` + колонка `type`, один вектор, один сериализатор, `kLandmarkYieldOrder`, `landmark_by_id` (v62; v60 — мёртвые колонки, v61 — сид uint32) | `landmarks.md` |
| Экономика: кредит-прежде-дебета везде, найм в казну (`hireGold` × уровневый закон), минт-колонка `FactionDef::mint`, `rt.carryCap`, дверь `econ_produce_day(site=Village)` | `economy.md` |
| Бой: броня в авторезолве тождеством `kArmorHalving` (дом — npc.h), единый XP-закон `xpReward` (exp_from_fight мёртв), цепная молния 3D | `combat.md`, `monsters.md` |
| Летопись: дверь `record_deed` в `macro/squad.h`, все писатели ПЛАТЯТ славу, колонка `victimShare` (Traded не платит), журнал/одержимость, валидация анналов, history_-шина мертва | `chronicle.md` |
| Мир: климат косинусный (CANON S19), один закон зрения (игрок в SquadIndex, авторезолв не решает встречу с игроком), один A* + `kRoadClasses` + честные грунтовки, Dead-свип | `macroworld.md`, `macrosim.md` |
| Реестры: `FeatureDef{bedWeight,opticalCost,civStrength}`, `kDepositDefs`, `kMoodRows`, `kNpcMapColor`, `kNpcLootId`, `spellbook_rule_active`, фауна-колонки LandmarkDef | `zones.md`, `resources.md` |
| Субмир: честные мосты (деки/опоры/арки), экспорт позиции башни шпиля, `read_sign` по клетке, один `place_body_ring` | `dungeons.md` |
| App: смоук-харнесс = `src/app/smoke.cpp`+`smoke.h`+`app_state.h`, main.cpp 12.7k→5.4k; 19 тестов на `tests/check.h` | `ARCHITECTURE.md` |
| ГРУНТ БЕЗ ПОЛОС (2026-08-29 вечер): кольцо материала = GROUND-кольцо `nbGround` (вода входит алиасом `ground_biome()` = свой сырой климат), водная ветка `pick_ground_biome_axis` снесена; негативный контроль в `material_seam_test` воспроизводит снесённый закон | `microworld.md` §cross-seam |
| КВЕСТ = ОРДИНАЛ (2026-08-29 вечер, v63): `Quest.ordinal` при ACCEPT из `nextQuestOrdinal`; оффер = POD-провенанс {giver, slot, bornDay} (и дедуп дня `settledQuestOffers`); кодекс = реестр `macro/codex.h`, анлок = биты; события Quest*/CodexUnlock несут ординал в `a` | `chronicle.md` (раздел долгов) |

## ЧТО ДЕЛАТЬ ДАЛЬШЕ — по убыванию готовности

### 1. GameEvent → срез летописи — РАЗБЛОКИРОВАН квест-ординалами

«События кадра» = срез летописи между закладками
(`EventBus::frame_first_seq` есть; history_-шина уже мертва). События ещё
возят строки (SpellCast.s1, ReputationChange.s1-фракция, ApplyEffect.s1-верб
и т.д.). `NodeContext` не видит мира — дать читающий доступ формой
`FactNaming`. Это самый тяжёлый из готовых треков — начинать свежим
контекстом, с разведки всех эмиттеров/читателей `s1`/`s2`.

### 2. Машинерия S9-переходов — ЗАКОН РЕШЁН (владелец, 2026-08-29), ждёт №1

**Переход = РЕАКЦИЯ НА ФАКТЫ летописи, не ежедневный закон-проход**
(CANON S9, уточнение записано). Ландмарки в норме — независимые модули;
переход — редкая мини-механика («иногда деревня может стать городом, но
очень редко»), расширяемая новыми реакциями. Строить ПОСЛЕ «GameEvent →
срез» его средствами. Механика готова: `lm.type = X` + `rebake_world` в
санкционированных точках (`landmark_grid.h`); Ruin/Lair/Shrine/Mine/Tower
имеют строки реестра без экземпляров. Готовые первые реакции: смерть места
(факт Died уже пишется с 2026-08-29 — честная смерть населения) → Ruin;
истощённый шпиль (depleted) → Ruin. Профиль производства при переходе
сменится сам — econSite теперь колонка реестра.

### 3. Слухи (дверь готова)

Слух = запись летописи, прочитанная чужими устами: услышал → копия в журнал
(та же дверь, что участие/локальность/одержимость). Нужна механика
ПОЛУЧЕНИЯ (таверна? встреча?) — это геймплей, вопрос владельцу.

### 4. Переезд склада HP/листа/позиции игрока (~550 мест)

Хвост «игрок = НПЦ» (память `player-squad-merge-track`): склад HP/лист/
позиция ещё в PlayerState. Отдельный заход, не смешивать с другими.

### 5. Отложенные решения — СПИСОК ВЛАДЕЛЬЦУ

(Закрыто вечером 2026-08-29: civ re-tie ПРИНЯТ — civ-пулл через
optical_sweep, близнец-BFS мёртв; econSite = колонка LandmarkDef с
кросс-чеком словарей в world_tick.cpp; полы популяции СНЯТЫ — честная
смерть до нуля + факт Died, тест в world_tick_parity_test.)

- **мосты глазами** и **берега рек глазами** (фикс полос b8aa772): кинуть
  взгляд в игре, вердикты по виду;
- **цена микро/макро-шага по мосту** + battle-тайл под декой — клеточные
  законы (problems.md §33).

## Учтённые строковые долги (НЕ чинить попутно — у каждого свой дом)

- предметы строковые всюду (`Inventory.add("bread")`) → дом: трек «РПГ-слой
  №5» (DOD-инвентарь 256 стаков, память `rpg-layer-track`);
- `Quest.title/description` в сейве → дом: ?27-локализация (отложено
  владельцем);
- строковые id маркеров (`"quest_7_0"`) → мелкий отдельный заход, не горит;
- остальные `s1` событий → это и есть трек №1 выше.

## Грабли (наследуемые + свежие 2026-08-29)

- зелёный ctest ≠ зелёная сборка: сначала `BUILD_OK`, потом вердикт;
- `%s` в printf с не-строкой компилятор не ловит;
- сравнение упакованных байтов рода летописи — только через распаковку;
- python-вырезы по индексам режут соседние функции — Edit точным текстом;
- авторские тексты (кодекс, лор) при переносе копировать ДОСЛОВНО — пересказ
  своими словами = порча канона (поймано на переносе кодекса);
- смоук `load_game` требует `open_load` перед собой в цепочке;
- поле POD-структуры при агрегатной инициализации — сверить ПОРЯДОК полей
  ({7,3,2} для {giver,bornDay,slot} — не то, что кажется).
