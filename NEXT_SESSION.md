# Промт следующей сессии

Обновлён 2026-09-10 (ночь). Прочитай AGENTS.md; режим гайдед — код по
одобрению на каждую порцию, вердикты в файлы (postdemoaudit.md / problems.md
/ CANON.md). Стадия: ГЛУБОКИЙ РЕФАКТОР «игрок == НПЦ» (реестр остатков =
`problems.md` §41) поверх трека багфикс/полиш перед контентом демо.

## КАК РАБОТАТЬ (читать первым, это не формальность)

**Замысел и архитектура живут в голове владельца, а не в коде и не в доках.**

1. **Спрашивай владельца до правки.** Порция маленькая и согласуется:
   «беру X, причина, форма правки, чем докажу» — и ждём ответа. Одобрение
   одной правки не распространяется на следующую.
2. **Не додумывай замысел.** Найденное поведение может быть АВТОРСКОЙ
   механикой; прочитать комментарий НА МЕСТЕ ВЫЗОВА и спросить.
3. **Развилки — вариантами с ценой и рекомендацией**, решает владелец.
4. **Вердикт пишется В ФАЙЛ**, иначе всплывёт заново.
5. **Свидетель раньше правки**: красный до, зелёный после, негативный
   контроль. `cmake --build build --target check` + `sh smoke.sh all 12345`
   гонять вместе; известная красная база смоуков = SMOKE-1
   `prepare_spell_auras`, SMOKE-2 `cast_spell` (на сидах 1/7/999 ещё
   SMOKE-4 `toggle_flight`). Одиночный зелёный перегон красного = SMOKE-7.
6. **Авторские тексты владельца — ДОСЛОВНО.**
7. **«Игроку X, НПЦ Y» — запрещённая рамка**: игрок особен ТОЛЬКО
   камерой/инпутом/входами.

## ✅ ГДЕ МЫ (HEAD ~4451b62+, сейв v90, глазами НЕ проверено НИЧЕГО с v85)

Сессия 2026-09-10 запушила ПЯТЬ посадок:

1. **SAVE-5 (v87)** — каждая загрузка с 2026-08-27 подменяла тело игрока
   свежим хаском (генезис пути загрузки поднимал второго носителя
   ординала). Закрыто: `PlayerTag` = честный байт записи снапшота
   (`MacroNpcRecord.playerFlag`), `boot_world(anchorPlayer=false)` на
   загрузке, `possessedMacroSpawnId`/`reattach_player_to_macro_spawn`
   мертвы. Ловушка в смоуке `load_boot` (carriers=1 flags=1 door_honest=1).
2. **Подпосадка 4 «игрок-как-сквад» (v88)** — `gs.player.x/y` и
   entry-поля PlayerState МЕРТВЫ: клетка = `MacroCell` носителя флага
   (двери `player_flag_entity/cell/visual`, `player_jump_to_cell` — inline
   в player_entity.h), ходок = банк `rt.moveBudget` по закону try_move
   (клетка = 1.0, диагональ не дороже), глайд/камера = общий интегратор
   визуала, entry-каденс = `rt.tickAccum` (kAiTicks). Один интегратор
   макро-хода на весь мир.
3. **§41 корень 2 (без бампа)** — базы MP/SP = колонки `CombatTemplate`
   (`mp`/`sp` int16, дефолт 100), `bar_ceilings` без дефолтов, три двери
   `body_max_hp/mp/sp`, static_assert канона 100/100/100.
4. **§41 корень 3 (v89)** — `SpellBook` = компонент КАЖДОГО макро-тела:
   битсеты конверта 256 (вердикт «точно не больше 256»), рождается нулями,
   едет в записи снапшота; дверь `player_spellbook`; учёба через
   `apply_events(book)`. Хвост: КАСТ НПЦ (spellbook_cast зовёт только
   инпут) — придёт с ИИ-работой.
5. **§41 корень 1, посадка А (v90)** — лист = ВЛАДЕЕМЫЙ компонент
   именованных родов (`npc.h kNamedKinds`: Merchant/Bandit/Witch/
   Sorceress/Adventurer; строка = новый род): рождается броском
   ординального сида в make_npc, левел-ап ПИШЕТ в компонент, сейв несёт
   опт-ин блоком (hasSheet). Транзиенты (ротация/караваны) деривируют
   генерик на лету. Дверь `sheet_of`/`owned_sheet` (squad.h).

ctest 95/95, ноль ворнингов, смоуки — только известная база.

## ⏭ ЧТО ДАЛЬШЕ (порядок утверждён владельцем)

**1. ПОСАДКА Б листа — переезд `PlayerState::sheet` на компонент сквада
игрока** (~72 места src + сейв-блок игрока → v91; тесты/смоук ещё ~90).
Форма утверждена: его лист = тот же владеемый `CharacterSheet`-компонент
(Adventurer уже в kNamedKinds; создающая дверь `ensure_macro_player_entity`
рождает компонент из листа создания персонажа). По дороге умирают:
`storedSheet` в `auto_battle_side_of` (лист = компонент, параметр не
нужен), ветка `PlayerSquadTag` в feed_squads_daily (`npc_ai.cpp` — дневной
закон), ветка форажинга игрока, `player_effective_sheet(gs.player)` →
через дверь. ГРАБЛИ: мостов-анестезий не строить (двух складов листа быть
не должно ни один тик — резать как подпосадку 4: двери → читатели →
смерть поля одним разрезом, компилятор = свидетель); ссылка на компонент
НЕ живёт через тик (спавн реаллоцирует).

**2. СТОЛ АНКЕТ дизайн-персонажей** (вердикты владельца 2026-09-10 СНЯТЫ):
- анкета = ИНДИВИД-субъект («он как игрок»), НЕ архетип; архетипы = позже
  обобщение существующей ротации ландмарков;
- смерть НАВСЕГДА (мир помнит летописью, ресспавна нет);
- лист в анкете: авторские числа ИЛИ бросок — колонка строки;
- «ведьмак берёт контракты» = ОБЩАЯ доска предложений поселений с игроком
  (конкуренция!);
- у каждого СВОЯ ИИ-модель: диспетч `AIBehaviour` уже DOD (свободные
  функции одной сигнатуры + enum) — новая модель = функция + строка;
  `effective_behaviour` уже умеет перекрытие на сущности;
- строка анкеты: id-ординал, имя, body=NPCType, лист, фракция, дом
  (ландмарк/клетка), behaviour, agenda-параметры;
- примеры владельца: царь-крестьянин нападает на магов в Оркгухе, дракон
  с вылетами вокруг своей горы, проповедник город↔деревня, «ведьмак» по
  большаку (это ПРИМЕР типа системы, не Witcher).
Первые 2–3 анкеты = проба конвейера (как шпили были пробой контента).

**3. Остатки §41**: корень 5 `LastHit.playerOwned` (два закона XP +
глушение NpcDeath — форму согласовать); потом вердикт `?35` (CANON: один
реестр или два — «2 тоже норм, ОБСУЖДАЕМ! точно не 3 и не 4»).

**4. ГЛАЗА ВЛАДЕЛЬЦА — КРИТИЧНО, разрыв v85→v90**: макро-марш игрока
(скольжение, диагонали чуть быстрее), вход/выход субмира с нужной грани,
побег из encounter (скользит 2 клетки), сейв→загрузка (полосы/сумка/
ростер/книга ОБЯЗАНЫ выживать — SAVE-5 был именно про это), одержимость +
выход как лорд, спелл-панель, бой/автобой, плюс старые хвосты: аффиксы,
рекавери/лук/прилавок, звуки, частицы (дождь день 5), arrival-слайд.
Старые сейвы мертвы по закону.

**5. Потом — реестр `postdemoaudit.md` §15**: TEXT-1..3 (TEXT-1 ЖДЁТ
авторского текста владельца — сцена ведьмы), UI-2→UI-1, режим C.

## Грабли (не наступать)

- Ссылка на ECS-компонент НЕ живёт через симулируемый тик (спавн
  реаллоцирует; перечитывать get<> после тика).
- Вью/читатель по снятому компоненту — МОЛЧА пустой проход; миграцию вести
  разрезом с удалением поля (компилятор = свидетель) + полный смоук-прогон.
- Тестовые таргеты линкуют ПОДМНОЖЕСТВО .cpp — новые двери макро-слоя
  делать inline в заголовке (player_flag_* так живут) или проверять линк.
- Мост-анестезия (проекция склад→сущность каждый тик) ЗАПРЕЩЁН — §41
  механика 1; SAVE-5 родился ровно из такого моста.
- smoke.sh фильтрует stderr до `[smoke]` — диагностику печатать этим
  префиксом или гонять бинарь напрямую.
- По-кадровые линейные сканы = тайминг-флаки (SMOKE-7); горячие поиски —
  кэш в reg.ctx() с ревалидацией (PlayerSquadCache).
- Каталог/ординалы сейва: только АППЕНД; бампаем kSaveVersion, миграций
  нет (сейчас v90).
- int/int = тихий ноль; смоук-токены ПО ОДНОМУ; smoke.sh ИЗ КОРНЯ; -O3,
  не -O0; zsh ломается об `===`.

## Спящие ветки и очередь после демо — без изменений

См. редакцию 365043a в истории git — разделы «Очередь после демо», «Спящие
ветки», «ЭКОНОМИКА» этой сессией не менялись; живы хвосты: mp-шов («НПЦ
платит за каст» — теперь в одной бите от книги), MacroPos-скретч →
int-марш, SUB-6 голый лорд, биллинг ходьбы при макро-одержимости платит
свой сквад (уедет с посадкой Б).
