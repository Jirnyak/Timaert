# Промт следующей сессии

Обновлён 2026-09-10 (вечер). Прочитай AGENTS.md; режим гайдед — код по
одобрению, вердикты в CANON.md / postdemoaudit.md. Стадия: ГЛУБОКИЙ РЕФАКТОР
«игрок == НПЦ» (вердикт владельца 2026-09-10, реестр остатков =
`problems.md` §41) поверх трека багфикс/полиш перед контентом демо.

## КАК РАБОТАТЬ В ЭТОЙ СЕССИИ (читать первым, это не формальность)

**Замысел и архитектура живут в голове владельца, а не в коде и не в доках.**
Код показывает, что построено; чего он не показывает — построено ли это ТАК,
КАК задумано. Поэтому:

1. **Спрашивай владельца до правки, а не после.** Порция берётся маленькой и
   согласовывается: «беру `X`, вижу такую-то причину, предлагаю такую-то форму
   правки, вот чем буду доказывать» — и ждём ответа. Одобрение на одну правку
   не распространяется на следующую.
2. **Не додумывай замысел за владельца.** Найденное поведение может оказаться
   АВТОРСКОЙ механикой; за 2026-09-09 так снялись три моих находки подряд
   (`SMOKE-5`/`SUB-8`, `RPG-13`, `RPG-14`), и ещё одна (`ECON-3`) оказалась
   вердиктом «пол честен, замысел». Прежде чем звать поведение дефектом —
   прочитать комментарий НА МЕСТЕ ВЫЗОВА (там регулярно записан замысел) и
   спросить.
3. **Предлагать варианты, а не один готовый ответ.** Если развилка дизайнерская
   — выложить варианты с ценой каждого и своей рекомендацией, решает владелец.
4. **Вердикт пишется В ФАЙЛ** (`postdemoaudit.md`: курсивом под находкой +
   строка в журнале §16), иначе через сессию всё всплывёт заново.
5. **Свидетель раньше правки.** Красный до, зелёный после, негативный контроль
   там, где свидетель может согласиться сам с собой. `ctest` + смоук-набор
   гонять вместе: зелёный `ctest` НЕ достаточен (доказано этим аудитом).
6. **Авторские тексты владельца переносятся ДОСЛОВНО**, не пересказываются.
7. **«Игроку X, НПЦ Y» — запрещённая рамка** (владелец капсом, дважды): игрок
   особен ТОЛЬКО камерой/инпутом/входами; всё остальное — законы мира.

## ✅ ГДЕ МЫ: рефактор «игрок == НПЦ», сделано к 2026-09-10 (ночь)

Сделано и запушено (HEAD = 7bafbcb):

0. **SAVE-5 НАЙДЕН И ЗАКРЫТ (v87, ecec280)** — каждая загрузка с мержа
   2026-08-27 подменяла тело игрока свежим хаском (генезис пути загрузки
   поднимал второго носителя ординала, двери держали его кэшем;
   сохранённый сквад ходил призраком). Вердикт владельца: сейв = честный
   снимок, `PlayerTag` = байт записи (`MacroNpcRecord.playerFlag`);
   `possessedMacroSpawnId` + `reattach_player_to_macro_spawn` мертвы;
   ловушка в смоуке `load_boot` (негативный контроль прогнан красным).
   Детали: `postdemoaudit.md` §1 SAVE-5 + журнал §16.
0б. **ПОДПОСАДКА 4 «игрок-как-сквад» ЗАКРЫТА (v88, 7bafbcb)** — скаляры
   `gs.player.x/y` и entry-поля PlayerState МЕРТВЫ: клетка = `MacroCell`
   сквада-носителя флага (`player_flag_entity/cell/visual`,
   `player_jump_to_cell`); ходок = банк `rt.moveBudget` по закону
   try_move (клетка = 1.0 — диагональ игрока больше не дороже, вердикт
   «один закон марша»); глайд/камера = общий интегратор визуала
   (исключение игрока снято); entry-байты и каденс = rt носителя
   (kAiTicks); генезис выводит стартовую клетку из мира; квесты получают
   клетку параметром. У мира ОДИН интегратор макро-хода. ГЛАЗАМИ НЕ
   ПРОВЕРЕНО. Ранее в тот же день:

1. **«Полосы на тело» ЗАКРЫТ, глазами владельца ПРОВЕРЕН** («вроде всё
   работает как задумано»): `CombatStats` мёртв, полосы игрока = `ecs::Pools`
   его сквада (`ecs/pools.h`, POD 36 Б, entt-free), дверь `player_pools()`;
   одна дверь «лист→тело» `refresh_body_from_sheet` (потолки + **доля у
   всех** на рескейле, dead stays dead, SP-долг переживает); **регенов в
   субмире НЕТ ни у кого**, реген ровно один — `rest_pools` стоя в макромире
   (`macro/recovery.h`); сейв v85. Детали: memory pools-on-body-track,
   `postdemoaudit.md` §16.
2. **Трек масштабов, подпосадки 1–3 ЗАПУШЕНЫ, глазами НЕ проверены**
   (закон в CANON S2: «два масштаба — РАЗНЫЕ СИСТЕМЫ, разницу держит ТИП»):
   - `AvatarTag` — флаг тела игрока в сцене; `PlayerTag` остался чисто
     макро-флагом лидера. «Два флага, по одному на масштаб».
   - `MacroVisual` — визуал макро-карты отделён от сценового `VisualPos`.
   - `MacroCell{u32}` — клетка сквада = ОДНО число, row-major индекс
     плоского тор-массива (po2: `cell_index`/`cell_x`/`cell_y`,
     `ecs/components.h`); у макро-сущностей НЕТ `Position` вовсе — класс
     SUB-1 (сценовый систем видит сквад) закрыт ПОСТРОЕНИЕМ с обеих сторон.
     Сейв v86 (`MacroNpcRecord.cell` u32). Дробный марш — стековый скретч
     `MacroPos{float}` в npc_ai: декод у входа think, энкод после settle.
   - Попутно закрыты: `AI-2` (армия мёртвого лидера — в пул дезертиров
     универсально, инвентарь победителю либо стоимостью в пул лута —
     `settle_dead_squads`), SUB-2, свидетель systems.cpp.
3. ctest **95/95**; `sh smoke.sh all 12345` — красные = база (`SMOKE-1`
   `prepare_spell_auras`, `SMOKE-2` `cast_spell`; на сидах 1/7/999 ещё
   `SMOKE-4` `toggle_flight`). Тайминг-вспышки одиночных сценариев — класс
   `SMOKE-7`, ловушки ПЕЧАТАЮТ причину (пример: `pending=1 pause=0x06` —
   мир стоял под оверлеями); одиночный прогон 3/3 зелёный = не находка.

## ⏭ ЧТО ДАЛЬШЕ (порядок утверждён владельцем 2026-09-10)

**1. ~~Подпосадка 4~~ — СДЕЛАНА (см. «ГДЕ МЫ» 0б; отметка в §41 корне 4).**

**2. Корни §41 (`problems.md`) — все УТВЕРЖДЕНЫ владельцем к работе,**
брать по одному, форму правки согласовывать:
- ~~базы MP/SP → колонки CombatTemplate~~ — СДЕЛАНО (3357bcc): mp/sp
  int16 в авторской секции, три двери body_max_hp/mp/sp, static_assert
  канона 100/100/100; поведение байт-в-байт;
- лист (Sheet) → компонент. НОВЫЙ ВЕРДИКТ владельца (2026-09-10,
  ММОРПГ-модель): лист = ВЛАДЕЕМОЕ состояние каждого ПЕРСИСТЕНТНОГО
  персонажа (лорд/царь/герой/друг), качается, ЕДЕТ В СЕЙВ; сид — только
  бросок рождения. Транзиенты (караваны/крестьяне ротации) — без листа
  либо генерик. Владелец поднял СИСТЕМУ ПЕРСИСТЕНТНЫХ ПЕРСОНАЖЕЙ («как
  бы игроков») — форму ОБСУДИТЬ до кода (предложение агента:
  персистентность = наличие листа-компонента; sheet_of: компонент → он,
  иначе генерик из ряда; кто получает лист при генезисе — вопрос);
- ~~SpellBook → компонент~~ — СДЕЛАНО (eafad22, v89): битсеты конверта
  256 (вердикт снят), компонент КАЖДОГО макро-тела, в записи снапшота;
  дверь player_spellbook; хвост = каст НПЦ (ИИ-работа);
- ~~четвёртый корень (позиция)~~ — СДЕЛАН подпосадкой 4; пятый корень
  `LastHit.playerOwned` (два закона XP + глушение NpcDeath) — форму
  согласовать отдельно.

**3. Вердикт `?35` (CANON) — «один реестр или два» — вернуть владельцу.**
Владелец: «2 реестра тоже норм, ОБСУЖДАЕМ ЭТО! точно не 3 и не 4» — т.е.
решение не отложено и не «дисциплина навсегда»; после подпосадки 4 выложить
за/против двух `entt::registry` (макро/сцена) против одного с тег-гигиеной.

**4. Хвосты трека масштабов (мелкие, по ходу):**
- `MacroPos`-скретч → int-математика марша (убьёт последний float в думке);
- `SUB-6` — голый лорд под землёй;
- mp через шов сцена↔макро — придёт с работой «НПЦ платит за каст»
  (вердикт «позже»); лечащие спеллы отложены («пока все отдыхают в макро»).

**5. ГЛАЗА ВЛАДЕЛЬЦА** — накопилось: подпосадки 1–3 масштабов (макро-марш,
вход/выход субмира, бой, сейв v86 совместимость НЕ обещана — ординалы
навсегда, старый сейв мёртв по закону), плюс старые хвосты: аффиксы,
рекавери/лук/прилавок, звуки, частицы (дождь день 5), arrival-слайд.

**6. Потом — обратно в реестр `postdemoaudit.md` §15:** `TEXT-1..3`
(`TEXT-1` ЖДЁТ АВТОРСКОГО ТЕКСТА владельца — сцена ведьмы `intro.cpp`, я её
НЕ сочиняю; `TEXT-2` — 7 площадок строками на языке мира, варианты списком;
Build-вкладка = одна внутримировая строка), `UI-2`→`UI-1` (одна цепочка
демо-отказа), затем режим C (`RPG-1`, `RPG-11`, `SUB-4`, `RPG-7`,
`CANON-S9`).

## Грабли (не наступать) — пополнено рефактором

- **Ссылка на ECS-компонент НЕ живёт через симулируемый тик**: спавн
  реаллоцирует хранилище, ссылка виснет молча (смоук читал 54/120 из
  освобождённой памяти). Перечитывать `get<>` после каждого тика.
- **Вью/читатель по снятому компоненту — МОЛЧА ПУСТОЙ проход**, не ошибка
  компиляции. Миграцию компонента вести по выписанной карте рядов и
  доказывать ПОЛНЫМ прогоном смоуков: так ловились опечатка вью ротации,
  файндеры смоуков и три сегфолта `get<Position>` на макро-сущности.
- **По-кадровые линейные сканы = тайминг-флаки свипа** (класс SMOKE-7):
  поиск сквада игрока кэширован в `reg.ctx()` (`PlayerSquadCache` с
  ревалидацией) — новые горячие поиски строить так же.
- Свидетель на «сделано лишний раз» — только на ЧАСТИЧНОМ расходе
  (двойной дебет невидим при выгребании досуха; ECON-1 прятался так).
- Инварианты копилок писать через `earned = бар + carry` (трункация int
  ломает «×2 раза = ×2 суммы»).
- Смоук, мутирующий ECS перед фото, откладывает кадр ≥1; frameless-ловушка.
- kLootProfiles/kCatalog/kAffixDefs в АНОНИМНОМ неймспейсе items.cpp — новые
  sm-функции ПОСЛЕ его закрытия (ловится линкером).
- Каталог предметов и ординалы сейва: только АППЕНД; бампаем версию,
  миграций нет (сейчас v86).
- int/int деление = тихий ноль; printf %f с int = UB; клампы
  effective_sheet поднимают атрибут 0→1.
- Смоук-сцепки: токены ПО ОДНОМУ; walk связкой subworld_enter; smoke.sh ИЗ
  КОРНЯ репо; compound `cd build && ...` из чужого cwd молча не собирает.
- Проверять на -O3 (боевые флаги), не на -O0; zsh ломается об `===` в
  командах.

## Очередь после демо — крупное и мелочь (НЕ ПОТЕРЯТЬ)

0. СЛУЖБА ФРАКЦИИ — отложена за демо (memory faction-service-track).
1. МЕТАНИЕ: `Delivery::Thrown` зарезервирован; огнестрел = будущий
   Missile-ряд с жирным флэтом.
2. АРТЕФАКТЫ/УНИКИ: дверь готова (set_affix + врождённая колонка bonus) —
   нужен стол артефактов и раскладка по миру (контент).
3. ЛУТ-КЛЮЧ НА ПЕЙЛОАД: сундук/труп с CorpseLoot без Structure не находится
   взглядом.
4. СМОУК-ПРИМИТИВЫ: goto_landmark / interact_aimed / take_stairs; идея
   «смоук-токен = консольная команда».
5. UI-ТЕКСТУРЫ: VK_ERROR_OUT_OF_POOL_MEMORY (портреты 48×48 без капа).
6. Класс «вход не сбрасывает состояние движка»: ревизия полей
   SubworldEngine, переживающих leave().
7. Стены канваса следов + сплэши дождя (трек частиц).
8. Лут-профили/магазины новыми оружейными рядами (контент, числа гайдед);
   .wav-ассеты в assets/sound (перекрытие синтеза автоматом).

## Спящие ветки (сознательно, НЕ дефекты)

- material/quality байты ItemRef едут в сейве без писателей/читателей —
  проснутся внутри value_of со своими таблицами (контент-стадия).
- Форажинг ИГРОКА спит: Adventurer.upkeep = kNpcUpkeepNone.
- XP-гранты quest_engine/effect_applicator на БАЗОВОМ листе — дверь приедет
  с переездом Sheet-компонента (корень §41).
- Body/Mind-теги спеллов спят до апдейта паладинов/клириков (S15).
- HUD-полоска рекавери — отложена.
- spell_heal/baseHeal нули у всех — контент; лечащие спеллы отложены.

## ЭКОНОМИКА — отложенная очередь (вернуться после РПГ)

Хвосты трека баланса (эталоны /tmp/guard512_*, /tmp/scent512_*,
balance_out_navgraph.log; balance_run гонять только при изменении законов
экономики/АИ): тюнинг популяции дубль-прогоном (popEnd 103–118k против
navgraph 250–275k; крутилки следа/охоты НЕ свипованы); минт 999/12345 = 0
(геология к подножиям — вердикт записан); вселение vs растворение ротации
(problems.md §35, вердикт не снят); отказ ценой, рейсовый паром,
военно-полит слой §34.6, кран спавн-кошельков, перф §34.8 — начинать с
профиля 7×32.
