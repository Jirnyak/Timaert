# Промт следующей сессии — ДЕВ-КОНСОЛЬ ДЛЯ ТЕСТОВ + castTime

Обновлён 2026-09-07. Прочитай AGENTS.md; режим гайдед — код по одобрению,
вердикты в CANON.md. Стадия: ЦЕЛЕВОЙ БАГФИКС/ПОЛИШ перед контентом демо
(трек = memory bugfix-prerelease-track). Сценарий демо и имя — финальные
(см. CANON/release.md).

## ✅ СДЕЛАНО 2026-09-06/07 — багфикс-сессия №1 (два трека + дверь восстановления)

1. СТОЛБЫ-СЕПАРАЦИЯ: летуна больше не толкают с земли — тела в мувере =
   вертикальные колонны своего РОСТА (BodyCrowd.height из Sprite.height =
   ряд × телосложение), толчок только при пересечении колонн; дракон 3.5м
   толкает то, что над человеком свободно. Глаза владельца ✅.
2. БЛОК ГОВОРИТ: дверь урона ставит HitFlash + DamageFx{blocked} → искра
   Spark вместо крови, статус/лог «glances off», универсально для всех
   ударов. Глаза владельца ✅ («не пробиваю стражей, смог критануть»).
3. ЗВУК РОДИЛСЯ: SfxId MeleeSwing/Hit/Blocked — первые живые ряды с аудита
   2026-08-29; ЗАКОН РЕЗОЛВЕРА ДЛЯ ЗВУКА: нет .wav → процедурный синтез
   при init (audio.cpp synth_sfx_chunk, детерминированный), файл в
   assets/sound перекроет. Мост engine→app: take_pending_sfx.
4. ДВЕРЬ ВОССТАНОВЛЕНИЯ (CANON S14 «ОДИН РЫЧАГ НА РУЧКУ» — дух подхода для
   всех механик): recovery_steps = база ÷ quickness_pct(Spd) ÷ generic
   (Armsmaster/Spellcraft/Атлетика); типовые скиллы = только МОЩЬ; сырые
   Str/Intl-прибавки (вердикт 03-го отменён). БАЗА ОРУЖИЯ = МАССА
   (weapon_swing_seconds: рука 1.5с + 0.5с/кг; колонки скорости у оружия
   НЕТ); гуманоидные ряды НПЦ на той же кривой, звери — свои темпы.
   kPlayerMeleeCooldown и float-таймер игрока мертвы (гейт =
   Combat.cooldownSteps, общий тик). Пустой взмах платит кулдаун и свистит.
5. Логи владельца: TIMAERT_COMBAT_LOG=1 → [melee]-строки с cd=; прогон №1
   прочитан (145 свингов), темп переякорен по вердикту «новичок = слабак».
   ЖДЁТ ГЛАЗ: второй прогон темпа (кулак ~1.4с, страж ~2.2с, звери быстрые).

## План следующей сессии (порядок предложен, владелец решает)

1. ДЕВ-КОНСОЛЬ = ЕДИНАЯ ТЕСТ-СИСТЕМА (вердикт владельца 2026-09-07:
   «командами собирать себе состояния игры; минимум систем — максимум
   функциональности»). Машинерия есть (app/debug_console.h: реестр команд
   без свитча, register_cmd в main.cpp) и уже покрывает: spawn (таблица
   НПЦ/монстров) / spawn_squad / spawn_fauna / test_battle / give-take
   (каталог предметов) / gold / items / spells / learn / learnall / addexp /
   levelup / tp / heal / rest / killall / godmode / flight / possess /
   settime / simspeed. ДОБИВКА — недостающие столы тем же паттерном
   «таблица печатает свои id, команда пишет через СУЩЕСТВУЮЩУЮ дверь»:
   - `skills` (лист 33 рядов) + `skill <id|all> <rank>` — ранг через
     кламп-двери с рекомпутом максим (гейт «что-то изменилось»!);
   - `attrs` + `attr <id|all> <score>` — то же для восьмёрки;
   - `loots` + `loot <profileId> [rolls]` — броски ЧЕРЕЗ единый лут-реестр
     (тот же, что смерть/пропы) прямо в инвентарь;
   - `gear` / `equip <itemId> [cell]` / `unequip <cell>` — через двери
     анатомии (BodyEquipment сквад-сущности, дверь листа отработает сама);
   - `sheet` — печать базового+эффективного листа с ТЕМПАМИ (cd руки,
     каст) — окно в дверь восстановления для тестов;
   - `exec <file>` — прогнать файл консольных строк: СОСТОЯНИЕ = текстовый
     файл из тех же команд, никакой системы пресетов (вопросы ниже).
   ВОПРОСЫ ВЛАДЕЛЬЦУ (обсудить до кода): §нюансы в чате сессии 2026-09-07.
2. ✅ СДЕЛАНО ТОЙ ЖЕ СЕССИЕЙ: castTime = честный замах (был UI-лжецом):
   не-sustained микро-каст вылетает через recovery_steps(castTime, лист,
   Спеллкрафт) шагов после нажатия (App.pendingCast*, резолв у
   spellbook_tick тем же resolve_active_cast, что мгновенный путь);
   M&M-дефолты v1: движение НЕ блокирует, уроном не прерывается, прицел
   в момент ВЫЛЕТА, sustained мгновенны, выход/смерть роняет спелл.
   Смоуки ждут замах поллингом по App.pendingCastSteps (та же цифра).
   Отложено сознательно: индикатор замаха на HUD, звук каста.
3. Хвосты глаз: лужа смерти/капель/угли+дым/дождь (день 5 ~07-12ч) — из
   трека частиц; второй прогон темпа боя; arrival-слайд (авторский текст
   владельца).
4. Звуки-ассеты: когда появятся .wav — просто положить melee-swing/hit/
   blocked.wav в assets/sound (перекрытие автоматом); очередь на новые
   SfxId-ряды: входящий удар по игроку, каст, смерть.

## Грабли (не наступать)

- «ИГРОКУ X, НПЦ Y» — ЗАПРЕЩЁННАЯ РАМКА (владелец капсом, дважды за
  2026-09-07): игрок особен только камерой/инпутом/входами; любой тюнинг
  формулируется по рядам и законам мира (memory: no-player-special-framing).
- Позиционные init при росте POD-структур — греп ВСЕ сайты (BodyDesc.height
  сдвинул бы speed у любого позиционного, ловилось ворнингом literal-conv).
- ПЕР-ШАГОВЫЕ пересчёты дериватов — ТОЛЬКО с гейтом «что-то изменилось»
  (безусловный recompute воскрешал spRegen — cde1ec5).
- КЛАМП effective_sheet поднимает атрибут 0→1 — ожидания тестов обязаны
  ходить ТОЙ ЖЕ дверью; Attributes{} по умолчанию = ЕДИНИЦЫ, не нули
  (тест закона восстановления ловил ровно это).
- int/int деление дробей ран = тихий ноль; printf %f с int-аргументом = UB.
- Смоук-сцепка macro_travel_sp,macro_recovery,subworld_recovery одной
  сессией красна и на базлайне — гонять токены ПО ОДНОМУ; walk — связкой
  subworld_enter,subworld_walk.
- Рукотворный NavWorld в тестах: подгони bakedSeed/bakedLandmarks/mapW/H.
- Ординалы сейва навсегда: append-only; бампаем версию, миграций нет (v81).
- zsh: `===`-разделители в командах ломаются; писать echo-маркеры словами.

## Спящие ветки (сознательно, НЕ дефекты)

- Форажинг ИГРОКА спит: Adventurer.upkeep = kNpcUpkeepNone → сквад игрока
  хлеб не ест вовсе — вопрос экономики владельцу.
- XP-гранты quest_engine/effect_applicator на БАЗОВОМ листе — дверь приедет
  с переездом PlayerState (~550 мест, отдельный заход).
- Body/Mind-теги спеллов спят «до апдейта паладинов и клириков» (S15);
  с вердиктом «один рычаг» их спелл-прибавка = сырой INT (Спеллкрафт ушёл
  в темп) — сила без школьного множителя до пробуждения школ.
- Рост тел в толпе: тела без Sprite (голые фикстуры) берут человеческую
  колонну по умолчанию — честная дверь пер-тела уже есть (Sprite.height).

## Очередь после демо — крупное и мелочь

0. СЛУЖБА ФРАКЦИИ — ОТЛОЖЕНА ЗА ДЕМО (вердикт владельца 2026-09-06: от неё
   ничего не зависит, сценарий демо её не использует); трек 2026-09-05
   (присяга → не враги → войны за жалованье) взведён, детали = гайдед-разбор
   при заходе (memory: faction-service-track).

1. ЛУТ-КЛЮЧ НА ПЕЙЛОАД: сундук/труп с CorpseLoot без Structure не
   находится взглядом — ключ интеракции должен быть на пейлоад, не на вид.
2. СМОУК-ПРИМИТИВЫ (п.5 трека реестров): goto_landmark / interact_aimed /
   take_stairs — недостроенные кирпичи смоуков. (+ идея 2026-09-07:
   «смоук-токен = консольная команда», один словарь — обсудить при заходе.)
3. UI-ТЕКСТУРЫ ЛОВУШКА: segfault-риск VK_ERROR_OUT_OF_POOL_MEMORY
   (портреты 48×48 без капа в дескрипторном пуле) — короткий заход.
4. КЛАСС БАГА «вход не сбрасывает состояние движка»: ревизия полей
   SubworldEngine, переживающих leave(), не делалась (pendingSfx уже
   чистится с 2026-09-06).
5. Стены канваса следов + сплэши дождя — за демо (трек частиц).

## ЭКОНОМИКА — отложенная очередь (НЕ ПОТЕРЯТЬ; вернуться после РПГ)

Хвосты трека баланса (эталоны: /tmp/guard512_*, /tmp/scent512_*, старший —
balance_out_navgraph.log; balance_run НЕ в check, гонять только при изменении
законов экономики/АИ):
1. ТЮНИНГ ПОПУЛЯЦИИ дубль-прогоном: popEnd 103-118k против navgraph 250-275k.
   Ручки: N строк ростера, labourShift, темп вендоров, веса скора аукциона,
   страх/патруль, крутилки следа/охоты (kScentDecayDays=4, kScentDiffusionShift=3,
   kScentQuantShift=2, kHuntBoldShift=1, kHuntScentFloor=8 — НЕ свипованы).
2. Минт 999/12345 = 0: геология к подножиям (вердикт записан).
3. Вселение vs растворение ротации — problems.md §35 (вердикт не снят).
4. Отказ ценой (провиант-порог), рейсовый паром, военно-полит слой (§34.6),
   кран спавн-кошельков (honest-spawn-wealth), перф §34.8: НАЧИНАТЬ С ПРОФИЛЯ
   7×32 (подозреваемые: deposit каждого think и дневная диффузия 34 планов —
   лечатся по-разному). Замер scent 4×512: минт сид 1 −26% (охота режет караваны
   серебра?) — посмотреть при тюнинге.
