# Готовые промпты для отдельных сессий — треки из аудита 2026-08-03

Каждый промпт самодостаточен: копируй целиком в новую сессию Claude Code.
Все ссылки file:line проверены на HEAD после P0-фиксов (a2f3c27); если код
сдвинулся — сессия обязана перепроверить grep'ом. Общий протокол зашит в
каждый промпт: **план → моё одобрение → код+тесты → диф → коммит после «ок»**.

---

## Открытые проблемы, найденные в ходе трека P1 (2026-08-05)

Не баги «сломано сейчас», а расхождения между тем, что игра ОБЕЩАЕТ, и тем,
что она делает. Каждая либо уже отдана в сессию ниже, либо ждёт решения
владельца. Всё перепроверено grep-ом на HEAD.

1. **Четыре атрибута из девяти ничего не делают.** `calculate_derived`
   (macro/attributes.h) считает шесть производных, а до игры доходят ДВЕ —
   `rawPhysDamage` и `rawSpellDamage`. `critBase` (LCK), `expMult` (WIS),
   `tradeDiscount` и `relationBonus` (CHA) читаются ТОЛЬКО в UI: критов в
   проекте нет вообще (grep по «crit» — ни одного попадания в боевом коде),
   множитель опыта не применяется при начислении, торговая скидка и бонус к
   отношениям не участвуют ни в торговле, ни в дипломатии. То есть LCK, WIS и
   CHA сейчас — косметика в листе персонажа. `moveSpeedMult` был таким же
   (показывался как «Move x1.24», до ног не доходил) — его подключили
   2026-08-04; остальные ждут. → относится к Сессии 9 (закон атрибутов).
2. **Баланс выносливости не сходится в игре.** Марш и покой разведены
   (kMarchRecoveryPct), цена калибрована в игровых часах, но покой возвращает
   10 SP за игровой час = **2.4 SP за РЕАЛЬНУЮ секунду**, а клетка луга стоит
   0.4 SP: секунда стояния оплачивает шесть клеток. Три ручки на выбор
   владельца — скорость восстановления на покое (рекомендуется), цена клетки,
   длина суток. Связано с Сессией 11: если сутки удлинятся, часть уйдёт сама.
3. **Смоуки — ЗАКРЫТО 2026-08-05.** Все 30 сценариев зелёные на четырёх
   мирах, ctest 43/43. Главное, что вскрылось: `smoke.sh` не задавал сид, а
   `choose_new_game_seed` берёт `SDL_GetTicks()` — каждый прогон был ДРУГОЙ
   планетой, поэтому «пять красных» в старых заметках оказались девятью.
   Теперь сид пришпилен (12345) и есть свип:
       sh smoke.sh cast_spell 12345,1,7,999
   **Гоняй свип, а не только умолчание** — двое красных прятались именно за
   одним миром. Разбор пяти классов ошибок — в памяти
   `broken-smokes-2026-08-05`, их стоит перечитать перед написанием смоука.
4. **Класс бага: постоянное состояние движка, не сброшенное во входе.**
   `SubworldEngine::enter()` не задавал `playerZ_`, и тот хранил высоту конца
   ПРОШЛОЙ сессии субмира — вход в низину после долгого пути ронял игрока с
   неба (починено cb45191). Остальные поля движка во входе заслуживают такой
   же ревизии: что ещё переживает leave() и не переустанавливается?
5. **Привала/сна в игре нет.** `kRestRecoveryPct = 0.10` объявлена в
   movement_cost.h и не используется НИГДЕ: восстановление только пассивное,
   по времени. При этом выносливость теперь спроектирована вокруг «марш →
   отдых», и отдых пока что означает просто «стоять».
6. **`EventTag::LandmarkChangeOwner` не эмитится ниоткуда.** Цель квеста
   InteractCell теперь задаёт контракт (событие несёт клетку в ix/iy), но
   эмиттера нет — то есть смена владельца ландмарка в мире не происходит или
   не сообщается. → смежно с Сессией 2 (макро-стратегия отсутствует).
7. **Добавление навыка трогает пять мест** (поля Skills, enum SkillId, два
   свича skill_value, таблица UI, веса ролей) и ~48 обращений по имени поля.
   → Сессия 8 (реестр навыков).
8. **Время — четыре независимых ритма** и никакого фиксированного тика.
   → Сессия 11.
9. **`target_radius` разошёлся на две функции — ЗАКРЫТО 2026-08-05.** Теперь одна
   `body_radius()` в `sub/body.h`, и **всё читает таблицы** (решение владельца).
   Разбор и таблица «что реально сдвинулось» — `problems.md` §18.
   Остался сознательный долг: шаг 5 читает `Sprite.scale` — визуальный размер
   билборда — как физическую ширину. Лечится выдачей настоящего радиуса каждому
   спавнеру, после чего шаг удаляется; пока живёт в ОДНОМ месте.
10. **`Projectile.lifeTimer` носит три разных смысла** (разбор — `problems.md`
   §17): дальность (ракеты NPC: `(attackRange+4)/speed`), фитиль (метеоры
   армагеддона: `0.3+delay` при `v=0`, `explodeOnExpiry`) и страховку от вечного
   полёта у болтов игрока. Владелец 2026-08-05 сознательно ОТЛОЖИЛ разведение:
   когда снаряды получат параболу, бюджет дальности выпадет из физики сам
   («минимум систем, максимум функционала»). Страховка уже не нужна — коробка
   окна закрыта с шести сторон. За фитилём смысл владелец признал.
   Возвращаться к этому ВМЕСТЕ с параболой, не раньше.
11. **Прицел и снаряд сведены не до конца.** Высота ствола поднята к глазу
    (`kBodyEyeM`, `problems.md` §17а), и вплотную теперь попадает — проверено
    владельцем в игре. Но его исходная жалоба была шире: «прицел не совпадал со
    снарядом… надо всё собрать в единую систему». Остаток не диагностирован.
    Кандидаты на проверку: перекрестье UI против фактического направления
    `(cos yaw·cos pitch, sin yaw·cos pitch, sin pitch)`, поправка на смещение
    дула вбок, и что именно рисует прицел на экране. СПРОСИ владельца — он
    предложил щупать и объяснять детали в игре.
12. **Документация отстала на эпоху** (ARCHITECTURE описывает удалённые
   сущности) плюс ~190 упоминаний TypeScript как источника истины.
   → Сессии 4 + 7, делать одним проходом.

---

## Сессия 1 — P1-баги (фракции + SP-долг + квестовые разрывы) — ✅ СДЕЛАНО 2026-08-04

Закрыта целиком (коммиты 9450318..ac44afb), с добавками, которые владелец решил
по ходу: «вольные» как фракция всего ничейного, игрок — обычная строка реестра
(репутация = его строка матрицы), фракция отряда = фракция владельца сквада,
спавн без владельца наследует фракцию земли, `travel`/`athletics` и общий закон
навыков «ранг = процент, кап 100», выносливость — один закон на оба слоя с
раздельными состояниями «марш/покой». Промпт ниже оставлен как история задачи.

```
Работаем по протоколу: план → моё одобрение → код+тесты → показать диф →
коммит только после моего «ок». Каждый фикс — отдельный коммит. Ничего не
менять без согласования.

Почини пакет P1-багов из аудита 2026-08-03 (см. память audit-2026-08-03):

1. ФРАКЦИИ ГОРОЖАН: src/sub/spawn.cpp:167 и :458 дают КАЖДОМУ жителю
   поселения faction_index("empire") независимо от королевства-владельца —
   город Old Magica спавнит имперцев, субмир противоречит макромиру.
   Правильный образец уже есть: settlement_faction_index() в
   src/macro/npc_spawn.cpp:127. Прокинь kingdomIdx из resolve_context
   (sub/engine.cpp ~:1005) в спавн населения. Заодно:
   src/content/quests/procedural.cpp:63 faction_of() игнорирует контекст и
   всегда возвращает "empire" — квесты чужих городов качают репутацию
   империи; верни фракцию королевства из ctx.
2. SP-ДОЛГ УТЕКАЕТ В HP: src/macro/travel.cpp:75-78 и вербатим-копия в
   src/app/main.cpp:1740-1743 — SP не флорится в 0, долг компаундится в HP.
   Один хелпер, оба места, юнит-тест на границу.
3. DestroyNpc сравнивает entity-id с ordinal ТИПА:
   src/events/quests/quest_engine.cpp:126 `(int(ev.a) == o.npcType || ev.ix
   == o.npcType)` — засчитывает не те убийства; тот же паттерн на :146 для
   interact_cell. Почини семантику + тест.
4. Квестовый XP не даёт level-up: quest_engine.cpp:77-79 голый `exp +=`;
   единственный вызов try_level_up — sub/engine.cpp:2019. Прогони награду
   через тот же путь.
5. Фракции сравниваются ПО УКАЗАТЕЛЮ: src/sub/engine.cpp:1740-1741
   `a == kPlayerFactionId` для const char* — работает только из-за интерна.
   Переведи relation-callback на интернированные int-индексы
   (battlePlayerFaction_ уже есть, engine.cpp:1768).

Верификация: полная сборка -O3, ctest (сейчас 41/41 + новые), для фракций —
негативный контроль в тесте (город не-империи ⇒ не empire).
```

## Сессия 2 — O(N·M) сканы спеллов → bucket-сетка
> Переписана 2026-08-05 после разведки: задача та же, но выяснилось ТРИ
> подводных камня, которых в старом тексте не было, и владелец принял решение
> по мирным жителям. Все file:line сверены на HEAD 2026-08-05.

```
Протокол: план → моё одобрение → код+тесты → показать диф → коммит после
моего «ок». Ничего не менять без согласования.

ПРОБЛЕМА. src/sub/spell_effects.cpp нарушает записанный закон проекта
(ARCHITECTURE.md:52 «Never write O(N²) scans for proximity, line-of-sight, or
AI targeting»). Полный скан всех живых тел на КАЖДЫЙ снаряд КАЖДЫЙ тик:
  • find_projectile_hit  — spell_effects.cpp:128
  • apply_spell_blast    — :149
  • apply_spell_beam     — :176
  • apply_spell_chain    — внутренний view на :251
problems.md:88 и :99 хранят разбор ровно этой формы бага (тогда это стоило
~10⁶ пар/тик), так что прецедент есть и он дорогой.

ГОТОВАЯ СТРУКТУРА УЖЕ ЕСТЬ И УЖЕ ОПЛАЧЕНА. Движок строит две bucket-сетки
каждый тик: build_unit_grid(battleFine_, …) на engine.cpp:1950 и
build_unit_grid(battlePick_, …) на :1952 (тип UnitGrid, battle.h). battlePick_
называется буквально «contact picking» — сетка «чего тело касается», то есть
ровно то, что спрашивает снаряд. battleEnts_ даёт обратное отображение
index→entity. Порядок тика правильный: сетки строятся ДО вызова
tick_spell_projectiles (engine.cpp:2581), значит они свежие. Образец обхода
бакетов — contact_scan в battle.cpp:464-491. Для луча есть рабочий DDA
bin-walk в sub/collide.cpp.

КАМЕНЬ 1 — НАБОРЫ ЦЕЛЕЙ НЕ СОВПАДАЮТ. battle_ собирается по
  reg.view<Position, Health, Combat, SubworldTag>   (engine.cpp:1857)
а снаряд бьёт всё, что проходит is_spell_target: Position + Health, живое,
с SubworldTag или PlayerTag — БЕЗ Combat. То есть тела без Combat поражаемы,
но в сетке их нет, и подмена скана сеткой «в лоб» молча перестанет по ним
попадать. Это тот самый класс тихой поломки из problems.md:117 («пустая маска
— бесшумная ошибка»). Мишень в смоуке cast_spell как раз без Combat, так что
регрессия поймается — но полагаться на удачу нельзя.

РЕШЕНИЕ ВЛАДЕЛЬЦА (2026-08-05): расширить боевой набор до ВСЕХ тел с Health,
и «безоружные — это не наша забота, все честно идут по своей фракции; что
крестьянин безоружный — это его проблема, у него для этого есть flee».
То есть НИКАКИХ спецслучаев: реальная фракция из NPCKind, никаких -1, никаких
«пассивных» записей. Следствие, которое владелец принимает осознанно: мирные
становятся видимы для ИИ и бандиты начнут их резать — это желаемое поведение,
деревня оживает. Учти при сборке: d.speed/d.reach/d.sight читаются из Combat
(engine.cpp:1868-1870) — для тел без Combat нужны честные нули, и проверь,
что разброс позиций (engine.cpp:1981-1993) и цикл ударов (:2030) ведут себя
с ними корректно (у безоружного нет чем бить — он не должен попасть в strike).

КАМЕНЬ 2 — БЮДЖЕТ ПОСЕЩЕНИЙ. contact_scan ограничен prm.maxPickVisits
(battle.h:343, сейчас 64) и ДЕТЕРМИНИРОВАННО обрывает перебор. Для наведения
ИИ это законное приближение. Для попадания снаряда — НЕТ: промах из-за
исчерпанного бюджета это баг, а не приближение. Запрос снаряда обязан быть
полным по своему радиусу; не переиспользуй бюджет вслепую.

КАМЕНЬ 3 — СЕТКА НА НАЧАЛО ТИКА. Сетка построена по позициям после разброса
steer_battle, а снаряды двигаются ПОСЛЕ. Значит сетка — только широкая фаза:
расширь радиус запроса на максимальный шаг тела за тик, а точную проверку
делай по реальной Position сущности. Иначе широкая фаза начнёт терять цели.

ФОРМА ПРАВКИ (согласована с владельцем). spell_effects.cpp намеренно ничего
не знает о движке и получает колбэки (heightFn, solidFn, canHitFn) — держись
этого паттерна. Добавь колбэк ПЕРЕЧИСЛЕНИЯ КАНДИДАТОВ, например
  int (*neighborsFn)(void* user, float x, float y, float r,
                     std::uint32_t* out, int maxOut)
Тогда широкая фаза (сетка) живёт в движке, который ею владеет, а правила
попадания — is_spell_target и точная 3D-дистанция — остаются в spell_effects.
nullptr = старый полный скан, чтобы headless-тесты и юниты не сломались.

ВЕРИФИКАЦИЯ. Различай две вещи, они тянут в разные стороны:
  • семантика ПОПАДАНИЙ обязана остаться прежней — это чисто структурный
    переход, и его надо доказать: сцена с фиксированным сидом, старый скан vs
    новый запрос дают ИДЕНТИЧНЫЙ набор целей (старый скан оставить в тесте
    эталоном, не в продакшене);
  • поведение ИИ ИЗМЕНИТСЯ намеренно (мирные становятся целями) — это решение
    владельца, а не регрессия. Не «чини» его обратно, если смоук заворчит:
    покажи владельцу, что именно изменилось, и спроси. Плюс полная сборка -O3, ctest (43/43 на 2026-08-05), и смоуки
на НЕСКОЛЬКИХ мирах — sh smoke.sh <token> 12345,1,7,999 (см. память
broken-smokes-2026-08-05: сид теперь пришпилен, свип ловит мирозависимое).
Обязательно прогнать: cast_spell, subworld_self_fireball,
subworld_missile_feedback, subworld_reputation_hit, subworld_enemy_feedback,
trigger_battle_start, attack_first_npc.
```

## Сессия 3 — Чистка мёртвого кода (~1200 строк) + тулинг

```
Протокол: план → одобрение → код → диф → коммит после «ок». Это
МЕХАНИЧЕСКАЯ чистка по готовому списку аудита 2026-08-03 — ничего не
рефакторить сверх списка.

Удалить (подтверждено: 0 вызовов, только decl+def):
- src/macro/flag_generator.{h,cpp} целиком (285 строк, ноль включений)
- shaders/npc_sprite.glsl + его строка в CMakeLists.txt:223
- dispatch.cpp: rect_clear_for_urban (:353), flatten_footprint (:397) —
  вытеснены add_house_obb; поправь устаревший заголовок-коммент
  tests/house_pad_flatten_test.cpp (он теперь локает add_house_obb)
- 14 никем не вызываемых функций: player_sell_price, resources_for_terrain
  (economy.cpp), generate_settlement_inventory (items.cpp:300 — но проверь
  память: она «dead-but-kept as instructed» для unified-container — СПРОСИ
  меня), generate_unique_name, default_vowels/consonants (language.cpp),
  reset_animation, animation_name, category_name (character_paperdoll.cpp),
  spell_duration, spellbook_can_cast (spell_book.cpp)
  [tick_projectiles (ecs/systems.cpp) — УЖЕ УДАЛЁН 2026-08-05, не ищи его]
- 18 мёртвых inline: core/math.h (mat4_perspective/ortho/translate/scale/
  rotate_y, v2, v4 — GL-эра), sub/map_data.h (opposite_dir, dir_from_offset,
  angular_distance, find_tile_near), sub/camera.h (move_vector,
  move_vector_3d), macro/army.h drain_squad, attributes.h exp_from_quest,
  remove_perk, markers.h remove_marker, map_factory.h structure_is_decayed
Затем: включи -Wunused-function (убери -Wno-unused-function из
CMakeLists.txt:209 и из тест-таргетов) и почини всё, что вылезет.
Отдельным коммитом: вычисти 19 EMSCRIPTEN-веток из CMakeLists (браузерный
таргет мёртв, workflow уже удалён) — с полной пересборкой и ctest.
Отдельное решение (спроси меня): vk_sprite_array.{h,cpp} (486 строк,
собран но не подключён — удалить или доделать миграцию UI-папердолла) и
u_riverMap (мёртвый GPU-биндинг: macro.frag:12 + vk_macro_renderer.cpp:
176-183, 4× раздутие; удаление = сдвиг биндингов 4→3, 5→4).
Верификация: полная сборка -O3 зелёная, ctest 41/41, validated GPU smoke.
```

## Сессия 4 — Синхронизация документации с кодом
> Делать ВМЕСТЕ с Сессией 7 (чистка упоминаний TypeScript) — это один проход по
> тем же файлам. Поправки после 2026-08-04: kSaveVersion теперь **17**, тестов
> **42**, у `Skills` появился `athletics`, у фракций — строки `freefolk` и
> `player`, `spawn_hostile_npc` переименован в `spawn_npc_body`.

```
Протокол: план → одобрение → правки → диф → коммит после «ок». Только доки,
код не трогать.

Доки отстали от кода на эпоху (аудит 2026-08-03, память audit-2026-08-03):
1. kSaveVersion: ВЕЗДЕ написано 10, код = 15 (src/macro/state.h:34, история
   бампов v11-v15 в state.h:20-32). ARCHITECTURE.md ×4, MASTER_PROMPT.md ×3,
   audit.md.
2. ARCHITECTURE.md:53 и :841 требуют sm::SpatialHash — ФАЙЛ УДАЛЁН. Перепиши
   правило вокруг реальных механизмов (sub/collide.h bin grid, battle.h
   UnitGrid).
3. FT_Tree удалён в v14 (features.h:18-26: FT_None/Road/DirtRoad, леса — в
   tree_layer.h), а ARCHITECTURE:473-529 описывает старую схему.
4. ARCHITECTURE:802-810 «ground pinning» — ОТМЕНЁН честной гравитацией
   (92e86b1): height.h vertical_step, ecs::Airborne. Опиши актуальную
   физику (po2-семья уже задокументирована в Discreteness & Number Style).
5. Занижения: структуры/тени/точечные света давно рендерятся
   (ARCHITECTURE:1149-1207 говорит «not implemented»), частицы вообще не
   упомянуты; 512-кап теперь kMaxEntityInstances=16384.
6. Счётчики тестов: «28/28» ×16 мест (реально 41 ctest / 44 таргета —
   лучше заменить на формулировку без числа).
7. audit.md: добавь Pass-5 шапку — закрыто §8.2/§8.8/§6.4/§6.5.4/§4.3
   (токен: проверено, живого ключа в истории НЕТ), P0 из аудита 2026-08-03
   исправлены; открытое перечисли явно.
Проверяй каждое утверждение grep'ом по коду, не доверяй старому тексту.
```

## Сессия 5 — Частицы: blend-класс «материя» (живой баг)

```
Протокол: план → одобрение → код → диф → коммит после «ок». Это
графика/Vulkan — верификация глазами обязательна.

Живой шипованный баг: кровь и пыль рендерятся АДДИТИВНО — кровь светится
как лампа, пыль выгорает в белый шар. Полный разбор и дизайн фикса:
proposals/particle-matter-blend-class.md (сальваж моей прошлой сессии).
Суть: FxPreset (src/sub/particles.h:46-57) не имеет поля blend-режима, а
единственный particle-пайплайн захардкожен additive
(vk_renderer_3d.cpp ~:518-525). Фикс: второй класс материала alpha-over
(матерь) рядом с additive (энергия), выбор — ОДНИМ полем таблицы пресетов;
партиционируй инстанс-пак по классу, второй пайплайн через уже существующий
create_mesh(additive=false) + matter-фрагмент-шейдер (straight alpha, без
white-hot lift из particle.frag). Blood/Dust → matter, Ember остаётся
energy. Верификация: gpu_smoke3d ночная пара (см. память
subworld-universal-lighting: GPU_SMOKE_FX + capture-хуки), СМОТРИ кадры
глазами: кровь тёмно-красная и непрозрачная, пыль песочная, магия/огонь не
изменились. Validated smoke exit 0. Помни правило: мутация ECS + фото =
отложить капчу ≥1 кадр (память gpu-smoke3d контракт).
```

## Сессия 6 (бонус, когда дойдут руки) — interaction-система

```
Протокол: план → одобрение → код → диф → коммит после «ок».
Реализуй по готовой спеке proposals/subworld-interaction-system.md:
nearest_lootable() ключом по payload (ecs::CorpseLoot), а не по
Structure::Corpse — семя универсальных контейнеров (сундук/мешок/бочка без
изменений движка). Спека содержит файлы, тест-план (5 кейсов) и критерии
верификации. Behavior-preserving для существующего лута.
```

---

## Сессия 7 — документация: миграция с TypeScript ЗАВЕРШЕНА

Готовить эту сессию имеет смысл последней в треке P1: правки выносливости
тоже меняют rpg.md / progression.md.

```
Работаем по протоколу: план → моё одобрение → правки → показать диф →
коммит после моего «ок». Только документация и комментарии, НИ ОДНОЙ правки
логики кода — если найдёшь расхождение кода с доками, чини ДОКУ, а код
выноси в отдельный список ко мне.

КОНТЕКСТ (решение владельца 2026-08-04): миграция с TypeScript завершена.
C++ в src/ — единственная и полноценная версия игры. Паритета с TS больше
нет и никогда не будет; решения принимаются по существу («как лучше и
универсальнее и элегантно для проекта, минимум систем максимум
функциональности»). timaert_c/ удалён в уборке 2026-08-03. Старые сейвы
ничего не стоят (AGENTS.md:162).

ЗАДАЧА. Вымести из проекта утверждения, что TS — источник истины, и
обновить факты, отставшие на эпоху. Объём: ~82 упоминания в src (комментарии)
и ~110 в *.md. Каждое — перепроверить grep-ом на HEAD, а не править вслепую.

1. Формулировки. «Mirrors state.ts», «TS-faithful», «port of», «TS
   authority», «паритет» — переписать на утверждение о самом коде. Где
   комментарий объясняет ПОЧЕМУ так сделано, сохранить причину, убрав ссылку
   на TS как на авторитет. Тесты с именами *_parity_test НЕ переименовывать
   (это отдельный трек, скажи мне отдельно, если считаешь нужным).
2. Стухшие факты, найденные аудитом (перепроверь каждый!):
   • ARCHITECTURE.md:327 — enum Lineages (Empire|Magika|Timaert|Barbarians)
     как источник отношений фракций: enum УДАЛЁН, отношения идут через
     темпераменты в macro/faction.h;
   • kSaveVersion: доки говорят 10, в коде 16 (state.h);
   • FT_Tree удалён (v14), ARCHITECTURE его ещё описывает;
   • правило про sm::SpatialHash ссылается на УДАЛЁННЫЙ файл;
   • ground-pinning в доках противоречит честной гравитации (sub/height.h);
   • потолок деревьев 512 → 16384 (tree_layer);
   • счётчики тестов (в доках 28, реально 42 в ctest).
3. Новое из трека P1 2026-08-04 (проверь по git log, коммиты 9450318..):
   фракция = королевство-владелец поселения; «вольные» (freefolk) — фракция
   всего ничейного; ИГРОК — обычная строка реестра «player», его репутация
   ЕСТЬ его строка матрицы отношений (PlayerState::reputation удалён);
   фракция отряда = фракция владельца сквада; спавн без владельца наследует
   фракцию земли (politik.cellOwner); spawn_hostile_npc переименован в
   spawn_npc_body; award_exp — единственный путь начисления опыта;
   kNoNpcType в контракте события NpcDeath. Опиши это там, где доки
   описывают фракции/репутацию/квесты (design.md, quests.md, rpg.md,
   ARCHITECTURE.md, macroworld.md).
4. audit.md — пометить закрытые пункты, а не переписывать историю.

ВЕРИФИКАЦИЯ: `grep -ri 'typescript\|\.ts\b\|TS-faithful\|паритет' src *.md`
должен возвращать только осмысленные остатки (например историческую справку
в MANIFEST/audit), и ты обязана объяснить каждый оставшийся. Сборка -O3 +
ctest прогнать в конце (правки комментариев не должны ничего сломать).
```

---

## Сессия 8 — ОДИН реестр навыков (плоская таблица вместо пяти мест)

```
Работаем по протоколу: план → моё одобрение → код+тесты → диф → коммит после
моего «ок». Ничего не менять без согласования.

КОНТЕКСТ. Закон навыков уже принят и работает (rpg.md «Attributes add, skills
multiply»): атрибут даёт прямую прибавку, навык умножает, ОДИН РАНГ = ОДИН
ПРОЦЕНТ, кап kMaxSkillRank = 100, выражается через skill_bonus_mult /
skill_cost_mult (macro/attributes.h). Кап держится в одной двери —
spend_skill_point. Игра в разработке, навыков будет МНОГО.

ПРОБЛЕМА. Добавить один навык сегодня = тронуть ПЯТЬ мест:
  1. поля struct Skills (macro/attributes.h)
  2. enum SkillId (там же)
  3. два свича skill_value (там же, const и не-const)
  4. таблица строк UI kSkillUiRows (src/ui/overlays.cpp:1095)
  5. таблица весов ролей RoleWeights::skill[] (macro/character_sheet.h:44,
     колонка на роль, порядок обязан совпадать с SkillId)
Итого ~48 обращений по имени поля по всему проекту (grep 'skills\.').
Это ровно то, чего в проекте уже нет у фракций (macro/faction.h) и биомов.

ЗАДАЧА. Сделать как у фракций: ОДНА строка данных на навык.
  • ranks плоским массивом: struct Skills { int rank[kSkillCount]; } —
    владелец требует DOD, плоские массивы (см. память «Discrete/integer style»);
    skill_value(s, id) становится &s.rank[int(id)] — свичи исчезают;
  • kSkillDefs[] : id, машинное имя, отображаемое имя, описание эффекта,
    вид множителя (бонус/стоимость) — UI итерирует реестр, не свою копию;
  • веса ролей — колонка в том же порядке, со static_assert на длину;
  • добавление навыка = одна строка реестра + одна колонка весов, и всё.
ОСТОРОЖНО: 48 мест вида skills.fighter → skills.rank[int(SkillId::Fighter)]
ЛИБО именованные аксессоры-обёртки (обсуди со мной, что читаемее). Skills лежит
в сейве POD-блоком → бампни kSaveVersion (сейв не мигрируем, AGENTS.md:162).
Тест: добавление фиктивного навыка не требует правок вне реестра; ранги
сохраняются/грузятся; кап и закон (rank==percent) продолжают держаться —
проверки уже есть в macro_travel_parity_test, не сломай их.
```

## Сессия 9 — закон АТРИБУТОВ (отложено владельцем 2026-08-04)

```
Протокол: план → одобрение → код+тесты → диф → коммит после «ок».

Навыки уже приведены к закону «ранг = процент, кап 100». Атрибуты — НЕТ, и
противоречат заявленному «атрибут даёт прямую прибавку»:
  • spd и lck асимптотичны: spd/(spd+50), lck/(lck+50) (macro/attributes.h
    calculate_derived) — насыщаются, балансировать чтением невозможно;
  • остальные линейны и БЕЗ КАПА (maxHp = 100 + vit*10 и т.д.).
Задача: привести к одному виду (линейно + кап, значение капа обсуди со мной —
навыки капнуты на 100), убрать асимптотики. ЭТО ЗАТРАГИВАЕТ БОЙ: крит
(critBase), скорость (moveSpeedMult), урон, торговлю, а значит и NPC —
CharacterSheet общий, project_combat выводит из него бой. Нужен балансовый
тест того же рода, что уже есть для путешествий (tests/macro_travel_parity_test
test_travel_balance_holds_its_intent): формулируй ЗАМЫСЕЛ в игровых величинах,
а не константы.
```

---

## Сессия 11 — ВРЕМЯ — ✅ СДЕЛАНО 2026-08-05 (одна целочисленная лестница: 64 тика/с, сутки 8192 тика = 128 реальных секунд, субмир ÷16, год 128 суток; `core/time.h`). Промпт ниже — история.

Владелец 2026-08-05: «нужно элегантное универсальное решение по игровому
времени — сколько тиков и секунд в сутках, сколько суток в году, насколько
медленнее в субмире». Это фундамент: на времени висят путешествие, отдых,
экономика, сезоны, ИИ, небесная механика и весь дневной цикл.

```
Работаем по протоколу: план → моё одобрение → код+тесты → диф → коммит после
моего «ок». Ничего не менять без согласования. Сначала ИЗМЕРЬ и покажи мне
таблицу, потом предлагай.

ЗАМЕРЕНО 2026-08-04/05 (перепроверь grep-ом, код мог сдвинуться):
  • ФИКСИРОВАННОГО ТИКА НЕТ. main.cpp главный цикл: dt = реальное время между
    кадрами, обрезается сверху 0.1 с; всё принимает dt. На 144 Гц шагов вдвое
    больше, чем на 60 Гц.
  • Мировые часы: world_tick.cpp advance_world_clock —
    fractionalMinute += dt * kWorldMinutesPerSecond, где
    kWorldMinutesPerSecond = 1440 / kWorldRealSecondsPerDay (world_tick.h),
    kWorldRealSecondsPerDay = 100 ⇒ 14.4 игровых минут в реальную секунду.
    Минуты/часы/сутки — целые в WorldTime, но НАКОПИТЕЛЬ дробный (float).
  • Субмир: kSubworldTimeScale = 0.10 (main.cpp) — часы там идут в 10 раз
    медленнее. Из-за этого одно и то же путешествие имеет разную цену «за
    игровой час» на двух слоях (в цене выносливости это уже обойдено: она
    считается за КЛЕТКУ, а не за время, см. macro/movement_cost.h).
  • Третий независимый ритм: макро-ИИ kAiTickSec = 0.5 с (macro/npc_ai.h).
    Четвёртый: дневная симуляция поселений — очередь pendingDailyTicks,
    по kSubworldDailyTicksPerFrame = 1 за кадр.
  • Год: kDaysPerSeason = 30 × 4 сезона = kDaysPerYear = 120 (macro/seasons.h).
  • Скорость ходьбы: kMacroWalkCellsPerSecond = 6 клеток/реальную секунду
    (macro/movement_cost.h) ⇒ 25 клеток за игровой час. Клетка ≈ 1024 тайла
    субмира (sub/map_data.h kCellSize) ⇒ при тайле ≈ 1 м это ~25 км/ч.
  • Восстановление: 10 SP/HP/MP за игровой час (macro/player_recovery.cpp),
    подавляется в пути (kMarchRecoveryPct). Полный бар ≈ 11 игровых часов.

ЧТО РЕШИТЬ (владелец хочет ДИСКРЕТНОСТЬ и степени двойки, где не вредит):
 1. Ввести ли ЦЕЛОЧИСЛЕННЫЙ мировой тик как единицу времени (а dt кадра —
    только его накопитель). Что это даёт: точный сейв/загрузку, отсутствие
    дрейфа, одинаковую симуляцию независимо от частоты кадров, переводы
    сдвигами. Владелец предложил сутки = 128 реальных секунд (2^7).
 2. НАТЯЖЕНИЕ, которое надо разрешить явно: в сутках 1440 игровых минут, а
    1440 не степень двойки. 128 с × 64 тика/с = 8192 тика/сутки (2^13), но
    8192/1440 = 5.69 тика на минуту — не целое. Варианты: показывать часы
    пересчётом из тиков (минута — производная, не хранимая); либо поделить
    сутки не на 1440 минут; либо тик, кратный минуте (напр. 1440 или 2880
    тиков в сутках) ценой отказа от степени двойки. ПОКАЖИ мне варианты с
    последствиями, не выбирай молча.
 3. Субмир: множитель 0.10 → целочисленное отношение (1/8? 1/16?).
 4. Свести четыре ритма (кадр, мировая минута, kAiTickSec, дневная очередь)
    к ОДНОЙ лестнице: тик → минута → час → сутки → сезон → год, где каждый
    уровень выражен целым числом предыдущих.
 5. Длина суток в реальных секундах — ЧУВСТВО игры. Сейчас 100 с: полный
    цикл «день марша + ночь отдыха» проживается за ~1.5 реальных минуты,
    поэтому выносливость не ощущается как ресурс. Обсуди со мной, но помни:
    это НЕ лечит баланс выносливости само по себе (128 с — всего +28%).

ВЕРИФИКАЦИЯ: тест на лестницу времени (тиков в минуте × минут в часе × ... =
тиков в сутках, без остатка), тест на отсутствие дрейфа (N маленьких dt и
один большой дают ту же дату), тест на сейв/загрузку времени, и обновление
балансового теста путешествий (tests/macro_travel_parity_test
test_travel_balance_holds_its_intent считает часы марша из
kMacroWalkCellsPerSecond и kWorldMinutesPerSecond — он должен продолжать
выражать ЗАМЫСЕЛ, а не старые константы). Затронутые доки: macrosim.md,
seasons.md, celestial.md, macroworld.md, rpg.md.
```

---

# ТРЕК 2026-08-06 — сквозной аудит и то, что из него выросло

**Где мы на самом деле.** Долг большой, но он теперь ИЗМЕРЕН и разложен: полный
разбор — `audit.md` ЧАСТЬ III (III.0-III.28), реестр дефектов — `problems.md`
§19-§22. Главное, что изменилось за эту сессию: **линейка починена**, поэтому
дальше можно верить числам. Было: три теста не могли покраснеть, `smoke.sh`
всегда возвращал 0, `wait_visible` был заглушкой, один тест ОХРАНЯЛ баг, тесты
компилировались другой арифметикой, чем игра, а `ctest` гонял устаревшие
бинарники. Всё это закрыто (`tests/check.h`, цель `check`, паритет `-ffast-math`,
`CMakeLists` 1460→688 строк).

**Законы, которые теперь записаны и обязательны:** AGENTS.md → «Testing — the
law» (10 правил, у каждого назван дефект, которым оно оплачено) и «Persistence»
(сейв = полный снимок ТОЛЬКО макромира; субмир — контекст; сохраняться можно
только на карте; у каждого субмирового действия обязан быть макро-возврат).

**Что уже построено в эту сессию и на что можно опираться:**
* `macro/macro_stock.{h,cpp}` + `ecs::MacroDebt` — ОДНА таблица «макро-запасов»
  (строка = `{id, read, write}`, описывает оба направления сразу) и расписка,
  без которой занять нельзя. Строки: `tree_count`, `population`. Гашение
  немедленное, дельта со знаком. Тест `macro_stock_test` (свойства, не числа).
* `emplace_humanoid_body()` в `sub/spawn.cpp` — ОДНО рождение субмирового тела
  из строки таблицы + контекста. Через него идут горожане и отряд игрока.
* `tests/body_contract_test.cpp` — контракт тела, с прогнанным негативным
  контролем.
* `macrosim.md` → «Squad as THE macro entity» — архитектура сквадов со слов
  владельца (3 решения), ещё НЕ реализованная.

Состояние: `check` 51/51, сборка без предупреждений, смоуки зелёные.

---

## Сессия 12 — досвипать тесты на check.h (24 файла) + эталоны → инварианты

```
Протокол: план → моё одобрение → код+тесты → диф → коммит после «ок».

КОНТЕКСТ. tests/check.h — единственный способ провалить тест (вердикт никто не
возвращает; тест с НУЛЁМ проверок = провал). Переведено 6 файлов; ОСТАЛОСЬ 24 с
заряженным `int fail()` — мина сработает в день, когда кто-нибудь завернёт
секцию в bool-функцию (именно так умерли три теста). Список: grep -l 'int
fail(const char' tests/*.cpp.

ЗАДАЧА, в порядке ценности:
1. Перевести все 24 на check.h. Механически НЕЛЬЗЯ: `if (cond) return fail(...)`
   превращается в CHECK(!(cond), ...) только там, где продолжение безопасно;
   где дальше идёт разыменование — CHECK_OR_RETURN. Читай каждый файл.
2. Правило владельца 2026-08-06: ЭТАЛОНЫ-КОПИИ продакшен-формул заменить
   инвариантами-свойствами. Конкретно: subworld_generator_parity_test
   (expected_forest_glades / expected_edge_anchor — построчные копии
   dispatch.cpp), feature_layer_parity_test (build_reference_feature_layer),
   biome_classifier_test («зеркало шейдера», написанное в самом тесте и не
   читающее shaders/macro.frag), таблицы весов числами в pathfinding_parity /
   macro_travel_parity. Вместо равенства копии — свойства: «поляна лежит внутри
   клетки», «стык дороги совпадает у обеих сторон шва», «горы дороже луга».
3. Содержательные дыры, найденные аудитом (audit.md III.23, problems.md §19):
   sentinel `hp_of() == -1` в spell_casting_effects_test обесценивает 8 проверок
   урона; rpg_loot_test обещает «другой сид → другая раскладка» и не проверяет;
   road_river_generation_test:96 сравнивает 0 с 0 при пустом массиве;
   rpg_rules_test:42 сверяет функцию с самой собой.
4. В каждом переведённом файле: цикл, который меряет, обязан утверждать, что он
   что-то намерил (samples > 0 && bad == 0).

ВЕРИФИКАЦИЯ: cmake --build build --target check (не голый ctest!) — и для
КАЖДОГО файла, где ты меняешь смысл проверки, прогони негативный контроль:
временно сломай то, что тест охраняет, убедись, что он краснеет, верни.
```

## Сессия 13 — единое рождение тела — ✅ СДЕЛАНО 2026-08-06

Закрыта целиком (коммиты d55fdc0, 62a5a1a, a9f2373). Разбор — `problems.md` §23.
Итог: пять рукописных рождений свелись к **двум формам одной оси** —
ПРОИЗВОДНОЕ (воплощение числа/строки; ничего не хранит сверх сида) и
ОТСЛЕЖИВАЕМОЕ (видимая форма макро-сущности; лицо, раны и имущество — состояние
оттуда). Заодно закрылся 19.13 (убитый внизу лорд мёртв на карте, раны доезжают
наверх в тот же тик) и незамеченный дефект: атака лорда на карте УНИЧТОЖАЛА его
сущность в момент удара, независимо от исхода боя. Пять новых долгов, найденных
по дороге, записаны в §23 — рост тела не рисуется, тинт гуманоида мёртв, именного
NPC не существует, `tradeDiscount` не доезжает до цен, произвольный спавн не
переживает возврат в клетку (лечится разреженными оверрайдами, не массивом на
клетку). Промпт ниже оставлен как история задачи.

## Сессия 13 (история) — единое рождение тела: оставшиеся три сайта

```
Протокол: план → одобрение → код+тесты → диф → коммит после «ок».

КОНТЕКСТ. `emplace_humanoid_body()` (sub/spawn.cpp) уже собирает тело из СТРОКИ
таблицы (kNpcTypeDefs → project_combat) плюс КОНТЕКСТА вызова. Через него идут
горожане и отряд игрока; отряд перестал быть невидимым не заплаткой, а тем, что
у него больше нет своего пути. Контракт закреплён body_contract_test.

ОСТАЛОСЬ ТРИ:
  • проекция макро-NPC в субмир (sub/spawn.cpp, project_macro_npcs_into_subworld)
  • враждебный спавн (sub/engine.cpp:1672, spawn_npc_body)
  • макро-рождение (macro/npc_spawn.cpp:45, make_npc) — ДРУГОГО РОДА: там нет
    тела субмира, есть макро-сущность. Владелец: макро-сущность — это СКВАД
    (см. Сессию 15). Не своди его с субмировым насильно; пусть обе стороны
    читают одну строку таблицы для общих величин.
Расхождения, которые обязаны исчезнуть (аудит III.18): NpcInventory нет у
горожан и проекций, NpcTraits только у макро, VisualPos.speed 32/48, усечение
урона, радиус тела и масштаб спрайта из вызова вместо таблицы.

ВЕРИФИКАЦИЯ: расширь body_contract_test на каждый переведённый сайт (он уже
устроен как перепись компонентов); check зелёный; смоуки subworld_enter,
attack_first_npc, trigger_battle_start; негативный контроль на каждый новый
инвариант.
```

## Сессия 14 — NPC не освещаются (полдня, видно глазами)

```
Протокол: план → одобрение → код → диф → коммит после «ок». Графика:
верификация КАДРОМ обязательна.

БАГ (audit.md III.13). shaders/npc.frag не включает lighting.glsl — единственный
lit-шейдер без него (проверь: grep -l 'lighting.glsl' shaders/*.frag). Регрессия
внесена коммитом 7cd71e2 при переносе паперdoll-композиции на GPU: из шейдера
ушли shadowFactor(), lit_surface() и point_lights_flat(). Следствие: NPC
светятся на полной яркости в полночь, не получают ни теней, ни точечного света —
факел стражника освещает землю вокруг него и не освещает его самого. Вершинный
шейдер уже выдаёт vWorld и vLightClip (npc.vert:22-23) — оба выхода мертвы;
пайплайн уже биндит litSet (vk_renderer_3d.cpp:2130).

ЗАДАЧА: вернуть NPC в общий путь освещения — include, push-блок, вызовы
lit_surface + point_lights_flat + shadowFactor. Заодно почини render.md:217-225,
который утверждает, что это уже так (и называет несуществующую константу 0.75).

ВЕРИФИКАЦИЯ: gpu_smoke3d, НОЧНАЯ пара кадров до/после (см. память
subworld-universal-lighting: GPU_SMOKE_LIGHT/_NIGHT/_SHOT). Смотри глазами:
ночью NPC темнеет вместе с землёй, у факела стражника появляется тёплое пятно
НА НЁМ. Правило: мутация ECS + фото = отложить капчу ≥1 кадр.
```

## Сессия 15 — СКВАД как макро-сущность (архитектурная, неделя)

```
Протокол: план → моё одобрение → код+тесты → диф → коммит после «ок».
Сначала ПОКАЖИ план структур данных, потом код.

ЗАМЫСЕЛ ВЛАДЕЛЬЦА записан целиком в macrosim.md → «Squad as THE macro entity»
(2026-08-06). Коротко: макро-сущность — это СКВАД, а не человек. Одиночка на
карте = сквад из одного, сам себе лидер (второго вида макро-сущности нет).
Слот 0 всегда лидер, потому что на нём висит ролевая система: перки и артефакты
лидера бафают войско ЧЕРЕЗ ЛИСТ и не одним числом (может быть +10 HP всем, может
быть что угодно) — значит это НАБОР модификаторов из листа и снаряжения лидера,
применяемый при рождении тела, а не второй множитель рядом с листом. Встреча
геометрическая: забежал в клетку, где на карте стоит сквад — встретил именно
этих людей. Убил всех — сквад исчезает с карты (следствие правила «в скваде не
может быть 0»). Убил лидера, бойцы живы — сквад живёт БЕЗЛИКИМ до конца боя, и
только потом выжившие уходят в пул дезертиров (GameState::deserterPool сейчас
сериализуется и не имеет ни одного игрового писателя), из которого макро-сим
поднимает банды.

ОПОРА, которая уже есть: ростер — это макро-ЗАПАС, значит он становится третьей
строкой macro/macro_stock.h, и «сквад не может иметь 0 членов» оказывается не
спецслучаем, а следствием общего правила; тела рождаются одним
emplace_humanoid_body.

ДОБАВЛЕНО ВЛАДЕЛЬЦЕМ 2026-08-06 — ДВЕ ЧАСТИ, БЕЗ КОТОРЫХ СЕССИЯ НЕПОЛНА:
 * ВСТРЕЧА ПРИНУДИТЕЛЬНА. Враждебный сквад на карте не «стоит и ждёт»: как в
   Mount & Blade, столкновение открывает предбоевое взаимодействие (диалог,
   откуп, бегство, бой), и уже бой переносит в субмир против ровно тех людей,
   что названы в ростере. Сегодняшний путь `route_macro_npc_attack` — зародыш
   этого взаимодействия; НЕ удалять, а переделать здесь.
 * АВТОБОЙ. Два ИИ-сквада обязаны уметь подраться БЕЗ субмира, иначе макро-сим
   не может вести войну. Один резольвер на всё: уровни, численность, лист
   лидера и его баффы, контекст (рельеф клетки, усталость, кто напал из
   засады). Он же — кнопка «авторасчёт» для боёв игрока, чтобы не было двух
   законов боя. Требование владельца: результат авторасчёта должен сходиться с
   ручным боем настолько, чтобы игрок, дерущийся руками, играл в ту же игру,
   что и мир вокруг него.

СЕГОДНЯШНЕЕ СОСТОЯНИЕ: макро-NPC — ОДИНОЧНЫЕ сущности (ecs::MacroNpcRuntime),
сквады существуют только как SoldierSquad игрока, пул дезертиров пуст, автобоя
нет вообще (две макро-сущности не могут подраться никак).
Это затрагивает спавн, ИИ, сейв, встречу и поссессию разом — поэтому отдельная
сессия и план ДО кода.
```

## Сессия 16 — поле фауны как честный макро-запас

```
Протокол: план → одобрение → код+тесты → диф → коммит после «ок».

РЕШЕНИЕ ВЛАДЕЛЬЦА 2026-08-06: нынешнее поведение — ДЫРА, а не замысел. Живность
рождается из биома и возрождается при каждом возврате клетки в окно
(sub/engine.cpp repopulate_after_recenter; сид клетки — чистая функция
координат): вырезал стаю, отошёл на две клетки, вернулся — стая на месте.
Бесконечная ферма XP и лута. Лечить не правилом про респавн, а ЧЕСТНЫМ ПОЛЕМ
фауны в макромире — по образцу слоя деревьев (macro/tree_layer.h): у клетки есть
поголовье, охота его выбивает, макро-сим восстанавливает годами.

ФОРМА: третья строка macro/macro_stock.h (`fauna_count`), спавн фауны
СПРАШИВАЕТ `read` вместо того чтобы катить таблицу вслепую, каждое существо
уносит расписку, смерть гасит долг — то есть та же симметрия, что у населения.
Поле должно попасть в сейв (сейв = полный снимок макромира, AGENTS.md).

ВЕРИФИКАЦИЯ: тест-свойства (охота уменьшает поголовье; клетка не может уйти в
минус; возврат в клетку НЕ воскрешает вырезанных; регров идёт в игровом времени)
плюс смоук на возврат в клетку.
```

## Сессия 17 — сейв: полный снимок макромира

```
Протокол: план → одобрение → код+тесты → диф → коммит после «ок».

ПРАВИЛО (AGENTS.md → Persistence, владелец 2026-08-06): сейв — полный снимок
ТОЛЬКО макромира; размер не ограничение (до гигабайта нормально, простота
разработки важнее байт); субмир не сохраняется, потому что он выводится из
макромира и сида; сохраняться можно ТОЛЬКО на карте.

ЧТО СЕЙЧАС ТЕРЯЕТСЯ (audit.md III.6-III.8, problems.md §19):
  • ВСЕ ECS-сущности макромира — убитые воскресают, ушедшие возвращаются в точку
    спавна (spawn_macro_npcs перегенерирует от сида);
  • прогресс сюжета — registerIntroStory=false на загрузке уносит и главу 1
    (доказано прогоном: logic=3 → logic=1);
  • WorldTickRuntime — очередь дневных тиков, остаток подтиков, состояние
    jitter-RNG (мир после каждой загрузки бросает ту же последовательность);
  • runtime-спавны квестов (SpawnEntity) исчезают навсегда.
Приоритет владельца именно в этом порядке.

ОБЯЗАТЕЛЬНО ЗАКРЫТЬ ПОПУТНО: провал сохранения сейчас МОЛЧАЛИВ (bool отброшен в
main.cpp:1933 и :8481, refresh_save_summary перечитывает старый файл), а
PlayerState::eventLog растёт вечно под капом kMaxSmallVector=8192 — заряд уже
вставлен. И запретить сохранение в субмире (следствие правила).

ВЕРИФИКАЦИЯ: save_roundtrip_test обязан сверять КАЖДОЕ поле с НЕдефолтным
значением (сегодня из 18 полей Objective сверяется одно, а поля v21
originIsVillage/destIsVillage не выставлены даже в фикстуре); плюс сценарий
«убил лорда → сохранился → загрузился → он мёртв».
```

## Сессия 18 — econ v1 + закон цены от запаса

```
Протокол: план → одобрение → код+тесты → диф → коммит после «ок».

РЕШЕНИЕ ВЛАДЕЛЬЦА 2026-08-06: старую экономику (macro/economy.cpp) НЕ чинить —
она умрёт с подключением econ v1. Вести econ_day.

ЧТО ЗНАТЬ ПРО СТАРУЮ, чтобы не удивляться на переходе (audit.md III.1-III.5):
ни один товар в мире не может быть произведён никогда (все деревни добывают
только зерно, каждый рецепт требует два РАЗНЫХ ресурса) ⇒ богатство всех
городов 0, настроение навсегда Tense; settle_trade_route創ёт деньги из воздуха
(получатель не платит); popCap = 100 + wealth*0.5 при городах в 600-7000 —
перевёрнутый знак роста, который выстрелит на первом же честном потоке зерна.

ЗАДАЧА:
1. Три бага в econ_day (audit.md III.1 блок): дефицит cloth/bricks проваливается
   в пустоту (не голод и не unmetComfort), fedPop пишется присваиванием и
   вторая «голодная» строка перетирает первую, первый рецепт таблицы забирает
   всех рабочих города (голодание последующих строк).
2. Подключить econ_day к мировому тику (шаг W2 из work_vector №1) + Stockpile.
3. ЗАКОН ЦЕНЫ, решение владельца: модель starcluster (economy.md там же) —
   scarcity = (demand + rate*lookahead + 1) / (supply + rate*lookahead + 1),
   price = base * scarcity^elasticity. Цена — ПРОИЗВОДНАЯ ЗАПАСА, сделка игрока
   двигает запас, у контрагента есть казна, которая может кончиться. Тогда
   бесконечные деньги (купить 81 — продать 92 тому же торговцу, audit III.2)
   гаснут САМИ проскальзыванием, без правил про спред. Владелец: «чтобы econ v1
   это учла потом, не потерять».

ВЕРИФИКАЦИЯ: econ_v1_test сегодня проверяет сохранение, но НЕ проверяет ни
unmetComfort, ни разнообразие выпуска, ни запрет рецепта без входов (такой
рецепт творит товар из ничего) — добавить. Плюс тест «арбитраж гаснет»:
последовательность купить-продать не может быть прибыльной.
```

## Сессия 19 — Vulkan: ресурсы, уничтожаемые внутри открытого кадра

```
Протокол: план → одобрение → код → диф → коммит после «ок».

ТРИ НАРУШЕНИЯ ОДНОГО ПРАВИЛА (audit.md III.9, III.14). kMaxFramesInFlight = 2, а
acquire_frame ждёт фенс ТОЛЬКО своего слота — значит кадр N−1 ещё исполняется:
  1. UI-текстура мини-карты субмира уничтожается и пересоздаётся каждые 2 с,
     пока её семплирует кадр в полёте (ui/overlays.cpp:3389, ui_gpu.cpp:74);
  2. SubworldEngine::prepare_frame зовёт upload() МЕЖДУ acquire и end_frame —
     vkDestroyBuffer при росте инстанс-буфера и vkUpdateDescriptorSets на
     занятом сете (sub/engine.cpp:2617, vk_renderer_3d.cpp:971, :1595). Заявленный
     в render.md:698 «Fence contract» в коде отсутствует;
  3. инстанс-буферы NPC/существ/частиц — по одному экземпляру, барьер ПОСЛЕ
     записи (vk_renderer_3d.cpp:867) — WAR-хазард; рядом лежит правильный
     образец: lightBuf_[kFramesInFlight] — кольцо.
Честное лечение — отложенное удаление по фенсу (не vkDeviceWaitIdle) и кольцо на
инстанс-буферы.

ПОПУТНО: tests/gpu_smoke.cpp рассинхронизирован с macro.frag (Push 32 Б против
56 Б, 4 дескриптора против 6) — а валидация в нём ВКЛЮЧЕНА, то есть самая
громкая ошибка живёт в инструменте проверки.

ВЕРИФИКАЦИЯ: прогон с валидацией (debug.md §2) — ноль VUID кроме известной
teardown-утечки; ночная пара кадров до/после, чтобы доказать отсутствие
визуальной регрессии.
```

## Сессия 20 — документы под код (большой список готов)

```
Протокол: только документы, код НЕ трогать. План → одобрение → правки → диф.

ГОТОВЫЙ СПИСОК: audit.md III.28 — десять имён, которые документы предписывают
как рабочие, а в коде их НЕТ (spawn_hostile_npc — цитируют 4 документа,
kCrowdPenalty, tick_combat_move, sm::SpatialHash — AGENTS.md прямо велит его
использовать, kMaxPointLights, generate_spires, kObjectiveCheckers, консольная
`control`, vulkan_plan.md, translation.md); числа (тесты 43/45/46/26 при 51,
kSaveVersion 10/12/18 при 21, год 100 дней при 128, «kAiTicks = полчаса» при
5.6 минуты, кап 512 при 16384); пять подсистем числятся неотгруженными, будучи
отгруженными (тени, точечный свет, дома/стены, макро-свет, высотная окклюзия);
четыре документа описывают GPU-симуляцию толпы в настоящем времени при НУЛЕ
compute-шейдеров.

ОТДЕЛЬНО README: блок 1-81 подан как текущий и описывает 2D-прототип на
SDL2/C++17, включая ВЫДУМАННУЮ ЦЕЛИКОМ таблицу «Core System Configuration
Parameters» (ни один из четырёх параметров не существует ни в src/, ни в
CMakeLists). Раздел Controls при этом точен — он единственный живой.
Владелец решает: переписать README целиком или чинить по строкам.

ПРАВИЛО: проверяй каждое утверждение grep-ом по коду; где расходятся код и
документ — прав КОД, но если тебе кажется, что прав документ, это находка для
владельца, а не повод править код.
```
