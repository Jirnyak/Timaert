# Суд по канону — первый проход, 2026-08-20

Судья — [CANON.md](CANON.md). Вопрос к каждому найденному куску не «работает ли он», а
**какой системе канона он принадлежит и не является ли он её второй реализацией**.

**Чем судили:** прицельная разведка по репозиторию (207 файлов, 67k строк) — таблицы,
точки роста, места, где канон делает проверяемое утверждение. Каждая улика ниже — с
файлом и строкой, проверена глазами.

**Чем НЕ судили (и это надо сказать вслух):** дисциплина O(N) в горячих циклах, вывод
каждой константы, дубли внутри `sub/`, соответствие боевого стиринга канону, качество
тестов. Их отсутствие в списке — не оправдание, а непройденный участок.

---

## I. Документы, которые велят строить НЕ ТО (чинить документ, не код)

Это самый дорогой класс: такой документ читает следующий агент и работает по нему.

**1. `AGENTS.md` → Hard Rules → «GPU-driven simulation, no cheats» + `ARCHITECTURE.md`
§GPU-Driven Simulation / §Residency & Embodiment / `MANIFEST.md` шапка.**
Канон S5: GPU-симуляция макромира **отложена в далёкое будущее**; масштаб держат
запечённые пути и O(N)-дисциплина. Улика: в `shaders/` **тридцать файлов и ни одного
compute-шейдера** — вся масса и так на CPU. То есть документ описывает не «то, что есть» и
даже не «то, что решено», а отменённое направление, и делает это в разделе, который агент
обязан исполнять. → Переписать под S5, оставив ссылку «когда-нибудь».

**2. `lore.md` §1.1/§3.5 и `zones.md` — знак поля НАОБОРОТ.** Там `−127` = насыщенная
магия, `+127` = чёрная энергия; канон S15 (владелец, 2026-08-20): **плюс = магия, минус =
тёмная энергия**. Поле ещё не построено — значит цена правки сегодня нулевая, а завтра это
перевёрнутая карта мира. → Переписать доки.

**3. `AGENTS.md` → Source Authority: «`C:\Timaert\src` (TypeScript/Svelte) is the gameplay
behavior authority. Before changing a gameplay system, read the matching TS module».**
TS-паритет мёртв (решение владельца, зафиксировано в памяти проекта), C++ и есть игра.
Правило же обязывает сверять поведение с прототипом, которого на этой машине нет. →
Удалить.

## II. Где прав КОД, а канон был неточен (правится канон)

Обязательная половина суда — иначе канон превращается в список претензий.

**4. Производство ПОСТРОЕНО.** Я записал «НЕ ПОСТРОЕНО», а в `macro/econ_day.h` живут
строки рецептов (`kRecipes`, `RecipeDef`, `econ_produce_day`), рабочие-дни и прямая
формулировка «buildings are ABSTRACT (owner ruling) — a recipe names the KIND» — то есть
ровно канонический «производит ландмарк как целое». S10 исправлен.

**5. Логистический рост населения ПОСТРОЕН** (`econ_day.h:171
population_delta_per_day`) — и построен раньше, чем был записан в канон.

## III. Дефекты кода против канона

### A. Числа, назначенные там, где канон требует ВЫВЕСТИ

**6. Потолок населения — константа, а не ёмкость земли.** S8/S25: `K` логистики обязан
читаться из ресурсов (тот же скор, что решает, где стоит поселение). Улика:
`econ_day.h:175` — `1 − population / kPopCarryingCap`, один потолок на весь мир.
`macro/settlement_score.h:70,81` умеет считать ёмкость участка (`site capacity`,
`kVillageCapacityQuota`), и размещение её уже читает — а рост не читает. Следствие: город
в тундре и город на чернозёме упираются в одинаковый потолок, истощение земли не тормозит
никого, открытая жила никого не растит. → `K` = ёмкость округи через ту же дверь.

**7. Автолевелов ДВА, а приговорён был один. — ИСПРАВЛЕНО 2026-08-20** (оба снесены,
`zoneLevel` ушёл из спавн-API, негативный контроль в `subworld_spawn_parity_test`, 75/75). S12: место решает, КОГО ты встретишь;
наценка на тело после выбора — автолевел. Улика `sub/spawn.cpp:599-612`:
```
if (landmark == City || Village) levelBonus += floor(sqrt(landmarkPop / 100));
if (zoneLevel > 2) { levelBonus += zb; hpMult = damageMult = 1 + zb*0.18; }
```
Про зонный (+18%) знали. Про **населенческий** — нет: в крупном городе стражник растёт в
уровне просто потому, что город большой. Оба — наценка после выбора. → Оба на снос,
контекст обязан выбирать строку существа, а не двигать его числа.

### B. Механики-двойники (канон объявил одну систему, живут две)

**8. Найм — вторая торговля.** S25: сквад есть инвентарь существ, юниты есть товар, обмен
один. Улика: `ui/overlays.cpp:1904-1917` — свой путь `hire_price_for` / `hire_npc` со
своей ценой и своей кнопкой, рядом с пакетным бартером предметов. Два склада, два
ценника, две UI-механики над одной сущностью. → Слить в один обмен.

**9. Пейпердолл-композит жив** при действующем спрайт-законе S16 («вид = строка, отряд =
один спрайт, композит на снос»): `app/main.cpp`, `gpu/bb_instance.h`, `gpu/vk_sprite_array.h`,
`shaders/body.frag`, `shaders/shadow_body.frag`, `ui/map_screen.cpp`. Снос начат — в дереве
лежат незакоммиченные `src/assets/sprite_bank.{h,cpp}`. → Довести, а не заморозить.

### C. Мёртвые концы

**10. Пул дезертиров — сток без стока.** S4: из пула мир поднимает разбойничьи и
дезертирские отряды, и «изрядная часть опасности мира» приходит оттуда. Улика: писатель
есть (`macro/squad.h:50 drain_dead_leader_squads`, вызывается из `npc_ai.cpp:1141`),
**читателя нет ни одного** — только запись, сохранение и обнуление на новой игре. Люди
падают туда навсегда. → Либо построить подъём отрядов, либо признать, что опасность мира
берётся не отсюда.

### D. Систем нет там, где канон говорит, что они есть

**11. Мир не может меняться на уровне мест (S9).** Ландмарки создаются только на генерации
и при загрузке (`macro/state.cpp:181,284`, `macro/save.cpp:1087,1096`); ни одного
`erase`, ни одной смены вида, ни одного основания нового места после буга. Канон требует:
растут, гибнут, меняют владельца, ПЕРЕХОДЯТ в другой вид по порогу, возникают — включая
ландмарки игрока. Это самая крупная дыра между «мир живёт» и тем, что есть.

**12. Единой двери контекста нет (S6). — ПОСТРОЕНА 2026-08-24** (трек двери,
`c9f2bea..37f80a8`; [context.md](context.md) = THE-док: конверт `MacroWorld` + один сборщик
`cell_facts`, `resolve_context` — его потребитель; погода и поле тьмы подключаются колонкой
факта + строкой world_fields, не кодом). Исходная находка: `sub/map_data.h:122 CellContext` — это контекст
ГЕНЕРАЦИИ клетки (высота, биом, фича, ландмарк, деревья, плодородие), а не сборщик вкладов
для механик: зона приезжает в спавн отдельным аргументом, стоимость шага живёт своей
таблицей, экономика — своей. Пока двери нет, каждая новая система мира (погода, поле тьмы)
будет подключаться «в семь мест» — то есть не подключится.

**13. Универсального запекателя и фонового допекателя нет (S7). — ЗАПЕКАТЕЛЬ ПОСТРОЕН
2026-08-24** (трек двери: `rebake_world` перепекает производный мир целиком на
загрузке/сезоне/переходах — см. G2); ФОНОВОГО допекателя по тору по-прежнему нет — эта
половина открыта. Исходная находка: зоны пекутся один раз на
буте (`app/main.cpp:2041`) и никогда не перепекаются; общего механизма «запечь поле по
тору», которым пользуются все поля, тоже нет. Сегодня это латентно (мир пока не меняется —
см. п.11), завтра, вместе с живыми ландмарками, это станет ложью карты.

**14. Погода — не поле (S19).** `sub/sky.h:131 weather_at` выводит осадки из календаря и
позиции; в макромире её нет, вклад в механики нулевой. Сам файл честно пишет «macro field
tomorrow» — то есть код знает, что он времянка.

**15. Поле магия↔тьма не построено (S15).** `zones.md` сам это признаёт. С ним не построены
и культы как источник — единственное дополнение, вошедшее в замысел.

**16. Политики как державы нет (S24).** Есть королевства, столицы, дороги, территория
Вороного и матрица отношений (`macro/politik.h`) — то есть КАРТА принадлежности. Нет
капитала фракции, веса лордов, налогов, решений лидера, передела земли войной. Дипломатия
сегодня — таблица, которую канон явно объявил лишь входом политики.

**17. Звука мира нет вообще (S23).** `macro/audio.h:16` — `enum class SfxId { Witch, Count }`,
и **ни одного вызова `play_sfx` за пределами самой аудиосистемы**. Шаг, удар, попадание,
дождь не звучат ничем. Музыка есть. → S23 не нарушен, он просто не начат.

**18. За окном 3×3 пусто (S2).** Дешёвого ЛОД-горизонта нет; честная рамка при этом уже на
пределе цены, так что расширять её — запрещено каноном. Дырка ровно там, где игрок смотрит
вдаль.

---

## Что делать в порядке цены

1. **Документы (I.1–I.3)** — час работы, а стоят они каждого следующего агента, который
   прочитает «строй GPU-симуляцию» и «сверяйся с TypeScript».
2. **Два автолевела (III.7)** — приговор уже вынесен, теперь известны обе точки.
3. **`K` из ёмкости земли (III.6)** — одна формула, и мир начинает отвечать на ресурсы
   ростом, а не константой.
4. **Дверь контекста (III.12)** — без неё погода, поле тьмы и следопыты подключаться
   некуда; это фундамент под три следующих трека.
5. **Живые ландмарки (III.11)** + **запекатель (III.13)** — один трек: место меняется,
   поля за ним перепекаются.
6. **Найм = торговля (III.8)**, **пул дезертиров (III.10)** — обе дешёвые, обе закрывают
   по одной лишней сущности.

Не в списке намеренно: звук, политика-держава, поле тьмы, ЛОД-горизонт — это НЕ дефекты, а
ненаписанные системы; их цена — трек каждая, и очередь им назначает владелец.


---

# Второй проход — шесть агентов по всем 26 системам, 2026-08-20

Первый проход был прицельным (15 проб). Этот — шесть параллельных аудиторов, каждый со
своей зоной канона, каждому велено доказывать находку чтением и проверять вызывающих через
`rg`. Ниже — сведённый результат, дубли сняты, **всё с file:line**.

## A. Живые дефекты, которых не видно в игре

**A1. Мёртвые деревни платили за живых горожан. ИСПРАВЛЕНО (30db59d).**
`macro_stock.cpp` утверждал, что города и деревни делят одно пространство id. Не делят
(`state.cpp:155` `s.id = int(i)`, `state.cpp:265` `vil.id = villageId++`). Поиск шёл сначала
по городам: убийство в деревне 3 списывало людей у ГОРОДА 3, а если id деревни больше
последнего города — смерть не платил никто. Тест не мог поймать: в его мире стояли id 7, 8 и
42 — три числа, которые не могут столкнуться.

**A2. Весь бестиарий ронял ПУСТОЙ лут. ИСПРАВЛЕНО (78071d9).** После слияния таблиц
`kind < NPCType::Count` стало истинным для всех, ветка существ умерла, а `npc_loot_id(волк)`
возвращал `""` → профиль `nullptr`. Девятнадцать строк существ роняли только золото.

**A3. Авторезолв и рукопашная дрались РАЗНЫМИ волками. ИСПРАВЛЕНО (78071d9).**
`macro/auto_battle.h:86` всегда считал существ через лист, а рождение тела — по сырой строке
× 1.15^(lvl−1). S13 запрещает буквально это.

**A4. Сквад во главе со зверем исчезал целиком. ИСПРАВЛЕНО (78071d9).**
`spawn_tracked_body` отказывал строке-существу → `sub/spawn.cpp` делал `continue`, теряя
лидера ВМЕСТЕ с ростером, хотя макро-ИИ его тикал.

**A5. Поле опасности РАЗРЫВНО на шве тора. — ИСПРАВЛЕНО** (заход швов 2026-08-20:
`NOISE_BASE_CELLS = 128` — степень двойки, каждая октава замыкается на мир; помечено здесь
2026-08-24). Исходная находка: `zones.cpp:25` `NOISE_BASE_CELLS = 96` при мире
1024: `1024/96 = 10.67`. `value_noise_wrap` заворачивает хэш, но не координату — аудитор
проверил численно репликой: `f(0,0) ≠ f(1024,0)` на всех пяти октавах, расхождение до 0.15.
S1: бесшовность — свойство построения. → шаг шума вывести из размера мира (64 или 128).

**A6. Проекция сквада молча теряет людей.** `sub/spawn.cpp:32` `kMaxProjectedMacroNpcs = 128`
без вывода; варбанда в 200 бойцов теряет 72 человека — без флага, без лога, без записи
наверх. Рядом боевой путь сделан правильно (`engine.cpp:2248` ставит `battleGatherTruncated_`).
S26 запрещает молчаливое обрезание.

**A7. Закон навыка противоречит сам себе.** `attributes.h:60` объявляет `1 + ранг·0.01` с
выводом «ранг = процент, до удвоения», а `:316,:317,:354,:355` считают четыре ядровых стата
инлайном по `·0.05` без клемпа: bodybuilding 100 даёт ×6 к HP вместо обещанного ×2.
Это самые дорогие числа игры — лист есть единственный источник боевых чисел (S14).

**A8. Скорость ходьбы внизу и марша наверху расходятся вчетверо. — ЗАКРЫТ ВЫВОДОМ
2026-08-24** (трек двери, 37f80a8): марш = 8 клеток/игровой час (чистые данные 1-го уровня
владельца; старые 32 были курьерским галопом — карта скакала, а не пол врал), и 96 тайлов/с
внизу теперь ВЫВЕДЕНО из марша (8000 тайлов/растянутый игровой час = 93.75, +2.4%
именованный допуск). Исходная находка:
`main.cpp:141` `kSubworldWalkTilesPerSecond = 96` = 8 клеток/игровой час против
`movement_cost.h:79` `kMacroWalkCellsPerHour = 32`. При масштабе S1 (1 тайл ≈ 1 м) это
96 м/с ≈ 345 км/ч. У макро-константы вывод есть, у субмировой — нет.

**A9. Игрок — тело шириной три метра.** `engine.cpp:88` `kPlayerBodyRadius = 1.5f` против
`army.h:38` `0.55f` у каждого человека обеих таблиц; обоснование в комментарии — «как в
TS-прототипе», который мёртв. Рядом `kPlayerMeleeRange/Cooldown/BaseMeleeDamage` — второй
числовой набор рядом с листом (S14).

## B. Экономика: монета не производится, а только исчезает

**B1.** Единственный источник монеты — посев при рождении мира (`econ_day.cpp:268`,
8 на душу в городе). Уничтожителей три: `wallet_spend_up_to` в найме (`overlays.cpp:1920`),
в отдыхе (`:2046`) и в штрафе квеста — ни один не начисляет контрагенту.
**B2.** `generate_loot_gold` (`items.cpp:348`) чеканит монету в КАЖДЫЙ труп поверх кошелька,
который тому же телу уже начеканили при спавне (`npc_spawn.cpp:196`) — два независимых
минта на одном теле.
**B3.** Награда квеста (`quest_engine.cpp:69`) чеканится из воздуха, хотя заказчик известен
(соседняя ветка штрафа уже умеет писать долг на его склад).
**B4.** Караван не торгует, а КОНФИСКУЕТ: `npc_ai.cpp:650-688` `haul_between` переносит
товар в обе стороны без встречного движения ценности. Ценовой закон `macro/economy.h`
существует только для двух экранов игрока — мир цен не знает.
**B5.** Деревня не производит ничего: все девять строк `kRecipes` имеют `site = City`,
`tick_villages_` вызывает только `settle_landmark_day`. При этом деревня ест полную лестницу
нужд. Норма деревни — вечный голод.
**B6.** Пол населения (`world_tick.cpp:69`, minPop 10/5) РОЖДАЕТ людей из воздуха и делает
поселение бессмертным: вырезанная до трёх душ деревня получает обратно пятерых.

## C. Вторые словари и обходы единственных дверей

**C1. — ИСПРАВЛЕНО 2026-08-24** (трек двери: `LandmarkType` — единственный словарь;
`LandmarkKind` и `CellLandmarkKind` снесены вместе с переводчиком, Lair/Shrine/Mine/Tower
доезжают). Исходная находка: три словаря ландмарков + переводчик: `LandmarkType` (9 строк, реестр),
`LandmarkKind` (5, fauna.h:24), `CellLandmarkKind` (5, map_data.h:65), перевод —
`engine.cpp:1109`. Цена доказуема: **Lair, Shrine, Mine, Tower физически не могут доехать до
микромира** — логово на карте станет чистым полем.
**C2. — ИСПРАВЛЕНО 2026-08-24** (запечённая сетка `macro/landmark_grid.h` в ЕДИНОМ порядке
приоритета `for_each_landmark`; рукописные сканы снесены — живые поля резолвятся из
GameState в момент вопроса). Исходная находка: `landmark_iter.h` называет себя «THE landmark enumeration», а рядом живут четыре
рукописных скана: `macro_lighting.cpp:128`, `engine.cpp:1060` (сама дверь контекста!),
`fauna.cpp:206`, `spires.cpp:35`.
**C3.** Кап 16384 объявлен ЗНАЧЕНИЕМ трижды (`battle.h:56`, `vk_renderer_3d.cpp:144`,
`spell_effects.cpp:67`); комментарии ссылаются друг на друга, `static_assert` нет ни одного.
**C4. — ИСПРАВЛЕНО 2026-08-24** (`macro/macro_world.h`: ОДИН конверт, собирается раз на
владельца — `macro_world(app)`; растёт полем, а не аргументом; `deposits` доезжают всюду,
null-слой = нулевой вклад fail-closed). Исходная находка: `MacroWorld` собирается агрегатной инициализацией разной арности в шести местах;
`deposits` не передаёт НИ ОДНА площадка субмира, а строки депозитов отказывают молча
(`macro_stock.cpp:170`).
**C5. — ИСПРАВЛЕНО 2026-08-24** (один `biome_at_cell` в `map_generator.h`: вода — по
запечённой МАСКЕ, sea-level ушёл из запросов; `biome_at` остался чистой математикой для
зеркала шейдера и генезиса). Исходная находка: каскад биома написан четырежды (`biomes.h:69`, `engine.cpp:1035`,
`tree_layer.cpp:28`, `zones.cpp:107`) и копии расходятся на граничном байте.
**C6.** Три резолвера «токен → строка» над одной таблицей; один (`npc_type_from_token`) был
if-цепочкой, знавшей 7 из 11 ролей и ни одного существа — **исправлен** (78071d9).

## D. Мёртвый код (проверено поиском вызывающих)

- `ZoneLayer::field` — 4 МиБ на мир, аксессоры `field_at`/`has_field_storage` без вызывающих
  в `src/` (`zones.h:20,42,54`) — **снесён 2026-08-24** (трек двери: непрерывное значение
  живёт только ВО ВРЕМЯ запекания; тесту, которому нужна точность, `generate_zones` отдаёт
  опциональный `continuousOut`);
- `iron_depletion`, `iron_discovery_chance_per_day`, `discover_iron_vein`
  (`deposit_layer.h:106-115`) — ноль вызывающих, при этом их закон переписан заново инлайном
  в `macro_stock.cpp:391`, **и мёртвую тройку охраняет тест** (`deposit_layer_test.cpp:162`);
- `econ_gather_day` + `struct Deposit` (`econ_day.cpp:65`) — вызывающих нет, добыча живёт в
  `ai_gatherer`;
- `EconFactSink` — все производственные вызовы передают `nullptr`: голод, жатва и
  производство не доходят ни до шины, ни до журнала игрока;
- `Objective::subX/subY` (`quest_types.h:36`) — координаты МИКРОМИРА в снимке макромира,
  пишутся и читаются сейвом, не устанавливаются и не читаются ни одной механикой;
- `gen_ruin` (`gens/dispatch.cpp:1617`) — полноценный генератор, достижимый только через
  `CellLandmarkKind::Ruin`, который в продакшне не присваивается нигде; **его вызывает только
  тест**, который сам подставляет контекст;
- `SubworldMode::Open` (`map_data.h:56`) — `resolve_mode` не возвращает его никогда.

## E. Тесты, которые не могут упасть

**E1.** Защита «тест без проверок = провал» (`check.h:24`) обезврежена в **43 из 76**: 24
файла кладут одну `CHECK(true, "every gate above held")`, а настоящие утверждения гонят через
`if (…) return fail(…)`, который в счётчик не пишет; 19 файлов не включают `check.h` вовсе.
**E2.** `faction_relations_test.cpp:40` — зеркало `relation()` НЕ содержит правила
«свой к своему = 100», поэтому утверждение о дружественном огне внутри фракции не проверяет
ничего: удали правило из продакшна — тест зелёный.
**E3.** `subworld_generator_parity_test.cpp:43` — посимвольная копия генератора полян вместе
с солями шума; доказывает, что автор умеет копировать.
**E4.** `resource_growth_test.cpp:103` — `CHECK(a >= 8192 || !by2y)` истинно и в том случае,
ради которого написано; `macro_stock_test.cpp:73` — два конъюнкта тавтологичны;
`carried_light_spawn_test.cpp:89` пинит КОЛИЧЕСТВО светящихся строк и покраснеет от законной
новой строки.
**E5.** `rpg_loot_test` утверждал как инвариант, что строка-существо возвращает пустой
лут-профиль — то есть охранял разрез колонки (**перепинен** в 78071d9).

## F. Картинка и генерация

**F1.** Вода — единственная поверхность внизу, которая не зовёт `lit_surface`:
`water.frag:80` ОБРАТНО вычисляет время суток из переданного скаляра
(`dayF = (amb − 0.08)/0.32` — дословная инверсия `lighting.h:256`) и светит своим законом.
`shadow_common.glsl` не включён вовсе: **тень горы и тень облака не ложатся на воду**, при
том что канон S18 говорит «облако над головой ЕСТЬ тень под ногами».
**F2.** `struct.frag:48` — вид пропа это десятиветочная `if`-цепочка по типу с зашитыми
цветами; `map_data.h:323` честно признаёт «строка здесь и ветка там».
**F3.** `base_generator.cpp:70` `terrain_mod_for` — свитч по виду ландмарка ВНУТРИ примитива
движка, с числами без вывода (плато 280 тайлов для города); комментарий называет это «ONE
data table», но таблицы нет.
**F4. — ИСПРАВЛЕНО 2026-08-24** (трек двери: `pick_town_row` — толпа катится ТЕМ ЖЕ законом
спавна по полосе `kHabTown`, старые 55/21/21/3 стали весами строк, профессии гейтятся
живыми депозитами в радиусе — железный город отличим от болотного). Исходная находка: `spawn.cpp:106` `pick_civilian_type` — состав городской толпы зашит процентами
55/21/21/3 в ОБЩЕМ спавнере; три профессии-собирателя туда не попадают вообще.
**F5. — ИСПРАВЛЕНО 2026-08-24** (трек двери: habitat — БИТМАСКА-колонка таблицы тел;
тринадцать листов и лестница свитчей снесены, зона/сезон/погода подключаются данными —
`roll_spawns` по закону соответствия). Исходная находка: `fauna.cpp:78` `get_fauna_table` — единственная дверь «кто здесь водится» — это
`switch(ландмарк)` → порог по деревьям → `switch(биом)` на 11 веток. Ни зоны, ни сезона, ни
погоды подключить некуда, кроме как новой веткой (S6).

## G. Сейв и знание

**G1.** `save.cpp:25` `kMaxPayloadBytes = 64 МиБ` при каноническом «до гигабайта нормально», и
при превышении `save.cpp:1270` возвращает `false` БЕЗ голоса.
**G2.** — **ИСПРАВЛЕНО 2026-08-24** (трек двери, `c9f2bea..37f80a8`): ОДИН `rebake_world`
перепекает производный мир целиком (зоны, ландмарк-сетка, cost grid, свет, GPU-поле зон) на
каждой загрузке, на сезонном сеттле и на КАЖДОМ переходе макро↔микро (S7 буквально) — CPU
синхронно, текстуры через dirty-флаш макро-пути (закон Сессии 19 «никаких дренажей в
кадре»). Исходная находка: загрузка НЕ перепекает мир: `boot_world_from_save` печёт зоны и сетку стоимости из
девственного мира, потом подменяет поселения/деревья/депозиты — и перепекает только свет
(`main.cpp:2279`). Расписаний перепекания четыре и все разные; зоны не перепекаются никогда.
S7 требует одного честного перепекания на каждой загрузке и переходе слоёв.

---

**Чего аудит НЕ покрыл** (сказано вслух каждым агентом): `sub/battle.cpp` (стиринг, тревога,
сетки), `politik.cpp` построчно, `vk_renderer_3d.cpp` целиком, `gens/dispatch.cpp` (2330
строк) на числа с потолка, тороидальность рек и Вороного, дисциплина O(N) в горячих циклах
рендера, `src/gpu/`, большая часть `ui/`.

## H. Механический свип: мёртвый код и дубли (вызывающие проверены поиском)

**H1. Второй A\* слово в слово. — ЦЕНОВАЯ ПОЛОВИНА ЗАКРЫТА 2026-08-24** (трек двери, вместе
с §7.9: планировщик дорог ходит по THE закону шага — `build_cost_grid` + климб на ребре,
его частная вторая таблица цен мертва; дороги кладутся по тому же закону, по которому по ним
маршируют). Сам клон A\*-АЛГОРИТМА (`RoadIndexedHeap`) ЖИВ — слияние алгоритмов не
делалось, закрыта таблица. Исходная находка: `spawners.cpp:108-392` (`road_octile_torus`,
`RoadIndexedHeap`, `find_road_path`) — построчный клон `pathfinding.cpp:22-271` над ТЕМ ЖЕ
`costGrid`, тем же тором и той же эвристикой `0.4142136f`. Отличий два: генерационный
scratch (перф) и порог воды. ~280 строк. → слить, `blockAbove` параметром.

**H2. ДВА макро-ИИ драйвера, и игра выбирает между ними по тому, ГДЕ ИГРОК.** Живой баг из
этого (потерянный указатель на депозиты) **исправлен (0a80266)**; **копии контекста мертвы
2026-08-24** (трек двери: `TickContext` ВСТРАИВАЕТ конверт `MacroWorld` — оба драйвера берут
его целиком, семнадцать переписываемых полей ушли). Исходная находка: сама двойня осталась:
`tick_macro_npc_ai` и `tick_macro_npc_ai_budgeted` собирают по своей копии контекста из
семнадцати полей. → один драйвер с бюджетом, полный проход = бюджет без предела.

**H3. `Settlement` и `Village` — две структуры на одну строку канона.** `Village` это
`Settlement` минус гарнизон плюс `nearestCityId`; 89 упоминаний в 20 файлах, всегда парой с
близнецом (`tick_settlements_`/`tick_villages_`, два блока сейва, по два цикла в `zones`,
`macro_lighting`, `spires`, `settlement_score`, `npc_ai`). Раскол не косметический — он
ПРИЧИНА отсутствующего поведения: у `tick_villages_` просто нет вызова `econ_produce_day`,
поэтому деревни ничего не производят (B5), и `generate_quests_for_village`
(`procedural.cpp:458`) не вызывается никем — деревни не выдают квестов. → одна строка
ландмарка (S9), гарнизон и «ближайший город» — колонки.

**H4. 133 строки, которые не компилирует ни одна цель.** `audio.cpp:454-586` — `#else`
ветка `TIMAERT_HAS_SDL_MIXER`, а `CMakeLists.txt:127` при отсутствии микшера делает
`FATAL_ERROR`. Внутри — ВТОРАЯ таблица ассетов (`switch`-цепочки с именами файлов) рядом с
живыми `kMusicAssets`/`kSfxAssets`. → удалить вместе с `#if`.

**H5. Цепная молния недостижима, и тест это ПАРКУЕТ.** `spell_effects.cpp:324-383`
`apply_spell_chain` возвращается на первой строке всегда: три поля `ecs::Projectile`
(`chainRemaining/chainDecay/chainRadius`) читаются, но не пишутся нигде; `SpellDef::chainCount`
заполнен в 8 строках и не читается никем. `spell_casting_effects_test.cpp:471` описывает это
прозой и намеренно не утверждает ничего — S26 запрещает («тест никогда не охраняет дефект»).

**H6. Зелья нельзя выпить.** `items.cpp:356 use_item` — пять вызовов, ВСЕ в тесте
`item_use_parity_test`. В интерфейсе инвентаря есть только ярлык типа предмета, кнопки
«использовать» нет нигде. Зелёный ctest удостоверяет фичу, которой в игре нет.

**H7. — ИСПРАВЛЕНО в треке двери** (помечено 2026-08-24): мёртвая половина
(`collect_landmarks` с помощниками и весь `.cpp`) удалена, заголовок расклеен — один
`#pragma once`, реестр-таблица осталась. Исходная находка: `landmark_registry.cpp` целиком мёртв (`collect_landmarks` без вызывающих; его
приватные помощники `mood_label`/`marker_kind`/`detail_line` живут только ради него), а
**заголовок физически склеен из двух файлов** — два `#pragma once` на строках 10 и 36.

**H8. Шесть копий `wrapi`** (`optics.h:90`, `macro_lighting.cpp:49`, `map_generator.cpp:174`,
`features.h:61`, `pathfinding.cpp:27`, `deposit_layer.h:49`) при живом `core/torus.h:7`; две
копии `wrap_delta` (`macro_overlay.cpp:43`, `map_screen.cpp:26`); две `cell_count_for`
(`features.h:51`, `map_generator.h:33`).

**H9. Однократный командный буфер Vulkan написан шесть раз** (`vk_texture.cpp:115,238,442,556`,
`vk_sprite_array.cpp:150,344`) — пул→барьер→копия→барьер→фенс→ждать→разрушить. ~200 строк, и
вся поверхность утечек фенса/пула схлопывается в одну точку.

**H10. — ИСПРАВЛЕНО 2026-08-24** (оба UI-близнеца ушли в единый `biome_at_cell`
из `map_generator.h` — с границами и маской). Исходная находка: `biome_at_cell` (`macro_overlay.cpp:67`) ≡ `chart_biome` (`map_screen.cpp:104`), но у
первого нет проверок границ и альфа-маски → чтение за пределами короткого `rgba`.

**H11. Поля, в которые только пишут:** `NpcCharacter::tintR/G/B` (билборд берёт цвет из
строки спрайта), `SpellDef::duration/scalingDuration/projectileLife` (заданы во всех 8
строках, не читаются), `DepositLayer::revision`, `BattleStats::steered`,
`CompletedSaveJob::saveMs`, `DerivedBonuses::relationBonus`, `RoadAxisSet::anchored`.

**H12. Мёртвые значения перечислений:** **14 из 24 `PerkID`** (`GodsMark`, `Saint`,
`DeathWord`, `Antimagus`, `MagicBody`, `BloodMagic`, `Autist`, `Specialization`,
`Generalist`, `Apostle`, `Demiurg`, `Revenant`, `Stonks`, `Sacrilegist`) — ни строки в
`kPerkList`, ни эффекта, ни упоминания; держатся комментарием «ради совместимости сейва» с
форматом, который проект объявил ничего не стоящим. Плюс `GameSubStateKind::Paused` и
`ViewingMap`.

**H13. Мелочь на один заход:** `commodity_def`, `deposit_commodity_id`,
`structure_is_decayed` (дубль проверки знака, инлайн в `map_data.h:520`),
`find_saved_subworld` (сырая обёртка над `_ref`-формой) — ноль ссылок; парсер KV-файла в
`keymap.cpp:96` — копия `ui_settings.cpp:53` (комментарий это признаёт); четыре мёртвых
`#include`; три протухших TODO, включая `main.cpp:2105` `rebuild_landmarks (PHASE C)` —
функции с таким именем не существует, задача решена иначе; пролог смоука продублирован
пятью копиями в `main.cpp`.

**Что вернулось ЧИСТЫМ** (сказано вслух, чтобы никто не перепроверял): закомментированного
кода нет НИ ОДНОЙ строки; TODO всего три, `FIXME`/`HACK`/`XXX` — ноль; система фракций
действительно едина (один склад отношений, одна дверь `faction_relation`); солнце одно
(`celestial.h:137 sun_dir` служит и небу, и свету).


---

# Что закрыто в этот заход (2026-08-20)

Отмечено здесь, чтобы следующий агент не пошёл строить построенное.

**Канон и доки.** GPU-декларации приведены к S5 в девяти файлах; авторитет в AGENTS.md —
`CANON.md` вместо мёртвого TS-прототипа; знак поля магия↔тьма перевёрнут в `lore.md` и
`zones.md`.

**Автолевел (III.7).** Оба снесены, `zoneLevel` ушёл из спавн-API, негативный контроль.

**Дезертиры (III.10).** Пул получил читателя: `raise_deserter_bands`, исход дня = √(пул),
детерминизм от (worldSeed, day), 29 проверок закона.

**Две системы тел (S16).** Слиты в одну: один перечень поведения, одна таблица на 30 строк,
сплошные id (`0x100` выжжен), одно рождение, лист у всех. Побочно вылечены три немых бага —
пустой лут всего бестиария, расхождение авторезолва с рукопашной, исчезновение сквада во
главе со зверем.

**Ледж (A1).** Деревня больше не платит городом того же номера; коллизия внесена в фикстуру
как контроль.

**Добыча под землёй (H2).** Потерянный указатель в бюджетном драйвере ИИ — мир больше не
останавливает рудники, пока игрок в подземелье.

**Швы.** Один `wrapi` вместо шести копий; `torus_delta` через `std::remainder` вместо
однопериодной складки; камера следует коротким путём; период шума закрыт на мир в шейдере
карты, в поле опасности, в лесном массиве, в речном меандре; период горных хребтов исправлен
(он резал три обрыва посреди каждого мира); клетка микромира названа завёрнутым индексом, а
шум детализации закрыт на тайловый размах мира.

**Дорожный A\* (H-путь).** Пол цены поднят до 1.0, наценка перенесена на открытую землю —
эвристика снова допустима, переиспользование дорог впервые возможно.

**ОСТАЛОСЬ САМЫМ КРУПНЫМ** (список обновлён 2026-08-27 — ДВЕРЬ УРОНА ЗАКРЫТА
целиком, см. combat.md: пять сайтов hp, шесть решателей враждебности, фракция
в таблице существ, дубль ширины тела, два if-чейна по трём фракциям и немой
авторезолв — всё мертво; прежний список от 2026-08-24 — швы 3×3 закрыты замером
(ff8fd07), дверь/закон спавна/закон шага закрыты треком двери, см. ниже):
43 из 78 тестов обезвредили защиту «ноль проверок = провал» (check.h);
`Settlement` и `Village` — две структуры на одну строку канона (и поэтому деревни не
производят и не выдают квестов); монета в мире только уничтожается; вода не зовёт общий
закон освещения и не принимает теней; III.6 `kPopCarryingCap` — потолок-константа вместо
ёмкости земли (уходит в Инк 8 трека двери).


---

# Док-свип — 2026-08-23/24

Заход по ДОКУМЕНТАМ: 44 дока сверены с каноном и кодом (4 разведчика), правки внесены
(4 правщика), всё с проверкой каждого утверждения по src/ перед записью.

**Закрыто из прошлых судов.** I.1–I.3 добиты ДО КОНЦА: TS-авторитет, переживший заход
08-20 в `MANIFEST.md:389`/`:13`/§Gaps, в таблицах `ARCHITECTURE.md` («TS module» как индекс)
и в `design.md` §0 — истреблён везде, включая строки «TS origin» во всех системных доках;
GPU-остатки (`ARCHITECTURE.md:144` «land the compute kernels», `MANIFEST.md:338` «core goal
is a compute-shader workload», `macroworld.md:139`, `vulkan.md` P7 «pending») сняты; знак
поля магия↔тьма исправлен в последнем пропущенном месте (`lore.md` таблица источников).

**Снесённые системы больше не описываются как живые.** Пейпердолл вычищен из `render.md`
(тени 48×48, два прохода тел, 32 света, одна теневая карта — всё переписано по коду:
один body-пасс, 16 светов, две каскадные карты вокруг камеры), из `problems.md` §2/§4
(плашки «СИСТЕМА СНЕСЕНА»), `work_vector.md` §5.4, `trailer.md` §5, `MASTER_PROMPT.md`;
«monsters are sheet-less» / 0x100-дискриминатор вычищен из `monsters.md` (переписана
половина дока под одну таблицу 30 строк), `microcombat.md`, `rpg.md`, `MASTER_PROMPT.md`.

**Удалено по решению владельца:** `proposals/macro-parties.md`, `census-followups.md`,
`unified-container-system.md`, `subworld-interaction-system.md` (остаток — строкой бэклога
в session-prompts: лут находится взглядом только при `Structure` рядом, `engine.cpp:1740`),
`project-assessment-2026-07-31.md`; `audit.md` ЧАСТЬ I вырезана (8/10 хедлайнов мертвы).
`MASTER_PROMPT.md` переписан в ~110 строк онбординга; `design.md` разжалован из «design
target» (эталон = CANON.md), вычищены автолевел/«100 дней»/мораль/OpenGL; `release.md` —
решение владельца «дата не цель»: календарь снят, остался бессрочный чеклист. `README.md`
переписан по реальности (C++23/Vulkan, `build/timaert`; выдуманные конфиг-таблица и
«Historical»-сборка удалены).

**Уточнено измерением.** E1: защита `check.h` обезврежена в **43 из 78** файлов
(24 кладут `CHECK(true, …)` и гонят проверки через `return fail()` мимо счётчика,
19 не включают `check.h` вовсе) — заявление session-prompts «С12 закрыт целиком»
относилось к другой правке. У двери урона (work_vector §4) найден **пятый** сайт
вычитания hp: `macro/npc_ai.cpp:1138` (к четырём известным).

**ОСТАЛОСЬ ПОСЛЕ СВИПА (не докочищено, ждёт отдельного захода):**
- комментарии-лжецы в КОДЕ (правка кода — за владельцем): `macro/fauna.h:93-95` и
  `macro/character_sheet.h:22,223` («0x100 marks monster», «monsters are sheet-less»),
  `gpu/vk_sprite_array.h:20-26` («48×48 paperdolls»), шапка `sub/body.h` (старый порядок);
- `ARCHITECTURE.md:1289-1339` §Renderer3D — устаревший блок (GL_TRIANGLES, «shadows not
  implemented») тронут точечно, целиком не переписан;
- `MANIFEST.md` §Highlights «heraldic flags» — кода флагов не найдено, заявление не сверено.

**Решение владельца по фокусу: следующий трек = ДВЕРЬ КОНТЕКСТА (S6).**


---

# Трек двери контекста — что закрыто, 2026-08-24

Инкременты 1–7 (`c9f2bea..37f80a8`), THE-док трека — [context.md](context.md); `check`
78/78 зелёных. Находки выше помечены по месту; здесь сводка, чтобы следующий агент не пошёл
строить построенное.

**Дверь (III.12).** ПОСТРОЕНА: конверт `macro/macro_world.h` (растёт полем, null-слой =
нулевой вклад) + ОДИН сборщик `macro/cell_facts.h`; `resolve_context` субмира — его
потребитель с генерационно-приватными добавками; сезон — своя колонка `seasonTempOffset`.

**Перепекание (G2, хвост III.13).** ОДИН `rebake_world`: весь производный мир, в порядке
зависимостей, на загрузке, сезонном сеттле и каждом переходе макро↔микро (S7 буквально);
CPU синхронно, текстуры через dirty-флаш (закон Сессии 19).

**Вторые словари (C1/C2/C4/C5/H10/H7).** `LandmarkType` — единственный словарь + запечённый
`landmark_grid.h`; конверт собирается раз; один `biome_at_cell` (вода по запечённой маске);
UI-близнецы биома и склеенный/полумёртвый `landmark_registry` ушли.

**Драйверы и дороги (H2, ценовая половина H1 + §7.9).** `TickContext` встраивает конверт —
копии из семнадцати полей мертвы; планировщик дорог ходит по THE закону шага, его частная
таблица цен мертва. Клон A\*-алгоритма (`RoadIndexedHeap`) ЖИВ — закрыта таблица, не
слияние.

**Закон спавна (F4/F5 + D).** Опасность = байт-КОНТИНУУМ 0..255 (десять ступеней — только
подписи), сила строки — ВЫВОД из её же боевой мощи, habitat — битмаска-колонка; толпа города
— тем же законом по `kHabTown`, профессии по живым депозитам; `ZoneLayer::field` (4 МиБ без
читателей) снесён. Автолевел остаётся мёртвым — зона меняет КОГО, не числа на теле.

**Закон шага и марш (A8, A5-хвост).** Полотно + вклады (дорога 1.0 / грунтовка 1.5 / поле
1.8, непрерывная крона, климб на ребре `PathCostData::height8`) — для всех ходоков; марш
8 клеток/час при 1 SP/клетку (чистые данные 1-го уровня), 96 тайлов/с внизу — ВЫВОД
(93.75 + 2.4% именованный допуск); период шума зон закрыт на мир ещё заходом швов.

**В полёте — Инк 8:** вклады цены/роста/авторезолва (`AutoBattleSide::terrain`, сезонный
`yieldMul`) и III.6 (`kPopCarryingCap` → ёмкость земли через ту же дверь).

---

# Канон-аудит и рефакторинг — 2026-08-29 (сейв v59 → v62)

Большой заход «сверить код с каноном и снести вторые правды». Итог: `check`
зелёный, смоуки зелёные, доки сведены тем же днём. По следам прежних записей —
что ЗАКРЫТО:

**Один рекорд ландмарка (вердикт владельца, v62).** `struct Landmark` с
колонкой `type` + один `GameState::landmarks`; структуры/векторы
`Settlement`/`Village`/`Spire` и шесть сериализаторов мертвы (один
`write_landmark`/`read_landmark`); `for_each_landmark` несёт приоритет
владения клеткой (`kLandmarkYieldOrder`), `landmark_by_id` — один поиск.
S9-переход стал записью в колонку — машинерия переходов разблокирована
(следующий трек). Попутно v60 — мёртвые колонки (`tempMin`/`tempMax`), v61 —
сид мира честный `uint32`.

**Экономика — закон сохранения.** Кредит-прежде-дебета во всех дверях
(`transfer_value`, `barter_swap`, добыча, производство возвращает входы, лут и
награды сообщают дельту, недоплата = факт долга); найм платит В КАЗНУ
поселения (цена = колонка `NpcTypeDef::hireGold` × единый уровневый закон
`soldier_level_factor`); минт = колонка `FactionDef::mint` (варвары наконец
чеканят `coin_barbar` — strcmp-цепочка не матчила ни одну строку); ёмкость
каравана = `rt.carryCap` из листа × `haulMult` (константа 256 кг мертва);
дверь производства деревни открыта (`econ_produce_day(site=Village)`; рецептов
Village пока ноль — балансовый, не структурный долг). Один стрей:
`deliver_bag_home` ещё дебетует прежде кредита.

**Бой — согласие шкал.** Авторезолв учитывает броню тождеством
`kArmorHalving` (эффективные HP = алгебраическая инверсия множителя двери;
дом константы теперь `macro/npc.h`, тест согласия смягчает удары тем же
законом); ЕДИНЫЙ XP-закон — колонка `xpReward` на каждой строке
(существа = 5·(L₀+1), вывод сохраняет старую выплату), `exp_from_fight`
мёртв; цепная молния судит жертву в 3D (S13).

**Летопись.** Дверь дел `record_deed`/`grant_renown` переехала app → `macro/squad.h`
— микро-дверь и оба сделочных писателя теперь ПЛАТЯТ славу (файл+плата
неразделимы, тест-щит с негативным контролем); колонка `victimShare`
(вердикт: `Traded` доли жертвы не платит — «торговля — маленькое дело»);
журнал зануляет `nextInCell` в копии, одержимость = участие; анналы
валидируются при чтении, `annalsFull` виден в UI; шина потеряла
`WorldHistoryEntry` — четвёртое хранилище прошлого мертво.

**Мир.** Климат косинусный по широте (S19 дописан: зональность — замысел,
излом на шве — дефект); ОДИН закон зрения — игрок в общем `SquadIndex`,
частный канал восприятия игрока мёртв, threat-step никогда не авторезолвит
встречу с игроком; ОДИН A* (`find_path` + `PathScratch`), дорога = строка
`kRoadClasses`, грунтовки честным A* (лерп мёртв), иерархия камень→грунт
эмерджентна через цену мощёного полотна — клон `RoadIndexedHeap` из записи
2026-08-24 ЗАКРЫТ слиянием; Dead-сквады сметаются в конце обоих ИИ-драйверов.

**Реестры вместо частных таблиц.** `FeatureDef{bedWeight, opticalCost,
civStrength}`; `kDepositDefs`; `kMoodRows`; `kNpcMapColor`; `kNpcLootId`
типизирован под guard; `spellbook_rule_active` вместо сравнения имени
"flight"; колонки фауны/спавна в `LandmarkDef` (кросс-guard с fauna.h).
Мёртвый код снесён: `baseValue`/`baseHp`/трио `discover_iron_vein`
(живой закон — `GrowthDomain::Geology`)/`kZoneLabels`/аудио
EmpireTheme+Witch/легаси сейв-ветка/алиасы квест-событий; ~15
комментов-лжецов переписаны.

**Субмир.** Мосты — честная кладка (деки `Structure::Bridge` над водой,
опоры-цилиндры, арки = отсутствие твёрдого, отсев по верху спана против
посадки рельефа; закон твёрдости общий — dungeons.md); позиция башни шпиля
экспортируется модулем генератора (`dgn/dispatch.h`, S17); `read_sign` — по
клетке пропа; `place_body_ring` — одна копия.

**App.** Смоук-харнесс вынесен в `src/app/smoke.cpp` (+`smoke.h`,
`app_state.h`); `main.cpp` 12.7k → 5.4k строк без изменения поведения;
19 тестов переведены на `tests/check.h` (хвост E1).

**ИЗМЕРЕНО И НЕ СМЁРЖЕНО (решение за владельцем):** civ-запекание через
`optical_sweep` — квантованное поле идентично, сырые float расходятся на
ульпы (замер записан в `zones.cpp`); BFS оставлен до вердикта «re-tie».
**Ждёт глаз владельца:** вид мостов в игре. **Не решены клеточные законы:**
SP-цена шага по мосту, battle-тайл под декой (problems.md §33).

---
---

# Суд по канону — ВТОРОЙ проход, 2026-09-16/17

Судья — CANON.md (2723 строки, прочитан целиком). Судимое — весь src/ (282
файла, ~97k строк) восемью направлениями: экономика/поля, сквады/игрок/зеркало,
бой/РПГ/магия, навигация/тор, ландмарки/летопись/сейв, генерация субмира,
сквозной свип S6/S16/S26, внутренние противоречия самого канона. Каждая улика
проверена чтением кода, не одним грепом. Вердикты владельца — 2026-09-17,
дословно.

**Что сверено и ЧИСТО** (перечислено, чтобы не пересуживать): зеркало
(record_of/state_of/pools_of без ветки на игрока, траты в запись, лут из
записи), одна дверь броска (dice.h/roll_strike/crit_procs, ноль to-hit),
фракционная слепота снарядов, один гейт recoverySteps, темп=масса, летопись-POD
(32 байта, два яруса, record_deed факт+слава, victimShare=false, дедуп запросом),
тор-заворот (единый wrapi, порталы по сегментам, трансляционный тест), один
предикат «нельзя встать лагерем», брод только Fleeing, threat derived, Kingdom
вырезан (структур нет), дань >>3/>>5 под static_assert, kRecipes={output,site},
craft_item = чеканка, kWorkCyclesPerBar/kGatherPerCycle/kMintMetal мертвы,
ECS без кучных контейнеров, инвентарь 256, keymap по скан-кодам с цитатой живой
привязки, звук из мира, пять слоёв генерации без связей вбок, свои стены видов,
kGateClearM в height.h, очаг-средняя, дальняя высота = та же функция на
коарс-октавах, воздух с высотой (air_law_test).

## ВЕРДИКТЫ ВЛАДЕЛЬЦА 2026-09-17 (по вопросам аудита)

1. **Монета — просто товар, ворота снести.** «снести полностью монета это
   просто товар со стоимостью 10 для серебра 100 для золота 1 для медяка
   (никаких особых механик и ворот) всё через бартер» → `is_currency_item`
   (currency.h:39) и спецветка ценника (trade_widgets.h:332) умирают. Форма
   номиналов медяк/серебро/золото 1/10/100 — уточнить при постройке (что с
   фракционными монетами и составом {серебро 1}→32).
2. **Профессии-типы в толпе — снести полностью.** «все профессии у нас теперь
   эмерджентные это просто что решает делать сквад в макромире то есть
   профессии нет» → Woodcutter/Miner/Quarryman/ClayDigger из уличной толпы
   (fauna.cpp kHabTown, crowdRoles города) и их лут-профили — снос; enum-слоты
   остаются мёртвыми ординалами сейва.
3. **TreeLayer — та самая форма.** Замысел: «поле с числами в каждой клетке
   которое повторяет связность топологию мира… единые штабеля расширяемые поля
   карты ресурсов над миром» — система сводится к плоским полям на род,
   добавление ресурса (медь и т.д.) = новое поле. Где не доведено — доделывать,
   форму лучшего слияния (TreeLayer vs ResourceGrid) обсудить.
4. **SettlementHistory — снести.** «сноси да не надо (у нас минимум систем есть
   уже единая система фактов и событий и лог истории)».
5. **Одержимость — дочистить как эффект спелла.** «просто эффект спелла типа
   меняется флажок игрок на другую сущность (и просто помнится оригинал) — при
   выходе сброс если одержим генерик нпц/мобов если одержим лордом в субмире
   который контекстно анкета макромира то остаётся им» → закон шва: производное
   тело — сброс при выходе из сцены; носитель анкеты — одержимость переживает
   шов.
6. **Брошенный сквад — стоит неактивно, видимо.** «должен стоять просто
   неактивно никаких костылей и хардкодов это буквально просто перенос флажка».
7. **Player-константы — снести.** «плеер надо убрать мы всё это чистим и
   приводим к единой системе игрок == нпц никаких плеер специфик говноконстант»
   → kPlayerMeleeRange и родня — вывести из строки/листа.
8. **Каст ≠ выстрел.** «каст у нас через систему спеллов а есть ещё система
   стрельбы (метание/луки/арбалеты/мушкеты и тд)» → Missile-склейка
   (character_sheet.h:463) неправильна, доставка стрельбы — своя колонка без
   INT-добавки.
9. **Летопись — одна дверь.** Прямые chronicle_record (факты земли) «сводим
   сливаем в одну систему».
10. **Ландмарки бывают безымянными честно.** «безымянный это честный случай
    именованного типа просто нули вместо чар строки имени» — единой системой,
    без спец-веток.
11. **Руина кольцами — НЕ находка.** «У НАС DOD ИНКАПСУЛЯЦИЯ РУИНУ НЕ ДОЛЖНО
    ВОЛНОВАТЬ ЧТО ТАМ ПОМЕНЯЛОСЬ В ГОРОДЕ И НАОБОРОТ! ЭТО ОЧЕНЬ ВАЖНО» —
    вердикт одного вида не судит чужой модуль (внесено в CANON S17).
12. **Противоречия канона Д1–Д6 разрешены** и помечены в CANON.md 2026-09-17:
    жалованье СГОРАЕТ (пул лута); содержание (еда+жалованье) РАЗ В СЕЗОН, ЗА
    СЕЗОН ВПЕРЁД; недоимка = единое окно сезона, 1/8 ростера в начале сезона;
    права чеканки как колонки не существует; опыт копят НОСИТЕЛИ АНКЕТ
    макромира (ростер не копит); «циклы» труда — история, закон в S14.1.

## РЕЕСТР ДЕФЕКТОВ КОДА (к починке отдельными сессиями)

### Блок 1 — одержимость + вердикт №5 — **ЗАКРЫТ ЦЕЛИКОМ 2026-09-17**
(сессия А: вырез машинерии + спелл possession, коммиты e2d15dbf..c25938ea)
- ✅ player_entity.cpp:295 — refresh_player_body читает NPCType из NPCKind
  записи флага (фолбэк Adventurer = собственный сквад, для которого литерал
  и был правдой).
- ✅ engine.cpp:886-897 + main.cpp:3435 — срыв флага на входе ВЫРЕЗАН: флажок
  переживает шов, носимое тело сцены — проекция записи флага (MacroOrigin,
  лист, гир, пер-тик рефреш). Свидетель: exit_remap re-enter (doors_alive,
  body_is_his).
- ✅ spawn.cpp:1363-1366 — заём ростера именует MacroSpawnId + клетку записи
  флага (для собственного сквада это прежний kPlayerSquadOrdinal); фракция
  ростера — правило знамени записи флага.
- ✅ spawn.cpp:1404-1414 — исключение PlayerSquadTag из проекции снято:
  брошенный сквад стоит в сцене видимо и без сознания («без сознания» =
  отсутствие флажка, прочитанное дверью ИИ через зеркало: лидер — по записи,
  его люди — по ростерному займу). Свидетель: exit_remap abandoned.
- ✅ main.cpp:577-603 + squad.h:944-957 — все шесть дверей авторезолва
  (сторона, гир, потери, рана, факты, XP) — запись ФЛАГА.
- ✅ engine.cpp:2946-2948 — перекраска только у тела БЕЗ NPCKind (хаск);
  одержимое тело носит свои цвета.
- ✅ npc_ai.cpp:2985-2995 — след кладут оба неводимых свипом сквада: запись
  флага (идёт) и флаглесс-оригинал (стоит).
- ✅ engine.h:118 — kPlayerMeleeRange СНЕСЁН: рука = attackRange строки
  носимого тела (двери player_arm_reach / Combat.attackRange), интеракции
  данжа тянутся ею же, коридоры (cave/house/dispatch) привязаны
  static_assert'ом к kAdventurerCombat.attackRange.
Сам спелл: possession (kSpellDefs, Dark→Void, Targeted, tier 4, мана 200,
30 м), гейт СТРОГО targetLevel < casterLevel + ранг VoidMagic, провал жжёт
ману/рекавери, закон шва по двум формам рождения record.h, 1 хоп запрещён.
Machinery: possess_aim/possess_by_id/консольный possess вырезаны; дверь
переноса — header-only sub/possess.h.

### Блок 2 — S5/экономика
- ✅ **2026-09-18 (сессия В)** state.h — shipsAtCell СНЕСЁН вместе со сканом
  nearest_region_ship: счётчик пришвартованных = число фичи в ЕДИНОМ слое
  разработки (gs.worked), «ближайший причал» = обход gs.builtFeatures. Тем же
  ходом scar-строки природы стали полями (ResourceGrid) — в GameState не
  осталось ни одного unordered_map, утверждение S5 стало правдой.
- state.h:804 — пул лута: вливы есть, раздачи НЕТ ни одной (канон помечает ✓).
- ✅ **2026-09-18** econ_day.cpp — посев капитала с «± четверть разброса от
  мирового сида» (768..1279 из 1024 по солёному xorshift), тремя монетами
  через честную размену add_value_in_coins; свидетель econ_v1_test судит
  ПОЛОСУ вокруг базы плюс детерминизм одной соли плюс то, что разброс
  действительно разбрасывает.
- ✅ **2026-09-18 (сессия Г+Д)** economy.cpp — trait_price_mult переведён на
  kTraitPriceRows (npc.h) по образцу kMoodRows; строки читаются в порядке
  enum, колонка pricesMarket хранит «темпер молчит → цену НЕ трогаем».
- world_tick.cpp ~190-345 — tick_settlements_/tick_villages_ два клона одного
  суточного закона; :247 хардкод "bread" в гарнизонном содержании.
- ✅ **2026-09-18 (сессия Г+Д)** landmark_registry.h — static_assert связал
  labourShift строки City с kHeadsPerCityWorker (1<<3 == 8).
- ✅ **2026-09-18** currency.h + trade_widgets.h — ворота, СПИСОК ВАЛЮТ и
  кошелёк снесены целиком (вердикт №1): 12 монет данными каталога (3 номинала
  × 4 фракции, mint-колонки реестра фракций), один закон платежа по плотности
  ценности, выход чеканки 128 закреплён МАССОЙ (свидетель
  mint_yield_is_mass_honest), медь и золото введены полями геологии.
- ✅ **2026-09-18** fauna.cpp + crowdRoles + лут-профили — профессии толпы
  снесены (вердикт №2); буквальный вердикт владельца «пусть пока в городе
  только стражники и пизанты» — Merchant и Witch тоже вон из страйпа
  (известная цена: ai_mage_hunt без амбиентной добычи), гарнизон 60/40
  Guard/Peasant. Свидетель проекции ПЕРЕВЁРНУТ: утверждает отсутствие чужих
  родов на улице.
- ✅ **2026-09-18** SettlementHistory снесена целиком (вердикт №4): структура,
  поле Landmark, сейв-ветка, писатель world_tick, UI-вкладка и строка «History
  samples» — одна память места (летопись S20.1).
- smoke.cpp:5474 — рукописная копия формулы труда в смоуке.
- npc_ai.cpp:3706-3712 — аукцион: дорога делителем-евклидом, тейк константой 32
  (после эмерджентной ходки — руки×спины); принятая форма, отметить при треке
  грузоподъёмности.

### Блок 3 — бой/РПГ
- ✅ **2026-09-19 (сессия Е, ЗАГЛУШКОЙ по вердикту владельца)** Перк-система:
  третья валюта построена (perkPoints 5-5-5/1-1-1, маска PerkMask 256 бит в
  листе, сейв v100, НПЦ бюджетно-идентичен, свидетель в sheet_registry_test);
  комменты «каждый 10-й» умерли. ГРАФ УЗЛОВ осознанно НЕ ПОСТРОЕН — вердикт
  дословно в CANON S14: «сами перки мы делать не будем… пока просто заглушка».
- ✅ **2026-09-19 (сессия Е)** NPC multPct=100 снят ОБОИМИ концами одним
  коммитом (S13 «один закон боя»): project_combat.multPct = лучший
  тренированный скилл домена атаки (sheet_strike_mult_pct — оружейные для
  мили, школы для кастов), читают spawn combat_from_sheet И auto_battle
  fighter_power. Свидетель в sheet_registry_test (домены не текут, кулак —
  билд); пролог PASS (не ваншоты: ранги низких уровней = +20-30%).
- ✅ **2026-09-19 (сессия Е)** tierPct и scalingPower СНЯТЫ (вердикт владельца
  «влить в кубы», опора — низ дуги INT=0): формула спелла = (бросок + INT) ·
  процент ШКОЛЫ, последний float ушёл из урона. Колонка scalingPower умерла
  целиком, её значение влито в dice строк (fireball 30→36, ice_shard 40→56,
  energy_beam 25→28, armageddon 80→160); tier снова значит ровно «ярус шпиля».
  Свидетель считает матожидание одной строкой — третья ручка покраснеет.
- ✅ **2026-09-19 (сессия Е)** БРОНЕВЫЕ СКИЛЛЫ достроены: род = колонка `skill`
  строки предмета (вердикт владельца), вклад вещи+её аффиксов множится рангом
  её рода, строчная броня существа — обучением носителя; оба конца (defense_of
  и авторезолв) одним коммитом; «Без брони» СПИТ поимённо (pctPerRank 0).
  Побочно: два слагаемых брони в defense_of сведены в одно — аффиксы больше не
  считаются нетренированными рядом с тренированной вещью.
- spells.h:216 + components.h:587 — friendlyFire: колонка значит «есть AoE»,
  байт снаряда мёртв, флейвор ice_shard обещает фракционный щит. Переименовать/
  схлопнуть.
- ✅ **2026-09-19 (сессия Е)** РАСКЛЕЙКА СДЕЛАНА (вердикт №8 закрыт): строка
  называет свой спелл ОРДИНАЛОМ (`spell_ordinal(...)`, constexpr), каст берёт
  кубы/тип/школу СПЕЛЛА и INT, выстрел — скилл стрельбы БЕЗ добавки атрибута,
  удар — STR. Свидетели в sheet_registry_test, включая негативный контроль
  (свинг всё ещё берёт STR, значит ноль у выстрела — закон, а не мёртвая
  дверь). Долг контента: у Void нет урон-спелла, лич временно на Arcane.
- ✅ **2026-09-19 (сессия Е)** aim_target трёхмерен: 3D-вектор взгляда вместо
  yaw (atan2-сплющивание у вызова умерло), дистанция и конус 3D, свидетели
  3-4-5 в targeting_test. Тем же днём ПОСТРОЕН пошаговый режим P (вердикт
  дословно в CANON S13, смоук turn_based_cycle).
- 9→8 типов урона — признанный долг с сейв-миной ArmorSlash (bonus.h:117);
  ✅ **2026-09-18** шапка damage_types.h помечена: «9×9» = состояние кода,
  8 = цель (вердикт владельца). Код и enum не тронуты — долг открыт.
- ✅ **2026-09-18 (сессия Г+Д)** тексты: «Burning DOT», «Shock interrupts»,
  «No friendly fire» вычеркнуты; свидетель ПЕРЕВЁРНУТ — spell_casting_effects_test
  пинил литерал «Burning DOT» (охранял дефект) и теперь подметает закон «ни
  одна строка pros/cons не цитирует своё statusEffect-слово». Сами колонки
  statusEffect/effects — сироты по-прежнему, это отдельный ход.

### Блок 4 — навигация
- ✅ **2026-09-18 (сессия Г+Д)** kShipWaterWeight снесён — цена воды под
  парусом читает kNavSeaWeight, ту же, по которой запечён морской ярус.
- nav_field.cpp:615-626 — nav_step в своей округе ходит цепочку родителей (до
  2^14) против «три чтения»; чинится ходьбой от себя вниз по distHome.
- ✅ **2026-09-18 (сессия Г+Д)** threat_field.h:80 читает kNavNoRegion —
  routeNext хранит округа, а не дистанции.
- ✅ **2026-09-18 (сессия Г+Д)** spawners.cpp спрашивает biome_from_climate;
  предикат тот же (не растёт на Tundra/Snow/Desert), но края полос —
  дверины. ГЕНЕРАЦИЯ СДВИНУЛАСЬ: холодная и жаркая полосы сузились, леса
  больше. Величина в деревьях не измерена.
- Мост до 32 дней стоит как вода для ЦЕНЫ шага (costGrid перепекается сезоном,
  NavWorld — сразу) — числовое следствие непостроенного допекателя.
- ✅ **2026-09-18 (сессия Г+Д)** torus.h wrapf починен (`if (m >= size) m = 0`)
  и контракт [0, size) записан.

### Блок 5 — ландмарки/сейв/летопись
- ✅ **2026-09-18 (сессия Г+Д)** nextLandmarkOrdinal само-лечится по
  загруженным ландмаркам, образцом квестов.
- politik.h:59-68 — politik.cities: замороженный дубль правды о городах; живые
  читатели: якорь спавна игрока, HUD. Дефект модели S9.
- ✅ **2026-09-18 (сессия Г+Д)** фолбэк безымянного = колонка label его
  строки реестра, для ВСЕХ видов (побочно заговорили руины/логова/шахты/
  башни). ЦЕНА НА ВЕРДИКТ: слово «Depleted» из текста ушло.
- ✅ **2026-09-18 (сессия Г+Д)** кап слотов говорит вслух и в релизе, клетка
  честно остаётся ничьей.
- Легаси-строки в сейве: GameSubState.eventId/enemyId, маркеры, logic-узлы,
  Reward.itemId/faction — строка вместо ординала (v63-класс).
- ✅ **2026-09-18** macro_stock.cpp — факт земли Discovered проведён через
  record_deed (вердикт №9): дверь и подшивает, и платит; без ECS (полевой
  тестовый мир) память мира всё равно пишется — fail-open на памяти.
- Счётчики квестов (state.h:646-650) вместо летописи; роды QuestTaken/
  QuestDone/OwnerChanged/Battle/Spawned без единого писателя.
- spawn.cpp:1339 — боец сквада игрока без земли теряется молча (S28: WARN).

### Блок 6 — генерация субмира / дальний мир
- ✅ **2026-09-18 (сессия Г+Д), по вердикту владельца.** Разбор: это были НЕ
  одна величина, а два средних ОДНОГО FrontagePlan — фасад (4.5) и шаг по
  улице (6.0, фасад + зазор); настоящий дефект был в том, что ОДИН вопрос
  «сколько улицы нужно N домам» задавался разными из них (город — шагом,
  квартал — фасадом, отсюда «квартал на 25% короче»).
  **Вердикт владельца, дословно:** «верхний квартал ЭТО ЧИСТО АРХИТЕКТУРНАЯ
  СТРУКТУРНАЯ ВЕЩЬ ГЕНЕРАЦИИ ГОРОДА ТИПА КОМПАРТМЕНТЫ НИГДЕ КРОМЕ ЭТОГО ОНА
  НЕ ИГРАЕТ РОЛЬ! ВЫРЕЗАТЬ ЛИШНЕЕ И СЛИТЬ» (+ «жителям наплевать, в каком они
  квартале, просто все честно осциллируют между домом и улицей»).
  Сделано: полоса плота объявлена один раз, оба средних ВЫВЕДЕНЫ, FrontagePlan
  читает те же константы, спрос на улицу — одна функция city_street_demand,
  которую оба компартмента зовут одними словами. Проверено грепом: слова
  upper/quarter нет ни в одном файле, кроме gens/city.cpp — отдельного
  поведения жителей квартала не существовало и не заводилось.
  Измерено: built 82841 → 83722 тайлов (+1.1%), bare −881. ГЛАЗАМИ НЕ ПРОВЕРЕНО.
- ✅ **2026-09-18 (сессия Г+Д)** kBodyWidth выведен как 2×kNpcBodyRadiusDefault.
- ✅ **2026-09-18 (сессия Г+Д)** dispatch.cpp:143 читает
  kInteriorFightSpanTiles.
- vk_renderer_3d.cpp:54 — kFarWorldHalfSpanM=24 клетки: де-факто константа
  дальности, воздух не успевает (~60% контраста на кромке); финал — лестница
  колец. Лес и снеговая линия дальнего мира (решение ?31-2) не начаты.
- far.frag:45-47 — дальний грунт светится мимо lit_surface: стык проявится
  освещением под облаком/горной тенью.
- dgn/*: waterLevel=0 литералом мимо собственной колонки (5 мест); dgn-диспетчер
  остался switch-ем (урок gens уже записан).
- Поля следов: планы 1024² (267 МБ worst-case) против канонной сметы 512²
  (67 МБ) + read_scent отвергает меньший kFactionCount, который канон обещает
  грузить — ОТКРЫТЫЙ ВОПРОС (ниже).

### Блок 7 — комментарии-лжецы — **ЗАКРЫТ 2026-09-18 (сессия Г+Д)**
Все перечисленные ниже правлены (world_fields.h оказался уже закрыт сессией
В; engine.h:108 — сессией А). Сверх списка найдена и снесена ЧЕТВЁРТАЯ копия
лжи «макро-тик заморожен под землёй» (engine.cpp:778): заморозки нет и быть
не должно (AGENTS «no cheats»), макро-ИИ думает медленным бюджетом —
main.cpp:3437. Мёртвый радиально-концентрический план удалён целиком (~100
строк, ноль потребителей).

world_fields.h:17-19 (благословляет sparse, поимённо опровергнут S5);
spawn.h:413-431 + spawn.cpp:1418-1424 + engine.cpp:833-846/913/3185/4380
(мёртвый possession-словарь как живая архитектура); base_generator.h:227-231
(0.3/0.15 против кода — а дальний мир зеркалит эти колонки);
city_layout.h:7-15 + ~100 строк мёртвого радиально-концентрического плана как
«source of truth» (живой закон — kit/lanes); damage_types.h:1-9 («9×9»);
macro_stock.cpp:297 (ложное обещание reach для леса); стейл «id within its
kind» (landmark_grid.h:33, landmark_iter.h:20, cell_facts.h:33) против v54;
world_gen.cpp:217 (мёртвые gs.spires); squad.h:907 + components.h:509
(PlayerState::army); engine.h:108-113 (формула эпохи до фазы 3); map_factory.cpp:45-48
(kHmaxQuant со стухшим выводом); мёртвые #include unordered_map в заголовках
полей (items.h:25, tree_layer.h:31, resource_field.h:42, npc_ai.cpp:34-35).

## ВОПРОСЫ 1-10 — ВСЕ ЗАКРЫТЫ ВЕРДИКТАМИ 2026-09-17
1. **Монеты: три типа фракционных монет как ДАННЫЕ каталога** («это буквально
   просто типы предметов… не какая-то особая хардкод система а просто дата в
   таблицу итемов»): состав 1 медь → 32 медных, 1 серебро → 32 серебряных,
   1 золото → 32 золотых. Арифметика сходится сама: при номиналах 1/10/100
   цены металлов = 32/320/3200, нейтральность двора (yield × номинал == цена
   состава) держится тем же static_assert.
2. **Сезон у ВСЕХ**: «ВСЁ ПОДВОДИМ К ЕДИНОЙ СИСТЕМЕ СЕЗОНОВ то есть все
   эконом соц списания балансы по сезонам и город и армия» — ежедневный якорь
   econ_consume_day умирает, помечено в CANON S10.
   **ПОСТРОЕНО 2026-09-17/18 (сессия сезонов, сейв v95):** единое окно ДНЯ
   ГРАНИЦЫ (S19.2, размазка по ординалу умерла): еда ландмарков
   (econ_consume_season, «покрыто целиком или не тронуто»), гарнизоны
   (хлеб+жалованье ×32, 1/8 один раз), сквады и игрок одной дверью
   (squad_season_window), дань — на границе; ротация сезонная (бессрочные
   артели, суд желаемого числа, условие создания = сезон содержания);
   крестьяне за еду (зп 0), оплата стоимостью (pay_value_dense).
3. **SP жгут только перемещение и труды** (крафт/добыча/строительство), бой —
   нет; помечено в S14.
4. **Поля следов 1024² — как в коде** («выглядит всё адекватно»); подтверждено
   аудитом: это честные плоские торо-связные поля по-фракционно, НЕ хеш —
   критерий связности мира соблюдён. Смета S10 поправлена (268 МБ). Строгий
   сейв-гейт кода оставлен.
5. **8 типов урона/брони — НА ВСЮ ИГРУ**, школы = наборы спеллов; помечено в
   S15.
6. **Один закон нуля SP**: любая затрата ниже 0 универсально квадратично бьёт
   по HP (как в Elin); помечено в S14.1 (согласование с «отказом ног» беглеца —
   при постройке).
   **ПОСТРОЕНО 2026-09-18:** exhaustion_bite = долг² / kExhaustionBiteDivisor
   (32, крутилка прогона); крафт-кнопка и валка игрока проведены через
   кусающую дверь (apply_stamina_cost); «отказ ног» оказался политикой ИИ
   уже в коде (беглец платит, погоне вода запрещена стоянием) — второй закон
   не строился.
7. **Ростер игрока — общее содержание** («игрок == нпц»): Adventurer.upkeep=0
   умирает; помечено у Фазы 6.
   **ПОСТРОЕНО 2026-09-18:** гейт лидера умер — окно судит КАЖДУЮ душу её
   строкой; игрок платит через ту же squad_season_window (жалованье сгорает
   в пул), CHA-скидка на жалованье снесена, дневной платёж игрока вырезан.
8. **Деревня — свой модуль, про город не знает НИКАК**: рассыпь-хутор — норма
   вида, законы фронтажа города деревню не судят (DOD-вердикт S17). Находка
   «деревня без фронтажа» снята.
9. **Канон-гигиена сделана** («давай»): ?31-дубль → ?36, легенда о ?28/?29,
   сейв сведён к 10 ГБ, ссылки смерти S17 → S4, арифметика 1-1-1 = 104/99/104
   (5+99, скилл-старт = выучивания), запрет 1-хопа записан в S4.
10. **Машинерию вселения ПОКА ВЫРЕЗАТЬ** («ПОКА ВЫРЕЖИ… И ВСЁ СВЯЗАННОЕ С
    ПОСЕСОМ»): possess_aim, консольный possess и хвосты — на снос; модель
    флажков остаётся; вселение вернётся спеллом (закон шва — вердикт №5 выше).
    Помечено в S4. Это ЗАМЕНЯЕТ ремонт дыр блока 1: их не чинить, а вырезать
    вместе с машинерией.

## НАХОДКИ ВЕЧЕРА 2026-09-18 (дубль-прогоном, не грепом)

- **Мир не добывал НИ ЖЕЛЕЗА, НИ ГЛИНЫ, НИ СЕРЕБРА** за 256 дней: артель искала
  жилу в назначенном боксе 16 клеток, металл по геологии в горах, горных
  деревень не бывало (вето счёта расселения). Двор не чеканил ВООБЩЕ (minted=0
  и до, и после сессии В). ПОЧИНЕНО: карта округи + снос вето + спрос у всего
  (чеканка 0→384, железо 0→1192, серебро 0→12).
- **spire_climb КРАСНЫЙ** на сиде 12345 (шпиль на высоте: крыша ниже земли
  входа, dz=−25.6, onRoof=0) — вскрыт сносом вето, по памяти тот же дефект был
  открыт на сиде 7. Владелец отложил: «им потом отдельно займёмся».
- **Хронический голод горных хуторов** (94 645 душ-дней за 64 дня): капкан
  «нет излишка → нет рейса сбыта → нет закупки хлеба». План калибровки — в
  NEXT_SESSION.md.
- **Один сид не доказывает металл**: два прогона одного сида дали железо 0 и
  1192 — траектории расходятся, нужен многосидовый прогон.

## НАХОДКА СЕССИИ Г+Д 2026-09-18 — subworld_recovery КРАСНЫЙ, дефект СТАРЫЙ
### ✅ ЗАКРЫТО 2026-09-19 (сессия Е): свидетель переписан на ЗАКОН ЕДИНИЦ —
### визуал не смеет уехать от своей клетки; медленное мышление макромира
### легально и печатается; негативный контроль встроен; 3 сида зелёные.

Смоук `subworld_recovery` падает с «scene tick touched a MACRO squad»:
макро-сквад за 60 реальных секунд под землёй сдвигается РОВНО НА ОДНУ клетку
(559,126 → 560,127), визуал не трогается.

ЭТО НЕ РЕГРЕССИЯ СЕССИИ. Доказано прогоном пред-сессионной сборки (30b3f9df,
отдельное дерево): она красная на сидах **1 и 42** с тем же текстом. Правка
Г7 (леса) перекатила мир и сид 12345 перестал быть везучим — зелень смоука
на нём была удачей фикстуры, а не покрытием.

МЕХАНИЗМ НАЙДЕН: макро-ИИ под землёй НЕ заморожен и не должен быть (AGENTS
«no cheats… НПЦ никогда не замораживаются»). main.cpp:3437 гоняет
tick_macro_npc_ai_budgeted медленным бюджетом — за полдня выходит ровно один
шаг. Значит ЛЖЁТ УТВЕРЖДЕНИЕ СМОУКА, а не код: «a scene tick must not touch a
single macro body — not its cell, not its visual» писалось против дефекта
SUB-1 (интерполятор ТАЩИЛ визуал в чужих единицах), а запрещает заодно
законное медленное мышление мира.

**ВЕРДИКТ ВЛАДЕЛЬЦА 2026-09-18, дословно:** «по замыслу, если мы в субмире,
то макромир симулируется как ни в чём не бывало, просто медленнее ровно от
того, насколько медленнее в субмире идёт время — макросимуляция всегда есть
фоном, это основа игры». Значит код прав, а свидетель подлежит переписке на
то, что он на самом деле стерёг (визуал и единицы). Работа занесена в реестр
накопленных красных — work_vector.md §11.2; там же §11.1 spire_climb
(«им потом отдельно займёмся»).

## НАРЕЗКА РЕМОНТНЫХ СЕССИЙ (обновлена по вердиктам)
А. ВЫРЕЗ ПОСЕСА (вердикт №10): possess_aim + консольная дверь + четыре дыры
   блока 1 умирают вместе с машинерией; player-константы (kPlayerMeleeRange →
   из строки/листа, вердикт №7 первой волны); брошенный сквад — вырез снимает
   вопрос (без машинерии не бросишь). Периметр согласовать в сессии.
Б. СЕЗОННЫЙ БАЛАНС — **ПОСТРОЕНО 2026-09-17/18** (сессия сезонов, сейв v95):
   все списания в день ГРАНИЦЫ сезона (S19.2), недоимка 1/8 одним окном,
   Adventurer.upkeep-гейт умер, сезонная ротация (бессрочные артели), один
   закон нуля SP, оплата стоимостью, крестьяне за еду. Дубль-прогон — числа
   в треке сезонов.
В. СНЯТИЕ СПЕЦПУТЕЙ — **ПОСТРОЕНО 2026-09-18 (сейв v96)**: монета = товар
   (12 строк, ворота/список валют/кошелёк снесены, платёж по плотности
   ценности, выход 128 по массе), медь и золото полями, профессии толпы и
   SettlementHistory снесены, СЛОЙ РАЗРАБОТКИ построен и третий хеш мира
   (shipsAtCell) умер вместе со сканом, летопись сведена к одной двери,
   посев капитала с разбросом. Числа дубль-прогона — в треке сессии.
Г. Дрейф-константы и сентинелы — **ПОСТРОЕНО ЦЕЛИКОМ 2026-09-18** (Г1-Г12;
   Г2 закрыт вердиктом владельца тем же днём: квартал — компартмент, законы
   слиты). Хвост той же пачки, НЕ входивший в наряд: хардкод "bread" в
   гарнизонном содержании (world_tick.cpp:247) — открыт, занесён в реестр
   накопленных красных (work_vector.md §11.3).
Д. Комментарии-лжецы (блок 7) — **ПОСТРОЕНО 2026-09-18**, ноль поведения
   кроме трёх вычеркнутых обещаний в текстах спеллов.

СВЕРХ НАРЯДА, вердиктами владельца вечера 2026-09-18 (детали — NEXT_SESSION.md
«СЕССИЯ Г+Д»):
- CANON S2 — закон непрерывной макросимуляции как основы и истины игры
  (повод: в коде жили ЧЕТЫРЕ копии противоположной лжи, а канон молчал).
- AGENTS правило 9 — ЗАКОН ЯВНОГО СМЫСЛА строки и колонки. Это обобщение
  всего, что оба прохода суда выпалывали поштучно: колонка-сирота, стена по
  виду, число с потолка, второй словарь.
- Анкеты МЕСТ + пять ремёсел + снос EconSite: «что место умеет» перестало
  быть видом места и стало рангом, который можно вырастить.
- Категория вещи как единственный словарь «что это такое»; CommodityTier и
  мёртвая колонка econSite снесены. ОСТАТОК (шаг 3, не сделан): товарное
  ординальное пространство — дань, память каравана, materialMask — на
  категории, с бампом сейва.
Е. РПГ: перки (канон-✓ снят быть не может — либо строить, либо пометить НЕ
   ПОСТРОЕНО), NPC multPct=100, спелл-множители tierPct/scalingPower,
   Missile-расклейка (вердикт №8 первой волны: каст ≠ выстрел).
Ж. Навигация: nav_step «три чтения», допекатель цены моста, wrapf-контракт.
