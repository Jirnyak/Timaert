# Status: TMA_SETTLEMENT_NPC_ACTIONS_BKR

Domain: Settlement Build tab and NPC proximity actions

## Checklist

- [x] Extract own prompt from `TIMAERT BATCH.md`.
  - Evidence: CLI extraction by exact prompt id.
- [x] Read project protocol docs.
  - Evidence: `AGENTS.md`, `matwej.md`, `translation.md`, `MERGE_PLAN.md`, `ARCHITECTURE.md`.
- [x] Read TS authority files for settlement/NPC/trade behavior.
  - Evidence: `SettlementOverlay.svelte`, `NpcProximityPanel.svelte`, `InteractionOverlay.svelte`, `TradeOverlay.svelte`, `GameScreen.svelte`, `economy.ts`, `npc.ts`.
- [x] Read native files and direct callers in scope.
  - Evidence: `overlays.*`, `macro_overlay.*`, `main.cpp`, `economy.*`, `state.*`, `items.*`, `npc.h`, `npc_ai.*`, `event_types.h`, `sub/engine.*`, `screens.*` routing references.
- [x] Settlement Build surface.
  - Result: Added a `Build` settlement tab with exact disabled backend dependencies. No decorative build button.
- [x] NPC trade surface.
  - Result: Proximity panel trade now opens for any entity with real `ecs::NpcInventory`, uses TS-style TradeOverlay buy/sell multipliers, and applies persisted `ecs::NpcTraits` price modifiers.
- [x] NPC attack surface.
  - Result: Attack routes the selected nearby macro NPC into normal subworld combat via `SubworldEngine::spawn_hostile_npc`, projecting selected inventory, traits, and visual identity; no legacy combat resolver was reintroduced.
- [x] MSVC build.
  - Evidence: `cmd /d /s /c '"C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\Common7\Tools\VsDevCmd.bat" -arch=x64 -host_arch=x64 >nul && cmake --build build-msvc'` completed successfully on 2026-05-15.
- [x] Runtime screenshot: Build tab.
  - Evidence: `smoke_04_ui.png` generated by smoke script with `open_settlement_build`.
- [x] Runtime screenshot: NPC panel.
  - Evidence: `smoke_07_ui.png` generated by smoke script with `focus_npc_panel`.
- [x] Final file report appended.
  - Evidence: `Docs/AgentLogs/LOG_TMA_SETTLEMENT_NPC_ACTIONS_BKR.md`.

## Current Status

VERIFIED: implementation slice is present, the MSVC build is clean, Build/NPC/Attack screenshots were regenerated, and the domain actions are backed by real native state or honest backend contracts.

## 2026-05-15 Continuation

Current task: Re-audit TS authority and native owned files for any remaining Build/NPC action parity that can be completed without inventing missing save/combat backend contracts.

Files under active review:
- `src/ui/overlays.*`
- `src/ui/macro_overlay.*`
- `src/app/main.cpp`
- `src/macro/economy.*`
- `src/macro/items.*`
- `src/sub/engine.*`

Working rule: implement only real C++ behavior backed by existing native state. Keep disabled states only where required backend symbols are still absent.

Progress:
- [x] NPC trade transaction feedback added.
  - Evidence: native buy/sell now validates inventory removal, mutates real inventories/gold, shows a transaction message, and appends `LogType::Economy` entries matching TS `TradeOverlay.svelte` behavior.
- [x] NPC attack route completed.
  - Evidence: proximity `Attack` returns the selected entity to `src/app/main.cpp`, which enters `SubworldEngine` and calls `spawn_hostile_npc` with the NPC type/name/level/seed plus selected inventory, traits, and visual identity.
- [x] Re-run MSVC build after continuation patch.
  - Evidence: full MSVC build passed after smoke capture hooks.
- [x] Runtime smoke capture for Build, NPC panel, and Attack route.
  - Evidence: `new_game,wait_boot_done,open_settlement_build,wait_visible,capture_frame,focus_npc_panel,wait_visible,capture_frame,attack_first_npc,wait_visible,capture_frame,quit` passed and wrote `smoke_04_ui.png`, `smoke_07_ui.png`, and `smoke_10_attack_ui.png`.
- [x] Required tests re-run.
  - Evidence: `quest_lifecycle_test`, `save_roundtrip_test`, `pathfinding_parity_test`, `spell_casting_effects_test`, `road_river_generation_test`, `character_paperdoll_test`, `combat_squad_test`, and `subworld_generator_parity_test` passed.

Final continuation status: VERIFIED for this domain.

## 2026-05-15 Physical-Bottom Final Status

Changes:
- [x] Re-audited TS authority and native routing after the final continuation request.
  - Evidence: Build remains an honest disabled contract; NPC Trade and Attack use real native inventory/economy/subworld contracts.
- [x] Added TS-style suppression for native NPC proximity badge rows when another native overlay is open.
  - Evidence: `macro_overlay_blocks_npc_proximity` plus `draw_npc_proximity_panel(..., showRows)` keep row drawing out of settlement/quest/codex/map/character/diplomacy/story/dialog/event overlays while preserving active NPC popups.
- [x] Refreshed Hecton-origin docs/tasks/logs into the quarantined Timaert import tree only.
  - Evidence: selected Hecton source files `2646`, copied `2`, missing after refresh `0`, stale-by-size after refresh `0`, import tree files `3028`.
- [x] Verified build, runtime, screenshots, tests, and polish scan.
  - Evidence: MSVC build PASS; domain smoke PASS; four fresh `1280x800` nonblank PPMs copied into `Docs/AgentLogs`; focused tests PASS; no dotnet rebuilds run.

Final continuation status: VERIFIED for this domain.

## 2026-05-15 Overlay Suppression And Import Refresh

Changes:
- [x] Re-audited TS authority for SettlementOverlay, NpcProximityPanel, InteractionOverlay, TradeOverlay, GameScreen, economy, npc, and features behavior.
  - Evidence: TS Build has no build-project contract; TS TradeOverlay price modifiers are already native; TS proximity hides while another overlay is open.
- [x] Suppressed native proximity badge rows while another native overlay is open.
  - Evidence: `macro_overlay_blocks_npc_proximity` now gates rows on settlement, quest, codex, map, character, diplomacy, story/dialog/event overlays.
- [x] Preserved already-open native NPC popups while suppressing badge rows.
  - Evidence: `draw_npc_proximity_panel(..., showRows)` still renders Talk/NPC Trade popups when rows are hidden, and internally hides rows while an NPC popup is active.
- [x] Refreshed the Hecton-origin documentation import without writing to Hecton.
  - Evidence: `MANIFEST_REFRESH_2026-05-15_TMA_SETTLEMENT_NPC_ACTIONS_BKR_2026-05-15_161319.tsv` records selected source-relative copies under `Docs/Imported/Hecton8/2026-05-15_docs_tasks_logs`.

Import verification:
- Selected Hecton source files observed: `2646`.
- Copied missing files: `2`.
- Refreshed stale files: `0`.
- Remaining selected missing files: `0`.
- Remaining selected stale-by-size files: `0`.
- Exact Timaert/Samosbor/TMA content/name matches observed in Hecton: `3`.
- Import tree total files: `3028`.
- Files under imported `Docs`: `2325`.
- Files with a `Tasks` path component: `296`.
- Files with an `AgentLogs` or `AgentLogs_Combined` path component: `1040`.
- Files with a `Reports` path component: `117`.

Verification:
- MSVC build passed: `cmake --build build-msvc --target timaert -- -j1`.
- Clean-first rebuild was attempted once and blocked by external locked test executables/object files; normal build recovered and linked `build-msvc/timaert.exe`.
- Domain smoke passed: `new_game,wait_boot_done,open_settlement_build,wait_visible,capture_frame,focus_npc_panel,wait_visible,capture_frame,open_npc_trade,wait_visible,capture_frame,attack_first_npc,wait_visible,capture_frame,quit`.
- Individual screenshot smokes passed and stable PPMs were copied to `Docs/AgentLogs`:
  - `TMA_SETTLEMENT_NPC_ACTIONS_BKR_20260515_1645_build.ppm`
  - `TMA_SETTLEMENT_NPC_ACTIONS_BKR_20260515_1646_npc_panel.ppm`
  - `TMA_SETTLEMENT_NPC_ACTIONS_BKR_20260515_1647_npc_trade.ppm`
  - `TMA_SETTLEMENT_NPC_ACTIONS_BKR_20260515_1648_attack.ppm`
- Pixel check passed for all four PPMs: `1280x800`, nonblank `9/9` sampled pixels.
- Tests passed: `quest_lifecycle_test`, `save_roundtrip_test`, `feature_layer_parity_test`, `pathfinding_parity_test`, `spell_casting_effects_test`, `road_river_generation_test`, `character_paperdoll_test`, `combat_squad_test`, `subworld_generator_parity_test`.
- Dotnet rebuilds: NOT RUN.

Final continuation status: VERIFIED for this domain.

## 2026-05-15 Live Settle Update

Changes:
- [x] Re-ran domain smoke after final build settle and copied stable screenshot evidence into `Docs\AgentLogs`.
  - Evidence: root PNGs and `Docs\AgentLogs\TMA_SETTLEMENT_NPC_ACTIONS_BKR_smoke_*.png` are `1280x800` and nonblank by sampled colors.
- [x] Re-ran the Hecton import missing-file sweep after more Hecton logs appeared.
  - Evidence: selected Hecton source files observed: `1955`; remaining selected missing files: `0`.

Final artifact state:
- Import tree files: `2970`.
- Data files excluding import index and manifests: `2940`.
- Files under imported `Docs\`: `2276`.
- Files under imported `Root\`: `24`.
- Files with any `Tasks` path component: `296`.
- Files with any `AgentLogs` or `AgentLogs_Combined` path component: `1003`.
- Files with a `Reports` path component: `114`.

Final continuation status: VERIFIED for this domain.

## 2026-05-15 Final Verification, Import Delta, And External Build Breakers

Changes:
- [x] Reverified Settlement Build, settlement Trade, NPC Trade, and NPC Attack routing against the current `build-msvc\timaert.exe`.
  - Evidence: smoke script opened Build, focused the NPC panel, routed Attack into active subworld combat, and wrote fresh nonblank PNG captures.
- [x] Reconciled the Hecton documentation import snapshot after concurrent agents appended new logs.
  - Evidence: selected Hecton source files observed by this pass: `1937`; remaining selected missing files: `0`; delta provenance is `MANIFEST_DELTA_2026-05-15_TMA_SETTLEMENT_NPC_ACTIONS_BKR.tsv`.
- [x] Removed two current native verification blockers introduced outside this UI domain.
  - Evidence: `src/macro/spawners.cpp` now preserves full-map A* for same-island road detours after component pruning, restoring `road_river_generation_test`.
  - Evidence: `src/content/spells/registry.cpp` matches the current `SpellDef` schema with secondary tags, tag counts, status effect fields, and correctly aligned scaling/projectile fields, restoring `spell_casting_effects_test`.

Verification:
- `cmake --build build-msvc --target timaert -- -j1`: PASS (`ninja: no work to do` after final source settle).
- Domain smoke: PASS for `new_game,wait_boot_done,open_settlement_build,wait_visible,capture_frame,focus_npc_panel,wait_visible,capture_frame,attack_first_npc,wait_visible,capture_frame,quit`.
- Screenshots regenerated and sampled nonblank: `smoke_04_ui.png`, `smoke_07_ui.png`, `smoke_10_attack_ui.png`.
- Tests passed: `quest_lifecycle_test`, `save_roundtrip_test`, `pathfinding_parity_test`, `spell_casting_effects_test`, `road_river_generation_test`, `character_paperdoll_test`, `combat_squad_test`, `subworld_generator_parity_test`.
- No dotnet rebuilds were run.

Final continuation status: VERIFIED for this domain.

## 2026-05-15 Proximity Direction And Cap Priority

Changes:
- [x] Fixed native nearby-NPC north/south direction parity with TS `GameScreen.svelte`.
  - Evidence: `direction_label` now maps positive `dy` to `N` and negative `dy` to `S`, matching TS `directionLabel(dx, dy)`.
- [x] Prioritized visible rows inside the fixed no-scrollbar proximity cap.
  - Evidence: capped rows now carry `rank = abs(dx) + abs(dy)`, retain lower-rank rows over diagonals when the fixed buffer is full, and use a fixed-size insertion sort before drawing.
- [x] Verified the Hecton documentation import placement.
  - Evidence: `Docs/Imported/Hecton8/2026-05-15_docs_tasks_logs` exists with `MANIFEST.tsv` and `IMPORT_INDEX_2026-05-15.md`; manifest line count is `1660`.

Verification:
- MSVC build passed; `build-msvc/timaert.exe` timestamp is newer than `src/ui/macro_overlay.cpp`.
- Domain smoke passed: `new_game,wait_boot_done,open_settlement_build,wait_visible,capture_frame,focus_npc_panel,wait_visible,capture_frame,attack_first_npc,wait_visible,capture_frame,quit`.
- Screenshots regenerated: `smoke_04_ui.png`, `smoke_07_ui.png`, `smoke_10_attack_ui.png`.
- Tests passed: `quest_lifecycle_test`, `save_roundtrip_test`, `pathfinding_parity_test`, `spell_casting_effects_test`, `road_river_generation_test`, `character_paperdoll_test`, `combat_squad_test`, `subworld_generator_parity_test`.

Final continuation status: VERIFIED for this domain.

## 2026-05-15 Hecton Import Audit And Derived Trade Discount

Changes:
- [x] Audited Hecton for misplaced Timaert/Samosbor docs, tasks, and logs.
  - Evidence: `Docs/Imported/Hecton8_Timaert_Samosbor_Import_Audit.md` records the source paths scanned and the result.
- [x] Kept unrelated Hecton logs out of Timaert.
  - Evidence: no Hecton-specific status/log/rationale files were copied because no Timaert/Samosbor/TMA artifacts matched by name or content.
- [x] Matched TS `TradeOverlay.svelte` price plumbing more closely.
  - Evidence: settlement and NPC buy pricing now consume `calculate_derived(...).tradeDiscount` instead of passing raw CHA into local helper formulas.

Verification:
- MSVC build passed after the derived-discount patch.
- Domain smoke passed: `new_game,wait_boot_done,open_settlement_build,wait_visible,capture_frame,focus_npc_panel,wait_visible,capture_frame,attack_first_npc,wait_visible,capture_frame,quit`.
- Screenshots regenerated: `smoke_04_ui.png`, `smoke_07_ui.png`, `smoke_10_attack_ui.png`.
- Tests passed: `quest_lifecycle_test`, `save_roundtrip_test`, `pathfinding_parity_test`, `spell_casting_effects_test`, `road_river_generation_test`, `character_paperdoll_test`, `combat_squad_test`, `subworld_generator_parity_test`.

Final continuation status: VERIFIED for this domain.

## 2026-05-15 Fixed Proximity Row Buffer

Changes:
- [x] Removed the remaining growable row collection from the NPC proximity render path.
  - Evidence: `draw_npc_proximity_panel` now uses a fixed 12-row `std::array`, counts total nearby NPCs separately, and renders the existing `+N more nearby` summary without heap growth.
- [x] Added a defensive invalid-map early-out for proximity direction math.
  - Evidence: the panel now returns before torus distance work when `mapW` or `mapH` is non-positive.

Verification:
- MSVC build passed after the fixed-buffer patch.
- Minimal boot smoke passed: `new_game,wait_boot_done,quit`.
- Domain smoke passed: `new_game,wait_boot_done,open_settlement_build,wait_visible,capture_frame,focus_npc_panel,wait_visible,capture_frame,attack_first_npc,wait_visible,capture_frame,quit`.
- Screenshots regenerated: `smoke_04_ui.png`, `smoke_07_ui.png`, `smoke_10_attack_ui.png`.
- Tests passed: `quest_lifecycle_test`, `save_roundtrip_test`, `pathfinding_parity_test`, `spell_casting_effects_test`, `road_river_generation_test`, `character_paperdoll_test`, `combat_squad_test`, `subworld_generator_parity_test`.

Final continuation status: VERIFIED for this domain.

## 2026-05-15 Attack Route Upgrade

Changes:
- [x] Replaced the disabled NPC Attack surface with a real route from `src/ui/macro_overlay.cpp` to `src/app/main.cpp`.
  - Evidence: `NpcProximityResult::attackNpc` carries the selected entity; `route_macro_npc_attack` validates required components, enters subworld mode, spawns a hostile by NPC type/name/level/seed, and destroys the macro entity only after successful spawn.
- [x] Added deterministic smoke coverage for the route.
  - Evidence: `TIMAERT_SMOKE_SCRIPT=new_game,wait_boot_done,attack_first_npc,wait_visible,capture_frame,quit` produced `[smoke] npc_attack routed active=1` and screenshot `smoke_10_attack_ui.png`.
- [x] Updated translation tracker.
  - Evidence: row `0.11` now marks proximity panel Talk/Trade/Attack runtime-evidenced.

Known backend limit:
- Selected inventory, traits, and visual identity are now projected into subworld combat. Faction diplomacy and survivor persistence after combat still require an owning backend contract if the design wants non-lethal outcomes or faction consequences.

Verification:
- MSVC build passed after the attack route patch.
- Full domain smoke passed for Build tab, NPC panel, and Attack route.
- Domain tests passed as listed above.
- `subworld_generator_parity_test` passed in the latest run.

## 2026-05-15 Trait Pricing And Projection Upgrade

Changes:
- [x] Added `ecs::NpcTraits` as a compact POD component matching TS NPC trait cardinality.
  - Evidence: macro NPC spawning now assigns 1-2 unique raw `NPCTrait` ids.
- [x] NPC Trade now applies TS `TradeOverlay.svelte` modifiers.
  - Evidence: Greedy increases buy prices and lowers sell prices; Generous lowers buy prices and raises sell prices; CHA discount applies to buying.
- [x] Settlement Trade now uses TS-style mood buy multipliers, validates inventory removal before mutating gold, shows transaction feedback, and writes `LogType::Economy` entries.
  - Evidence: Prosperous/Unrest/Revolt mood affects settlement buy prices, and sell price uses the TradeOverlay 50% base.
- [x] NPC Attack now projects selected macro NPC data into the subworld hostile.
  - Evidence: `route_macro_npc_attack` passes selected `NpcInventory`, `NpcTraits`, and `NpcCharacter` into `SubworldEngine::spawn_hostile_npc`.

Verification:
- MSVC build passed.
- Tests passed: `quest_lifecycle_test`, `save_roundtrip_test`, `pathfinding_parity_test`, `spell_casting_effects_test`, `road_river_generation_test`, `character_paperdoll_test`, `combat_squad_test`, `subworld_generator_parity_test`.
- Smoke passed: `new_game,wait_boot_done,open_settlement_build,wait_visible,capture_frame,focus_npc_panel,wait_visible,capture_frame,attack_first_npc,wait_visible,capture_frame,quit`.
- Screenshots regenerated: `smoke_04_ui.png`, `smoke_07_ui.png`, `smoke_10_attack_ui.png`.

## 2026-05-15 Stability And UX Hardening

Changes:
- [x] Fixed selected-component lifetime in the attack route.
  - Evidence: `route_macro_npc_attack` now copies selected `NpcInventory`, `NpcTraits`, and `NpcCharacter` before `SubworldEngine::enter`, preventing EnTT storage invalidation during subworld spawn setup.
- [x] Added a viewport-budgeted row cap to the NPC proximity panel.
  - Evidence: `draw_npc_proximity_panel` now uses `viewH` to cap visible rows and shows a compact `+N more nearby` summary instead of allowing the right-edge panel to grow off-screen.

Verification:
- MSVC build passed after the lifetime fix and row-cap patch.
- Smoke passed after the fix: `new_game,wait_boot_done,open_settlement_build,wait_visible,capture_frame,focus_npc_panel,wait_visible,capture_frame,attack_first_npc,wait_visible,capture_frame,quit`.
- Tests passed after the final patch set: `quest_lifecycle_test`, `save_roundtrip_test`, `pathfinding_parity_test`, `spell_casting_effects_test`, `road_river_generation_test`, `character_paperdoll_test`, `combat_squad_test`, `subworld_generator_parity_test`.

## 2026-05-15 Direction And Trade UX Hardening

Changes:
- [x] Fixed proximity direction labels to use row-local storage.
  - Evidence: each NPC row now owns a fixed `char dir[5]` instead of sharing a static label buffer.
- [x] Guarded invalid native NPC type ids in the proximity panel and trade popup.
  - Evidence: raw type ids are checked against `NPCType::Count` before casting; invalid data falls back to `NPCType::Peasant`.
- [x] Reset same-NPC trade feedback on an explicit Trade reopen.
  - Evidence: opening Trade clears stale transaction text even when the selected NPC entity did not change.
- [x] Added disabled-buy reasons to NPC and settlement trade.
  - Evidence: disabled buy controls now distinguish unknown item ids, out of stock, and insufficient gold.

Verification:
- MSVC build passed after the hardening patch.
- Domain smoke passed after the hardening patch: `new_game,wait_boot_done,open_settlement_build,wait_visible,capture_frame,focus_npc_panel,wait_visible,capture_frame,attack_first_npc,wait_visible,capture_frame,quit`.
- Tests passed after the hardening patch: `quest_lifecycle_test`, `save_roundtrip_test`, `pathfinding_parity_test`, `spell_casting_effects_test`, `road_river_generation_test`, `character_paperdoll_test`, `combat_squad_test`, `subworld_generator_parity_test`.

Final continuation status: VERIFIED for this domain.

## 2026-05-15 Physical-Bottom Final Status

Changes:
- [x] Re-audited TS authority and native routing after the final continuation request.
  - Evidence: Build remains an honest disabled contract; NPC Trade and Attack use real native inventory/economy/subworld contracts.
- [x] Added TS-style suppression for native NPC proximity badge rows when another native overlay is open.
  - Evidence: `macro_overlay_blocks_npc_proximity` plus `draw_npc_proximity_panel(..., showRows)` keep row drawing out of settlement/quest/codex/map/character/diplomacy/story/dialog/event overlays while preserving active NPC popups.
- [x] Refreshed Hecton-origin docs/tasks/logs into the quarantined Timaert import tree only.
  - Evidence: selected Hecton source files `2646`, copied `2`, missing after refresh `0`, stale-by-size after refresh `0`, import tree files `3028`.
- [x] Verified build, runtime, screenshots, tests, and polish scan.
  - Evidence: MSVC build PASS; domain smoke PASS; four fresh `1280x800` nonblank PPMs copied into `Docs/AgentLogs`; focused tests PASS; no dotnet rebuilds run.

Final continuation status: VERIFIED for this domain.

## 2026-05-15 Continuation: Feature Audit, Proximity Cleanup, Import Refresh

Checklist:
- [x] Re-read the Timaert assignment, root docs, status, and rationale before continuing.
  - DOD practice: used disk state as authority after context compaction; rejected acting on chat-only memory; microsecond estimate not benchmarked.
- [x] Re-audited TS features.ts against native feature generation.
  - Evidence: translation.md maps features.ts to src/macro/features.h plus src/macro/spawners.cpp::build_feature_layer; feature_layer_parity_test.exe passed after this continuation.
  - DOD practice: verified existing parity instead of changing a completed subsystem outside this prompt; rejected duplicate feature logic inside UI files; microsecond estimate not benchmarked.
- [x] Cleaned draw_npc_proximity_panel row rendering structure in src/ui/macro_overlay.cpp.
  - Evidence: fixed the brittle indentation/brace layout around the fixed row buffer, capped row sort, and ImGui draw window without changing the behavior surface.
  - DOD practice: behavior-preserving maintainability patch in owned UI domain; rejected refactoring into a new abstraction during a live multi-agent pass; microsecond estimate: no measured runtime delta.
- [x] Refreshed Hecton-origin docs/tasks/logs into the quarantined Timaert import tree only.
  - Evidence: manifest Docs/Imported/Hecton8/2026-05-15_docs_tasks_logs/MANIFEST_REFRESH_2026-05-15_TMA_SETTLEMENT_NPC_ACTIONS_BKR_2026-05-15_172551.tsv; selected 2694, copied 0, refreshed 41, errors 0, missing after refresh 0, stale-by-size after refresh 0.
  - DOD practice: Hecton read-only, source-relative quarantine under Timaert; rejected writing any Timaert report into Hecton or merging imported files into active Timaert task/log folders.
- [x] Verified native build and runtime screenshots without dotnet rebuilds.
  - Evidence: MSVC cmake --build build-msvc --target timaert -- -j1 passed; Build/NPC panel/NPC trade/Attack split smoke scripts passed; fresh PPMs are nonblank 1280x800 captures.
  - DOD practice: split smoke after the combined script twice exited during boot before UI actions; rejected treating a pre-UI combined-script failure as domain evidence when smaller action-specific scripts verified the required surfaces.
- [x] Verified focused native tests.
  - Evidence: quest_lifecycle_test, save_roundtrip_test, feature_layer_parity_test, pathfinding_parity_test, spell_casting_effects_test, road_river_generation_test, character_paperdoll_test, combat_squad_test, and subworld_generator_parity_test all passed.

Current disabled action status:
- Settlement Build remains an honest disabled state, not a fake action. Exact missing native dependencies are still: persistent Settlement building/project state, save-versioned storage, TS/native build-project catalog with costs/effects, and daily world-tick effects.

Final continuation status: VERIFIED for this domain.
## 2026-05-15 Continuation: Stale NPC Popup Hardening

Checklist:
- [x] Re-read the Timaert assignment, root docs, status, and rationale before continuing.
  - DOD practice: disk reports and root docs used as authority; rejected acting on chat-only memory; microsecond estimate not benchmarked.
- [x] Re-audited TS features.ts and native feature layer status.
  - Evidence: translation.md and ARCHITECTURE.md still mark features.ts as native-complete; feature_layer_parity_test.exe passed in this run.
  - DOD practice: verified the already-owned macro feature port instead of duplicating feature logic in settlement/NPC UI; microsecond estimate not benchmarked.
- [x] Hardened stale NPC popup lifetime in src/ui/macro_overlay.cpp.
  - Evidence: Talk popup now clears if its entity is invalid or dead; NPC Trade popup now requires a live Health component and clears stale trade message state when invalidated.
  - DOD practice: fail-closed UI state bound to backend entity/health contracts; rejected keeping a trade window open for a dead or destroyed NPC; runtime cost is a single registry validity/Health lookup for an open popup only.
- [x] Refreshed Hecton-origin docs/tasks/logs into the quarantined Timaert import tree only.
  - Evidence: manifest Docs/Imported/Hecton8/2026-05-15_docs_tasks_logs/MANIFEST_REFRESH_2026-05-15_TMA_SETTLEMENT_NPC_ACTIONS_BKR_2026-05-15_180620.tsv; selected 2730, copied 1, refreshed 3, errors 0, missing after refresh 0, stale-by-size after refresh 0.
  - DOD practice: Hecton read-only, Timaert quarantine only; rejected writing any Timaert report into Hecton or merging imported Hecton logs into active Timaert task/log folders.
- [x] Verified native build, runtime smokes, screenshots, and focused tests without dotnet rebuilds.
  - Evidence: MSVC app build passed; split Build/NPC panel/NPC trade/Attack smokes passed; screenshots TMA_SETTLEMENT_NPC_ACTIONS_BKR_20260515_181024_*.ppm are nonblank; focused tests all passed.

Current disabled action status:
- Settlement Build remains an honest disabled state. Exact missing native dependencies are still persistent Settlement building/project state, save-versioned storage, TS/native build-project catalog with costs/effects, and daily world-tick effects.

Final continuation status: VERIFIED for this domain.
## 2026-05-15 Continuation: Native Popup Exclusivity And Stable Import Sync

Checklist:
- [x] Re-read the Timaert assignment, root docs, status, and rationale before continuing.
  - DOD practice: disk status/rationale used as current state; rejected acting on chat-only memory; microsecond estimate not benchmarked.
- [x] Re-audited TS interaction/trade modal behavior against native NPC popup state.
  - Evidence: TS GameScreen clears interactingNpc when Trade/Fight starts; native now enforces one active Talk/Trade popup path through local clear helpers.
  - DOD practice: small domain-local state hardening; rejected a broad InteractionOverlay rewrite outside this pass.
- [x] Hardened native NPC popup mutual exclusion in src/ui/macro_overlay.cpp.
  - Evidence: open_npc_trade_panel clears Talk; Talk button clears Trade; close and invalidation paths use clear_talk_popup and clear_trade_popup.
  - DOD practice: fail-closed backend-bound UI state; rejected duplicated static-state cleanup branches.
- [x] Reconfirmed TS features.ts native parity.
  - Evidence: feature_layer_parity_test.exe passed in this run; no duplicate feature logic was added to UI files.
- [x] Refreshed Hecton-origin docs/tasks/logs into the quarantined Timaert import tree only, then ran a stable sync after Hecton changed during the pass.
  - Evidence: first refresh manifest MANIFEST_REFRESH_2026-05-15_TMA_SETTLEMENT_NPC_ACTIONS_BKR_2026-05-15_182909.tsv; stable sync manifest MANIFEST_STABLE_SYNC_2026-05-15_TMA_SETTLEMENT_NPC_ACTIONS_BKR_2026-05-15_185225.tsv; final selected 2792, copied 5 in stable sync, errors 0, missing after stable sync 0, stale-by-size after stable sync 0.
  - DOD practice: Hecton read-only, source-relative quarantine under Timaert; rejected writing any Timaert task/rationale/log into Hecton.
- [x] Verified native build, runtime smokes, screenshots, and focused tests without dotnet rebuilds.
  - Evidence: MSVC app build passed; split Build/NPC panel/NPC trade/Attack smokes passed; screenshots TMA_SETTLEMENT_NPC_ACTIONS_BKR_20260515_183819_*.ppm are nonblank; focused tests all passed.

Current disabled action status:
- Settlement Build remains an honest disabled state. Exact missing native dependencies are still persistent Settlement building/project state, save-versioned storage, TS/native build-project catalog with costs/effects, and daily world-tick effects.

Final continuation status: VERIFIED for this domain.
## 2026-05-15 Continuation: Build Disabled Dependency Wrap

Checklist:
- [x] Re-read the Timaert assignment, root docs, status, and rationale before continuing.
  - DOD practice: disk state used as authority; rejected acting on chat-only memory; microsecond estimate not benchmarked.
- [x] Re-audited Settlement Build against TS SettlementOverlay.svelte and Timaert docs.
  - Evidence: TS availableTabs still excludes Build; matwej.md and ARCHITECTURE.md still require Build to stay an explicit no-op until a build-project contract exists.
  - DOD practice: no fake build action; rejected editing save/schema/world tick outside the required backend contract.
- [x] Hardened the Build disabled dependency UI in src/ui/overlays.cpp.
  - Evidence: long required file/symbol values in the Build missing-contract table now use ImGui::TextWrapped, reducing clipping risk while preserving exact backend dependency names.
  - DOD practice: UX fix inside owned overlay file; rejected decorative buttons or new gameplay state.
- [x] Reconfirmed TS features.ts native parity.
  - Evidence: feature_layer_parity_test.exe passed in this run; no feature logic was duplicated in UI files.
- [x] Refreshed Hecton-origin docs/tasks/logs into the quarantined Timaert import tree only.
  - Evidence: stable sync manifest MANIFEST_STABLE_SYNC_2026-05-15_TMA_SETTLEMENT_NPC_ACTIONS_BKR_2026-05-15_191041.tsv; final selected 2814, copied 1, refreshed 2, errors 0, missing after stable sync 0, stale-by-size after stable sync 0.
  - DOD practice: Hecton read-only, source-relative quarantine under Timaert; rejected writing any Timaert task/rationale/log into Hecton.
- [x] Verified native build, runtime smokes, screenshots, and focused tests without dotnet rebuilds.
  - Evidence: MSVC app target reported no work to do; split Build/NPC panel/NPC trade/Attack smokes passed; screenshots TMA_SETTLEMENT_NPC_ACTIONS_BKR_20260515_191342_*.ppm are nonblank; focused tests all passed.

Current disabled action status:
- Settlement Build remains an honest disabled state. Exact missing native dependencies are still persistent Settlement building/project state, save-versioned storage, TS/native build-project catalog with costs/effects, and daily world-tick effects.

Final continuation status: VERIFIED for this domain.
## 2026-05-15 Continuation: Pre-Draw Popup Sanitize And Stable Import Sync

Checklist:
- [x] Re-read the Timaert assignment, status, rationale, and targeted TS authority before continuing.
  - Evidence: extracted <AGENT_PROMPT id="TMA_SETTLEMENT_NPC_ACTIONS_BKR" ...> from TIMAERT BATCH.md; scanned SettlementOverlay, NpcProximityPanel, InteractionOverlay, TradeOverlay, GameScreen, economy.ts, and npc.ts for Build/Trade/Attack contracts.
  - DOD practice: disk and TS authority used instead of chat-only memory; rejected Hecton instructions as non-authoritative for Timaert; microsecond estimate not benchmarked.
- [x] Hardened stale NPC popup timing in src/ui/macro_overlay.cpp.
  - Evidence: added valid_trade_npc_entity and sanitize_popup_state; draw_npc_proximity_panel now sanitizes Talk/Trade popup state before computing drawRowsEnabled; NPC Trade uses the same validation helper before inventory/economy reads.
  - DOD practice: fail-closed selected-entity validation with no all-NPC scan; rejected a broad modal manager or heap-backed popup state; per-frame cost is two null checks when no popup is open, plus selected-entity registry checks only when a popup exists.
- [x] Refreshed Hecton-origin docs/tasks/logs into the quarantined Timaert import tree only.
  - Evidence: Docs/Imported/Hecton8/2026-05-15_docs_tasks_logs/MANIFEST_STABLE_SYNC_SIZE_TIME_2026-05-15_TMA_SETTLEMENT_NPC_ACTIONS_BKR_20260515_201051.tsv; final selected 2888, copied 6, refreshed 20, missing 0, stale 0, errors 0 after four bounded rounds.
  - DOD practice: Hecton read-only; imported files stayed source-relative under Docs/Imported/Hecton8; rejected writing Timaert docs/logs/tasks into C:\hades\Hecton8.
- [x] Verified native build, domain smokes, screenshots, tests, and hygiene without dotnet rebuilds.
  - Evidence: forced timestamp touch then MSVC cmake --build build-msvc --target timaert -- -j1 rebuilt src/ui/macro_overlay.cpp and linked timaert.exe.
  - Evidence: split smokes passed for Build, NPC panel, NPC Trade, and Attack; fresh captures are Docs/AgentLogs/TMA_SETTLEMENT_NPC_ACTIONS_BKR_20260515_202416_build.ppm, _npc_panel.ppm, _npc_trade.ppm, and _attack.ppm, each 3072016 bytes and 64/64 nonzero samples.
  - Evidence: quest_lifecycle_test, save_roundtrip_test, feature_layer_parity_test, pathfinding_parity_test, spell_casting_effects_test, road_river_generation_test, character_paperdoll_test, combat_squad_test, subworld_generator_parity_test, and npc_spawn_contract_test all exited 0.
  - Evidence: chunked git diff --check passed for touched C++ files with line-ending warnings only.

Current disabled action status:
- Settlement Build remains an honest disabled state. Exact missing native dependencies are still persistent Settlement building/project state, save-versioned storage, a TS/native build-project catalog with costs/effects, and daily world-tick effects.

Final continuation status: VERIFIED for this domain.

## 2026-05-15 Continuation: Trade Detail Tooltips And Talk Escape

Checklist:
- [x] Re-read the Timaert assignment, status, rationale, and domain docs before continuing.
  - Evidence: extracted TMA_SETTLEMENT_NPC_ACTIONS_BKR from TIMAERT BATCH.md; rechecked status/rationale tails; audited matwej.md, ARCHITECTURE.md, MERGE_PLAN.md, and translation.md rows for this domain.
  - DOD practice: disk state and TS/C++ authority used instead of chat memory; rejected touching Hecton active docs or unrelated Timaert systems.
- [x] Added TS-style item detail tooltips to native settlement and NPC trade surfaces.
  - Evidence: src/ui/overlays.cpp and src/ui/macro_overlay.cpp now use draw_trade_item_tooltip on trade item labels, showing ItemDef name, description, and weight without building per-frame std::string text.
  - DOD practice: catalog-backed UI detail only; rejected fake inventory and rejected allocating formatted strings in the trade row loop.
- [x] Added Escape dismissal for the native Talk popup.
  - Evidence: src/ui/macro_overlay.cpp Talk window now handles ImGuiKey_Escape, matching the TS InteractionOverlay Leave [Esc] behavior.
  - DOD practice: small modal UX parity; rejected replacing the current proven in-panel Talk/Trade/Attack surface with a broad modal rewrite.
- [x] Updated the TS parity ledger for this domain.
  - Evidence: translation.md rows 0.11 and 0.13 now mention Talk Escape and native ItemDef description/weight trade tooltips.
- [x] Refreshed Hecton-origin docs/tasks/logs into Timaert quarantine only.
  - Evidence: stable sync manifest Docs/Imported/Hecton8/2026-05-15_docs_tasks_logs/MANIFEST_STABLE_SYNC_SIZE_TIME_2026-05-15_TMA_SETTLEMENT_NPC_ACTIONS_BKR_20260515_210941.tsv; a live Hecton log kept one stale file during bounded sync, then targeted retry copied the volatile files and final verification reported selected 2950, missing 0, stale 0, errors 0.
  - DOD practice: Hecton read-only, source-relative quarantine under Docs/Imported/Hecton8; rejected writing any Timaert reports to C:\hades\Hecton8.
- [x] Verified native build, domain smokes, screenshots, focused tests, and hygiene without dotnet rebuilds.
  - Evidence: deleted only stale build object artifacts under build-msvc after verifying paths; MSVC rebuilt src/ui/macro_overlay.cpp and src/ui/overlays.cpp. A stale zones object and then a non-responding timaert.exe lock interrupted link; stale object was rebuilt, exact locked process was stopped, and final timaert.exe link passed.
  - Evidence: split Build/NPC panel/NPC Trade/Attack smokes passed; captures TMA_SETTLEMENT_NPC_ACTIONS_BKR_20260515_212913_build.ppm, _npc_panel.ppm, _npc_trade.ppm, and _attack.ppm are each 3072016 bytes with 64/64 nonzero samples.
  - Evidence: quest_lifecycle_test, save_roundtrip_test, feature_layer_parity_test, pathfinding_parity_test, spell_casting_effects_test, road_river_generation_test, character_paperdoll_test, combat_squad_test, subworld_generator_parity_test, and npc_spawn_contract_test all exited 0.
  - Evidence: git diff --check passed for touched code/docs with line-ending warnings only; no build-msvc timaert.exe process remained.

Current disabled action status:
- Settlement Build remains an honest disabled state. Exact missing native dependencies are still persistent Settlement building/project state, save-versioned storage, a TS/native build-project catalog with costs/effects, and daily world-tick effects.

Final continuation status: VERIFIED for this domain.
